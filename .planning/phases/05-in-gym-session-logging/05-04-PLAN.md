---
phase: 05-in-gym-session-logging
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - packages/pr-rules/package.json
  - packages/pr-rules/tsconfig.json
  - packages/pr-rules/jest.config.js
  - packages/pr-rules/src/index.ts
  - packages/pr-rules/src/estimated-1rm.ts
  - packages/pr-rules/src/personal-records.ts
  - packages/pr-rules/src/warmup.ts
  - packages/pr-rules/src/__tests__/estimated-1rm.test.ts
  - packages/pr-rules/src/__tests__/personal-records.test.ts
  - packages/pr-rules/src/__tests__/warmup.test.ts
autonomous: true
requirements: [LOG-17, LOG-18]
windows_id_range: "133-142"

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PR detection, estimated-1RM and warm-up scaling all live in one shared pure package importable by both the mobile client and the NestJS API, so the rules can never be written twice and diverge — the precondition for Phase 10's cross-device PR authority (D-30)."
    - "Every exported function in `packages/pr-rules` is pure in `next-up.ts`'s sense: arguments in, no database call, no React, no ambient `Date.now()` except as a defaulted-but-overridable parameter, and every boundary condition is a fixture-free unit test."
    - "`estimated1RM(weightKg, reps)` returns null rather than a number whenever the formula is not valid — reps above the cutoff, reps at or below zero, or a non-positive weight — and the caller renders the absence explicitly (D-31)."
    - "`detectPrs` recognises exactly the four types D-30 names — heaviest weight, best estimated 1RM, most reps at a weight, best set volume — keyed to the `PR_TYPES` tuple published in 05-02, never a locally retyped string union (D-09, D-30)."
    - "`detectPrs` excludes warm-up sets and uncompleted sets from both the candidate and the prior-best inputs, because a warm-up is not a working effort (RESEARCH Pitfall 2)."
    - "`warmupSets(workingWeightKg, roundingIncrementKg)` is deterministic: the same working weight always produces the same rows, so a regenerate is a replace rather than a drift (LOG-17)."
    - "Nothing in this package reads `set_index` position to infer set type; every consumer filters on `set_type` explicitly (RESEARCH Pitfall 2)."
    # --- Edge predicates: LOG-17 (pure half) ---
    - "Each warm-up weight rounds to the nearest `roundingIncrementKg`, defaulting to 2.5, and a value landing exactly halfway between two increments rounds up — the tie case is pinned, not left to floating-point luck (LOG-17 adjacency)."
    - "`warmupSets` returns an empty array for a working weight of 0, null, NaN or a negative number, and never emits a row whose weight rounds to 0 (LOG-17 empty)."
    - "`warmupSets` returns its rows in ascending weight order, and that order is the order the caller renders and persists them in (LOG-17 ordering)."
    # --- Edge predicates: LOG-18 (pure half) ---
    - "A candidate set whose value exactly ties the prior best is not a PR; only a strict improvement produces a `DetectedPr` (LOG-18 adjacency)."
    - "A first-ever set for an exercise — every prior best null and the reps map empty — produces a PR for each of the four types the set has a defined value for, and none for `best_e1rm` when the rep count is past the validity cutoff (LOG-18 empty)."
    - "`detectPrs` returns its results in a stable, declaration-order sequence (heaviest weight, best e1RM, most reps at weight, best set volume) so two runs on identical input are byte-identical (LOG-18 ordering)."
    - "`mostRepsAtWeight` compares against the prior best at the candidate's EXACT weight, so 100kg x 8 does not count as a rep PR against a prior 90kg x 12 (LOG-18 adjacency, second boundary)."
    - statement: "The 40/60/80 percent warm-up scheme and the 10-rep estimated-1RM validity cutoff are this project's own reasoned defaults, not a vendor-verified specification — no public MacroFactor formula exists. Both are single named constants so replacing them is a one-file change."
      verification: backstop
  artifacts:
    - packages/pr-rules/package.json
    - packages/pr-rules/src/estimated-1rm.ts
    - packages/pr-rules/src/personal-records.ts
    - packages/pr-rules/src/warmup.ts
  key_links:
    - "`packages/pr-rules` depends on `@fitness/api-contracts` for `PR_TYPES` and `WARMUP_SET_TYPE`, so the client-side detector, the server-side validator and the Postgres CHECK all read one value list."
    - "`packages/pr-rules` is a workspace package under the existing `packages/*` glob and is picked up by `turbo run test` with no turbo.json change."
    - "`estimated1RM` is imported by `detectPrs` for the `best_e1rm` type — one implementation, not two."
---

## Phase Goal (verbatim from ROADMAP.md — not in user-story format)

> The user can walk into a gym with no signal and log a complete workout without friction. **This is the phase the app becomes real** — dogfooding starts here.

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| package | `@fitness/pr-rules` (`packages/pr-rules/`) |
| file | `packages/pr-rules/src/estimated-1rm.ts` |
| function | `estimated1RM` |
| const | `E1RM_MAX_VALID_REPS` |
| file | `packages/pr-rules/src/personal-records.ts` |
| function | `detectPrs`, `emptyPriorBest`, `foldPriorBest` |
| type | `PriorBest`, `CandidateSet`, `DetectedPr` |
| file | `packages/pr-rules/src/warmup.ts` |
| function | `warmupSets`, `roundToIncrement` |
| const | `WARMUP_STEPS`, `DEFAULT_ROUNDING_INCREMENT_KG` |
| type | `WarmupSet` |

## Broken-windows ledger reservation

Reserved `.planning/WINDOWS.md` ids **133–142**.

<objective>
Build the one shared pure package that owns every retrospective training rule this phase introduces: was this a record, what is this set worth as a one-rep max, and what warm-up ladder leads to this working weight.

Purpose: D-30 requires the PR rules to be importable by both the client (which needs an instant answer offline) and the server (which reconciles against fully merged history in Phase 10). ARCHITECTURE.md §4 makes the same argument for `progression-engine`: the fix is not to avoid running it twice, it is to avoid writing it twice.
Output: a new workspace package with three pure modules and a real unit suite, consumed by 05-06 (warm-up generation) and 05-08 (finish summary and PR writes).
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-in-gym-session-logging/05-CONTEXT.md
@.planning/phases/05-in-gym-session-logging/05-RESEARCH.md
@.planning/phases/05-in-gym-session-logging/05-PATTERNS.md
@.planning/phases/05-in-gym-session-logging/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @fitness/pr-rules and land the estimated-1RM estimator</name>
  <reversibility rating="reversible">A new workspace package sitting behind three named exports; moving it into `progression-engine` later is a rename, not a migration.</reversibility>
  <read_first>
    - `packages/progression-engine/package.json` and `packages/progression-engine/tsconfig.json` — copy both verbatim, renaming only the package name; this is the explicit precedent D-30 cites
    - `packages/api-contracts/jest.config.js` and `packages/api-contracts/package.json` — the `ts-jest` preset, the `jest-suite-integrity.cjs` reporter wiring, and the devDependency set a testable workspace package needs
    - `packages/api-contracts/src/session.ts` — `PR_TYPES`, `WARMUP_SET_TYPE`, `WORKING_SET_TYPE` from 05-02
    - `apps/mobile/lib/programs/next-up.ts` — the pure-module contract header comment and the clock-as-an-argument pattern every function here must follow
    - `pnpm-workspace.yaml` — confirms `packages/*` already covers the new package with no glob change
    - `.planning/research/PITFALLS.md` §8 — why no public MacroFactor 1RM formula exists and what that means for this constant
  </read_first>
  <files>packages/pr-rules/package.json, packages/pr-rules/tsconfig.json, packages/pr-rules/jest.config.js, packages/pr-rules/src/index.ts, packages/pr-rules/src/estimated-1rm.ts, packages/pr-rules/src/__tests__/estimated-1rm.test.ts</files>
  <action>
Create `packages/pr-rules/` by copying `packages/progression-engine`'s `package.json` and `tsconfig.json` and renaming the package to `@fitness/pr-rules`. Add a `test` script and a `jest.config.js` copied from `packages/api-contracts/jest.config.js` (ts-jest preset, node test environment, and the `../../scripts/jest-suite-integrity.cjs` reporter — a zero-test or skipped-suite run must fail here the same way it does everywhere else in this repo). Add `@fitness/api-contracts` as a workspace dependency and the same `jest` / `ts-jest` / `@types/jest` / `typescript` devDependencies `api-contracts` carries. `pnpm-workspace.yaml`'s `packages/*` glob already covers the directory and `turbo.json`'s `test` task is generic, so neither needs editing.

`packages/pr-rules/src/estimated-1rm.ts` (D-31): export `E1RM_MAX_VALID_REPS = 10` as a named constant and `estimated1RM(weightKg: number, reps: number): number | null`. Return null when `reps` is at or below 0, when `weightKg` is at or below 0, when either argument is not finite, and when `reps` exceeds `E1RM_MAX_VALID_REPS`. Return `weightKg` unchanged when `reps` is 1. Otherwise apply Epley: weight multiplied by (1 + reps/30). Write a contract header comment in `next-up.ts`'s voice recording the two things a reader needs and cannot infer: that both Epley and Brzycki degrade above roughly 10–12 reps so the cutoff is a deliberate project choice rather than a validated study threshold, and that a summary printing a confident 1RM off a set of 20 is worse than one printing nothing.

`packages/pr-rules/src/index.ts`: re-export all three modules (the other two land in Tasks 2 and 3).

`packages/pr-rules/src/__tests__/estimated-1rm.test.ts`: cover reps of 1 returning the weight itself, a mid-range value against a hand-computed expectation, the exact cutoff boundary (reps at `E1RM_MAX_VALID_REPS` returns a number, reps one above returns null), zero and negative reps, zero and negative weight, and non-finite inputs.
  </action>
  <verify>
    <automated>pnpm install &amp;&amp; pnpm --filter @fitness/pr-rules test &amp;&amp; pnpm --filter @fitness/pr-rules typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/pr-rules/package.json` declares `"name": "@fitness/pr-rules"` and a `test` script, and lists `@fitness/api-contracts` as a workspace dependency.
    - `pnpm --filter @fitness/pr-rules test` exits 0 with a non-zero test count (the suite-integrity reporter fails an empty run).
    - `packages/pr-rules/src/estimated-1rm.ts` exports `estimated1RM` and `E1RM_MAX_VALID_REPS`.
    - A test case asserts `estimated1RM(100, 10)` returns a number and `estimated1RM(100, 11)` returns null — the cutoff boundary, both sides.
    - A test case asserts `estimated1RM(100, 1)` returns exactly 100.
    - A source scan of `packages/pr-rules/src/` for `Date.now(` and for any `import` of a database, React or React Native module returns no matches — the package is pure.
    - `pnpm --filter @fitness/pr-rules typecheck` exits 0.
  </acceptance_criteria>
  <done>A shared pure package exists, builds and tests in the monorepo, and answers "what is this set worth as a one-rep max, and when is that question not answerable".</done>
</task>

<task type="auto">
  <name>Task 2: The four PR types as one pure detector</name>
  <reversibility rating="one-way">D-30: `personal_record` rows written under this rule set are reconciled under Phase 10's; rows written under a rule that later changes are the stale-derived-data failure PITFALLS §11 describes.</reversibility>
  <read_first>
    - `packages/pr-rules/src/estimated-1rm.ts` — the estimator written in Task 1, imported here for the `best_e1rm` type
    - `packages/api-contracts/src/session.ts` — `PR_TYPES`, `WARMUP_SET_TYPE`, `WORKING_SET_TYPE`
    - `apps/mobile/lib/programs/next-up.ts` — the pure-function-with-explicit-inputs contract
    - `.planning/phases/05-in-gym-session-logging/05-RESEARCH.md` §10 — the precise definitions of the four PR types and the shared-package placement argument
    - `.planning/research/ARCHITECTURE.md` §1 — the working-vs-warm-up volume-attribution exclusion rule these definitions inherit
    - `apps/api/src/db/schema/records.ts` — `personal_record`'s `pr_type` and `value` columns, the destination for a `DetectedPr`
  </read_first>
  <files>packages/pr-rules/src/personal-records.ts, packages/pr-rules/src/index.ts, packages/pr-rules/src/__tests__/personal-records.test.ts</files>
  <action>
Export from `packages/pr-rules/src/personal-records.ts`:

`interface PriorBest { heaviestWeight: number | null; bestE1rm: number | null; mostRepsAtWeight: Map<number, number>; bestSetVolume: number | null }` — `mostRepsAtWeight` keyed by the exact weight in kg.
`interface CandidateSet { weightKg: number | null; reps: number; setType: string; completed: boolean }`.
`interface DetectedPr { prType: PrType; value: number }` where `PrType` is imported from `@fitness/api-contracts` — do NOT retype the union locally, or the detector and the Postgres CHECK become two lists (D-09).
`emptyPriorBest(): PriorBest` — the all-null, empty-map starting value, so a caller never hand-builds one.
`foldPriorBest(sets: CandidateSet[]): PriorBest` — folds an exercise's completed, non-warm-up history into a `PriorBest`, so the caller passes rows rather than pre-aggregating.
`detectPrs(candidate: CandidateSet, priorBest: PriorBest): DetectedPr[]`.

`detectPrs` returns an empty array immediately when the candidate's `setType` equals `WARMUP_SET_TYPE`, when `completed` is false, or when `weightKg` is null — a warm-up is not a working effort and an uncompleted row is not a result. Otherwise it emits, in this fixed declaration order so two runs on identical input are byte-identical:
`heaviest_weight` when the candidate weight strictly exceeds `priorBest.heaviestWeight` or that is null;
`best_e1rm` when `estimated1RM(weightKg, reps)` returns non-null AND strictly exceeds `priorBest.bestE1rm` or that is null;
`most_reps_at_weight` when the candidate's reps strictly exceed the prior best at the candidate's EXACT weight (a lookup in `mostRepsAtWeight` keyed by that weight) or no entry exists — 100kg for 8 must not count as a rep record against a prior 90kg for 12;
`best_set_volume` when `weightKg * reps` strictly exceeds `priorBest.bestSetVolume` or that is null.
A tie is not a PR: only a strict improvement produces a row. Record that choice in a one-line comment, because "ties or beats" was a plausible alternative reading of D-30 and the next reader will wonder.

`packages/pr-rules/src/__tests__/personal-records.test.ts`: cover a first-ever set producing three PRs and no `best_e1rm` when reps exceed the cutoff; a first-ever set inside the cutoff producing all four; an exact tie on each of the four producing none; a strict improvement on each producing exactly that one; a warm-up-typed candidate producing none; an uncompleted candidate producing none; a null-weight candidate producing none; the exact-weight rep-record boundary; and `foldPriorBest` ignoring warm-up and uncompleted rows.
  </action>
  <verify>
    <automated>pnpm --filter @fitness/pr-rules test -- personal-records</automated>
  </verify>
  <acceptance_criteria>
    - `packages/pr-rules/src/personal-records.ts` exports `detectPrs`, `foldPriorBest`, `emptyPriorBest`, and the three interfaces.
    - `DetectedPr['prType']` resolves to the `PrType` imported from `@fitness/api-contracts`; a source scan of `personal-records.ts` finds no locally-declared string-union literal listing the four PR names.
    - A test case asserts a candidate exactly equal to each prior best produces an empty result array (the strict-improvement rule).
    - A test case asserts a candidate at 100kg x 8 produces no `most_reps_at_weight` PR when the prior best is 90kg x 12, and does produce one when the prior best at exactly 100kg is 7.
    - A test case asserts a `setType` of the warm-up value produces an empty array regardless of how heavy the candidate is.
    - A test case asserts `detectPrs` output order is `heaviest_weight`, `best_e1rm`, `most_reps_at_weight`, `best_set_volume` when all four fire.
    - `pnpm --filter @fitness/pr-rules test -- personal-records` exits 0 with a non-zero test count.
  </acceptance_criteria>
  <done>Given a completed working set and the exercise's prior bests, the package answers which of the four record types were beaten — deterministically, offline, and identically on client and server.</done>
</task>

<task type="auto">
  <name>Task 3: Deterministic warm-up scaling</name>
  <reversibility rating="reversible">The percentage scheme is a single exported constant array; replacing it is a one-file change and Phase 6 replaces the rounding increment with real plate math behind the same signature.</reversibility>
  <read_first>
    - `packages/pr-rules/src/index.ts` — the barrel to extend
    - `packages/api-contracts/src/session.ts` — `WARMUP_SET_TYPE`, the value the generated rows will carry
    - `.planning/phases/05-in-gym-session-logging/05-RESEARCH.md` §3 — the ruleset, its `[ASSUMED]` provenance, and the note that Phase 6 replaces the flat rounding increment with the gym profile's real plate increments
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Copywriting Contract, "Warm-up sheet" row — the copy that states three sets, which this function's arity must match
    - `apps/mobile/lib/programs/next-up.ts` — the pure-module contract
  </read_first>
  <files>packages/pr-rules/src/warmup.ts, packages/pr-rules/src/index.ts, packages/pr-rules/src/__tests__/warmup.test.ts</files>
  <action>
Export from `packages/pr-rules/src/warmup.ts`:

`DEFAULT_ROUNDING_INCREMENT_KG = 2.5`.
`WARMUP_STEPS` — an exported readonly array of three `{ fraction, reps }` entries: 0.4 at 10 reps, 0.6 at 5 reps, 0.8 at 3 reps, in that order. Exporting the table rather than inlining the numbers is what makes replacing the scheme a data change, and it is the same "a list constant, not hardcoded literals" discipline D-13 asks for on the action bar.
`roundToIncrement(value: number, increment: number): number` — rounds to the nearest multiple, with a value exactly halfway between two multiples rounding UP. Pin the tie explicitly rather than relying on the language's default, because a tie that silently rounds down produces a warm-up one plate lighter than the table says.
`interface WarmupSet { weightKg: number; reps: number }`.
`warmupSets(workingWeightKg: number | null, roundingIncrementKg = DEFAULT_ROUNDING_INCREMENT_KG): WarmupSet[]` — returns an empty array when the working weight is null, not finite, zero or negative; otherwise maps `WARMUP_STEPS` to rounded weights in ascending order, dropping any step whose rounded weight is 0 so an empty-barbell row is never emitted.

Add a contract header comment recording the provenance honestly: this is an evidence-informed convention, not MacroFactor's undocumented internal formula, and no public specification exists (PITFALLS §8). Also record that Phase 6 replaces `roundingIncrementKg` with the active gym profile's real plate increments behind this same signature.

`packages/pr-rules/src/__tests__/warmup.test.ts`: cover a 100kg working weight producing exactly three rows at the expected rounded weights and rep counts; the ascending-order assertion; the halfway rounding tie in both directions; a very light working weight where the 40 percent step rounds to 0 and is dropped; and null, zero, negative, NaN and Infinity inputs each producing an empty array. Add a determinism case calling `warmupSets` twice with identical arguments and asserting deep equality, which is what makes 05-06's regenerate-rather-than-append behaviour safe.
  </action>
  <verify>
    <automated>pnpm --filter @fitness/pr-rules test &amp;&amp; pnpm --filter @fitness/pr-rules typecheck &amp;&amp; pnpm --filter @fitness/pr-rules build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/pr-rules/src/warmup.ts` exports `warmupSets`, `roundToIncrement`, `WARMUP_STEPS` and `DEFAULT_ROUNDING_INCREMENT_KG`.
    - A test asserts `warmupSets(100)` returns exactly 3 rows whose weights ascend.
    - A test asserts `roundToIncrement(101.25, 2.5)` returns 102.5 — the halfway tie rounds up.
    - Tests assert each of `null`, `0`, `-50`, `NaN` and `Infinity` returns an empty array.
    - A determinism test asserts two calls with identical arguments deep-equal each other.
    - `packages/pr-rules/src/index.ts` re-exports all three modules and `pnpm --filter @fitness/pr-rules build` emits `dist/index.d.ts`.
    - `pnpm --filter @fitness/pr-rules test` exits 0 across all three suites.
    - `pnpm test` at the repo root exits 0 (turbo picks up the new package with no config change).
  </acceptance_criteria>
  <done>Given a working weight, the package returns a deterministic three-step warm-up ladder — and honestly returns nothing when there is no sensible ladder to build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied set data → pure functions | Numbers originating from user keypad entry reach the detector and the scaler |

This package performs no I/O, holds no secrets, opens no network connection and reads no ambient state, so most STRIDE categories have no surface here. The categories that do apply are recorded rather than omitted.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Tampering | `DetectedPr.prType` | medium | mitigate | The union is imported from `@fitness/api-contracts`'s `PR_TYPES` rather than retyped, so a detector emitting a type the Postgres CHECK rejects is a compile error rather than a runtime rejection at push time (ASVS V5). |
| T-05-04-02 | Denial of service | `warmupSets` / `detectPrs` on hostile numeric input | low | mitigate | Every entry point guards non-finite and non-positive inputs and returns an empty result rather than looping or emitting NaN rows; pinned by explicit NaN and Infinity cases. |
| T-05-04-03 | Spoofing / Repudiation / Information disclosure / Elevation of privilege | — | low | accept | No identity, no persistence, no I/O, no privileged operation exists in this package. Recorded as not-applicable rather than omitted, so a later reader does not read the absence as an oversight. |
</threat_model>

## Flagged planner assumptions (unresolved edge probes)

- **LOG-18 (unclassified)** — the finish-summary requirement produced no shape classification. This plan resolves only its computational half; the rendering and the "did you beat it" comparison boundaries belong to 05-08. Flagged, not resolved here.

<verification>
- `pnpm --filter @fitness/pr-rules test` green across all three suites with non-zero counts.
- `pnpm --filter @fitness/pr-rules typecheck` and `build` green.
- `pnpm test`, `pnpm typecheck` and `pnpm lint` green at the repo root — the new package joins the turbo graph without a config change.
- The `[ASSUMED]` provenance of the warm-up scheme and the 1RM cutoff is recorded in the source, not only in research.
</verification>

<success_criteria>
- `@fitness/pr-rules` exists as a pure workspace package with a real, non-empty unit suite.
- `estimated1RM` returns null past the validity cutoff instead of a confident wrong number.
- `detectPrs` recognises exactly four PR types, excludes warm-ups and uncompleted sets, and treats a tie as not-a-PR.
- `warmupSets` is deterministic and returns nothing rather than a nonsense ladder for a nonsense input.
</success_criteria>

<output>
Create `.planning/phases/05-in-gym-session-logging/05-04-SUMMARY.md` when done.
</output>
