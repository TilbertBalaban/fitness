---
phase: 05-in-gym-session-logging
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - apps/api/src/sync/patch-update-set.ts
  - apps/api/src/sync/sync.service.ts
  - packages/api-contracts/src/sync.ts
  - apps/api/test/personal-record-sync.e2e-spec.ts
  - apps/api/test/session-annotations-sync.e2e-spec.ts
autonomous: true
requirements: [LOG-13, LOG-16, LOG-18]
windows_id_range: "123-132"

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`personal_record` has a server-side apply path: a pushed op reaches Postgres instead of being rejected as `unknown_table`, closing the half of D-30 that WINDOWS #19 records as missing."
    - "`toPersonalRecordValues(id, userId, data)` takes `userId` as a hard function argument sourced from the authenticated session and never reads `data.user_id`, matching `toUserExercisePreferenceValues` and `toExerciseValues` without exception (D-04 aggregate-root ownership, ASVS V4)."
    - "`personal_record` is wired as a SINGLETON root — added to `TABLE_MAP`, `SINGLETON_ROOT_TYPES`, `ROOT_TABLE_BY_TYPE` and `AGGREGATE_RANK` at rank 0 — because it owns no synced children and is never referenced as a parent, exactly like `exercise` and `user_preference`."
    - "`SESSION_STATUSES`, `SET_TYPES` and the new `PR_TYPES` validator Sets are all built from the `packages/api-contracts` tuples published in 05-02, never from retyped literals, so the client-facing `invalid_field` rejection and the Postgres CHECK can never drift apart (D-09)."
    - "`hasInvalidField` rejects a `personal_record` op whose `pr_type` is outside the vocabulary and whose `value` fails `isNonNegativeDecimalOrNull`, reusing the existing decimal validator rather than writing a second one (ASVS V5)."
    - "A PATCH naming only `notes` writes only the `notes` column on `logged_set`, `session_exercise` or `workout_session`; every other column on the row is untouched (LOG-16, `patchAwareSet`)."
    - "A PATCH naming `paused_at`, `accumulated_paused_seconds` and/or `rest_target_at` writes only those columns on `workout_session` (LOG-12's and LOG-08's write paths)."
    - "A PATCH naming `auto_advance_enabled` or `warmup_sets_enabled` writes only that column on the `user_preference` singleton row and validates as a boolean (LOG-13)."
    - "PowerSync remains the sole ingress: no REST controller is added that writes a set, a session, or a personal record (D-03)."
    - "`ops/powersync/sync-rules.yaml` needs no change — it already SELECTs `personal_record` for `auth.user_id()`, so wiring the push half completes the round trip on its own."
    - "`PUSH_DEFERRED_TABLES` no longer lists `personal_record` and `PUSH_APPLIED_TABLES` does, and the comment naming Phase 9 as its owner is corrected to Phase 5 (D-30 moved it)."
    - "WINDOWS #19's stale nine-tables claim is amended in place rather than left standing: Phase 4 already wired six of the nine, and this plan wires the seventh, leaving `equipment_profile`, `body_metric` and `progress_photo`."
    # --- Edge predicates: LOG-16 ---
    - "Pushing a note whose text equals the stored value is applied as a no-op-equivalent PATCH that changes only `notes` and bumps no other column (LOG-16 adjacency)."
    - "A push naming `notes` with a JSON null clears the column to SQL NULL; a push omitting `notes` entirely leaves the stored note intact (LOG-16 empty)."
    - "Three separate note PATCHes in one batch — one per table — each apply to their own table only, in the batch's aggregate rank order, with no cross-table read (LOG-16 ordering)."
    # --- Edge predicates: LOG-13 ---
    - "A `user_preference` PATCH naming `auto_advance_enabled` with a non-boolean is rejected as `invalid_field` before the apply phase (LOG-13 adjacency)."
    - "A `user_preference` push for a user with no existing row creates the row with both new flags at their true defaults (LOG-13 empty)."
    - "A batch containing both a `warmup_sets_enabled` PATCH and an `auto_advance_enabled` PATCH for the same user applies both without either clobbering the other (LOG-13 ordering)."
    # --- Edge predicate: LOG-18 apply path ---
    - statement: "PowerSync Service pull-side delivery of `personal_record` rows to a second device is asserted only by the already-shipped sync-rules query, not by a running service in this plan; a live cross-device pull needs the self-hosted PowerSync Service restarted against the current rules."
      verification: backstop
  artifacts:
    - apps/api/test/personal-record-sync.e2e-spec.ts
    - apps/api/test/session-annotations-sync.e2e-spec.ts
  key_links:
    - "`sync.service.ts`'s three validator Sets are constructed from `WORKOUT_SESSION_STATUSES`, `SET_TYPES` and `PR_TYPES` imported from `@fitness/api-contracts` — the same tuples the Postgres CHECK constraints in 05-02 were built from."
    - "`toPersonalRecordValues`'s `userId` parameter is fed from the authenticated session upstream in `applyBatch`, identically to `toExerciseValues` and `toUserExercisePreferenceValues`."
    - "`PERSONAL_RECORD_PATCH_FIELDS` maps `id` and `userId` to null so both are always written from the server-resolved values, never from the op payload."
---

## Phase Goal (verbatim from ROADMAP.md — not in user-story format)

> The user can walk into a gym with no signal and log a complete workout without friction. **This is the phase the app becomes real** — dogfooding starts here.

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| function | `toPersonalRecordValues` (in `apps/api/src/sync/sync.service.ts`) |
| const | `PERSONAL_RECORD_PATCH_FIELDS` (in `apps/api/src/sync/patch-update-set.ts`) |
| type | `PersonalRecordValues` (in `apps/api/src/sync/patch-update-set.ts`) |
| const | `PR_TYPES` validator Set (in `apps/api/src/sync/sync.service.ts`) |
| file | `apps/api/test/personal-record-sync.e2e-spec.ts` |
| file | `apps/api/test/session-annotations-sync.e2e-spec.ts` |

## Broken-windows ledger reservation

Reserved `.planning/WINDOWS.md` ids **123–132**. This plan also **amends entry #19 in place** (a correction to an existing row, not a new id).

<objective>
Complete the server half of the sync contract Phase 5 needs: `personal_record` gains an apply path so a PR written on the gym floor survives to Postgres and pulls back to the browser, and the three new annotation surfaces — notes, pause accounting, and the two workout preferences — become patchable columns validated against the vocabularies 05-02 published.

Purpose: D-30 makes `personal_record`'s missing apply path this phase's problem, not an assumption. Without it every PR row this phase writes is rejected as `unknown_table` and silently never leaves the device.
Output: one new singleton root wired through all four maps, three PATCH field-map extensions, tuple-sourced validators, and two new API e2e specs against real Postgres.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-in-gym-session-logging/05-CONTEXT.md
@.planning/phases/05-in-gym-session-logging/05-RESEARCH.md
@.planning/phases/05-in-gym-session-logging/05-PATTERNS.md
@.planning/phases/05-in-gym-session-logging/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source every session validator from the published tuples and make the new columns patchable</name>
  <precondition>The live Postgres reachable via the API's `DATABASE_URL` already has 05-02's ten columns and three CHECK constraints applied — `pnpm --filter api db:verify` is green.</precondition>
  <read_first>
    - `apps/api/src/sync/sync.service.ts` lines 1–200 — the import block, `TABLE_MAP`, `SINGLETON_ROOT_TYPES`, `ROOT_TABLE_BY_TYPE`, `AGGREGATE_RANK`, and lines 176–186 where `SESSION_STATUSES` / `SET_TYPES` are retyped literal Sets while `LOAD_TYPES` / `ROUTINE_STATUSES` are built from shared tuples — the target shape
    - `apps/api/src/sync/sync.service.ts` lines 640–740 — the `hasInvalidField` per-table branches, including the existing `status`, `set_type`, `weight_kg` and `load_type` checks and `isNonNegativeDecimalOrNull` at line 568
    - `apps/api/src/sync/patch-update-set.ts` — `PatchFieldMap`, `patchAwareSet`, and the `WORKOUT_SESSION_PATCH_FIELDS` / `SESSION_EXERCISE_PATCH_FIELDS` / `LOGGED_SET_PATCH_FIELDS` / `USER_PREFERENCE_PATCH_FIELDS` maps plus their `*Values` interfaces
    - `packages/api-contracts/src/session.ts` — the three tuples published in 05-02
    - `apps/api/src/db/schema/session.ts` and `preference.ts` — the columns added in 05-02, for the `*Values` interface fields
    - `apps/api/test/patch-partial-update.e2e-spec.ts` — the existing shape of a "PATCH writes only the named column" assertion
  </read_first>
  <files>apps/api/src/sync/patch-update-set.ts, apps/api/src/sync/sync.service.ts</files>
  <action>
Rebuild the two retyped literal Sets and add the new one. In `sync.service.ts`, import `WORKOUT_SESSION_STATUSES`, `SET_TYPES as SET_TYPE_TUPLE` and `PR_TYPES as PR_TYPE_TUPLE` from `@fitness/api-contracts`, then define `SESSION_STATUSES = new Set<string>(WORKOUT_SESSION_STATUSES)`, `SET_TYPES = new Set<string>(SET_TYPE_TUPLE)` and a new `PR_TYPES = new Set<string>(PR_TYPE_TUPLE)`, matching the `LOAD_TYPES` / `ROUTINE_STATUSES` construction two lines below them. Update the surrounding comment so it says these are built from the shared tuples for the same reason `LOAD_TYPES` is: the client-facing rejection and the Postgres CHECK cannot drift. `SESSION_STATUSES` now accepts `paused`, which is what makes D-29's pause writable through sync at all.

In `patch-update-set.ts`, extend the three existing `*Values` interfaces and their maps:
`WorkoutSessionValues` gains `notes: string | null`, `name: string | null`, `pausedAt: Date | null`, `accumulatedPausedSeconds: number`, `restTargetAt: Date | null`; `WORKOUT_SESSION_PATCH_FIELDS` gains `notes: 'notes'`, `name: 'name'`, `pausedAt: 'paused_at'`, `accumulatedPausedSeconds: 'accumulated_paused_seconds'`, `restTargetAt: 'rest_target_at'`.
`SessionExerciseValues` gains `notes: string | null` and `removedAt: Date | null`; `SESSION_EXERCISE_PATCH_FIELDS` gains `notes: 'notes'` and `removedAt: 'removed_at'`.
`LoggedSetValues` gains `notes: string | null`; `LOGGED_SET_PATCH_FIELDS` gains `notes: 'notes'`.
`UserPreferenceValues` gains `autoAdvanceEnabled: boolean` and `warmupSetsEnabled: boolean`; `USER_PREFERENCE_PATCH_FIELDS` gains `autoAdvanceEnabled: 'auto_advance_enabled'` and `warmupSetsEnabled: 'warmup_sets_enabled'`.
Every one of these maps to its wire key rather than to null, because each is a genuinely client-owned value — contrast `id` / `userId`, which map to null precisely because they are server-resolved.

In `sync.service.ts`'s `to*Values` functions, read the new fields off the op data in the file's existing style: notes as `d.notes ?? null` with an empty string normalised to null so "no note" has one representation; `paused_at`, `rest_target_at` and `removed_at` through the same `d.x ? new Date(d.x) : null` form the file already uses for `archived_at`; `accumulated_paused_seconds` defaulting to 0; the two preference flags defaulting to true.

In `hasInvalidField`, add per-table checks in the existing style: reject a `notes` or `name` value that is present and neither a string nor null; reject a non-integer or negative `accumulated_paused_seconds`; reject a `paused_at` / `rest_target_at` / `removed_at` that is present and not a parseable ISO timestamp string or null; reject a non-boolean `auto_advance_enabled` or `warmup_sets_enabled`.
  </action>
  <verify>
    <automated>pnpm --filter api typecheck &amp;&amp; pnpm --filter api test:e2e -- patch-partial-update</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/sync/sync.service.ts` imports the three tuples from `@fitness/api-contracts`, and `grep -n "new Set(\['in_progress'" apps/api/src/sync/sync.service.ts` returns no match — the retyped literal is gone.
    - `sync.service.ts` defines `PR_TYPES` as a Set built from the imported tuple.
    - `apps/api/src/sync/patch-update-set.ts`'s `WORKOUT_SESSION_PATCH_FIELDS` contains `notes`, `name`, `pausedAt`, `accumulatedPausedSeconds` and `restTargetAt` entries; `SESSION_EXERCISE_PATCH_FIELDS` contains `notes` and `removedAt`; `LOGGED_SET_PATCH_FIELDS` contains `notes`; `USER_PREFERENCE_PATCH_FIELDS` contains `autoAdvanceEnabled` and `warmupSetsEnabled`.
    - `pnpm --filter api typecheck` exits 0 — the `PatchFieldMap<V>` type forces every new `*Values` field to have a map entry, so a forgotten entry is a compile error rather than a silent omission.
    - `pnpm --filter api test:e2e -- patch-partial-update` exits 0, proving no existing PATCH behaviour regressed.
  </acceptance_criteria>
  <done>Every session-side vocabulary is validated from the published tuple, and the ten columns 05-02 added are patchable and type-checked end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Wire personal_record's apply path as a singleton root</name>
  <reversibility rating="one-way">D-30: `personal_record` rows written under this phase's rule set are reconciled under Phase 10's; a row that reaches Postgres attributed to the wrong user or with an out-of-vocabulary `pr_type` cannot be undone from the client.</reversibility>
  <read_first>
    - `apps/api/src/sync/sync.service.ts` lines 65–175 — `TABLE_MAP`, `SINGLETON_ROOT_TYPES`, `ROOT_TABLE_BY_TYPE`, `AGGREGATE_RANK`: the four places a new singleton root must be added
    - `apps/api/src/sync/sync.service.ts` lines 380–440 — `toExerciseValues` and `toUserExercisePreferenceValues` and the ownership comment above them ("userId always comes from the authenticated session argument, never from data"); this is the exact precedent `toPersonalRecordValues` mirrors
    - `apps/api/src/sync/sync.service.ts` around line 568 and 1580–1620 — `isNonNegativeDecimalOrNull` and the `onConflictDoUpdate` + `patchAwareSet` apply block each table gets
    - `apps/api/src/db/schema/records.ts` — `personalRecord`'s columns and its `personal_record_pr_type_check` constraint from 05-02
    - `packages/api-contracts/src/sync.ts` — `SYNCED_TABLES`, `PUSH_APPLIED_TABLES`, `PUSH_DEFERRED_TABLES` and the comment naming Phase 9 as `personal_record`'s owner
    - `apps/api/test/exercise-sync.e2e-spec.ts` and `apps/api/test/user-exercise-preference.e2e-spec.ts` — the singleton-root e2e shape the new spec mirrors, including how each authenticates and pushes
    - `ops/powersync/sync-rules.yaml` — confirm the existing `SELECT * FROM personal_record WHERE user_id = auth.user_id()` line; the pull half needs no change
  </read_first>
  <files>apps/api/src/sync/patch-update-set.ts, apps/api/src/sync/sync.service.ts, packages/api-contracts/src/sync.ts, apps/api/test/personal-record-sync.e2e-spec.ts</files>
  <action>
Add `personal_record` to all four maps in `sync.service.ts`: `TABLE_MAP` (mapping to the `personalRecord` Drizzle table imported from the records schema), `SINGLETON_ROOT_TYPES`, `ROOT_TABLE_BY_TYPE`, and `AGGREGATE_RANK` at rank 0 alongside the other singletons. It is a singleton root and not an aggregate root because it owns no synced children and is never named as a parent by another synced op — the same reasoning `exercise` and `user_preference` already carry.

Add `PersonalRecordValues` to `patch-update-set.ts` with fields `id: string`, `userId: string`, `exerciseId: string`, `prType: string`, `value: string`, `loggedSetId: string | null`, `achievedAt: Date`, `reconciledAt: Date | null`. Add `PERSONAL_RECORD_PATCH_FIELDS` mapping `id` and `userId` to null (always written from the server-resolved values) and every other field to its snake_case wire key. `value` is typed `string` and not `number` for the same reason `weightKg` is: Drizzle surfaces `numeric` as a string, and a value that never becomes a binary float cannot accumulate conversion error across a lifetime of aggregation.

Add `toPersonalRecordValues(id, userId, data)` immediately beside `toUserExercisePreferenceValues`, in its exact shape and with the same ownership comment restated: `userId` is a hard function argument sourced from the authenticated session, never `data.user_id` — a PUT naming another user's id in its payload is stored against the pusher's own id regardless. Map `exercise_id`, `pr_type`, `value`, `logged_set_id`, `achieved_at` and `reconciled_at` from the op data.

Add a `hasInvalidField` branch for `personal_record`: reject when `pr_type` is present and not a member of the `PR_TYPES` Set from Task 1; reject when `value` fails the existing `isNonNegativeDecimalOrNull` (reuse it directly — do not write a second decimal validator); reject when `exercise_id` is present and not a non-empty string; reject when `achieved_at` is present and not a parseable ISO timestamp.

Add the apply block for `personal_record` alongside the other singleton roots' blocks, using `onConflictDoUpdate` with `patchAwareSet(op, personalRecordValues, PERSONAL_RECORD_PATCH_FIELDS)` and the `serverSeq: nextSeq` bump the other root tables take.

In `packages/api-contracts/src/sync.ts`, move `'personal_record'` from `PUSH_DEFERRED_TABLES` into `PUSH_APPLIED_TABLES` and rewrite the two comments that reference it: the deferred block's "personal_record is Phase 9 (Records & Client Analytics — ANLY-01 owns PR detection)" is now wrong — D-30 moved PR detection into Phase 5, and Phase 9 keeps browsing, retrospective reconciliation and cross-device authority. Say that, in the file's existing comment voice.

Author `apps/api/test/personal-record-sync.e2e-spec.ts` mirroring `exercise-sync.e2e-spec.ts`'s structure against real Postgres: (1) a PUT of a valid `personal_record` op reaches Postgres with the pusher's own `user_id`; (2) a PUT whose payload names a DIFFERENT user's `user_id` is still stored against the pusher's id — the ownership assertion, and the one case that must never be dropped; (3) an op whose `pr_type` is outside the vocabulary is rejected as `invalid_field` before the apply phase; (4) an op whose `value` is negative is rejected; (5) a bogus `pr_type` that somehow reached a direct INSERT is rejected by the database CHECK (cross-check against 05-02's teeth case); (6) a PATCH naming only `reconciled_at` leaves `value` and `pr_type` unchanged.
  </action>
  <verify>
    <automated>pnpm --filter api test:e2e -- personal-record-sync</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/sync/sync.service.ts` names `personal_record` in `TABLE_MAP`, `SINGLETON_ROOT_TYPES`, `ROOT_TABLE_BY_TYPE` and `AGGREGATE_RANK`; `grep -c "personal_record" apps/api/src/sync/sync.service.ts` reports at least 5.
    - `apps/api/src/sync/sync.service.ts` defines `toPersonalRecordValues` whose second parameter is `userId: string`, and a source scan of that function body for `data.user_id` / `d.user_id` returns no match.
    - `packages/api-contracts/src/sync.ts`'s `PUSH_APPLIED_TABLES` contains `'personal_record'` and `PUSH_DEFERRED_TABLES` does not.
    - `pnpm --filter api test:e2e -- personal-record-sync` exits 0 with all six cases passing, including the cross-user ownership case.
    - `pnpm --filter api test:e2e -- sync-push` and `pnpm --filter api test:e2e -- sync-aggregate` both still exit 0 — the new root did not disturb aggregate resolution.
    - `pnpm --filter api typecheck` exits 0.
  </acceptance_criteria>
  <done>A `personal_record` pushed from a device reaches Postgres attributed to the authenticated user, is rejected when its `pr_type` or `value` is invalid, and pulls back down through the sync-rules query that already existed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove notes, pause and the workout preferences round-trip, and correct the stale ledger entry</name>
  <read_first>
    - `apps/api/test/patch-partial-update.e2e-spec.ts` — the "a PATCH writes only the columns it named" assertion shape
    - `apps/api/test/user-exercise-preference.e2e-spec.ts` — the singleton-row push shape for `user_preference`-class tables
    - `apps/api/test/program-sync.e2e-spec.ts` — the multi-table batch shape
    - `apps/api/src/sync/patch-update-set.ts` — the maps extended in Task 1
    - `.planning/WINDOWS.md` entry #19 — the stale nine-unwired-tables claim to amend
  </read_first>
  <files>apps/api/test/session-annotations-sync.e2e-spec.ts</files>
  <action>
Author `apps/api/test/session-annotations-sync.e2e-spec.ts` against real Postgres, covering the three annotation surfaces 05-02 added:

Notes (LOG-16): a batch pushing one note PATCH each to `workout_session`, `session_exercise` and `logged_set` applies all three, each to its own table, leaving every other column on all three rows byte-identical to before the push. A second push of the identical note text still changes only `notes`. A push naming `notes` with a JSON null clears the stored note to SQL NULL. A push that omits `notes` entirely leaves an existing note intact — this is the `patchAwareSet` contract and the case that catches a map entry accidentally set to null.

Pause (LOG-12): a PATCH naming `paused_at` sets it and leaves `accumulated_paused_seconds` unchanged; a follow-up PATCH naming both `paused_at` (null) and `accumulated_paused_seconds` clears the first and increments the second; a PATCH naming `status` with `paused` is accepted, and one naming a value outside the vocabulary is rejected as `invalid_field`.

Preferences (LOG-13, LOG-17): a `user_preference` push for a user with no existing row creates it with `auto_advance_enabled` and `warmup_sets_enabled` both true; a PATCH naming only `auto_advance_enabled` with false leaves `warmup_sets_enabled` true and `weight_unit` unchanged; a PATCH naming `auto_advance_enabled` with a string is rejected as `invalid_field`.

Then amend `.planning/WINDOWS.md` entry #19 in place with `gsd-tools windows` (do not renumber it, do not add a new id for the correction): the claim that nine tables have no apply path is stale — Phase 4 wired `routine`, `routine_day`, `routine_exercise`, `user_preference`, `routine_cycle` and `routine_exercise_cycle_target`, and this plan wires `personal_record`, leaving `equipment_profile` (Phase 6), `body_metric` and `progress_photo` (Phase 12). Record the amendment text so a later reader is not misled into re-solving a solved problem.

Also file one new ledger entry in the reserved 123–132 range for the pull-side backstop: the PowerSync Service was not restarted against `sync-rules.yaml` in this plan, so cross-device delivery of `personal_record` rows rests on the already-shipped query rather than on an observed pull.
  </action>
  <verify>
    <automated>pnpm --filter api test:e2e -- session-annotations-sync</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter api test:e2e -- session-annotations-sync` exits 0 with cases covering all three surfaces above.
    - The notes case asserts a full-row equality check on every column other than `notes` before and after the push, on all three tables.
    - The preference case asserts a fresh `user_preference` row is created with both new flags true.
    - `.planning/WINDOWS.md` entry #19's description names `equipment_profile`, `body_metric` and `progress_photo` as the remaining unwired tables and no longer claims nine.
    - A ledger entry in the 123–132 range records the un-restarted PowerSync Service as an unrun-verify against ROADMAP Phase 999.1.
    - `pnpm --filter api test:e2e` exits 0 for the full API suite.
  </acceptance_criteria>
  <done>Notes at three levels, the pause pair, and the two workout preferences each round-trip through the real sync apply path against real Postgres, and the ledger no longer carries a stale claim about the sync surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → `/v1/sync/push` | Untrusted op payloads carrying `user_id`, `pr_type`, `value`, `notes` and timestamp fields cross into Postgres |
| PowerSync Service → client pull | Rows leave Postgres scoped by `auth.user_id()` in `sync-rules.yaml` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Spoofing / Elevation of privilege | `toPersonalRecordValues` | high | mitigate | `userId` is a hard function argument sourced from the authenticated session; the function body never reads `data.user_id`. Pinned by e2e case 2 in `personal-record-sync.e2e-spec.ts`, which pushes a payload naming another user's id and asserts the row is stored against the pusher (ASVS V4). |
| T-05-03-02 | Tampering | `hasInvalidField` for `personal_record` | high | mitigate | `pr_type` validated against the `PR_TYPES` Set built from the published tuple; `value` validated by the existing `isNonNegativeDecimalOrNull`. The `personal_record_pr_type_check` Postgres constraint from 05-02 is the backstop for anything that bypasses the app layer (ASVS V5). |
| T-05-03-03 | Tampering | `patchAwareSet` on the new columns | medium | mitigate | Each new field maps to its own wire key, so a PATCH can only write the columns it names. The `PatchFieldMap<V>` type makes a forgotten entry a compile error. Pinned by the full-row-equality assertion in `session-annotations-sync.e2e-spec.ts`. |
| T-05-03-04 | Information disclosure | `notes` free text crossing the sync boundary | medium | mitigate | Note text is stored and returned verbatim as data, never interpreted; every read path is scoped to `auth.user_id()` by the existing sync rules. No new endpoint exposes it. Rendering safety is 05-06's concern on the client. |
| T-05-03-05 | Repudiation | `personal_record.achieved_at` from the client | low | accept | Single-user personal training log; a client-stamped achievement time is not a competitive claim. Phase 10 owns cross-device PR authority. |
</threat_model>

## Flagged planner assumptions (unresolved edge probes)

- **LOG-18 (unclassified)** — "workout summary on finishing: muscles trained, PRs achieved, per-exercise breakdown with estimated 1RM" produced no shape classification. This plan resolves only its transport half (a PR row reaching Postgres). The computation and rendering boundaries are 05-04's and 05-08's. Flagged, not resolved here.

<verification>
- `pnpm --filter api test:e2e` green for the whole API suite, including the two new specs.
- `pnpm --filter api typecheck` and `pnpm --filter @fitness/api-contracts typecheck` green.
- `pnpm typecheck` and `pnpm lint` green at the repo root.
- The PowerSync Service pull-side backstop is filed as a ledger entry in the reserved range, never silently skipped.
</verification>

<success_criteria>
- `personal_record` is in `PUSH_APPLIED_TABLES` and a pushed PR row reaches Postgres under the pusher's own user id.
- A push naming another user's id in a `personal_record` payload does not attribute the row to that user.
- Notes, pause fields and the two workout preferences each apply as narrow PATCHes without touching neighbouring columns.
- Every session-side validator Set is built from a `packages/api-contracts` tuple.
- WINDOWS #19 no longer claims nine unwired tables.
</success_criteria>

<output>
Create `.planning/phases/05-in-gym-session-logging/05-03-SUMMARY.md` when done.
</output>
