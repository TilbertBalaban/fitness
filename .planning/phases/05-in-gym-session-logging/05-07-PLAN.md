---
phase: 05-in-gym-session-logging
plan: 07
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-05"]
files_modified:
  - apps/mobile/app/(tabs)/workout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/components/WorkoutInProgressBanner.tsx
  - apps/mobile/components/__tests__/WorkoutInProgressBanner.test.tsx
  - apps/mobile/lib/db/session-lifecycle.ts
  - apps/mobile/lib/db/__tests__/session-lifecycle.test.ts
  - apps/mobile/lib/db/preferences.ts
  - apps/mobile/lib/db/__tests__/preferences.test.ts
  - apps/mobile/lib/session/finish-session.ts
  - apps/mobile/lib/session/__tests__/auto-advance.test.ts
  - apps/mobile/lib/session/auto-advance.ts
  - apps/mobile/e2e/durability.spec.ts
  - apps/mobile/e2e/session-lifecycle.spec.ts
autonomous: true
requirements: [LOG-02, LOG-11, LOG-12, LOG-13]
windows_id_range: "163-172"

estimate:
  tokens: 160000
  raw_tokens: 160000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All session-creation paths funnel through one `startSession` call rather than three call sites each stamping their own columns: starting today's programmed workout (LOG-01), starting a one-off (LOG-02), and — in 05-10 — adding a past workout (D-33)."
    - "A one-off session carries `routine_day_id` null, so `addSessionExercise` takes the `EMPTY_PRESCRIPTION` path and every target renders as the em dash rather than 0 or a blank (LOG-02)."
    - "Pause is a deliberate user action that stops the duration clock; a crash is not a pause, and the two share no state (D-29)."
    - "Displayed duration derives from `started_at` plus accumulated paused time — `elapsedWorkoutSeconds` from 05-05 — never from a single elapsed subtraction, which is why the pause pair exists as two columns (D-29, LOG-11)."
    - "An in-progress session surfaces as an unmissable banner above the Home tab's existing next-up card, carrying both a resume and a discard action, rather than silently auto-resuming (D-28)."
    - "The banner's presence costs nothing on the common no-session path: the conditional is on the query, not only on the render, mirroring `loadNextUp`'s cheap early-out (D-28)."
    - "Discard writes `status` of the discarded value after an explicit destructive confirmation; there is no automatic timeout-based abandoned state, because nothing in this phase's requirements asks for one."
    - "Auto-advance turns the pager to the next exercise when the current exercise's working sets are all complete, and is gated on `user_preference.auto_advance_enabled`, which defaults to true and is togglable from workout settings (LOG-13)."
    - "Every auto-advance and timer call site is gated on the typed `SessionScreenMode` value from 05-01, never on `session.status` or a boolean prop (D-32, UI-SPEC R10)."
    - "Force-quitting mid-workout and relaunching restores the session with every logged set intact, including warm-ups and an open pause — proven in a real browser, not asserted."
    # --- Edge predicates: LOG-13 (behavioural half) ---
    - "Auto-advance fires exactly when the last remaining WORKING set of the current exercise becomes completed; completing a warm-up set never triggers it (LOG-13 adjacency, behavioural)."
    - "On the last exercise of the session, auto-advance does not wrap to the first exercise and leaves the pager index unchanged (LOG-13 empty, behavioural)."
    - "With the preference off, completing the final working set changes no pager index and performs no navigation (LOG-13 ordering, behavioural)."
    # --- UI-SPEC E7 Active Workout screen (empty/error/loading half) ---
    - "The Workout tab with no session and no active program renders the heading No active program with the body Build or activate a program, or start a one-off workout., linking to the Program Library and to a one-off start action (UI-SPEC E7 empty)."
    - "There is no loading state for already-seeded local data; the session reads from local SQLite and cold start stays inside Phase 1's web bound (UI-SPEC E7 loading, R2, R6)."
    - "A load failure renders the shipped error pattern verbatim: Workout couldn't load, with the body Restart the app to try again. Your programs and history are safe. (UI-SPEC E7 error)."
    - "Long text on this screen wraps and grows, including the editing-mode date line, which wraps to a second line rather than truncating (UI-SPEC E7 long text, R4)."
    # --- UI-SPEC E8 Home In-Progress Banner ---
    - "The banner is not rendered at all when no in-progress or paused session exists, and the query itself is conditional so the common no-session path costs nothing; there is no empty-state variant of the banner (UI-SPEC E8 empty)."
    - "The banner has no loading state: it either renders complete or is absent, and the elapsed duration is recomputed on Home-tab focus rather than streamed (UI-SPEC E8 loading, R6)."
    - statement: "What Home renders when the in-progress-session query itself errors is pinned by a held-out UI-state test, because the banner's absence is otherwise indistinguishable from a failed query (UI-SPEC E8 error backstop)."
      verification: backstop
    - "Populated banner: a `bg-secondary` card with a `border-l-4 border-accent` left bar, the heading Workout in Progress with the elapsed duration inline in muted Label, and two side-by-side actions — Resume Workout as the accent primary CTA and Discard as destructive text opening its confirmation first (UI-SPEC E8 populated)."
    - "The paused variant is the only alternate rendering: the heading reads Workout Paused and the layout is otherwise identical (UI-SPEC E8 partial)."
    - "The card is full-width and its two actions wrap and grow rather than truncating their labels; the card grows in height at large font scale (UI-SPEC E8 overflow, R4)."
    - "At most one in-progress session can exist, so the banner is strictly zero-or-one with no many case and no plural copy (UI-SPEC E8 zero/one/many)."
    - "Heading and action labels wrap and grow, and the elapsed-duration string is bounded by its `H:MM` formatting (UI-SPEC E8 long text, R4)."
  artifacts:
    - apps/mobile/components/WorkoutInProgressBanner.tsx
    - apps/mobile/lib/db/session-lifecycle.ts
    - apps/mobile/lib/db/preferences.ts
    - apps/mobile/lib/session/auto-advance.ts
    - apps/mobile/lib/session/finish-session.ts
    - apps/mobile/e2e/session-lifecycle.spec.ts
  key_links:
    - "`startOneOffSession` calls the same `startSession` in `apps/mobile/lib/db/log-set.ts` that `startWorkoutFromProgram` calls — one funnel, so `captureCalendarDay` stamps `timezone`/`local_date` in exactly one place (D-06, D-33)."
    - "`WorkoutInProgressBanner` is rendered by `apps/mobile/app/(tabs)/index.tsx` above the existing Phase 4 next-up card, and its data comes from a query the screen only issues when a cheap guard says a session may exist."
    - "`shouldAutoAdvance` is a pure function taking the exercise's set rows, the preference flag and the pager index — no database, no navigation — so every boundary is a unit test rather than a browser case."
    - "`finishSession` is the single exit from the live screen; 05-08 changes only its navigation target to the summary route."
---

## Phase Goal (verbatim from ROADMAP.md — not in user-story format)

> The user can walk into a gym with no signal and log a complete workout without friction. **This is the phase the app becomes real** — dogfooding starts here.

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| file | `apps/mobile/lib/db/session-lifecycle.ts` |
| function | `startOneOffSession`, `pauseSession`, `resumeSession`, `discardSession`, `completeSession`, `loadInProgressSessionSummary` |
| file | `apps/mobile/lib/db/preferences.ts` |
| function | `loadWorkoutPreferences`, `setWorkoutPreference` |
| type | `WorkoutPreferences` |
| file | `apps/mobile/lib/session/auto-advance.ts` |
| function | `shouldAutoAdvance` |
| file | `apps/mobile/lib/session/finish-session.ts` |
| function | `finishSession` |
| file | `apps/mobile/components/WorkoutInProgressBanner.tsx` |
| component | `WorkoutInProgressBannerView`, `WorkoutInProgressBanner` |
| file | `apps/mobile/e2e/session-lifecycle.spec.ts` |

## Broken-windows ledger reservation

Reserved `.planning/WINDOWS.md` ids **163–172**.

<objective>
Close the loop around the live screen: a workout can be started without a program, paused and resumed deliberately, finished, discarded, found again from Home after the app was closed, and it advances itself between exercises unless the user turns that off.

Purpose: D-28 exists precisely because a session abandoned days ago and forgotten is the case silent auto-resume handles worst, and D-29 exists because a crash is not a pause. Both are lifecycle, not logging.
Output: the lifecycle write module, the Home banner, the auto-advance rule, the workout-settings surface, and the browser case that proves force-quit recovery holds with warm-ups and an open pause outstanding.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-in-gym-session-logging/05-CONTEXT.md
@.planning/phases/05-in-gym-session-logging/05-RESEARCH.md
@.planning/phases/05-in-gym-session-logging/05-PATTERNS.md
@.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md
@.planning/phases/05-in-gym-session-logging/05-01-SUMMARY.md
@.planning/phases/05-in-gym-session-logging/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start a one-off workout, and give the Workout tab its empty and error states</name>
  <read_first>
    - `apps/mobile/lib/db/log-set.ts` — `startSession`, `addSessionExercise`, `EMPTY_PRESCRIPTION`, and `startWorkoutFromProgram` from 05-01; the one funnel every creation path must go through
    - `apps/mobile/lib/calendar-day.ts` — `captureCalendarDay`, the only writer of `timezone` and `local_date` (D-06); this task adds no second writer
    - `apps/mobile/components/ExercisePickerModal.tsx` — the Phase 4 multi-select picker, reused through its existing props and NOT modified (05-06 also consumes it this wave)
    - `apps/mobile/app/(tabs)/workout.tsx` — the screen from 05-01 and 05-05, including `deriveWorkoutScreenState` and the mode provider
    - `apps/mobile/app/(tabs)/index.tsx` — `displayOrDash` and the existing error/empty-state chrome to reuse verbatim
    - `apps/mobile/components/ErrorBanner.tsx`, `apps/mobile/components/PrimaryButton.tsx` — the shipped error and CTA components
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Copywriting Contract (empty state, error state rows), §Screen & Component Inventory
  </read_first>
  <files>apps/mobile/lib/db/session-lifecycle.ts, apps/mobile/app/(tabs)/workout.tsx, apps/mobile/lib/db/__tests__/session-lifecycle.test.ts</files>
  <action>
Create `apps/mobile/lib/db/session-lifecycle.ts` and add `startOneOffSession({ exerciseIds }, db)` (LOG-02): call the SAME `startSession` from `apps/mobile/lib/db/log-set.ts` with `routineDayId: null`, then `addSessionExercise` per selected exercise with no `routineExerciseId` and no `cycleId`, so the `EMPTY_PRESCRIPTION` path applies. Do not add a second `db.insert(workoutSession)` anywhere — D-33 requires all three creation paths to funnel through one call, and a second call site is a second place that stamps `timezone` and `local_date`, which is `PITFALLS.md` §12 reopened.

`apps/mobile/app/(tabs)/workout.tsx`: extend `deriveWorkoutScreenState` so its `'no-session'` branch splits into the two states the UI-SPEC names — a state with an active program, offering "Start today's workout", and a state with none. The no-program state renders the heading "No active program" and the body "Build or activate a program, or start a one-off workout.", with a link to the Program Library and a "Start a one-off workout" action opening the unmodified `ExercisePickerModal` in multi-select and passing the selection to `startOneOffSession`. The one-off start action is available from BOTH no-session states, because a user with an active program may still want an unplanned session. The error state reuses the shipped copy verbatim: "Workout couldn't load" / "Restart the app to try again. Your programs and history are safe." There is no loading state for already-seeded local data (R6).

Also handle the two `resolveNextUp` results that are neither a workout nor a missing program: `time-off` and `program-complete`. Render each with its own line rather than falling through to "No active program", and keep the one-off start action available in both, since the whole point of a one-off is that it is not on the program.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "(session-lifecycle|workout)"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/lib/db/session-lifecycle.ts` exports `startOneOffSession`, and a source scan of the whole `apps/mobile/lib/db/` directory for `insert(workoutSession)` returns exactly one match, in `log-set.ts` (D-33's single funnel).
    - `session-lifecycle.test.ts` asserts a one-off session's row has `routine_day_id` null and that each of its `session_exercise` rows has all five `target_*` columns null.
    - `session-lifecycle.test.ts` asserts a one-off session's `timezone` and `local_date` are stamped by `captureCalendarDay` with the same values `startWorkoutFromProgram` would produce for the same clock.
    - `workout.test.tsx` asserts `deriveWorkoutScreenState` returns a distinct state for each of: failed, no active program, program on time off, program complete, workout available, session open.
    - `workout.test.tsx` asserts the no-program empty state renders the exact heading and body strings from the copywriting contract, and that the one-off start action is present in every no-session state.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>A user with no program, a user on time off, and a user whose block just ended can all start an unplanned workout from the Workout tab, and its rows degrade legibly to em dashes rather than showing invented targets.</done>
</task>

<task type="auto">
  <name>Task 2: Pause, resume, finish and discard — and prove force-quit recovery holds through all of them</name>
  <reversibility rating="one-way">D-29: duration accounting derived from a single elapsed subtraction cannot be converted into pause-aware accounting after sessions exist; every finished session's recorded duration would be wrong.</reversibility>
  <read_first>
    - `apps/mobile/lib/rest-timer.ts` — `elapsedWorkoutSeconds` and `formatClock` from 05-05, the pure accounting this task writes rows for
    - `apps/mobile/lib/db/schema.ts` — `workoutSession.pausedAt`, `accumulatedPausedSeconds`, `restTargetAt`, `status`, `endedAt`
    - `packages/api-contracts/src/session.ts` — `WORKOUT_SESSION_STATUSES`; use the exported values, never a string literal at a call site (D-09)
    - `apps/mobile/components/ArchiveDialog.tsx` — the shipped destructive-confirmation shape the discard dialog reuses
    - `apps/mobile/components/RestTimerBar.tsx` — the paused rendering (frozen muted readout, "Paused" label) this task drives
    - `apps/mobile/e2e/durability.spec.ts` — the close/reopen harness pattern the new recovery case extends
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Header Timer Bar (paused state), §Copywriting Contract (Discard Workout confirmation, Time-off / paused states, Primary CTA)
  </read_first>
  <files>apps/mobile/lib/db/session-lifecycle.ts, apps/mobile/lib/session/finish-session.ts, apps/mobile/app/(tabs)/workout.tsx, apps/mobile/lib/db/__tests__/session-lifecycle.test.ts, apps/mobile/e2e/durability.spec.ts, apps/mobile/e2e/session-lifecycle.spec.ts</files>
  <action>
Add to `apps/mobile/lib/db/session-lifecycle.ts`:
`pauseSession(sessionId, now, db)` — sets `paused_at` to `now` and `status` to the paused value from `WORKOUT_SESSION_STATUSES`. Leaves `accumulated_paused_seconds` alone.
`resumeSession(sessionId, now, db)` — reads the open `paused_at`, adds the elapsed pause to `accumulated_paused_seconds`, clears `paused_at` to null, and sets `status` back to the in-progress value. A resume with no open pause is a no-op rather than an error, so a double-tap cannot inflate the accumulator.
`completeSession(sessionId, now, db)` — closes any open pause first (so a session finished while paused accounts correctly), then sets `ended_at`, `status` to the completed value, and clears `rest_target_at`.
`discardSession(sessionId, db)` — sets `status` to the discarded value. It does not delete rows; D-28's discard is a status transition, and no requirement in this phase asks for a hard delete of an abandoned session.
`loadInProgressSessionSummary(userId, db)` — one narrow select returning `{ id, startedAt, status, pausedAt, accumulatedPausedSeconds }` for the user's single in-progress-or-paused session, or null, with `loadNextUp`'s cheap early-out when `userId` is falsy. This is the query the Home banner uses, and it selects five columns rather than the session tree.

`apps/mobile/lib/session/finish-session.ts` — `finishSession(sessionId, router, db)`: calls `completeSession`, cancels any scheduled rest alert, and navigates. In this plan the navigation target is the Home tab; 05-08 changes only that one line to the summary route, which is why it is a named function rather than an inline handler.

`apps/mobile/app/(tabs)/workout.tsx`: add the Pause and Resume actions and the "Finish Workout" primary CTA. Pause and Resume live on the session menu, not the action bar — D-29 makes pause a deliberate act, distinct from and unrelated to force-quit recovery, and the two share no state. Discard opens the `ArchiveDialog`-shaped confirmation with the exact copy from the contract: title "Discard Workout", body "This workout and everything logged in it will be deleted. This can't be undone. Discard anyway?", confirm "Discard". While paused, the header bar's left column reads "Paused" with a frozen muted readout, driven by passing the open `pausedAt` into `elapsedWorkoutSeconds`.

Extend `apps/mobile/e2e/durability.spec.ts` with the recovery case success criterion 4 names, harder than the existing one: seed a session, generate warm-up rows, complete two working sets, open a pause, then close and reopen the harness database and assert every row survives with its values, that `accumulated_paused_seconds` and the open `paused_at` are both intact, and that the restored duration equals the pre-close duration rather than jumping by the closed interval. A crash is not a pause — the open `paused_at` must still be open, not converted.

Author `apps/mobile/e2e/session-lifecycle.spec.ts` in the `durability` project: drive the real workout screen through pause, resume, finish and discard, asserting the header's frozen readout while paused, that resume increments the accumulator by roughly the paused wall-clock interval, that finish sets `ended_at` and the completed status, and that discard requires the confirmation before writing.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "session-lifecycle" &amp;&amp; pnpm --filter mobile test:e2e:durability -- --grep "durability|session-lifecycle"</automated>
  </verify>
  <acceptance_criteria>
    - `session-lifecycle.ts` exports `pauseSession`, `resumeSession`, `completeSession`, `discardSession` and `loadInProgressSessionSummary`, and a source scan of the file finds no bare `'in_progress'` / `'paused'` / `'completed'` / `'discarded'` string literal — every status comes from the `@fitness/api-contracts` tuple.
    - `session-lifecycle.test.ts` asserts `resumeSession` called twice in a row increments `accumulated_paused_seconds` exactly once.
    - `session-lifecycle.test.ts` asserts `completeSession` on a paused session closes the pause before stamping `ended_at`, and that the resulting accumulated pause includes the final open interval.
    - `session-lifecycle.test.ts` asserts `loadInProgressSessionSummary` issues no query at all when `userId` is null (D-28's cost constraint), and selects at most five columns when it does.
    - `pnpm --filter mobile test:e2e:durability -- durability.spec.ts` exits 0 including the warm-up-plus-pause recovery case, with an assertion that the open `paused_at` is still open after reopen.
    - `pnpm --filter mobile test:e2e:durability -- session-lifecycle.spec.ts` exits 0 including the discard-confirmation case.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>Pausing stops the clock and resuming restarts it without losing time; finishing and discarding are distinct, deliberate exits; and force-quitting mid-workout with warm-ups logged and a pause open loses nothing.</done>
</task>

<task type="auto">
  <name>Task 3: The Home in-progress banner, auto-advance, and workout settings</name>
  <read_first>
    - `apps/mobile/app/(tabs)/index.tsx` — `deriveHomeScreenState`, the next-up card block the banner sits above, and the `useFocusEffect` refocus pattern already used there
    - `apps/mobile/lib/db/session-lifecycle.ts` — `loadInProgressSessionSummary` and `discardSession` from Task 2
    - `apps/mobile/lib/rest-timer.ts` — `elapsedWorkoutSeconds`, `formatClock` for the banner's inline duration
    - `apps/mobile/lib/db/schema.ts` — `userPreference.autoAdvanceEnabled`, `warmupSetsEnabled` from 05-02
    - `apps/mobile/app/(tabs)/profile.tsx` — the existing settings-row chrome (the appearance control) the workout-settings section matches
    - `apps/mobile/lib/rest-alert.ts` — `getAlertPermission`, `openAlertSettings` from 05-05, for the notification re-request row
    - `apps/mobile/components/ExercisePager.tsx` — `clampPagerIndex` and the index contract auto-advance drives
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Home In-Progress Banner, §Copywriting Contract (Primary CTA, Discard confirmation), §Phase-Wide Rules R10
  </read_first>
  <files>apps/mobile/components/WorkoutInProgressBanner.tsx, apps/mobile/components/__tests__/WorkoutInProgressBanner.test.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/(tabs)/profile.tsx, apps/mobile/lib/db/preferences.ts, apps/mobile/lib/db/__tests__/preferences.test.ts, apps/mobile/lib/session/auto-advance.ts, apps/mobile/lib/session/__tests__/auto-advance.test.ts, apps/mobile/app/(tabs)/workout.tsx</files>
  <action>
`apps/mobile/components/WorkoutInProgressBanner.tsx` (D-28): hook-free `WorkoutInProgressBannerView` plus a thin wrapper. Per 05-UI-SPEC §Home In-Progress Banner: a full-width `bg-secondary` card with 16px padding and a `border-l-4 border-accent` left bar — the one departure from a plain card border in this app's vocabulary, reserved for exactly this banner. Heading "Workout in Progress" (or "Workout Paused" for the paused variant) with the elapsed duration inline in muted Label. Two side-by-side actions: "Resume Workout" as the accent primary CTA and "Discard" as destructive text that opens the Discard Workout confirmation before writing anything. The banner renders nothing when there is no session — there is no empty-state variant.

`apps/mobile/app/(tabs)/index.tsx`: render the banner above the existing next-up card. Make the QUERY conditional, not only the render (D-28): call `loadInProgressSessionSummary` only when the user id is present, and let its own cheap early-out cover the rest, so the common no-session path costs nothing. Recompute the elapsed duration on Home-tab focus using the existing `useFocusEffect` pattern rather than running a counter on this screen — Home is not the timer's home. Add the held-out UI-state case the UI-SPEC's E8 backstop asks for: pin what Home renders when the in-progress-session query itself throws, so the banner's absence is distinguishable from a failed query.

`apps/mobile/lib/db/preferences.ts`: `loadWorkoutPreferences(userId, db)` returning `{ autoAdvanceEnabled, warmupSetsEnabled }` with both defaulting to true when no `user_preference` row exists, and `setWorkoutPreference(userId, key, value, db)` writing the single column through the singleton row. Both flags are independent columns on that one row; writing one must leave the other and `weight_unit` unchanged.

`apps/mobile/lib/session/auto-advance.ts` (LOG-13): `shouldAutoAdvance({ sets, enabled, currentIndex, exerciseCount })` — pure, no database, no navigation. Returns the next index or null. It returns null when `enabled` is false, when the current exercise still has an incomplete WORKING set, when the just-completed set was a warm-up, and when `currentIndex` is already the last exercise (no wrap-around). Filter on `set_type` explicitly; never infer set type from `set_index` position.

`apps/mobile/app/(tabs)/workout.tsx`: call `shouldAutoAdvance` from the set-completion handler and move the pager index when it returns one. Gate the call on the typed `SessionScreenMode` value being live, never on `session.status` — that is UI-SPEC R10 and RESEARCH Pitfall 3, and a conditional based on session data rather than the mode value is exactly how the two modes drift apart.

`apps/mobile/app/(tabs)/profile.tsx`: add a Workout settings section matching the existing appearance-control row chrome, holding three rows: an auto-advance toggle bound to `autoAdvanceEnabled`, a warm-up-suggestions toggle bound to `warmupSetsEnabled`, and a notification row that shows the current permission from `getAlertPermission()` and offers "Turn On" via `openAlertSettings()` when it is denied — this is D-22's required re-request path, deep-linking to Settings rather than calling the request API a second time.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "(WorkoutInProgressBanner|preferences|auto-advance|index)"</automated>
  </verify>
  <acceptance_criteria>
    - `WorkoutInProgressBanner.test.tsx` asserts by direct invocation: the in-progress heading, the paused-variant heading, that Discard's handler opens a confirmation rather than writing directly, and that the view returns nothing for a null session.
    - `apps/mobile/app/(tabs)/index.tsx` calls `loadInProgressSessionSummary` inside a guard on the user id, and a unit case asserts no query function is invoked when the id is absent.
    - A held-out case in the Home suite asserts what renders when the in-progress-session query rejects, distinguishing it from the no-session rendering (UI-SPEC E8 error backstop).
    - `auto-advance.test.ts` asserts: null when disabled; null when a working set remains incomplete; null when the completed set was a warm-up; null on the last exercise; and the next index in the ordinary case.
    - `preferences.test.ts` asserts both flags default to true with no row present, and that writing one leaves the other and `weight_unit` unchanged.
    - `apps/mobile/app/(tabs)/workout.tsx` calls `shouldAutoAdvance` only inside a branch gated on the session-mode value; a source scan of the file for `session.status ===` returns no match (UI-SPEC R10).
    - `apps/mobile/app/(tabs)/profile.tsx` renders three workout-settings rows and the notification row's action calls `openAlertSettings`.
    - `pnpm --filter mobile typecheck` and `pnpm --filter mobile lint` both exit 0.
  </acceptance_criteria>
  <done>An in-progress workout is impossible to lose track of, the pager turns itself when an exercise is done unless you have turned that off, and the toggles that control it live in one settings section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Home tab render → session query | A conditional query decides whether an in-progress session is disclosed |
| destructive action → session status | Discard transitions a session out of the user's active flow |
| preference toggle → synced singleton row | Two booleans on a row that governs behaviour across devices |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-01 | Information disclosure | `loadInProgressSessionSummary` | medium | mitigate | The query filters `workout_session` by the authenticated user id and returns early with no query at all when the id is absent, so a signed-out render cannot surface another account's session. Pinned by the no-query-when-null unit case. |
| T-05-07-02 | Tampering | `discardSession` | high | mitigate | Discard is a status transition behind an explicit destructive confirmation with the shipped copy; it deletes no rows, so an accidental discard is recoverable by a later status change rather than being data loss. |
| T-05-07-03 | Tampering | `resumeSession` accumulator | medium | mitigate | A resume with no open pause is a no-op, so repeated taps or a replayed op cannot inflate `accumulated_paused_seconds` and silently shrink every future duration reading. Pinned by the double-resume unit case. |
| T-05-07-04 | Elevation of privilege | `setWorkoutPreference` | medium | mitigate | The write targets the `user_preference` singleton keyed on the authenticated user id; the server-side apply path from 05-03 re-resolves ownership from the session regardless of payload (ASVS V4). |
| T-05-07-05 | Repudiation | `ended_at` stamped from the client clock | low | accept | Single-user training log; the client clock is already the source for `started_at` and `logged_at` by design (D-06). |
</threat_model>

## Flagged planner assumptions (unresolved edge probes)

- **LOG-02 (unclassified)** — "start a one-off workout not tied to any program" produced no shape classification. Planner assumption: the ambiguous boundary is starting a one-off while a programmed session is already in progress. This plan surfaces the Home banner rather than allowing a second concurrent session, but does not enforce a database-level single-session invariant — an offline-first design cannot atomically enforce a global invariant without a round trip, the same reasoning `docs/program-vocabularies.md` records for `routine.status = 'active'`. Flagged, not resolved.

Phase-wide prohibition P3 (no engagement or retention nudging, streak pressure, or shaming framing) is authored in 05-05's `must_haves.prohibitions` and applies to the banner copy written here; it is not re-minted in this plan to avoid double-counting.

<verification>
- `pnpm --filter mobile test` green with non-zero counts in the four new suites.
- `pnpm --filter mobile test:e2e:durability` green including the extended `durability.spec.ts` recovery case and the new `session-lifecycle.spec.ts`.
- `pnpm typecheck` and `pnpm lint` green at the repo root.
- Native-only observations (pause behaviour under a real OS backgrounding, tab focus semantics on iOS) filed in the reserved 163–172 ledger range against Phase 999.1.
</verification>

<success_criteria>
- A one-off workout can be started from the Workout tab with no program, and its targets render as em dashes.
- Pause stops the duration clock, resume restarts it without losing or double-counting time, and a crash is not a pause.
- Force-quitting mid-workout with warm-ups and an open pause restores everything.
- An in-progress session is visible from Home with resume and discard actions, and costs no query when none exists.
- Auto-advance turns the pager when an exercise's working sets are done, and the toggle turns it off.
</success_criteria>

<output>
Create `.planning/phases/05-in-gym-session-logging/05-07-SUMMARY.md` when done.
</output>
