---
phase: 05-in-gym-session-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(tabs)/workout.tsx
  - apps/mobile/app/(tabs)/__tests__/workout.test.tsx
  - apps/mobile/app/__durability.web.tsx
  - apps/mobile/lib/db/test-support.ts
  - apps/mobile/lib/db/log-set.ts
  - apps/mobile/lib/db/session-query.ts
  - apps/mobile/lib/db/__tests__/session-query.test.ts
  - apps/mobile/lib/session/session-mode.tsx
  - apps/mobile/components/SetRow.tsx
  - apps/mobile/components/NumericKeypad.tsx
  - apps/mobile/components/ExerciseStrip.tsx
  - apps/mobile/components/ExercisePager.tsx
  - apps/mobile/components/ExercisePage.tsx
  - apps/mobile/components/__tests__/SetRow.test.tsx
  - apps/mobile/components/__tests__/NumericKeypad.test.tsx
  - apps/mobile/components/__tests__/ExerciseStrip.test.tsx
  - apps/mobile/e2e/workout-screen.spec.ts
  - apps/mobile/e2e/durability.spec.ts
autonomous: true
requirements: [LOG-01, LOG-03, LOG-04, LOG-05, LOG-06, LOG-07]
windows_id_range: "103-112"

estimate:
  tokens: 190000
  raw_tokens: 190000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward truths (D-01, D-02, D-05, D-07, D-11, D-12, D-16..D-20) ---
    - "Starting today's programmed workout from the Workout tab creates exactly one workout_session through startSession and one session_exercise per programmed exercise, each carrying the frozen target_* snapshot resolved for the session's cycle (D-02)."
    - "Every set entry is written to local SQLite by logSet the instant the checkmark is tapped; no in-memory set list, draft buffer, or save action stands between entry and durability (D-01)."
    - "Weight entered on the keypad is converted once through toCanonicalKg at the write boundary and stored as canonical kg; no lb value is ever persisted (D-05)."
    - "RIR is one integer column on the row (logged_set.rir) prefilled from session_exercise.target_rir; no min/max pair exists anywhere in this phase's code (D-07)."
    - "The Workout screen renders one exercise at a time on a swipeable pager reusing the DayDeck/react-native-tab-view pattern, never a single scrolling list of all exercises (D-11)."
    - "A pinned exercise strip above the pager renders one chip per session exercise in order_index order, carries per-chip completion state, highlights the current chip, and jumps the pager on tap (D-12)."
    - "Reps and RIR prefill from the session_exercise snapshot; weight prefills from the single named history-lookup function and never from the snapshot (D-16)."
    - "The previous session's actual weight and reps render greyed beneath their fields in the same row and are tappable to overwrite the field's current value (D-17, D-04 tap-to-autofill)."
    - "The keypad next-arrow walks weight then reps then RIR then complete, and RIR is edited by tapping the same field it was prefilled into (D-18)."
    - "One tap on the checkmark completes a set; a second tap undoes it in place with weight, reps and RIR intact and no edit mode entered (D-19)."
    - "No weight, reps or RIR field mounts a React Native TextInput on any platform; the docked NumericKeypad is the only input surface and the OS keyboard is never invoked (D-20, LOG-05)."
    - "SessionScreenMode is a single typed value provided once at the screen root with all three members ('live', 'editing', 'summary-correction') declared up front, so later plans add behaviour without redefining the contract (D-32)."
    # --- Edge predicates: LOG-03 ---
    - "When the previous session's weight equals the prefilled weight, the row still renders the reference line as its own element; equal values never merge into one displayed number (LOG-03 adjacency)."
    - "A set number with no prior session for that exercise renders the reference line as the literal No previous string and leaves the weight field blank (LOG-03 empty)."
    - "When two prior sessions hold the same set_index for the exercise, the reference resolves to the session with the greater started_at, and a tie on started_at resolves by the greater logged_at, then by id, so the result is deterministic (LOG-03 ordering)."
    # --- Edge predicates: LOG-04 ---
    - "Tapping a reference value whose number already equals the field's current value leaves the field value and its populated/placeholder styling unchanged (LOG-04 adjacency)."
    - "A reference line rendering No previous carries no press handler and no accent underline (LOG-04 empty)."
    - "Tapping the weight reference then the reps reference fills exactly those two fields and re-resolves neither the third field nor the other two (LOG-04 ordering)."
    # --- Edge predicates: LOG-06 ---
    - "An entered RIR of 0 is a value, not an absence: it renders as 0 with populated styling and writes 0 to logged_set.rir (LOG-06 adjacency)."
    - "An exercise whose snapshot target_rir is null renders the RIR field with the em-dash placeholder and writes null unless the user enters a value (LOG-06 empty)."
    - "Changing RIR on an already-completed set writes only the rir column and leaves set_index, weight_kg, reps and completed untouched (LOG-06 ordering)."
    # --- Edge predicates: LOG-07 ---
    - "Two checkmark taps in immediate succession leave the set uncompleted with its three field values intact — complete then undo is a round trip (LOG-07 adjacency)."
    - "A row whose reps field holds no value cannot reach completed=true; the checkmark press is rejected without writing (LOG-07 empty)."
    - "Undo returns the row to editable in place and does not change its position in the set list, which stays ordered by set_index (LOG-07 ordering)."
    # --- UI-SPEC E3 Exercise Strip ---
    - "The strip is never empty in live mode: at minimum the trailing dashed Add Exercise chip renders even when every exercise has been removed (UI-SPEC E3 empty)."
    - "The strip renders with no loading state; its chips come from local SQLite rows already seeded by startSession (UI-SPEC E3 loading, R6)."
    - "The strip is never mounted independently of a loaded session; a load failure renders the parent screen's error state instead (UI-SPEC E3 error)."
    - "Populated strip: current chip uses border-accent on bg-surface, a completed chip replaces its N/N fraction with a checkmark glyph, an in-progress chip shows N/M in muted Label type (UI-SPEC E3 populated)."
    - "Partial completion renders N/M with border-foreground-muted on bg-surface, and completing a warm-up set never changes the fraction's denominator (UI-SPEC E3 partial)."
    - "The strip scrolls horizontally and never wraps; a chip widens to fit its exercise name rather than truncating it (UI-SPEC E3 overflow)."
    - "A single-exercise session renders one chip plus the Add Exercise chip, left-aligned, with no centering special case and no plural copy anywhere in the strip (UI-SPEC E3 zero/one/many)."
    - "Long exercise names widen their chip and the strip scrolls to accommodate; no chip carries numberOfLines or ellipsizeMode (UI-SPEC E3 long text, R4)."
    # --- UI-SPEC E4 Set Row ---
    - "An exercise with zero sets is unreachable in live mode; the field-level empty case is a first-ever set number rendering No previous with a blank weight field (UI-SPEC E4 empty)."
    - "A set row never renders a skeleton or spinner; its reference values resolve from the local history lookup before the row paints (UI-SPEC E4 loading, R6)."
    - statement: "The set row's rendering when its local write fails unrecoverably is pinned by a held-out UI-state test rather than assumed (UI-SPEC E4 error backstop)."
      verification: backstop
    - "Populated row: a 24px set-number tap target, then weight / reps / RIR each as a tappable value stacked over its greyed reference with the accent-underline autofill affordance, then a 48x48 checkmark (UI-SPEC E4 populated)."
    - "A prefilled-but-uncommitted row renders regular-weight values with an outline checkmark; a committed row renders semibold values with a filled accent checkmark (UI-SPEC E4 partial)."
    - "The row never clips and never scrolls horizontally: set-number column fixed at 24px, checkmark column fixed at 48px, the three field columns flex and grow the row's height at large font scale (UI-SPEC E4 overflow, R4)."
    - "A one-set exercise renders a single row identically to any row of a many-set exercise, and no plural copy appears on the row (UI-SPEC E4 zero/one/many)."
    - "Field values wrap and grow rather than truncating; No previous and the em dash are the only text strings on the row (UI-SPEC E4 long text, R4)."
    # --- UI-SPEC E5 Numeric Keypad ---
    - "The keypad is unmounted when no field is focused; within a focused field an empty value renders the 2px accent cursor bar with no digits (UI-SPEC E5 empty)."
    - "The keypad mounts synchronously on field focus with no data dependency and no loading state (UI-SPEC E5 loading)."
    - "The keypad renders no error state: digits, decimal, backspace and the natural-unit steppers are its only inputs, so it cannot structurally produce an invalid value (UI-SPEC E5 error)."
    - "Populated keypad: a 40px reserved plate band, a 3x4 digit grid at 56x56 per key with hairline separators, a 48x48 stepper row, and the trailing accent next/submit arrow reading Done on the RIR field (UI-SPEC E5 populated)."
    - "A partially entered value renders exactly as typed with the cursor trailing the last digit and is not auto-committed until the submit arrow is tapped (UI-SPEC E5 partial)."
    - "The keypad is a flex sibling of the scrollable set-row content rather than an absolutely positioned overlay, so it structurally cannot cover a set row, and the edited row scrolls into view above the reserved band on mount (UI-SPEC E5 overflow, LOG-05)."
    - "The key grid's arity is fixed at 12 keys plus one stepper row and one submit arrow in every session, and carries no plural copy (UI-SPEC E5 zero/one/many)."
    - "Key glyphs are single characters at Display role and keys grow from the 56x56 floor at large OS font scale rather than clipping (UI-SPEC E5 long text, R4)."
    # --- UI-SPEC E7 Active Workout screen (populated half; empty/error/loading are 05-07's) ---
    - "Populated screen: header timer slot, exercise strip, a horizontal pager of exercise pages each carrying its action-bar slot and set rows, and the mode-appropriate primary action (UI-SPEC E7 populated)."
    - "A partially logged session renders completed rows with filled accent checkmarks and uncompleted rows with outline circles over prefilled values, and relaunching after a force quit restores exactly that partial state (UI-SPEC E7 partial)."
    - "Set rows scroll vertically inside the exercise page, the strip scrolls horizontally, and the header bar and keypad are non-scrolling flex siblings that never overlap the scroll region (UI-SPEC E7 overflow)."
    - "A one-exercise session renders one strip chip and one pager page with no pager-affordance change, and no plural copy appears on the screen (UI-SPEC E7 zero/one/many)."
  artifacts:
    - apps/mobile/lib/db/session-query.ts
    - apps/mobile/lib/session/session-mode.tsx
    - apps/mobile/components/SetRow.tsx
    - apps/mobile/components/NumericKeypad.tsx
    - apps/mobile/components/ExerciseStrip.tsx
    - apps/mobile/components/ExercisePager.tsx
    - apps/mobile/components/ExercisePage.tsx
    - apps/mobile/e2e/workout-screen.spec.ts
  key_links:
    - "workout.tsx reads its session through session-query.ts's batched loader (one select per table, inArray for children) and never issues a query inside a render loop."
    - "The checkmark's commit path calls logSet from apps/mobile/lib/db/log-set.ts — the same function durability.spec.ts already proves durable — and adds no second write path."
    - "resolveNextUp's {kind:'workout', cycle, day} result feeds startWorkoutFromProgram, which passes cycleId into addSessionExercise so the snapshot resolves the right cycle's overrides."
    - "SessionModeContext is read by every timer-scheduling and auto-advance call site added by later plans; nothing gates on session.status."
  prohibitions:
    - statement: "MUST NOT invent, guess, extrapolate, or auto-fill a working weight the user has never actually lifted for that exercise and set number — an unknown weight renders as absent, never as a suggested number."
      status: unresolved
      verification: test
---

## Phase Goal (verbatim from ROADMAP.md — not in user-story format)

> The user can walk into a gym with no signal and log a complete workout without friction. **This is the phase the app becomes real** — dogfooding starts here.

The ROADMAP `**Goal:**` line for Phase 5 is an outcome statement, not an `As a / I want to / so that` user story. No story was invented here. Run `/gsd mvp-phase 05` if a formal story is wanted.

## Artifacts this phase produces

New symbols and files created by **this plan** (the plan-review-convergence source-grounding pass reads this section):

| Kind | Symbol / path |
|---|---|
| file | `apps/mobile/lib/db/session-query.ts` |
| function | `loadLiveSession`, `loadSessionTree`, `previousSetReference` |
| type | `LiveSessionData`, `SessionExerciseRow`, `LoggedSetRow`, `PreviousSetReference` |
| file | `apps/mobile/lib/session/session-mode.tsx` |
| type | `SessionScreenMode` (`'live' \| 'editing' \| 'summary-correction'`) |
| symbol | `SessionModeContext`, `SessionModeProvider`, `useSessionMode` |
| file | `apps/mobile/components/SetRow.tsx` |
| component | `SetRowView`, `SetRow` |
| function | `setRowFieldState`, `formatFieldValue` |
| file | `apps/mobile/components/NumericKeypad.tsx` |
| component | `NumericKeypadView`, `NumericKeypad` |
| function | `applyKeypadPress`, `nextKeypadField`, `KEYPAD_KEYS` |
| type | `KeypadField` (`'weight' \| 'reps' \| 'rir'`), `KeypadValue` |
| file | `apps/mobile/components/ExerciseStrip.tsx` |
| component | `ExerciseStripView`, `ExerciseStrip` |
| function | `exerciseChipTone`, `exerciseChipFraction` |
| file | `apps/mobile/components/ExercisePager.tsx` |
| component | `ExercisePagerView`, `ExercisePager` |
| file | `apps/mobile/components/ExercisePage.tsx` |
| component | `ExercisePageView`, `ExercisePage` |
| function (in `workout.tsx`) | `deriveWorkoutScreenState`, `WorkoutScreenView` |
| type (in `workout.tsx`) | `WorkoutScreenState` |
| function (in `log-set.ts`) | `startWorkoutFromProgram` |
| file | `apps/mobile/e2e/workout-screen.spec.ts` |
| harness symbol | `WORKOUT_HARNESS_MODE`, `seedProgrammedSession` (in `test-support.ts`) |

## Broken-windows ledger reservation

This plan reserves `.planning/WINDOWS.md` ids **103–112**. Append with `gsd-tools windows add` only inside that range; do not renumber existing entries.

<objective>
Deliver the tracer slice of Phase 5: a user starts today's programmed workout from the Workout tab and logs a real set — weight, reps, RIR — on an in-app keypad, with the write durable the instant the checkmark is tapped (D-01), and then expands it to the full exercise strip + pager (D-11, D-12) and the previous-session reference values that make the two-tap common case real (D-16, D-17, LOG-03, LOG-04).

Purpose: this is the phase's headline path. Proving it end-to-end in a real browser before any other layer is built is what catches an architectural dead end after one commit instead of after ten.
Output: a working live-session screen, the batched session read pattern, the three new input components, and the browser-driven e2e that pins them.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-in-gym-session-logging/05-CONTEXT.md
@.planning/phases/05-in-gym-session-logging/05-RESEARCH.md
@.planning/phases/05-in-gym-session-logging/05-PATTERNS.md
@.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md
@.planning/phases/05-in-gym-session-logging/05-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "log one set of today's programmed workout" — one path only</name>
  <reversibility rating="one-way">D-01: a set that only becomes durable at finish is a set lost to a force-quit, and every later phase reads rows written by this path.</reversibility>
  <read_first>
    - `apps/mobile/lib/db/log-set.ts` — `startSession`, `addSessionExercise`, `resolvePrescriptionForCycle`, `logSet`; the whole existing write path this task builds on and extends
    - `apps/mobile/lib/db/programs/next-up-query.ts` — the house batched-read pattern (one select per table, assembled in memory, cheap early-out on the empty path)
    - `apps/mobile/lib/programs/next-up.ts` — `resolveNextUp` and its `{ kind: 'workout', cycle, day }` result shape; also the pure-module contract every new pure function here must match
    - `apps/mobile/app/(tabs)/index.tsx` — `deriveHomeScreenState` (the screen-state derivation pattern to copy), `displayOrDash` (the em-dash null convention to reuse)
    - `apps/mobile/components/CycleStrip.tsx` — the hook-free `*View` + thin stateful wrapper split every new component must follow
    - `apps/mobile/lib/db/schema.ts` — the local SQLite mirror: `workoutSession`, `sessionExercise`, `loggedSet`, `exercise`
    - `apps/mobile/lib/theme-colors.ts` — `useThemeColors`, `ThemeColors`
    - `apps/mobile/app/__durability.web.tsx` and `apps/mobile/lib/db/test-support.ts` — the existing browser harness this task extends
    - `apps/mobile/e2e/durability.spec.ts` — the Playwright shape (page.evaluate against `DURABILITY_HARNESS_GLOBAL`) the new spec follows
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Set Row, §Numeric Keypad, §Session Modes
  </read_first>
  <files>apps/mobile/lib/db/session-query.ts, apps/mobile/lib/session/session-mode.tsx, apps/mobile/app/(tabs)/workout.tsx, apps/mobile/components/SetRow.tsx, apps/mobile/components/NumericKeypad.tsx, apps/mobile/lib/db/log-set.ts, apps/mobile/lib/db/test-support.ts, apps/mobile/app/__durability.web.tsx, apps/mobile/e2e/workout-screen.spec.ts, apps/mobile/lib/db/__tests__/session-query.test.ts, apps/mobile/app/(tabs)/__tests__/workout.test.tsx, apps/mobile/components/__tests__/SetRow.test.tsx, apps/mobile/components/__tests__/NumericKeypad.test.tsx</files>
  <action>
Wire ONE entry point — "Start today's workout" on the Workout tab — through every layer to a durable `logged_set` row, and prove it in a real browser. No other call sites, no batching, real error handling on the single path.

`apps/mobile/lib/session/session-mode.tsx`: declare `export type SessionScreenMode = 'live' | 'editing' | 'summary-correction'` with all three members present from the start (D-32 requires the mode to be one typed value provided once at the screen root, not a boolean threaded down). Export `SessionModeContext`, `SessionModeProvider` and `useSessionMode()`; `useSessionMode()` throws when read outside a provider so a component can never silently default to `'live'`.

`apps/mobile/lib/db/session-query.ts`: add `loadSessionTree(sessionId, db)` following `next-up-query.ts`'s discipline exactly — four selects, never a query inside a loop: (1) the `workoutSession` row by id, (2) `sessionExercise` rows where `sessionId` matches, ordered by `orderIndex`, (3) `loggedSet` rows via `inArray(loggedSet.sessionExerciseId, ids)` from query 2, (4) `exercise` metadata via `inArray(exercise.id, exerciseIds)` from query 2. Assemble into `LiveSessionData` in memory with a plain `Map`. Add `loadLiveSession(userId, db)` returning the single `workout_session` whose `status` is `'in_progress'` for that user, or null, with the same cost-nothing early-out `loadNextUp` uses when `userId` is falsy.

`apps/mobile/lib/db/log-set.ts`: add `startWorkoutFromProgram(input, db)` taking `{ routineDayId, cycleId, slots }` and calling the existing `startSession` once, then `addSessionExercise` once per slot in `orderIndex` order passing `routineExerciseId` and `cycleId` so `resolvePrescriptionForCycle` freezes the right cycle's `override ?? base` values onto the row (D-02). Do not duplicate `startSession`'s `captureCalendarDay` call and do not add a second insert path — this is a funnel over the existing helpers, and D-33 will later route two more entry points through the same `startSession` call.

`apps/mobile/components/NumericKeypad.tsx` (D-20, LOG-05): a hook-free `NumericKeypadView` plus a thin `NumericKeypad` wrapper resolving `useThemeColors()`. Export `KeypadField = 'weight' | 'reps' | 'rir'`, the pure reducer `applyKeypadPress(value, press)` handling digit / `.` / backspace / `+` / `-`, and `nextKeypadField(field)` walking `weight` → `reps` → `rir` → `null` (D-18). Layout top to bottom per 05-UI-SPEC §Numeric Keypad: a 40px always-rendered empty band reserved for Phase 6's plate strip, a 3x4 digit grid at 56x56 per key, a 48x48 stepper row, and a trailing accent submit control whose label is a chevron glyph on weight/reps and the word Done on rir. Render the keypad as a bottom-pinned flex sibling of the scrollable content, not `position: absolute`.

`apps/mobile/components/SetRow.tsx` (D-16, D-17, D-18, D-19): a hook-free `SetRowView` taking plain props plus `colors`, and a thin `SetRow` wrapper. Layout left to right per 05-UI-SPEC §Set Row: 24px set-number tap target, weight / reps / RIR each a `Pressable` displaying its formatted value stacked over a reference slot, then a 48x48 checkmark `Pressable`. In this task the reference slot renders `displayOrDash`-style placeholder content only; Task 3 fills it from history. Reps prefill from `targetRepMax ?? targetRepMin`, RIR from `targetRir`, both from the `session_exercise` snapshot; weight starts blank. No `TextInput` anywhere in this file or `NumericKeypad.tsx` — the value display is a `Pressable`/`View` with a 2px accent cursor bar when active, which is what makes "the OS keyboard is never invoked" structural on iOS, Android and web alike rather than a per-platform suppression. No `numberOfLines`, no `ellipsizeMode`, no `allowFontScaling={false}` anywhere (R4).

Checkmark behaviour (D-19, D-01): first tap calls `logSet` with `completed: true`, the entered `weight` as `{ value, unit }` (unit from the user's `weight_unit` preference so `toCanonicalKg` does the one conversion), `reps`, `rir`, and returns the new row id; second tap issues an update setting `completed` back to false on that same row, leaving `weight_kg`, `reps`, `rir` and `set_index` untouched and the row editable in place. Reject a completion press when the reps field holds no value, without writing.

`apps/mobile/app/(tabs)/workout.tsx`: replace `PlaceholderScreen`. Export a pure `deriveWorkoutScreenState({ failed, session, nextUp })` returning `WorkoutScreenState = 'error' | 'loading' | 'no-session' | 'ready'` in the exact shape of `deriveHomeScreenState`, and a hook-free `WorkoutScreenView`. The default export mounts `SessionModeProvider` with `'live'` at the root and renders, for the session's FIRST exercise only in this task, its exercise name, its set rows and the docked keypad. `'no-session'` renders a Start-today's-workout action that resolves `resolveNextUp` and calls `startWorkoutFromProgram`. Error copy is the shipped pattern verbatim: heading "Workout couldn't load", body "Restart the app to try again. Your programs and history are safe."

Harness + e2e: extend `apps/mobile/lib/db/test-support.ts` with `seedProgrammedSession()` (inserts a routine, one day, two routine_exercises with real targets, then calls `startWorkoutFromProgram`) and export `WORKOUT_HARNESS_MODE`. Extend `apps/mobile/app/__durability.web.tsx` so that with `EXPO_PUBLIC_DURABILITY_HARNESS=1` and a `?screen=workout` query param it renders the real `WorkoutScreenView` against the harness database — the same real `@powersync/web` database the existing harness opens — so Playwright can click actual DOM produced by react-native-web. Author `apps/mobile/e2e/workout-screen.spec.ts` in the existing `durability` Playwright project: seed the session, tap the weight field, press digits on the keypad, press the submit arrow twice to walk to reps and rir, tap the checkmark, then reload the page and assert the row is still completed with the same three values.

**One deliberate coordination edit, called out because it looks unrelated:** `test-support.ts`'s `SCHEMA_VARIANT_DELTA` currently uses `notes` as the synthetic added column for `schema-redefinition.spec.ts`. Plan 05-02 makes `logged_set.notes` a real column, which would silently turn that test's premise into a no-op. Rename the synthetic column to `harness_probe` in `SCHEMA_VARIANT_DELTA` and in the `loggedSetV2` table it drives, keeping `side` as the removed column.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "(session-query|workout|SetRow|NumericKeypad)" &amp;&amp; pnpm --filter mobile test:e2e:durability -- workout-screen.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test -- --testPathPattern "(session-query|workout|SetRow|NumericKeypad)"` exits 0 with a non-zero test count in each of the four suites (the `jest-suite-integrity.cjs` reporter fails an empty or skipped suite).
    - `pnpm --filter mobile test:e2e:durability -- workout-screen.spec.ts` exits 0 and its case asserts the completed row's `weight_kg`, `reps` and `rir` survive a full page reload.
    - `apps/mobile/lib/session/session-mode.tsx` contains `export type SessionScreenMode` and its union names all three of `'live'`, `'editing'`, `'summary-correction'` (D-32).
    - `apps/mobile/lib/db/session-query.ts` contains `export async function loadSessionTree` and `export async function loadLiveSession`, and `grep -c 'db.select' apps/mobile/lib/db/session-query.ts` reports at most 5 for the whole file — the batched shape, not one select per row (PITFALLS §13).
    - `apps/mobile/lib/db/log-set.ts` contains `export async function startWorkoutFromProgram` and still contains exactly one `db.insert(workoutSession)` call site (D-33's single funnel precondition).
    - Comment-filtered source scan finds zero code occurrences of the input primitive named in the action: `grep -vE '^\s*(//|\*|/\*)' apps/mobile/components/SetRow.tsx apps/mobile/components/NumericKeypad.tsx | grep -c 'TextInput'` reports 0 (D-20's structural guarantee — the filter is there because a comment restating the rule must not satisfy or invalidate it).
<!-- planner-discipline-allow: TextInput -->
    - `apps/mobile/app/(tabs)/workout.tsx` exports `deriveWorkoutScreenState` and `WorkoutScreenView`, and `WorkoutScreenView` takes `colors` as a prop and calls no hook (direct-invocable by Jest with no renderer).
    - `apps/mobile/lib/db/test-support.ts`'s `SCHEMA_VARIANT_DELTA.added` names `harness_probe`, and `pnpm --filter mobile test:e2e:durability -- schema-redefinition.spec.ts` still exits 0.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>A user can start today's programmed workout on the Workout tab, enter weight/reps/RIR on the in-app keypad with no OS keyboard, tap the checkmark once to complete and once more to undo, and the completed row survives a full browser reload — proven by a Playwright case clicking real DOM.</done>
</task>

<task type="auto">
  <name>Task 2: Expand to every exercise — pinned strip and swipeable pager</name>
  <read_first>
    - `apps/mobile/components/CycleStrip.tsx` — the chip anatomy, `cycleChipTone` pure-helper shape, 48x48 hit target and horizontal-scroll-never-wrap rule to carry over verbatim
    - `apps/mobile/components/DayDeck.tsx` — `clampDeckIndex`, `DayDeckView`/`DayDeck` split, and the `react-native-tab-view` `TabView` usage with `renderTabBar={() => null}`
    - `apps/mobile/app/(tabs)/workout.tsx` — the screen written in Task 1, which this task composes the strip and pager into
    - `apps/mobile/components/SetRow.tsx` — the row the exercise page renders per set
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Exercise Strip, §Per-Exercise Action Bar (the action-bar slot only, contents are 05-06's)
  </read_first>
  <files>apps/mobile/components/ExerciseStrip.tsx, apps/mobile/components/ExercisePager.tsx, apps/mobile/components/ExercisePage.tsx, apps/mobile/app/(tabs)/workout.tsx, apps/mobile/components/__tests__/ExerciseStrip.test.tsx</files>
  <action>
Build the two navigation surfaces D-11 and D-12 name, and move the single-exercise rendering from Task 1 behind them.

`apps/mobile/components/ExerciseStrip.tsx` (D-12): copy `CycleStrip.tsx`'s structure — a pure exported `exerciseChipTone(state)` returning `{ borderStyle, borderTone, fill, indicator }` for the three chip states, a pure `exerciseChipFraction(completedWorkingSets, targetSets)` returning either an `N/M` string or the sentinel meaning "render the checkmark glyph instead" when completed equals total, a hook-free `ExerciseStripView`, and a thin `ExerciseStrip` wrapper resolving `useThemeColors()`. Per 05-UI-SPEC §Exercise Strip: two-line chip (exercise name on top, fraction beneath), current chip `border-accent` on `bg-surface`, completed chip `border-foreground-muted` on `bg-secondary` with an `Ionicons` `checkmark` glyph at 14px resolved through `useThemeColors()`, in-progress chip `border-foreground-muted` on `bg-surface`. `minWidth`/`minHeight` 48 on every chip. Horizontal `ScrollView`, never wrapping. A trailing dashed `+` chip is always rendered as the Add Exercise affordance — in this task it takes an `onAddExercise` prop the screen leaves as a no-op; 05-06 wires it to `ExercisePickerModal`. **Warm-up sets are excluded from the fraction's denominator and numerator** — count only rows whose `set_type` is the working value (RESEARCH.md Pitfall 2: `set_index` position never implies set type).

`apps/mobile/components/ExercisePager.tsx` (D-11): wrap `react-native-tab-view`'s `TabView` exactly as `DayDeck.tsx` does, parameterised on `session_exercise` rows instead of program days. Copy `clampDeckIndex` verbatim as `clampPagerIndex` — an exercise removed mid-session must not throw or blank the pager, the same way a deleted program day does not today. Export a hook-free `ExercisePagerView` taking `index`/`onIndexChange`/`width` and a thin `ExercisePager` owning `useWindowDimensions()` and the index state, re-clamping on `exercises.length` change.

`apps/mobile/components/ExercisePage.tsx`: a hook-free `ExercisePageView` rendering one exercise's header (name at Heading role), an `actionBarSlot` render-prop that 05-06 fills, and the list of `SetRow`s for that exercise ordered by `set_index` with warm-up rows sorted ahead of working rows regardless of raw index. Plus the thin `ExercisePage` wrapper.

`apps/mobile/app/(tabs)/workout.tsx`: compose — `SessionModeProvider` at the root, then `ExerciseStrip` above `ExercisePager`, with the strip's selected chip and the pager's index driven by one shared index value held in the screen, so tapping a chip jumps the pager and swiping the pager moves the highlight. Keep the docked `NumericKeypad` as a non-scrolling flex sibling beneath the pager, so it never overlaps a set row.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "(ExerciseStrip|workout)" &amp;&amp; pnpm --filter mobile test:e2e:durability -- workout-screen.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/components/ExerciseStrip.tsx` exports `ExerciseStripView`, `ExerciseStrip`, `exerciseChipTone` and `exerciseChipFraction`; `ExerciseStripView` calls no hook.
    - `ExerciseStrip.test.tsx` covers, by direct invocation with no renderer: the current chip, a completed chip rendering the checkmark sentinel rather than `N/N`, an in-progress chip rendering `N/M`, a warm-up row leaving the fraction unchanged, and a single-exercise session still rendering the trailing add chip.
    - `apps/mobile/components/ExercisePager.tsx` exports `clampPagerIndex` and a unit case proves `clampPagerIndex(5, 2)` returns 1 and `clampPagerIndex(0, 0)` returns 0.
    - Swiping the pager in `workout-screen.spec.ts` moves the highlighted strip chip, and tapping the second chip moves the pager to the second exercise — both asserted in the browser.
    - `pnpm --filter mobile typecheck` and `pnpm --filter mobile lint` both exit 0.
  </acceptance_criteria>
  <done>Every exercise in the session is reachable: the strip reads the workout at a glance with per-chip completion state, and the pager moves between exercises by swipe or by chip tap.</done>
</task>

<task type="auto">
  <name>Task 3: Previous-session reference values and tap-to-autofill</name>
  <reversibility rating="costly">D-16: the prefill source is read by the row, by the summary's did-you-beat-it comparison, and later replaced wholesale by Phase 8's recommendation — keeping it one named function is what makes that swap a single call site.</reversibility>
  <read_first>
    - `apps/mobile/lib/db/session-query.ts` — the loader written in Task 1, which this task extends with the history lookup
    - `apps/mobile/components/SetRow.tsx` — the reference slot left as a placeholder in Task 1
    - `apps/mobile/lib/db/schema.ts` — `loggedSet`, `sessionExercise`, `workoutSession` columns for the cross-session lookup
    - `packages/api-contracts/src/units.ts` — `toCanonicalKg` / `fromCanonicalKg`, the one conversion boundary (D-05); do not write a second converter
    - `.planning/phases/05-in-gym-session-logging/05-RESEARCH.md` § Common Pitfalls, Pitfall 1 (prefilling from the wrong "previous" set)
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Set Row, and §Copywriting Contract row "No-previous state"
  </read_first>
  <files>apps/mobile/lib/db/session-query.ts, apps/mobile/components/SetRow.tsx, apps/mobile/lib/db/__tests__/session-query.test.ts, apps/mobile/components/__tests__/SetRow.test.tsx, apps/mobile/e2e/durability.spec.ts</files>
  <action>
Add the one named history-lookup function D-16 requires and render its result as D-17's greyed, tappable reference.

`apps/mobile/lib/db/session-query.ts`: add `previousSetReference({ exerciseId, setIndex, beforeSessionId, userId }, db)` returning `PreviousSetReference | null` of shape `{ weightKg: string | null; reps: number; sessionId: string; loggedAt: string }`. It resolves the most recent PRIOR session's `logged_set` row for that exercise **at that same `set_index`** — not `ORDER BY logged_at DESC LIMIT 1`, which is Pitfall 1 and would make set 3 prefill from whatever the most recent set happened to be. Implement as batched selects in the house style: one select of the user's prior `workout_session` ids for that exercise ordered by `started_at DESC`, then one select of `logged_set` rows joined through `session_exercise` filtered by `inArray` on those session ids and `eq(loggedSet.setIndex, setIndex)` and the working `set_type`, resolved in memory. Ordering is deterministic: greater `started_at` wins; a tie resolves by greater `logged_at`; a remaining tie resolves by the greater `id`. Add a batched `previousSetReferencesForSession(sessionId, db)` that resolves every row of the open session in one pass so the screen never calls the single-row form inside a render loop.

`apps/mobile/components/SetRow.tsx`: fill the reference slot. Under the weight field render the previous actual weight converted for display through `fromCanonicalKg` at the display boundary only; under reps render the previous actual reps. Both at Label role in `text-foreground-muted` with an accent-tinted underline marking them tappable (05-UI-SPEC §Color: this is the one place accent touches Label-role text, because it is a live control disguised as reference text). Tapping a reference writes its number into the field it sits under and leaves the other two fields untouched. When no prior row exists, render the literal string `No previous` with no underline and no press handler, and leave the weight field blank — never a guessed number. Weight prefills from `previousSetReference`; reps and RIR keep prefilling from the `session_exercise` snapshot (D-16's deliberate split: the fields train you toward the program, the greyed numbers tell you what you actually did).

Extend `apps/mobile/e2e/durability.spec.ts` with one case: seed two prior sessions for the same exercise at the same `set_index` with different `started_at`, open a third, and assert the reference resolves to the later session's numbers and survives a reload.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "(session-query|SetRow)" &amp;&amp; pnpm --filter mobile test:e2e:durability -- durability.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/lib/db/session-query.ts` exports `previousSetReference` and `previousSetReferencesForSession`, and `PreviousSetReference` is exported as a type.
    - `session-query.test.ts` contains a case that logs a 3-set exercise in session A, opens session B, and asserts the set-2 reference equals session A's set-2 numbers and NOT session A's set-3 numbers (Pitfall 1's warning sign).
    - `session-query.test.ts` contains a case with two prior sessions holding the same `set_index` and asserts the resolved reference is the one with the greater `started_at`, and a second case pinning the `logged_at` tie-break.
    - `session-query.test.ts` contains a case asserting a first-ever exercise resolves to `null`.
    - `SetRow.test.tsx` asserts, by direct invocation: a null reference renders the exact string `No previous` with no press handler; a present reference renders a press handler that fills only its own field; and tapping a reference whose value already equals the field leaves the field value identical.
    - `SetRow.test.tsx` asserts a warm-up-typed row is excluded from the reference lookup's source set.
    - `pnpm --filter mobile test:e2e:durability -- durability.spec.ts` exits 0 including the new two-prior-sessions case.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>Logging a normal set takes two taps in the common case: the previous session's numbers are visible inline beside the fields, tappable to autofill, and a first-ever exercise honestly says No previous instead of guessing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → local SQLite | Keypad digits, reps and RIR reach `logSet` and land in the on-device database with no server round trip (D-01/D-03) |
| local SQLite → PowerSync upload queue | Every row written here is later pushed by the CRUD queue and re-validated server-side |
| Playwright harness route → app runtime | The `__durability` harness mounts real screens against a real database when an env flag is set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Tampering | `apps/mobile/app/__durability.web.tsx` workout-harness mode | high | mitigate | Gate the new `?screen=workout` harness branch behind the same inlined `process.env.EXPO_PUBLIC_DURABILITY_HARNESS !== '1'` early return the existing harness uses (T-02-30), so Metro's build-time substitution removes it entirely from a production export. Assert the guard's presence in the task's acceptance criteria. |
| T-05-01-02 | Tampering | `logSet` weight path | medium | mitigate | Weight reaches the write only through `toCanonicalKg`; the keypad reducer emits digits and one decimal point, so a non-numeric string cannot reach `weight_kg`. No second converter is introduced (D-05). |
| T-05-01-03 | Information disclosure | `session-query.ts` reads | medium | mitigate | `loadLiveSession` filters `workout_session` by the authenticated `userId` and children resolve only through that session's ids; there is no cross-user read path, matching `loadNextUp`'s existing shape. |
| T-05-01-04 | Repudiation | `logged_set.logged_at` | low | accept | Single-user personal training log; a client-stamped log time is not a multi-tenant claim. No mitigation warranted beyond the existing `now` parameter that keeps it testable. |
</threat_model>

## Flagged planner assumptions (unresolved edge probes)

The deterministic edge probe returned these rows as `category: unclassified`. Per the spec-less probe fallback they stay **`unresolved`** — they are neither auto-resolved with a backstop nor dropped:

- **LOG-01 (unclassified)** — "start today's workout from the active program" produced no shape classification. Planner assumption: the only ambiguous boundary is what happens when `resolveNextUp` returns `time-off` or `program-complete` rather than `workout`; this plan renders the `no-session` state and defers the explicit copy for those two cases to 05-07. Flagged, not resolved.
- **LOG-05 (unclassified)** — "keypad never obscures the value being edited" produced no shape classification. Planner assumption: the flex-sibling layout plus scroll-into-view makes this structural, but no automated check on this machine can measure occlusion at every viewport. Flagged, not resolved.

<verification>
- `pnpm --filter mobile test` green, with the four new suites reporting non-zero test counts.
- `pnpm --filter mobile test:e2e:durability` green, including `workout-screen.spec.ts`, `durability.spec.ts` and `schema-redefinition.spec.ts`.
- `pnpm typecheck` and `pnpm lint` green at the repo root.
- Any claim that could only be observed on a real device (native pager swipe, native font scaling on the three-field row) is filed as a `.planning/WINDOWS.md` unrun-verify entry in the reserved range 103–112 against ROADMAP Phase 999.1 — never silently skipped.
</verification>

<success_criteria>
- Starting today's programmed workout produces one session with one snapshotted `session_exercise` per programmed exercise.
- A set entered on the in-app keypad and completed with one tap is durable in local SQLite before any network call, and survives a full page reload.
- A second checkmark tap undoes the completion in place with all three values intact.
- The exercise strip and pager make every exercise reachable by tap or swipe.
- The previous session's weight and reps are visible inline and tappable to autofill, and a first-ever set says `No previous`.
- No `TextInput` exists in `SetRow.tsx` or `NumericKeypad.tsx`.
</success_criteria>

<output>
Create `.planning/phases/05-in-gym-session-logging/05-01-SUMMARY.md` when done.
</output>
