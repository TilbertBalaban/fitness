---
phase: 05-in-gym-session-logging
plan: 13
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements:
  - LOG-14
  - LOG-16
files_modified:
  - .planning/phases/05-in-gym-session-logging/05-UI-SPEC.md
  - .planning/phases/05-in-gym-session-logging/COVERAGE.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "05-UI-SPEC.md carries a written contract for the set-row long-press note affordance, including how it coexists with the existing tap-to-autofill and tap-to-complete targets and what a screen-reader user does instead of long-pressing."
    - "05-UI-SPEC.md carries a written contract for the Session Note row in the session Menu, naming which SessionScreenMode values expose it."
    - "05-UI-SPEC.md carries a written contract for the exercise reorder surface — the interaction E10 lists as a row but has never specified — covering all nine E-block states plus large-OS-font-scale behaviour under R4."
    - "The amendment preserves SessionActionSheet's fixed arity of four rows (E10 empty / zero-one-many); Reorder becomes actionable, not a fifth row."
    - "Every new contract reuses an already-shipped pattern by name (DragHandle + reorder-drag.ts from Phase 4, NoteSheet's three levels, RoutineActionSheet's row anatomy) rather than inventing a new component vocabulary."
    - "`.planning/phases/05-in-gym-session-logging/COVERAGE.md` exists and is a valid capability matrix — every row carries a decision and every OPT-OUT carries a one-line reason — so the `api-coverage.verify-pre` gate cannot block the phase seal."
    - "The browser Notification API surface (the `.web.ts` sibling) is re-decided from the same full-coverage baseline as the native surface, not inherited from it."
  artifacts:
    - ".planning/phases/05-in-gym-session-logging/05-UI-SPEC.md — `## Amendment A — Gap-Closure Surfaces` plus new `## UI Considerations` blocks E12 and E13"
    - ".planning/phases/05-in-gym-session-logging/COVERAGE.md — the expo-notifications and browser-Notification capability matrices"
  key_links:
    - "UI-SPEC Amendment A -> 05-14 (set-row long-press + Session Note row) and 05-15 (reorder sheet) — the contract those plans implement against"
    - "COVERAGE.md -> the `api-coverage.verify-pre` seal gate"
  prohibitions:
    - "No capability row is fabricated for a capability the SDK does not have, and no OPT-OUT is recorded without a reason."
    - "The amendment never re-specifies a surface 05-01…05-10 already shipped and verified — it only adds the three contracts that were missing."
    - "Phase-wide prohibition P3 (05-05: no engagement/retention nudging, streak pressure, or shaming framing) governs every copy string the amendment introduces — referenced, not re-minted."
---

<objective>
Author the three missing UI contracts before anything is built against them, and produce the
`COVERAGE.md` the seal gate requires.

Two of this run's gaps have no design contract at all: `05-UI-SPEC.md` defines no set-level or
session-level note trigger (its Per-Exercise Action Bar section is the only note surface specified),
and it defines no reorder interaction anywhere — E10 lists Reorder as one of four fixed rows and
specifies nothing for it, which is exactly why `ExercisePage.tsx` dismisses that row as a documented
no-op (WINDOWS #116, #118). Building first and describing afterwards is how a surface ends up
unverifiable; this plan writes the contract, and 05-14/05-15 implement against it.

Separately, the API-coverage detector fires on this phase and no `COVERAGE.md` exists, so
`api-coverage.verify-pre` will block the seal regardless of how the gaps close.

Purpose: give 05-14 and 05-15 a contract to implement and a verifier something to check against.
Output: an amendment section plus two new `## UI Considerations` E-blocks in `05-UI-SPEC.md`, and a
reasoned `COVERAGE.md`.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md
@.planning/phases/05-in-gym-session-logging/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend 05-UI-SPEC.md with the three missing surface contracts</name>
  <files>.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/05-in-gym-session-logging/05-UI-SPEC.md (in full: Set Row, Per-Exercise Action Bar, Session Modes, Phase-Wide Rules R4/R7/R10, Copywriting Contract, the E4/E6/E10 `## UI Considerations` blocks, and the `### Backstop Summary — planner action required` section at the end)
    - apps/mobile/components/SetRow.tsx (`SetRowView`'s existing press targets: the set-number Pressable, the three `renderSetField` Pressables, each field's nested reference Pressable, and the 48×48 checkmark)
    - apps/mobile/components/SessionActionSheet.tsx (`SESSION_EXERCISE_ACTIONS`, the fixed four-row constant, and `RemoveExerciseDialog`'s overlay shape)
    - apps/mobile/components/DragHandle.tsx and DragHandle.web.tsx (`DragHandleView`'s glyph, its `Reorder {name}` accessibilityLabel, the 48×48 floor, the native pan gesture and the web pointer-capture sibling)
    - apps/mobile/lib/programs/reorder-drag.ts (`SLOT_ROW_HEIGHT`, `computeDropTarget`, `neighboursForIndex` — the pure drop arithmetic both DragHandle siblings share)
    - apps/mobile/app/(tabs)/workout.tsx (the session `Menu` popover, ~lines 426-458: the `Menu` Pressable, the Pause/Resume row, the Discard row)
    - apps/mobile/components/NoteSheet.tsx (`HEADING` map: `Set Note` / `Exercise Note` / `Session Note`)
    - apps/mobile/lib/session/session-mode.tsx (`SessionScreenMode`'s three members)
  </read_first>
  <action>
  Append a new top-level section `## Amendment A — Gap-Closure Surfaces (2026-08-25)` after
  `## Phase-Wide Rules` and before `## Screen & Component Inventory`, plus two new blocks at the end
  of `## UI Considerations`. Do not rewrite or renumber any existing section, and do not touch the
  existing E1–E11 blocks except to add the two rows named below.

  **A.1 — Set Row note affordance (amends Set Row / E4).** Specify:
  - A **long press anywhere on a set row** opens `NoteSheet` at `level: 'set'` for that
    `logged_set`. Default `delayLongPress` (500 ms) — no custom delay.
  - **Coexistence with the existing tap targets:** the row's press targets are nested (set-number,
    three field Pressables, each field's inner reference Pressable, checkmark), and a nested
    `Pressable` swallows a gesture from its parent — so the same `onLongPress` handler is attached
    to every one of those press targets as well as the row container, rather than to the container
    alone. `onPress` still fires only when the press was not a long press, so tap-to-autofill
    (D-17) and tap-to-complete (D-19) are unchanged: no delay is introduced on any existing tap,
    and no existing single-tap affordance is placed behind a disambiguation window.
  - **Accessibility:** a long press is unavailable to a screen-reader user, so every row press
    target additionally declares an `accessibilityActions` entry `{ name: 'note', label: 'Add note' }`
    handled by `onAccessibilityAction` and routed to the same handler. The row's own
    `accessibilityHint` reads **"Long press to add a note"**.
  - **Note-present tell:** a set row whose `logged_set.notes` is non-null renders a 6px `bg-accent`
    dot immediately before the checkmark column, matching E6's Note-button badge treatment exactly
    (same size, same colour, same "does this already have a note" question) — the only new visual
    element on the row. A row with no note renders no dot and no reserved gap.
  - **Modes:** available in `live` and `editing`. **Not** available in `summary-correction` — the
    summary's expanded correction rows are a distinct, numeric-correction-only surface (E9), and
    adding a note affordance there is out of scope for this run; state this explicitly rather than
    leaving it implicit (D-32/R10).
  - Add one row to the existing **E4 — Set Row** table: `Note present / absent | explicit | A row
    with a note shows a 6px bg-accent dot before the checkmark; a row without one shows nothing and
    reserves no gap. Long press opens NoteSheet at level 'set'; the accessibility action 'Add note'
    is the non-gestural equivalent.`

  **A.2 — Session Note row (amends Active Workout screen / E7).** Specify:
  - The session `Menu` popover `workout.tsx` already renders gains a third row, **"Session Note"**,
    between Pause/Resume and Discard, opening `NoteSheet` at `level: 'session'` for the
    `workout_session` row. Same 48×48 row geometry and same `text-foreground` treatment as the
    Pause/Resume row; Discard stays last and stays `text-destructive`, so the destructive action is
    never adjacent to the newly-added benign one.
  - Selecting it closes the menu before the sheet opens (the menu is a popover, not a stack).
  - **Modes:** `live` only, because the Menu itself is only rendered in `live` mode (it hangs off
    the header-timer region, which `editing` mode replaces with the session's date per the Session
    Modes table, and `summary-correction` has its own layout). LOG-16's three levels are all
    reachable in `live`; `editing` and `summary-correction` expose the exercise-level note only.
    State this scope explicitly.
  - A session that already has a note shows the same 6px `bg-accent` dot trailing the row's label.
  - Add one row to the existing **E7 — Active Workout screen** table: `Session menu | explicit | The
    Menu popover renders exactly three rows in live mode — Pause/Resume, Session Note, Discard —
    fixed arity, no conditional hiding; Discard stays last and destructive-coloured.`

  **A.3 — Reorder Exercises sheet (new surface, resolves E10's unspecified Reorder row).** Specify a
  new component `ReorderExercisesSheet`, opened by `SessionActionSheet`'s existing Reorder row.
  `SESSION_EXERCISE_ACTIONS` keeps its fixed arity of four — Reorder stops being a no-op, it does
  not become a fifth row.
  - **Shape:** the same overlay + `ScrollView` card as `SessionActionSheet`/`RemoveExerciseDialog`
    (`bg-background/80` overlay, `max-w-[400px]`, `rounded-md bg-surface p-lg`), heading
    **"Reorder Exercises"**, a vertical list of every non-removed session exercise in current
    `order_index` order, and a single trailing **"Done"** button (same Cancel-row geometry the
    sibling sheets use, but labelled Done since every drag has already been committed).
  - **Row anatomy:** exercise name (Body, wrap-and-grow per R4) over its completion fraction
    (`3/4`, Label, muted — the same fraction the exercise strip shows, warm-ups excluded from the
    denominator), with a trailing `DragHandle` (the shipped Phase 4 component: 48×48,
    `reorder-three-outline`, `accessibilityLabel` `Reorder {name}`). The `.web.tsx` sibling resolves
    automatically — no `Platform.OS` branch (D-08).
  - **Drag arithmetic:** reuses `reorder-drag.ts`'s `computeDropTarget`/`neighboursForIndex`
    verbatim. Because R4 forbids clipping, a row at large OS font scale grows past
    `SLOT_ROW_HEIGHT` (72), which would make a fixed-constant drop calculation progressively wrong;
    the sheet therefore measures its first row's laid-out height via `onLayout` and passes it as the
    drag unit, falling back to `SLOT_ROW_HEIGHT` before first layout. `computeDropTarget` gains an
    optional `rowHeight` parameter defaulting to `SLOT_ROW_HEIGHT`, so Phase 4's existing callers are
    untouched.
  - **Handle visibility:** the handle renders only when the sheet lists two or more exercises,
    reusing Phase 4's own `exercise-count >= 2` rule (04-UI-SPEC's D-23 amendment) rather than
    inventing a second rule.
  - **Commit:** on drop, the sheet computes the new ordered id array and calls
    `reorderSessionExercises(sessionId, orderedIds)`; the list re-renders from the returned order.
    Removed exercises (`removed_at` non-null) are neither listed nor renumbered.
  - **Modes:** available in `live` and `editing` — both render exercise pages through `ExercisePage`
    and therefore both reach `SessionActionSheet`. Not available in `summary-correction`, which has
    no overflow sheet. State this explicitly (D-32/R10).
  - **Copy:** heading **"Reorder Exercises"**; empty body **"No exercises to reorder"** with
    **"Add an exercise from the workout screen to get started."**; confirm **"Done"**. Add these to
    the Copywriting Contract table as a new row. No streak, nudge or shaming framing (P3).
  - Add a full new `### E12 — Reorder Exercises Sheet` block at the end of `## UI Considerations`
    with all nine state rows, each `explicit`:
    `Empty / no data` (a session whose every exercise was removed lists nothing and shows the empty
    copy above with Done still enabled — never a dead end, mirroring E11's rule);
    `Loading / in-flight` (none — reads already-loaded session state, R6);
    `Error / failure` (no async failure of its own; a failed `reorderSessionExercises` write leaves
    the list at its last committed order and the sheet stays open);
    `Populated / happy path` (one row per non-removed exercise in `order_index` order, name over
    fraction, trailing 48×48 handle);
    `Partial / incomplete` (partially-completed exercises show their real `N/M` fraction and are
    fully draggable — completion never gates reordering);
    `Overflow / truncation` (the card scrolls internally at its `max-h-full`; rows never clip and
    the list never scrolls horizontally);
    `Zero / one / many` (zero renders the empty copy; one renders a single row with no handle, since
    a one-element list has nowhere to go; many scroll internally — no plural copy anywhere);
    `Long text` (names wrap and grow the row per R4; the drag unit is the measured row height, so
    growth stays consistent with the drop arithmetic);
    `Large OS font scale` (rows grow rather than clip, the 48×48 handle floor is held, and the
    measured-row-height rule above keeps a one-row drag equal to a one-position move).
  - Add a `### E13 — Session Menu` block covering the same nine states for the three-row popover
    (fixed arity, no loading, inherits the screen's error state, no plural copy, labels wrap-and-grow).

  **Backstop accounting (no silent drops).** The existing `### Backstop Summary — planner action
  required` lists six unresolved backstops. Of those, **E4 Set Row — Error / failure** is in scope
  for this gap-closure run (05-14 touches E4) and is carried forward as a `backstop` must-have
  there. The other five — E8 Home In-Progress Banner error, E9 Workout Summary empty and error,
  E11 History tab populated and partial — name surfaces this run does not touch; append a line to
  the Backstop Summary recording that they remain **open and unresolved after gap closure**, so
  they are visibly deferred rather than quietly dropped.
  </action>
  <verify>
    <automated>grep -c "Amendment A — Gap-Closure Surfaces" .planning/phases/05-in-gym-session-logging/05-UI-SPEC.md && grep -c "E12 — Reorder Exercises Sheet" .planning/phases/05-in-gym-session-logging/05-UI-SPEC.md && grep -c "E13 — Session Menu" .planning/phases/05-in-gym-session-logging/05-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Amendment A — Gap-Closure Surfaces" .planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` returns 1.
    - `grep -c "### E12 — Reorder Exercises Sheet" ...05-UI-SPEC.md` returns 1 and that block contains 9 state rows.
    - `grep -c "### E13 — Session Menu" ...05-UI-SPEC.md` returns 1.
    - `grep -c "Reorder Exercises" ...05-UI-SPEC.md` returns at least 2 (contract + Copywriting Contract row).
    - The `SESSION_EXERCISE_ACTIONS` arity-of-four statement appears verbatim in A.3.
    - The Backstop Summary gains a line naming E8, E9 (both rows) and E11 (both rows) as still open after gap closure.
    - `git diff --stat .planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` shows additions only for existing sections other than the two E-block table rows A.1/A.2 explicitly add.
  </acceptance_criteria>
  <done>05-14 and 05-15 have a written, state-complete contract to implement against, and no previously-specified surface was rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the API coverage matrix</name>
  <files>.planning/phases/05-in-gym-session-logging/COVERAGE.md</files>
  <read_first>
    - apps/mobile/lib/rest-alert.ts (every `Notifications.*` call this phase actually makes: `setNotificationHandler`, `setNotificationChannelAsync`, `scheduleNotificationAsync` with a DATE trigger, `cancelScheduledNotificationAsync`, the permission getters, and the `Linking` deep-link to OS settings)
    - apps/mobile/lib/rest-alert.web.ts (the browser Notification API sibling: permission request, `new Notification(...)`, the visibility re-arm)
    - apps/mobile/components/NotificationPermissionPrompt.tsx (the D-22 onboarding request path)
    - .planning/phases/05-in-gym-session-logging/.plan-pre-contributions.md (the matrix format, the INTEGRATE-by-default rule, and the "a second integration against the same need re-decides from the same baseline" rule)
  </read_first>
  <action>
  The detector returned `{"detected": true}` on this phase. Re-reading the phase scope confirms it
  does integrate an SDK with a real capability surface — `expo-notifications` on native and the
  browser `Notification` API on web — so write the matrix rather than the
  `No external API integration:` declaration.

  Write `.planning/phases/05-in-gym-session-logging/COVERAGE.md` with a short preamble and **two**
  markdown-table matrices, because native and web are two integrations serving the same need and the
  second must be re-decided from the same full-coverage baseline rather than inheriting the first's
  opt-outs.

  **Matrix 1 — expo-notifications (native).** Enumerate only capabilities the SDK actually exposes;
  do not invent rows. Ground every INTEGRATE row in a real call site in `rest-alert.ts`. At minimum
  cover: scheduling a local notification on a DATE trigger; cancelling a scheduled notification;
  reading permission status; requesting permission; the foreground presentation handler; Android
  notification channels; deep-linking to OS settings when the OS will no longer prompt; interval /
  daily / weekly / calendar triggers; notification response listeners (tap-to-open routing);
  notification categories and interactive actions; badge count; dismissing or reading presented
  notifications; custom notification sounds; remote push tokens. Every capability starts at
  `INTEGRATE`; each `OPT-OUT` carries a one-line reason grounded in a phase decision (for example
  D-21's single wall-clock target for the non-DATE triggers, D-25/D-30's no-remote-push posture for
  push tokens, R9's in-app sound/haptic baseline for custom sounds).

  **Matrix 2 — browser Notification API (web sibling).** Re-decide the same need from the full
  baseline: permission query, permission request, showing a notification, the visibility-change
  re-arm, notification actions, `tag`/`renotify` coalescing, ServiceWorker `showNotification`
  (persistent notifications that survive a closed tab), `onclick` focus routing. Same rules: default
  INTEGRATE, every OPT-OUT reasoned.

  Do not fabricate a row for a capability that does not exist in either surface, and do not record
  an OPT-OUT without a reason — an unreasoned opt-out is the undecided hole this gate exists to
  close.
  </action>
  <verify>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/05-in-gym-session-logging | grep -c '"passed": true'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-in-gym-session-logging/COVERAGE.md` exists.
    - `node .claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/05-in-gym-session-logging` emits `"passed": true` and `"block": false` — the gate returns JSON, so the exit code alone is not the signal. Today it emits `"passed": false, "block": true, "coverage_present": false`; that must flip.
    - Every table row's decision is exactly `INTEGRATE` or `OPT-OUT`.
    - `grep -c "OPT-OUT |  *$" .planning/phases/05-in-gym-session-logging/COVERAGE.md` returns 0 — no OPT-OUT has an empty reason cell.
    - Both matrices are present and headed separately (native SDK, browser API).
    - Every `INTEGRATE` row names a real call site in `rest-alert.ts` or `rest-alert.web.ts`.
  </acceptance_criteria>
  <done>The phase can seal: `api-coverage.verify-pre` finds a valid, reasoned matrix instead of blocking on a missing one.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact -> executor | This plan produces contract documents that later plans implement verbatim; a wrong contract propagates into code |
| (no runtime boundary) | This plan ships no executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13-01 | Repudiation | `05-UI-SPEC.md` amendment | low | mitigate | The amendment is additive and committed as its own diff; the acceptance criteria assert the existing E1–E11 blocks are not rewritten, so the original contract stays auditable next to the amendment. |
| T-05-13-02 | Information disclosure | `COVERAGE.md` | low | accept | The matrix records capability decisions only — no credential, endpoint secret or token appears in it. |
| T-05-13-03 | Tampering | supply chain (package installs) | n/a | n/a | No package-manager install occurs; this plan writes two markdown files. |
</threat_model>

<ui_considerations_lift>
This plan **authors** the missing considerations rather than lifting them; the lift into
`must_haves` happens in the implementing plans:
- E4 Set Row rows (including the `⚠ backstop` **Error / failure** row) -> 05-14.
- E6 Per-Exercise Action Bar rows -> 05-14 (the note-badge and fixed-arity rules it must not break).
- E10 Session Action Sheet rows and the new E12 -> 05-15.
- The new E13 Session Menu rows -> 05-14.
No E-block row in scope for this run is dropped; the five out-of-scope backstops (E8, E9 ×2,
E11 ×2) are explicitly recorded as still open in Task 1's Backstop Summary edit.
</ui_considerations_lift>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Where |
|---|---|---|
| `## Amendment A — Gap-Closure Surfaces (2026-08-25)` | new spec section | `05-UI-SPEC.md` |
| `### E12 — Reorder Exercises Sheet` | new UI-Considerations block | `05-UI-SPEC.md` |
| `### E13 — Session Menu` | new UI-Considerations block | `05-UI-SPEC.md` |
| `ReorderExercisesSheet` | component name reserved by contract (built in 05-15) | `05-UI-SPEC.md` |
| `COVERAGE.md` | new phase artifact | `.planning/phases/05-in-gym-session-logging/` |
</artifacts_this_phase_produces>

<verification>
- Both `grep` gates in Task 1's `<verify>` return 1.
- `node .claude/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/05-in-gym-session-logging` reports `"passed": true` and `"block": false`.
- `git diff` on `05-UI-SPEC.md` shows no deletion of an existing E1–E11 row.
</verification>

<success_criteria>
- Three previously-unspecified surfaces now have written, state-complete contracts.
- `SessionActionSheet`'s arity-of-four contract is preserved in writing.
- The seal-blocking `COVERAGE.md` gap is closed with a reasoned matrix, not a placeholder.
- The five out-of-scope UI backstops are visibly deferred, not dropped.
</success_criteria>

<out_of_scope>
- Implementing any of the three surfaces — that is 05-14 and 05-15.
- Resolving the E8/E9/E11 backstops (Home banner error state, summary empty/error states, history row anatomy): those surfaces are outside this gap-closure run's four confirmed gaps and stay open.
- Any device/simulator design verification (no Xcode, no Android SDK — WINDOWS #110, #129).
</out_of_scope>

<output>
Create `.planning/phases/05-in-gym-session-logging/05-13-SUMMARY.md` when done.
</output>
