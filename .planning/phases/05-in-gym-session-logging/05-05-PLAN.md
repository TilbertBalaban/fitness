---
phase: 05-in-gym-session-logging
plan: 05
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/lib/rest-timer.ts
  - apps/mobile/lib/rest-alert.ts
  - apps/mobile/lib/rest-alert.web.ts
  - apps/mobile/lib/__tests__/rest-timer.test.ts
  - apps/mobile/lib/__tests__/rest-alert.test.ts
  - apps/mobile/components/RestTimerBar.tsx
  - apps/mobile/components/RestTimerFullScreen.tsx
  - apps/mobile/components/NotificationPermissionPrompt.tsx
  - apps/mobile/components/__tests__/RestTimerBar.test.tsx
  - apps/mobile/app/rest-timer.tsx
  - apps/mobile/app/(tabs)/workout.tsx
  - apps/mobile/e2e/rest-timer.spec.ts
  - docs/platform-modules.md
autonomous: false
requirements: [LOG-08, LOG-09, LOG-10, LOG-11]
windows_id_range: "143-152"

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The rest timer is a persisted wall-clock target timestamp on `workout_session.rest_target_at`, never a JS interval: remaining time is recomputed from that stored timestamp on every foreground, so a backgrounded, throttled or force-quit app shows the correct number the instant it is visible again (D-21)."
    - "Completing a set writes the rest target as the completion moment plus `session_exercise.target_rest_seconds` — the frozen snapshot value already resolved through the cycle override — and schedules exactly one OS-level alert for it (D-26, LOG-08)."
    - "The rest timer stops and clears at zero with no overtime count-up, and the actual rest taken is written to `logged_set.rest_taken_seconds`, a column that exists today and has never been written (D-26)."
    - "Extend adds a fixed increment to the stored target and reschedules the alert; skip clears the target and cancels the alert; undoing a completed set while its timer runs cancels the scheduled alert — all three operate on the stored timestamp, so all three survive backgrounding identically to the original schedule (D-27, LOG-10)."
    - "Platform divergence is a `.web.ts` sibling resolved at build time: `rest-alert.ts` schedules through `expo-notifications` and `rest-alert.web.ts` through the browser `Notification` API; no call site anywhere branches on `Platform.OS` (D-08, D-25)."
    - "Notification permission is requested before any workout is started, from the Workout tab's pre-session state, with rationale copy stating why — never as an OS dialog interrupting the gym floor (D-22)."
    - "A denied permission degrades to in-app sound plus haptic with a persistent inline note stating that background alerts are off and how to turn them on; the countdown still runs and still alerts whenever the app is foregrounded (D-23, R9)."
    - "The re-request path deep-links to OS Settings rather than calling `requestPermissionsAsync` a second time, because iOS will not re-prompt after a denial (D-22)."
    - "Both timers live in one persistent header bar above the exercise strip — duration counting up on the left, rest counting down on the right — and the rest column is dormant-but-present at `0:00` between sets so its position never moves under a thumb (D-24, LOG-11)."
    - "Tapping the rest column opens a full-screen countdown with extend and skip; the countdown runs identically whether that screen is open or not, because the stored-timestamp model makes that free (D-24, LOG-10)."
    - "The background/lock-screen half of LOG-09 cannot be observed on this machine and is filed as a `.planning/WINDOWS.md` unrun-verify entry against ROADMAP Phase 999.1 rather than claimed as verified (D-10, D-25)."
    # --- Edge predicates: LOG-08 ---
    - "Completing a set while a previous rest target is still outstanding cancels the outstanding alert before scheduling the new one, so exactly one alert is ever scheduled for a session (LOG-08 adjacency)."
    - "An exercise whose snapshot `target_rest_seconds` is null starts no rest timer on completion; the header's rest column stays dormant at 0:00 and no alert is scheduled (LOG-08 empty)."
    - "`remainingSeconds(targetMs, nowMs)` returns 0 and never a negative number for any `now` at or past the target, and is a pure function of its two arguments with no clock read of its own (LOG-08 ordering)."
    # --- UI-SPEC E1 Header Timer Bar ---
    - "The bar has no loading state: duration derives from `started_at` and rest from the stored target, so both hold a real value on first paint (UI-SPEC E1 loading, R6)."
    - "The bar has no failure mode of its own — a session that fails to load renders the parent screen's error state and the bar is never mounted — and a denied notification permission is not an error here (UI-SPEC E1 error, R9)."
    - "Two fixed columns in a `flex-row justify-between` with no scroll; the `H:MM:SS` readout is the sizing case and both columns hold position and size between dormant and active (UI-SPEC E1 overflow)."
    - "The bar grows in height rather than clipping at large OS font scale, and numerals are never abbreviated or ellipsised (UI-SPEC E1 long text, R4)."
    # --- UI-SPEC E2 Rest Timer Full-Screen ---
    - "No loading state: the screen opens from an already-running timer whose remaining time comes from the stored timestamp, so it renders its true value on the first frame (UI-SPEC E2 loading)."
    - "No network or async dependency and therefore no error state; a denied permission leaves the screen fully functional (UI-SPEC E2 error, R9)."
    - "Centered fixed layout with no scroll; if OS font scale pushes the control pair past the width they stack vertically rather than clipping (UI-SPEC E2 overflow, R4)."
    - "Control labels wrap and grow; the countdown numeral is never truncated (UI-SPEC E2 long text, R4)."
  artifacts:
    - apps/mobile/lib/rest-timer.ts
    - apps/mobile/lib/rest-alert.ts
    - apps/mobile/lib/rest-alert.web.ts
    - apps/mobile/components/RestTimerBar.tsx
    - apps/mobile/components/RestTimerFullScreen.tsx
    - apps/mobile/app/rest-timer.tsx
    - apps/mobile/e2e/rest-timer.spec.ts
  key_links:
    - "The set-completion path in `SetRow`/`workout.tsx` calls `startRest` once, which writes `workout_session.rest_target_at` and calls `scheduleRestAlert` from the platform-resolved `rest-alert` module — one call site, not a branch per platform."
    - "`RestTimerBar` reads its two numbers from `elapsedWorkoutSeconds` and `remainingSeconds`, both pure functions taking `now` as an argument, so the bar itself owns no timing logic."
    - "Undo in `SetRow` calls `cancelRestAlert` and clears `rest_target_at` in the same write, so a cancelled set never leaves an orphan alert scheduled (D-27)."
  prohibitions:
    - statement: "MUST NOT present a rest timer, countdown, or alert affordance that appears functional while background alerting is silently unavailable — a denied or unavailable notification permission must be stated inline on the workout screen, never hidden."
      status: unresolved
      verification: judgment
    - statement: "MUST NOT use the rest-timer notification channel, the finish summary, or the in-progress banner for engagement or retention nudging, streak pressure, or shaming framing — every message this phase emits is factual about the workout in progress or just finished."
      status: unresolved
      verification: judgment
---

## Phase Goal (verbatim from ROADMAP.md — not in user-story format)

> The user can walk into a gym with no signal and log a complete workout without friction. **This is the phase the app becomes real** — dogfooding starts here.

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| file | `apps/mobile/lib/rest-timer.ts` |
| function | `remainingSeconds`, `restTargetFrom`, `elapsedWorkoutSeconds`, `formatClock` |
| const | `REST_EXTEND_SECONDS` |
| file | `apps/mobile/lib/rest-alert.ts` / `apps/mobile/lib/rest-alert.web.ts` |
| function | `scheduleRestAlert`, `cancelRestAlert`, `requestAlertPermission`, `getAlertPermission`, `openAlertSettings`, `playInAppRestAlert` |
| type | `AlertPermission` (`'granted' \| 'denied' \| 'undetermined' \| 'unsupported'`) |
| file | `apps/mobile/components/RestTimerBar.tsx` |
| component | `RestTimerBarView`, `RestTimerBar` |
| file | `apps/mobile/components/RestTimerFullScreen.tsx` |
| component | `RestTimerFullScreenView`, `RestTimerFullScreen` |
| file | `apps/mobile/components/NotificationPermissionPrompt.tsx` |
| component | `NotificationPermissionPromptView`, `NotificationPermissionPrompt`, `BackgroundAlertsOffNote` |
| route | `apps/mobile/app/rest-timer.tsx` |
| file | `apps/mobile/e2e/rest-timer.spec.ts` |
| npm dependency | `expo-notifications` |

## Broken-windows ledger reservation

Reserved `.planning/WINDOWS.md` ids **143–152**.

<objective>
Make the rest timer real: it starts itself when a set is completed, keeps correct time through backgrounding because it is a stored wall-clock target rather than a JS interval, alerts the user through the OS on native and the browser on web, and degrades honestly to in-app sound plus a visible note when notification permission is denied. Plus the workout duration clock beside it in one persistent header bar.

Purpose: PITFALLS §6 names a timer that stops when the phone locks as the loved-versus-abandoned divider for this product category, and success criterion 3 fails without it.
Output: one pure timing module, one platform-split alert seam, the header bar, the full-screen extend/skip view, the permission surfaces, a browser-driven e2e, and an honest ledger entry for the half that cannot be observed here.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-in-gym-session-logging/05-CONTEXT.md
@.planning/phases/05-in-gym-session-logging/05-RESEARCH.md
@.planning/phases/05-in-gym-session-logging/05-PATTERNS.md
@.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md
@.planning/phases/05-in-gym-session-logging/05-01-SUMMARY.md
@.planning/phases/05-in-gym-session-logging/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate for expo-notifications before any install runs</name>
  <what-built>Nothing yet — this gate runs BEFORE the install. `05-RESEARCH.md`'s Package Legitimacy Audit returned a **`SUS` (`too-new`)** verdict for `expo-notifications`, the one net-new runtime dependency this phase adds. The researcher's own reading is that the flag is a false positive from a recency heuristic firing on a first-party Expo SDK package's frequent patch cadence (4.27M weekly downloads, source at `github.com/expo/expo`, no postinstall script) — but a `SUS` verdict is never auto-approvable and never silently installed.</what-built>
  <how-to-verify>
1. Open `https://www.npmjs.com/package/expo-notifications` and confirm the repository field points at `github.com/expo/expo`, the weekly download count is in the millions, and the publisher is the Expo org.
2. Run `npm view expo-notifications version` and confirm the resolved version is on the `57.x` train — this project pins every `expo-*` package to the installed Expo SDK major (`expo@57.0.12` in `apps/mobile/package.json`), the same discipline `docs/platform-modules.md` records for `react-native-gesture-handler` and `react-native-reanimated`.
3. Run `npm view expo-notifications scripts` and confirm there is no `postinstall`, `preinstall` or `prepare` script.
4. Confirm no other new runtime dependency is being added by this plan. `expo-keep-awake` was evaluated by the researcher and is deliberately NOT installed — screen-keep-awake sits in CONTEXT.md's Deferred Ideas block.
  </how-to-verify>
  <resume-signal>Type "approved" to allow `npx expo install expo-notifications`, or name a different version/package to use instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install expo-notifications, and build the pure timing math behind a platform-split alert seam</name>
  <reversibility rating="one-way">D-21: a timer that stops when the phone locks is the named loved-versus-abandoned divider for this product category, and success criterion 3 fails without the stored-timestamp model. A JS-interval implementation cannot be retrofitted into one.</reversibility>
  <read_first>
    - `apps/mobile/package.json` — the installed `expo` version the new package must pin against, and the existing dependency block
    - `apps/mobile/app.json` — the `plugins` array `expo-notifications` must be registered in
    - `docs/platform-modules.md` — the `.web.tsx`/`.web.ts` sibling convention (D-08), the native-capability web audit table, and the Notifications row that already anticipates this module
    - `apps/mobile/lib/db/powersync.ts` and `apps/mobile/lib/db/powersync.web.ts` — a shipped example of the platform-sibling pair in this codebase
    - `apps/mobile/lib/programs/next-up.ts` — the pure-module contract: no clock read inside a function body, `now` arrives as a defaulted argument
    - `apps/mobile/lib/db/log-set.ts` — where `startRest` / `clearRest` writes belong, beside the existing `logSet` write
    - `apps/mobile/lib/db/schema.ts` — `workout_session.rest_target_at` and `logged_set.rest_taken_seconds`
    - `.planning/phases/05-in-gym-session-logging/05-RESEARCH.md` §8 — the `scheduleNotificationAsync` DATE-trigger API, `setNotificationHandler`, the Android channel requirement, and the permission-flow rules
  </read_first>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/lib/rest-timer.ts, apps/mobile/lib/rest-alert.ts, apps/mobile/lib/rest-alert.web.ts, apps/mobile/lib/__tests__/rest-timer.test.ts, apps/mobile/lib/__tests__/rest-alert.test.ts</files>
  <action>
Install with `npx expo install expo-notifications` from `apps/mobile` so the version resolves against the installed SDK 57 train rather than latest-wins. Register the `expo-notifications` config plugin in `apps/mobile/app.json`'s `plugins` array. Record in the plan summary that this is a prebuild-affecting change requiring a fresh dev-client build, which this machine cannot produce (D-10).

`apps/mobile/lib/rest-timer.ts` — pure, testable with no notification library present at all, following `next-up.ts`'s contract exactly (arguments in, no ambient clock read, `now` a defaulted-but-overridable parameter). Export:
`remainingSeconds(targetTimestampMs: number | null, nowMs = Date.now()): number` — 0 when the target is null or already passed, never negative;
`restTargetFrom(completedAtMs: number, targetRestSeconds: number | null): number | null` — null when `targetRestSeconds` is null or non-positive, so an exercise with no prescribed rest starts no timer (D-26);
`elapsedWorkoutSeconds({ startedAtMs, accumulatedPausedSeconds, pausedAtMs, nowMs })` — D-29's accounting: elapsed since start, minus accumulated pause, minus the currently-open pause if any, floored at 0;
`formatClock(seconds: number): string` — `M:SS` under an hour and `H:MM:SS` at or above one, which is the sizing case the header bar's fixed columns are laid out for;
`REST_EXTEND_SECONDS = 30`, the fixed increment D-27's extend adds.

`apps/mobile/lib/rest-alert.ts` (native) and `apps/mobile/lib/rest-alert.web.ts` (web) — one seam, two implementations, identical exported signatures, resolved at build time by Metro. Neither file may contain a `Platform.OS` comparison; that is what the sibling exists to replace (D-08). Export from both:
`scheduleRestAlert(targetTimestampMs: number): Promise<void>` — cancels any outstanding alert first so exactly one is ever pending, then schedules;
`cancelRestAlert(): Promise<void>`;
`requestAlertPermission(): Promise<AlertPermission>`;
`getAlertPermission(): Promise<AlertPermission>` where `AlertPermission` is `'granted' | 'denied' | 'undetermined' | 'unsupported'`;
`openAlertSettings(): Promise<void>`;
`playInAppRestAlert(): Promise<void>` — the D-23 degraded path: sound plus haptic, invoked by the foregrounded countdown reaching zero regardless of permission state.

Native implementation: `Notifications.setNotificationHandler` registered once at module load so a foregrounded alert actually displays (the library's default suppresses foreground notifications entirely); `Notifications.setNotificationChannelAsync('rest-timer', ...)` at HIGH importance for Android 8+; `scheduleNotificationAsync` with a `SchedulableTriggerInputTypes.DATE` trigger at the target `Date` — use the current typed trigger union, not the older bare-object form; keep the scheduled identifier in a module-level variable and `cancelScheduledNotificationAsync` it before every new schedule. `getAlertPermission` uses `getPermissionsAsync`; `requestAlertPermission` uses `requestPermissionsAsync` and must never be called a second time after a denial on iOS — the caller's only path back is `openAlertSettings`, which uses `Linking.openSettings()`.

Web implementation: `Notification.requestPermission()` and `new Notification(title, { body })`. Return `'unsupported'` from both permission functions when `Notification` is absent from the global scope rather than throwing. Schedule with a `setTimeout` whose delay is recomputed against the stored wall-clock target, and re-arm it on every `visibilitychange` so a throttled or hidden tab fires at the right moment instead of drifting.

Notification content, for both platforms: title "Rest complete", body "Time for your next set." Factual, about the workout in progress. Nothing in this channel is a re-engagement nudge, a streak reminder, or a comment on the user's performance.

`apps/mobile/lib/db/log-set.ts` is NOT edited by this plan (05-06 owns it in wave 3). Put `startRest(sessionId, targetTimestampMs, db)` and `clearRest(sessionId, db)` in `apps/mobile/lib/rest-timer.ts`'s sibling call sites inside `RestTimerBar`'s wrapper and the `workout.tsx` completion handler instead, writing `workout_session.rest_target_at` through a plain Drizzle update — the same shape `SetRow`'s undo already uses.

`apps/mobile/lib/__tests__/rest-timer.test.ts`: cover `remainingSeconds` at, before and well past the target, with a null target, and across a multi-hour elapsed span; `restTargetFrom` with null, zero and negative rest seconds; `elapsedWorkoutSeconds` with no pause, an accumulated pause, an open pause, and both; `formatClock` at 0, 59, 60, 3599 and 3600 seconds.
`apps/mobile/lib/__tests__/rest-alert.test.ts`: with `expo-notifications` mocked, assert `scheduleRestAlert` cancels before scheduling so only one alert is ever pending, that `cancelRestAlert` is a no-op when nothing is scheduled, and that `requestAlertPermission` is not called again once the stored state is denied.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "rest-(timer|alert)" &amp;&amp; pnpm --filter mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/package.json` lists `expo-notifications` at a `57.x` version, and `apps/mobile/app.json`'s `plugins` array names `expo-notifications`.
    - `apps/mobile/lib/rest-timer.ts` exports `remainingSeconds`, `restTargetFrom`, `elapsedWorkoutSeconds`, `formatClock` and `REST_EXTEND_SECONDS`, and a source scan of the file for `Date.now(` finds it only in default-parameter position.
    - A test asserts `remainingSeconds(t, t + 60_000)` returns 0 and never a negative number.
    - A test asserts `restTargetFrom(now, null)` returns null.
    - A test asserts `elapsedWorkoutSeconds` with an open pause is frozen at the value it held when the pause opened.
    - Both `apps/mobile/lib/rest-alert.ts` and `apps/mobile/lib/rest-alert.web.ts` export the same six function names, and a comment-filtered scan of each reports 0 code occurrences of a runtime platform branch: `grep -vE '^\s*(//|\*|/\*)' apps/mobile/lib/rest-alert.ts apps/mobile/lib/rest-alert.web.ts | grep -c 'Platform'` reports 0 (D-08).
<!-- planner-discipline-allow: Platform -->
    - `pnpm --filter mobile test -- --testPathPattern "rest-(timer|alert)"` exits 0 with non-zero counts in both suites.
    - `pnpm --filter mobile typecheck` exits 0 and `pnpm --filter mobile build` (the web export) succeeds, proving the web sibling resolves.
  </acceptance_criteria>
  <done>The rest timer's arithmetic is a pure, fully unit-tested module, and scheduling an OS alert is one call behind a build-time platform seam with no `Platform.OS` branch anywhere.</done>
</task>

<task type="auto">
  <name>Task 3: The header timer bar, the full-screen countdown, and honest permission surfaces</name>
  <read_first>
    - `apps/mobile/lib/rest-timer.ts` and `apps/mobile/lib/rest-alert.ts` — the modules built in Task 2
    - `apps/mobile/app/(tabs)/workout.tsx` — the live screen from 05-01, including `SessionModeProvider` and the set-completion handler this task hooks into
    - `apps/mobile/components/CycleStrip.tsx` — the hook-free `*View` + thin wrapper split every component here follows
    - `apps/mobile/components/PrimaryButton.tsx` and `apps/mobile/components/ErrorBanner.tsx` — the shipped CTA and inline-note visual language to reuse rather than reinvent
    - `apps/mobile/app/reset-password.tsx` — an existing non-tab route, for the route-file shape `app/rest-timer.tsx` follows
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Header Timer Bar, §Rest Timer Full-Screen, §Copywriting Contract (rows: Primary CTA, Notification permission rationale, Notification degraded-state banner, Time-off / paused states), §Phase-Wide Rules R9
    - `apps/mobile/e2e/durability.spec.ts` and `apps/mobile/e2e/workout-screen.spec.ts` — the Playwright harness shape the new spec follows
  </read_first>
  <files>apps/mobile/components/RestTimerBar.tsx, apps/mobile/components/RestTimerFullScreen.tsx, apps/mobile/components/NotificationPermissionPrompt.tsx, apps/mobile/components/__tests__/RestTimerBar.test.tsx, apps/mobile/app/rest-timer.tsx, apps/mobile/app/(tabs)/workout.tsx, apps/mobile/e2e/rest-timer.spec.ts, docs/platform-modules.md</files>
  <action>
`apps/mobile/components/RestTimerBar.tsx` (D-24, LOG-11): hook-free `RestTimerBarView` plus a thin `RestTimerBar` wrapper resolving `useThemeColors()` and holding the one-second re-render tick. Per 05-UI-SPEC §Header Timer Bar: `flex-row justify-between` on `bg-secondary`, 16px horizontal and 8px vertical padding; left column label "Workout" in muted Label over the duration readout in Display semibold; right column right-aligned, label "Rest" over the countdown in Display semibold. Between sets the right column reads `0:00` in `text-foreground-muted` rather than being hidden — the column's position and size are identical dormant and active, so nothing shifts under a thumb reaching for the strip below. The whole right column is the hit target and pushes the full-screen route; the left column is informational and takes no press handler. In the paused state the left label reads "Paused" and its readout freezes in muted colour. The countdown numerals stay `text-foreground` at every value — there is no red or amber urgency treatment, because D-26 already means there is no over-time state to warn about.

`apps/mobile/components/RestTimerFullScreen.tsx` and the `apps/mobile/app/rest-timer.tsx` route (LOG-10, D-27): a full-screen route, not a bottom sheet. Centered countdown at `fontSize: 64` semibold — a one-time exception, not a new typography role. Two 48x48-minimum controls side by side beneath it: "+30s" adding `REST_EXTEND_SECONDS` to the stored `rest_target_at` and rescheduling the alert, and "Skip Rest" clearing the stored target and cancelling the alert. Both are Body-role semibold `text-accent` on `bg-secondary` — secondary weight, not the primary CTA fill. A 48x48 dismiss control at the top-left returns to the workout screen without altering the timer. At zero the countdown reads `0:00`, the screen does not auto-dismiss, and the extend/skip pair is replaced by a single full-width "Back to Workout" primary CTA. If OS font scale pushes the control pair past the available width they stack vertically rather than clipping.

`apps/mobile/components/NotificationPermissionPrompt.tsx` (D-22, D-23): two exported surfaces. `NotificationPermissionPrompt` renders on the Workout tab's pre-session state — before any workout has been started, which is what D-22's "during onboarding" means on the surface that owns the timer — with the copy from 05-UI-SPEC §Copywriting Contract verbatim: heading "Rest Timer Alerts", body "So your rest timer can alert you even with your phone in your pocket.", actions "Allow Notifications" and "Not Now". It renders only when `getAlertPermission()` returns `'undetermined'`. `BackgroundAlertsOffNote` renders on the live workout screen whenever permission is `'denied'` or `'unsupported'`: "Background alerts are off — your rest timer will still count down and sound/vibrate while the app is open." with a "Turn On" action calling `openAlertSettings`. It is dismissible for the current session but returns on the next one — nothing about the timer may appear to work while quietly not working.

`apps/mobile/app/(tabs)/workout.tsx`: mount `RestTimerBar` above the exercise strip; mount `NotificationPermissionPrompt` in the pre-session state and `BackgroundAlertsOffNote` in the live state. In the set-completion handler from 05-01, after `logSet` returns, compute the target with `restTargetFrom(completedAt, sessionExercise.targetRestSeconds)`, write it to `workout_session.rest_target_at`, and call `scheduleRestAlert` when it is non-null. In the undo handler, clear `rest_target_at` and call `cancelRestAlert` (D-27). When a set is completed while a target is still outstanding, write `logged_set.rest_taken_seconds` for the PREVIOUS completed set as the seconds actually elapsed between its completion and this one (D-26 — the column exists today and has never been written). Gate every scheduling call site on `useSessionMode() === 'live'`, never on `session.status` (D-32, UI-SPEC R10).

`apps/mobile/e2e/rest-timer.spec.ts`, in the existing `durability` Playwright project: grant browser notification permission through the Playwright context, complete a set in the harness workout screen, assert a `Notification` was constructed at the expected wall-clock target (stub the global `Notification` constructor in an init script and record its calls), assert the countdown recomputes correctly after the page is hidden and shown again via the visibility API, assert "+30s" moves the target and "Skip Rest" clears it, and assert undoing the completed set cancels the pending alert. Then run the same flow with permission denied and assert the inline note renders and the countdown still runs.

`docs/platform-modules.md`: add `rest-alert.ts` / `rest-alert.web.ts` to the platform-module table with a row recording what each side uses and what the web side cannot do, matching the file's existing shape.

File one `.planning/WINDOWS.md` unrun-verify entry in the reserved 143–152 range against ROADMAP Phase 999.1, stating precisely what was not observed: that a scheduled `expo-notifications` alert actually fires and is audible or visible while the app is fully backgrounded and the phone is locked, on a real iOS or Android device. Do not weaken success criterion 3 to match what this machine can prove.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "RestTimerBar" &amp;&amp; pnpm --filter mobile test:e2e:durability -- rest-timer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/components/RestTimerBar.tsx` exports `RestTimerBarView` (hook-free, taking `colors` as a prop) and `RestTimerBar`.
    - `RestTimerBar.test.tsx` asserts by direct invocation: the dormant state renders `0:00` in the muted tone with the same column structure as the active state; the paused state renders the "Paused" label with a frozen readout; a duration past one hour renders in `H:MM:SS`; and the countdown tone is the same at 5 seconds remaining as at 60.
    - `apps/mobile/app/rest-timer.tsx` exists as a route and `RestTimerFullScreen.tsx` exports a hook-free `RestTimerFullScreenView`.
    - `pnpm --filter mobile test:e2e:durability -- rest-timer.spec.ts` exits 0, including the hidden-then-visible recompute case, the extend case, the skip case, the undo-cancels case, and the permission-denied degraded case.
    - `NotificationPermissionPrompt.tsx` contains the rationale body string and the degraded-state body string exactly as written in 05-UI-SPEC §Copywriting Contract.
    - Every `scheduleRestAlert` call site in `apps/mobile/app/(tabs)/workout.tsx` sits inside a branch gated on the session-mode value; a source scan of that file for `session.status ===` returns no match (UI-SPEC R10).
    - `docs/platform-modules.md` contains a row for `rest-alert`.
    - A `.planning/WINDOWS.md` entry in the 143–152 range records the background/lock-screen delivery gap against Phase 999.1.
    - `pnpm --filter mobile typecheck` and `pnpm --filter mobile build` both exit 0.
  </acceptance_criteria>
  <done>The rest timer starts itself on set completion, keeps correct time across backgrounding, alerts through the browser on web and through the OS on native, offers extend and skip full-screen, and says so plainly when background alerting is off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `apps/mobile/node_modules` | A net-new runtime dependency enters the build |
| app → OS notification service | Notification content leaves the app and may render on a locked screen |
| browser tab visibility → scheduled alert | A hidden or throttled tab's timer must re-derive rather than drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | `npx expo install expo-notifications` | high | mitigate | The `SUS` (`too-new`) verdict from `05-RESEARCH.md`'s Package Legitimacy Audit is gated by the blocking human checkpoint in Task 1: registry repository field, weekly downloads, absence of install scripts, and a `57.x` version pin all confirmed before the install runs. Never auto-approvable. |
| T-05-05-01 | Information disclosure | notification body on a locked screen | medium | mitigate | The alert title and body are fixed strings ("Rest complete" / "Time for your next set.") carrying no exercise name, weight, or any other training detail, so a lock-screen preview reveals nothing about the user's session (ASVS V5 output minimisation). |
| T-05-05-02 | Spoofing | `Linking.openSettings()` deep link | low | accept | The re-request path opens the OS's own settings surface for this app with no parameters; there is no attacker-controllable target. Recorded rather than omitted. |
| T-05-05-03 | Denial of service | web `setTimeout` re-arm on `visibilitychange` | medium | mitigate | The re-arm computes its delay from the stored wall-clock target and clears any prior handle first, so repeated visibility flapping cannot accumulate handles or schedule a storm of alerts. |
| T-05-05-04 | Tampering | `workout_session.rest_target_at` via sync | low | accept | The column rides the existing `workout_session` PATCH path already scoped to the row's owner; a hostile value only mis-times that user's own rest timer. |
</threat_model>

## Flagged planner assumptions (unresolved edge probes)

- **LOG-09 (unclassified)** — "rest timer keeps correct time and alerts when the app is backgrounded or the screen is locked" produced no shape classification. Planner assumption: the web half is fully exercisable here and is exercised; the native background/lock half has no automated boundary that can be probed on this machine and is filed as an unrun-verify. Flagged, not resolved.
- **LOG-10 (unclassified)** — "extend or skip the rest timer, and view it full-screen" produced no shape classification. Planner assumption: the only ambiguous boundary is extending a timer that has already reached zero, which this plan resolves by replacing the extend/skip pair with a single Back-to-Workout action at zero. Flagged, not resolved.
- **LOG-11 (unclassified)** — "workout duration timer running for the session" produced no shape classification. Planner assumption: the boundaries are all pause-related and are covered by `elapsedWorkoutSeconds`'s unit cases; whether a duration should keep counting across a multi-day forgotten session is unresolved and belongs with 05-07's discard path. Flagged, not resolved.

<verification>
- `pnpm --filter mobile test` green with non-zero counts in the three new suites.
- `pnpm --filter mobile test:e2e:durability` green including `rest-timer.spec.ts`.
- `pnpm --filter mobile build` green (proves the `.web.ts` sibling resolves in the web export).
- `pnpm typecheck` and `pnpm lint` green at the repo root.
- The native background-delivery gap is a filed ledger entry against Phase 999.1, not a weakened criterion.
</verification>

<success_criteria>
- Completing a set starts the rest timer automatically from the snapshot's rest target, and the timer survives backgrounding because it recomputes from a stored timestamp.
- Extend, skip and undo all operate on that stored timestamp and reschedule or cancel the alert accordingly.
- The header bar shows a running duration and a dormant-but-present rest column that never shifts position.
- A denied permission leaves a fully working in-app countdown plus a visible note explaining what is off and how to turn it on.
- Success criterion 3's device half is filed, not claimed.
</success_criteria>

<output>
Create `.planning/phases/05-in-gym-session-logging/05-05-SUMMARY.md` when done.
</output>
