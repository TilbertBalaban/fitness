---
phase: 05-in-gym-session-logging
plan: 16
type: execute
wave: 5
depends_on:
  - 05-12
  - 05-14
  - 05-15
gap_closure: true
autonomous: true
requirements:
  - LOG-14
  - LOG-15
  - LOG-16
files_modified:
  - apps/mobile/e2e/durability.spec.ts
  - apps/mobile/e2e/schema-redefinition.spec.ts
  - apps/mobile/e2e/catalog-load.spec.ts
  - apps/mobile/e2e/workout-screen.spec.ts
  - apps/mobile/e2e/rest-timer.spec.ts
  - apps/mobile/e2e/session-lifecycle.spec.ts
  - apps/mobile/e2e/session-edit.spec.ts
  - apps/mobile/e2e/history.spec.ts
  - apps/mobile/e2e/workout-summary.spec.ts
  - .planning/WINDOWS.md

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every spec in the Playwright `durability` project has actually been executed at least once and its real result recorded — no spec is credited for existing, typechecking, or being registered."
    - "The full `durability` project passes: `pnpm --filter mobile test:e2e:durability` exits 0 with 0 failed and 0 skipped."
    - "SC4 is proven by execution, not by inspection: durability.spec.ts's 'force-quitting mid-workout with warm-ups logged and a pause open loses nothing on reopen' case (line ~219) passes against a real @powersync/web database in a real browser."
    - "VERIFICATION.md's `human_verification` item 'Run the full pnpm --filter mobile test:e2e:durability project once a browser session is explicitly authorized' is satisfied by a recorded run, so it no longer needs a human."
    - "Any genuine defect the first execution exposes is fixed at its source; no assertion is loosened, no case is skipped, and no retry is added to reach green."
    - "WINDOWS rows #109, #116, #118 and #123 are marked fixed only after the behaviour each describes is proven by a passing automated check — never on the strength of a code edit alone."
    - "Failures that are genuinely environmental or genuinely out of scope are recorded as new WINDOWS entries with their real cause, rather than absorbed silently."
  artifacts:
    - "A recorded, verbatim `durability` project run in 05-16-SUMMARY.md (spec count, pass/fail counts, exit code)"
    - ".planning/WINDOWS.md — #109, #116, #118, #123 resolved; any newly discovered deviation filed"
  key_links:
    - "playwright.config.ts durability testMatch -> the 12 specs (9 pre-existing + target-write-back + session-notes + reorder-exercises)"
    - "durability.spec.ts force-quit case -> Roadmap SC4"
    - "the passing suite -> VERIFICATION.md's behavior_unverified and human_verification items"
  prohibitions:
    - "A spec is never marked skipped, fixme or only to reach a green run."
    - "An assertion is never weakened, split into a weaker one, or wrapped in a retry to accommodate a failure."
    - "A failing case is never deleted; if its behaviour is genuinely out of scope it is recorded as an open WINDOWS entry with its real reason."
    - "A WINDOWS row is never marked fixed on the basis of a code edit alone — only on a passing automated check that would go red if the behaviour regressed."
    - "Phase-wide prohibition P3 (05-05) governs any user-facing copy touched while fixing a failure — referenced, not re-minted."
---

<objective>
Execute the Playwright suite that has never run, and make it green honestly.

VERIFICATION.md reports two `behavior_unverified` truths and a `human_verification` item whose sole
cause is that all nine specs were written, wired into `playwright.config.ts` and typechecked but
never executed, because the project previously forbade launching a browser without an explicit
request. That prohibition is lifted — the project `.claude/CLAUDE.md` Conventions section now carries
the user's standing grant, Chromium is already cached, and no `playwright install` is needed.

Three more specs land in this run (05-12's write-back, 05-14's notes, 05-15's reorder), each already
executed by its own plan. This plan runs the whole `durability` project together, which is the only
way to catch cross-spec interference (shared database filenames, leaked harness state, ordering
assumptions) that a single-spec run cannot.

Purpose: convert "the spec exists" into "the spec passes", and close SC4 with real evidence.
Output: a green `durability` run, a recorded verbatim result, and a WINDOWS ledger that reflects
what was actually proven.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/05-in-gym-session-logging/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full durability project and triage the result honestly</name>
  <files>(no source file is modified by this task)</files>
  <read_first>
    - apps/mobile/playwright.config.ts (the `durability` project's `testMatch` list, `fullyParallel: false`, `retries: 0`, `forbidOnly: true`, and the `webServer` block with its 180s startup timeout)
    - apps/mobile/e2e/durability.spec.ts (in particular the case at line ~219, "force-quitting mid-workout with warm-ups logged and a pause open loses nothing on reopen" — the SC4 case)
    - apps/mobile/lib/db/test-support.ts (`openTestPowerSync`/`closeTestPowerSync` and the per-spec `dbFilename` convention — the most likely source of cross-spec interference)
    - .planning/phases/05-in-gym-session-logging/05-VERIFICATION.md (the two `behavior_unverified` items and the `human_verification` list this run addresses)
    - .claude/CLAUDE.md (the Conventions section granting browser/E2E runs in this repository)
  </read_first>
  <precondition>`apps/mobile/node_modules/.bin/playwright` exists and Chromium is present in `~/Library/Caches/ms-playwright`; no `playwright install` step is planned, so a missing browser must be reported rather than installed silently.</precondition>
  <action>
  Run `pnpm --filter mobile test:e2e:durability` from the repository root — the whole project, all
  twelve specs, in one run. Capture the complete runner output.

  Modify no source file in this task. Its only product is an honest triage.

  Classify every failure into exactly one of four buckets and record which, per failing case:
  - **(a) spec defect** — a wrong selector, a missing wait, a stale harness call in the spec itself.
  - **(b) production defect** — the application genuinely does not do what the spec asserts.
  - **(c) cross-spec interference** — the case passes alone but fails in the full run (shared
    database filename, leaked global harness state, an ordering assumption). Confirm by re-running
    that single spec alone and recording both results.
  - **(d) environment** — the dev server did not start within the 180s webServer timeout, the browser
    is missing, a port is occupied.

  Record the verbatim final summary line and the exit code. If the run is green on the first attempt,
  record that too — a first-run green across twelve never-executed specs is a real and reportable
  outcome, not a reason to skip the triage section.
  </action>
  <verify>
    <automated>pnpm --filter mobile test:e2e:durability</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the literal final line of the runner (e.g. `N passed (Ms)` or `N failed, M passed`) and the exit code.
    - The SUMMARY names all twelve specs and each one's pass/fail state individually.
    - Every failing case is assigned exactly one of the four buckets, with the evidence that assigned it (for bucket (c), both the full-run and single-spec results).
    - `git status --porcelain apps/mobile/e2e apps/mobile/lib apps/mobile/components apps/mobile/app` is empty at the end of this task — nothing was fixed here.
  </acceptance_criteria>
  <done>The suite's real state is known and written down, with every failure attributed to a cause rather than a guess.</done>
</task>

<task type="auto">
  <name>Task 2: Fix what genuinely fails and re-run to green</name>
  <files>apps/mobile/e2e/durability.spec.ts, apps/mobile/e2e/schema-redefinition.spec.ts, apps/mobile/e2e/catalog-load.spec.ts, apps/mobile/e2e/workout-screen.spec.ts, apps/mobile/e2e/rest-timer.spec.ts, apps/mobile/e2e/session-lifecycle.spec.ts, apps/mobile/e2e/session-edit.spec.ts, apps/mobile/e2e/history.spec.ts, apps/mobile/e2e/workout-summary.spec.ts</files>
  <read_first>
    - The Task 1 triage record (which bucket each failure landed in)
    - The specific failing spec file(s) named by that triage
    - apps/mobile/lib/db/test-support.ts (`DURABILITY_HARNESS_GLOBAL`, `openTestPowerSync`, `closeTestPowerSync`, the raw readers) — the shared surface most bucket-(c) failures route through
    - apps/mobile/app/__durability.web.tsx (the harness object; a leaked `workoutHarness`/`editingHarness` between specs is a classic bucket-(c) cause)
    - .planning/phases/05-in-gym-session-logging/05-REVIEW-FIX.md (how this phase's earlier fixes were paired with a regression test — the same discipline applies here)
  </read_first>
  <action>
  Fix by bucket, and only by bucket:

  - **(a) spec defect** — correct the spec. The assertion's *strength* must not change: fix the
    selector, the wait or the harness call, never the expectation.
  - **(b) production defect** — fix the application code at its source and add or extend a unit test
    that goes red without the fix, following this phase's own review-fix discipline (every fix
    carries a regression test). If the defect belongs to a file another gap-closure plan owns, still
    fix it here and record the crossing in the SUMMARY.
  - **(c) cross-spec interference** — give the interfering specs genuinely independent state (a
    unique `dbFilename` per spec is the existing convention; close the database in an `afterEach`
    where a spec leaks one). Do not "fix" it by serialising harder, by adding a retry, or by
    reordering `testMatch` so the collision is hidden.
  - **(d) environment** — report it verbatim and stop; do not weaken a spec to survive a broken
    environment.

  Never set `test.skip`, `test.fixme` or `.only`; never add `retries`; never delete a failing case.
  If a case asserts behaviour that is genuinely out of this run's scope (for example something
  needing the live PowerSync Service stack), leave the case intact, record the finding, and file a
  WINDOWS entry — Task 3 handles the ledger.

  Re-run `pnpm --filter mobile test:e2e:durability` after each fix round until it is green, and
  record how many rounds it took.
  </action>
  <verify>
    <automated>pnpm --filter mobile test:e2e:durability && pnpm --filter mobile test && pnpm --filter mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test:e2e:durability` exits 0 with 0 failed and 0 skipped across all twelve specs.
    - `grep -rc "test.skip\|test.fixme\|\.only(" apps/mobile/e2e/*.spec.ts` returns 0 for every file.
    - `grep -c "retries" apps/mobile/playwright.config.ts` returns 1 and its value is still `0`.
    - `pnpm --filter mobile test` and `pnpm --filter mobile typecheck` both exit 0 — a spec fix did not break the unit suite.
    - Every bucket-(b) fix is paired with a named unit test that fails without it; the SUMMARY names each pairing.
    - The SUMMARY records the number of fix rounds and the verbatim final green line.
  </acceptance_criteria>
  <done>The whole durability project passes for real, and every fix is attributable to a diagnosed cause with a regression test behind it.</done>
</task>

<task type="auto">
  <name>Task 3: Close the ledger against what was actually proven</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - .planning/WINDOWS.md (rows 109, 116, 118, 123 — their exact descriptions, and the header's `open_count`/`fixed_count` counters)
    - The SUMMARY files of 05-11 through 05-15 (what each actually proved, and with which command)
    - .planning/phases/05-in-gym-session-logging/05-VERIFICATION.md (the gap statements the ledger rows mirror)
  </read_first>
  <action>
  Mark each of the four in-scope WINDOWS rows fixed **only** if a passing automated check now proves
  the behaviour it describes, and name that check when doing so:

  - **#123** (cycle id never threaded, write-back hits the base row) — closed by 05-11's stamp/thread
    plus 05-12's `target-write-back.spec.ts` green run.
  - **#118** (set/session note levels have no UI trigger) — closed by 05-14's wiring plus
    `session-notes.spec.ts` green run.
  - **#116** (Reorder row is a documented no-op) — closed by 05-15's sheet plus
    `reorder-exercises.spec.ts` green run.
  - **#109** (SetRow does not render the warm-up "W" badge) — closed by 05-14's relocation of the
    badge into `SetRowView` plus its named unit case.

  Use `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed <id>` for each so the header counters
  stay consistent; do not hand-edit the counter fields.

  If any of the four is not actually proven, leave it open and say why in the SUMMARY — a row marked
  fixed on the strength of a code edit alone is precisely the failure mode this phase's verification
  already caught once.

  File a new WINDOWS entry for anything the full run exposed that this gap-closure scope does not
  cover, with its real cause and the phase it should be addressed in. Explicitly leave open and
  unchanged: **#110** and **#129** (no Xcode/Android SDK — native/device verification), **#112** (the
  cross-device `personal_record` pull round trip needing a PowerSync Service restart), and **#131**
  (the deliberately-skipped unique constraint). All four remain deferred to ROADMAP Phase 999.1 or a
  later finding; say so rather than leaving their absence to inference.
  </action>
  <verify>
    <automated>test "$(grep -E '^\| (109|116|118|123) ' .planning/WINDOWS.md | grep -c '| open |')" = "0" && node .claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
  </verify>
  <acceptance_criteria>
    - Rows 109, 116, 118 and 123 all report `fixed` with a `resolved_at` timestamp in `.planning/WINDOWS.md`.
    - `grep -E '^\| (109|116|118|123) ' .planning/WINDOWS.md | grep -c '| open |'` returns 0 — none of the four is still listed open.
    - `node .claude/gsd-core/bin/gsd-tools.cjs windows status` runs clean and its counters match the row states.
    - `.planning/WINDOWS.md`'s header `open_count` and `fixed_count` are internally consistent with the row states (the CLI maintained them; they were not hand-edited).
    - Rows 110, 112, 129 and 131 are still `open` and their `description` text is byte-unchanged.
    - The SUMMARY names, per closed row, the specific command whose green result closed it.
  </acceptance_criteria>
  <done>The ledger says what is true: four rows closed by proof, four rows still open with their reasons intact.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner -> local dev server | Loopback only; `npx expo start --web` with `EXPO_PUBLIC_DURABILITY_HARNESS=1` |
| test harness route -> production bundle | The harness route mounts real production components; the run does not weaken its build-time gating |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-16-01 | Repudiation | test results reported without execution | high | mitigate | Every acceptance criterion in this plan is a real runner exit code plus the verbatim final line, and Task 1 forbids modifying any source file so the triage cannot be retro-fitted to a fix. This is the exact failure this phase's verification already caught once (nine specs credited as written but never run). |
| T-05-16-02 | Tampering | weakening a spec to reach green | high | mitigate | Explicit prohibitions plus grep-based acceptance criteria on `test.skip`/`test.fixme`/`.only`/`retries`; bucket-(b) fixes must each carry a named regression test that fails without them. |
| T-05-16-03 | Elevation of privilege | durability harness reachable in production | medium | accept | Unchanged by this plan — the harness route and its global name are already gated by `EXPO_PUBLIC_DURABILITY_HARNESS` and the build-time construction documented in `test-support.ts`; this plan adds no new export or global. |
| T-05-16-04 | Denial of service | dev server left running after the suite | low | accept | `reuseExistingServer` is enabled outside CI by design; a stray process on 8081 is a local nuisance, reported in the SUMMARY if it occurs. |
| T-05-16-05 | Tampering | supply chain (package installs) | n/a | n/a | No package-manager install occurs — Playwright and Chromium are already present. |
</threat_model>

<ui_considerations_lift>
This plan modifies no UI component. No `05-UI-SPEC.md` `## UI Considerations` row is lifted here and
none is dropped — the rows in scope for this gap-closure run are carried by 05-14 (E4, E6, E13) and
05-15 (E10, E12), and 05-13 records the five out-of-scope backstops (E8, E9 ×2, E11 ×2) as still
open.
</ui_considerations_lift>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Where |
|---|---|---|
| (no new source symbol) | — | this plan executes and fixes; new symbols are produced by 05-11…05-15 |
| WINDOWS rows 109/116/118/123 resolution | ledger state change | `.planning/WINDOWS.md` |
</artifacts_this_phase_produces>

<verification>
- `pnpm --filter mobile test:e2e:durability` exits 0, 0 failed, 0 skipped, twelve specs.
- `pnpm --filter mobile test` and `pnpm --filter mobile typecheck` exit 0.
- No `test.skip`, `test.fixme` or `.only` anywhere in `apps/mobile/e2e/`.
- `playwright.config.ts` still declares `retries: 0`.
- WINDOWS rows 109, 116, 118, 123 are `fixed`; 110, 112, 129, 131 are still `open`.
</verification>

<success_criteria>
- Every durability spec has been executed and passes.
- SC4's force-quit-with-warm-ups-and-pause case is proven by execution.
- VERIFICATION.md's `human_verification` full-suite item no longer needs a human.
- The four in-scope ledger rows are closed by proof, not by claim.
</success_criteria>

<out_of_scope>
Explicitly not planned here, and not silently omitted:
- The Playwright `sync` project (`sync.spec.ts`) — it needs the live PowerSync Service, the API and a Postgres with `wal_level=logical`, and the self-hosted service has not been restarted against the current `sync-rules.yaml` (WINDOWS #112). It stays deferred to ROADMAP Phase 999.1's cross-device sweep.
- Anything requiring a real iOS/Android device or simulator: SC3's "verified on a real device" rest-timer clause, the notification-permission-denied degrade UX, the two-tap/keypad-overlay feel check, and the cross-device `personal_record` pull round trip (WINDOWS #110, #112, #129). All stay deferred to ROADMAP Phase 999.1.
- The five out-of-scope UI backstops (E8 banner error, E9 summary empty/error, E11 history row anatomy/partial) — recorded as still open by 05-13.
</out_of_scope>

<output>
Create `.planning/phases/05-in-gym-session-logging/05-16-SUMMARY.md` when done.
</output>
