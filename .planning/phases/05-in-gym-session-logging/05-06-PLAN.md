---
phase: 05-in-gym-session-logging
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - apps/mobile/components/ExercisePage.tsx
  - apps/mobile/components/ExerciseActionBar.tsx
  - apps/mobile/components/TargetsSheet.tsx
  - apps/mobile/components/NoteSheet.tsx
  - apps/mobile/components/WarmupSheet.tsx
  - apps/mobile/components/SessionActionSheet.tsx
  - apps/mobile/components/__tests__/ExerciseActionBar.test.tsx
  - apps/mobile/components/__tests__/TargetsSheet.test.tsx
  - apps/mobile/components/__tests__/SessionActionSheet.test.tsx
  - apps/mobile/lib/db/log-set.ts
  - apps/mobile/lib/db/session-mutations.ts
  - apps/mobile/lib/db/__tests__/session-mutations.test.ts
  - apps/mobile/e2e/workout-screen.spec.ts
autonomous: true
requirements: [LOG-14, LOG-15, LOG-16, LOG-17]
windows_id_range: "153-162"

estimate:
  tokens: 165000
  raw_tokens: 165000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The per-exercise action bar is a single exported list constant, not hardcoded JSX: Warm-up, Targets and Note are always-visible members and the overflow is a fourth, so re-sorting the bar in Phase 6, 7 or 8 is a data change (D-13)."
    - "Adjusting a target mid-workout writes the frozen `session_exercise` snapshot only; the program authored in Phase 4 is untouched unless the user takes the separate, explicitly labelled write-back action (D-14, LOG-15)."
    - "Write-back targets whichever row the displayed value resolved from: when the session's cycle has a `routine_exercise_cycle_target` row for that field it updates the override, otherwise it updates the base `routine_exercise` row — because writing the base unconditionally would make the edit invisible whenever an override shadowed it (D-15)."
    - "Notes exist at set, exercise and session level as three independent writes to the three `notes` columns added in 05-02; an empty note is normalised to null before the write so absence has one representation (LOG-16)."
    - "The Note button carries a filled-dot badge exactly when a note already exists for that exercise (UI-SPEC §Per-Exercise Action Bar)."
    - "Generated warm-up sets are materialised as real `logged_set` rows with `set_type` of the warm-up value and `completed: false` at generation time, because D-01 makes every other row durable on entry and an un-persisted suggestion would be the one thing a force-quit loses (LOG-17, D-01)."
    - "Warm-up weights come from `warmupSets()` in `@fitness/pr-rules`; no percentage or rounding arithmetic is written inside a component (LOG-17, D-30's shared-pure-module argument)."
    - "Every consumer that needs the working sets filters on `set_type` explicitly and never infers set type from `set_index` position (RESEARCH Pitfall 2)."
    - "Removing an exercise mid-workout is non-destructive: it stamps `session_exercise.removed_at` and drops the exercise from the strip and pager, and any sets already logged for it stay queryable in history, which is exactly what the Remove Exercise confirmation copy promises (LOG-14)."
    - "Adding an exercise mid-workout and starting a one-off both open the same Phase 4 `ExercisePickerModal` in multi-select; this plan reuses it unmodified (LOG-14, D-13's landing-place argument)."
    - "A one-off exercise carrying `EMPTY_PRESCRIPTION` renders every target as the em dash rather than 0 or NaN, reusing the shipped `displayOrDash` convention (LOG-14)."
    - "The set-number tap target is rendered and real this phase, with a choice set currently limited to the working and warm-up values; Phase 7 wires the remaining five `SET_TYPES` values into the same already-rendered control (UI-SPEC R7)."
    # --- Edge predicates: LOG-14 ---
    - "Adding the same exercise twice from the picker creates two distinct `session_exercise` rows with distinct client-generated ids and consecutive `order_index` values — the second add is not deduplicated into the first (LOG-14 idempotency)."
    - "Removing an exercise while one of its sets is mid-entry leaves every already-written `logged_set` row intact and only stamps `removed_at`; no cascade delete runs and no in-flight keypad value is written after the removal (LOG-14 concurrency)."
    # --- Edge predicates: LOG-17 ---
    - "Tapping Warm-up a second time for the same exercise regenerates rather than appends: the existing uncompleted warm-up rows for that exercise are replaced, so the exercise never ends up with two ladders (LOG-17 idempotency)."
    - "A warm-up generation interrupted after some of its rows are written leaves those rows durable, and a re-run brings the exercise back to exactly the ladder `warmupSets()` returns for the current working weight (LOG-17 concurrency)."
    # --- UI-SPEC E6 Per-Exercise Action Bar ---
    - "The bar is never empty: it is a fixed constant list and no item is ever conditionally hidden, including Warm-up regardless of the `warmup_sets_enabled` preference (UI-SPEC E6 empty)."
    - "The bar has no loading state; the Note button's badge derives from a note lookup already resolved with the exercise page (UI-SPEC E6 loading, R6)."
    - "The bar has no async failure mode of its own and inherits the parent screen's error state (UI-SPEC E6 error)."
    - "Populated bar: four 48x48-minimum pressables, each a small Ionicons glyph above a Label-role caption, at the visual weight of a bottom-tab item rather than a text button (UI-SPEC E6 populated)."
    - "The only per-item variable state is the Note button's 6px accent dot badge; every other button renders identically in every session (UI-SPEC E6 partial)."
    - "The four items wrap to a second row rather than scrolling or clipping, and hold the 48x48 floor in every wrapped configuration (UI-SPEC E6 overflow, R4)."
    - "The bar's arity is fixed at four, so there is no zero or one case and no plural copy (UI-SPEC E6 zero/one/many)."
    - "Captions wrap and grow; no truncation or ellipsis appears on this surface (UI-SPEC E6 long text, R4)."
    # --- UI-SPEC E10 Session Action Sheet ---
    - "The sheet is a fixed constant list of four rows — Swap, Remove, Reorder, Info — mirroring `RoutineActionSheet`, and is never empty (UI-SPEC E10 empty)."
    - "The sheet has no loading state; it opens from already-loaded session state (UI-SPEC E10 loading)."
    - "The sheet has no async failure mode; Remove opens its confirmation first and declining dismisses with no change (UI-SPEC E10 error)."
    - "Populated sheet: four rows, each an Ionicons glyph plus a label, with Remove in the destructive colour and the other three in default foreground (UI-SPEC E10 populated)."
    - "The row set is constant and every row is always actionable; there is no partial-data rendering and no disabled-row state (UI-SPEC E10 partial)."
    - "At large OS font scale the sheet grows in height and scrolls internally rather than clipping a row (UI-SPEC E10 overflow, R4)."
    - "The sheet's arity is fixed at four rows, so there is no zero or one case and no plural copy (UI-SPEC E10 zero/one/many)."
    - "Row labels wrap and grow; no truncation (UI-SPEC E10 long text, R4)."
  artifacts:
    - apps/mobile/components/ExerciseActionBar.tsx
    - apps/mobile/components/TargetsSheet.tsx
    - apps/mobile/components/NoteSheet.tsx
    - apps/mobile/components/WarmupSheet.tsx
    - apps/mobile/components/SessionActionSheet.tsx
    - apps/mobile/lib/db/session-mutations.ts
  key_links:
    - "`TargetsSheet`'s write-back path calls `resolveWriteBackTarget`, which reads whether a `routine_exercise_cycle_target` row exists for the session's cycle and that field, and routes the update accordingly (D-15) — the same `override ?? base` resolution `resolvePrescriptionForCycle` already uses to read."
    - "`WarmupSheet` calls `warmupSets` from `@fitness/pr-rules` and writes each returned row through the existing `logSet` helper with the warm-up `set_type`; no second insert path is introduced (D-01)."
    - "`ExerciseActionBar`'s items come from one exported `EXERCISE_ACTIONS` constant that `ExercisePage` renders over, so Phase 6's plate math, Phase 7's superset action and Phase 8's progression wand are appended to a list rather than inserted into JSX (D-13)."
---

## Phase Goal (verbatim from ROADMAP.md — not in user-story format)

> The user can walk into a gym with no signal and log a complete workout without friction. **This is the phase the app becomes real** — dogfooding starts here.

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| file | `apps/mobile/components/ExerciseActionBar.tsx` |
| component | `ExerciseActionBarView`, `ExerciseActionBar` |
| const | `EXERCISE_ACTIONS` |
| type | `ExerciseActionId` (`'warmup' \| 'targets' \| 'note' \| 'overflow'`) |
| file | `apps/mobile/components/TargetsSheet.tsx` |
| component | `TargetsSheetView`, `TargetsSheet` |
| file | `apps/mobile/components/NoteSheet.tsx` |
| component | `NoteSheetView`, `NoteSheet` |
| file | `apps/mobile/components/WarmupSheet.tsx` |
| component | `WarmupSheetView`, `WarmupSheet` |
| file | `apps/mobile/components/SessionActionSheet.tsx` |
| component | `SessionActionSheetView`, `SessionActionSheet` |
| const | `SESSION_EXERCISE_ACTIONS` |
| file | `apps/mobile/lib/db/session-mutations.ts` |
| function | `setSessionExerciseTargets`, `resolveWriteBackTarget`, `writeBackTargets`, `setNote`, `generateWarmupSets`, `addExerciseToSession`, `swapSessionExercise`, `removeSessionExercise`, `reorderSessionExercises` |
| function (in `log-set.ts`) | `updateLoggedSet` |

## Broken-windows ledger reservation

Reserved `.planning/WINDOWS.md` ids **153–162**.

<objective>
Give each exercise page the actions a real workout needs: adjust targets for today without rewriting the program, attach a note at any of three levels, generate a warm-up ladder off the working weight, and add, swap, remove or reorder exercises mid-session.

Purpose: LOG-14 through LOG-17 are the difference between a logger that survives a real session and one that forces you out to the program builder the first time the rack is taken.
Output: the action bar and its three sheets, the overflow action sheet, and one mutations module that owns every session-scoped write these surfaces perform.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-in-gym-session-logging/05-CONTEXT.md
@.planning/phases/05-in-gym-session-logging/05-RESEARCH.md
@.planning/phases/05-in-gym-session-logging/05-PATTERNS.md
@.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md
@.planning/phases/05-in-gym-session-logging/05-01-SUMMARY.md
@.planning/phases/05-in-gym-session-logging/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The action bar, notes at three levels, and the mid-workout targets sheet</name>
  <reversibility rating="one-way">D-15: the resolution rule and the write rule must agree, and both are read by the Phase 4 builder, the Home next-up card and the session snapshot. A write-back that targets the wrong row makes a user's saved edit silently invisible next cycle.</reversibility>
  <read_first>
    - `apps/mobile/lib/db/log-set.ts` — `resolvePrescriptionForCycle`'s `override ?? base` read, which `resolveWriteBackTarget` must mirror exactly on the write side; also `logSet`'s durable-write-and-return shape
    - `apps/mobile/lib/db/schema.ts` — `sessionExercise` (the five `target_*` columns plus the new `notes`), `loggedSet.notes`, `workoutSession.notes`, `routineExercise`, `routineExerciseCycleTarget`
    - `packages/api-contracts/src/program.ts` — `ResolvedTarget`, `TargetOverride`, `resolveTarget`, `isEmptyOverride`; the write-back must not create an override row that overrides nothing
    - `apps/mobile/components/ExerciseSlotRow.tsx` — Phase 4's stepper anatomy, reused verbatim by the targets sheet rather than re-styled
    - `apps/mobile/components/ExercisePage.tsx` — the `actionBarSlot` render-prop 05-01 left for this plan to fill
    - `apps/mobile/components/TextField.tsx` and `apps/mobile/components/DetailSection.tsx` — the shipped input and section chrome the note sheet reuses
    - `apps/mobile/app/(tabs)/index.tsx` — `displayOrDash`, the em-dash null convention for a one-off exercise's absent targets
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Per-Exercise Action Bar, §Set Row (warm-up badge and one-off row rules), §Phase-Wide Rules R7
  </read_first>
  <files>apps/mobile/components/ExerciseActionBar.tsx, apps/mobile/components/TargetsSheet.tsx, apps/mobile/components/NoteSheet.tsx, apps/mobile/components/ExercisePage.tsx, apps/mobile/lib/db/session-mutations.ts, apps/mobile/lib/db/log-set.ts, apps/mobile/components/__tests__/ExerciseActionBar.test.tsx, apps/mobile/components/__tests__/TargetsSheet.test.tsx, apps/mobile/lib/db/__tests__/session-mutations.test.ts</files>
  <action>
`apps/mobile/components/ExerciseActionBar.tsx` (D-13): export `EXERCISE_ACTIONS` as a single ordered constant of `{ id, label, icon }` entries — `warmup` / "Warm-up" / `barbell-outline`, `targets` / "Targets" / `options-outline`, `note` / "Note" / `document-text-outline`, `overflow` / the vertical-ellipsis glyph. `ExerciseActionBarView` maps over that constant; it does not write four literal pressables. D-13 flags the split as a bet on which three stay frequent once Phases 6–8 add their own actions, and a list constant is what makes re-sorting it later a data change. Each item is a 48x48-minimum `Pressable` rendering an `Ionicons` glyph above a Label-role caption, at the visual weight of a bottom-tab item. The Warm-up item is always visible regardless of the `warmup_sets_enabled` preference — the preference gates auto-population, never visibility. The Note item carries a 6px accent dot in its top-right corner when `hasNote` is true. Items wrap to a second row at large font scale rather than scrolling or clipping.

`apps/mobile/components/NoteSheet.tsx` (LOG-16): one component serving all three levels, taking a `level` of set, exercise or session plus the current text. Reuses `TextField` and `DetailSection`. On save it calls `setNote({ level, id, text })` from the mutations module. Normalise an all-whitespace or empty string to null before the write so "no note" has exactly one representation.

`apps/mobile/components/TargetsSheet.tsx` (D-14, D-15, LOG-15): stepper fields for the five `target_*` values reusing `ExerciseSlotRow`'s stepper anatomy verbatim. Its primary Save action calls `setSessionExerciseTargets`, which writes the frozen `session_exercise` snapshot ONLY — the row the set rows already read, so the change is visible immediately and the program is untouched. Beneath it, a lower-weight secondary action labelled "Also update my program" (Body role, `text-accent`, no fill) calls `writeBackTargets`. Rendering write-back at lower visual weight than Save is deliberate: D-14 makes session-only the default and write-back the deliberate exception.

`apps/mobile/lib/db/session-mutations.ts` — the new module owning every session-scoped write these surfaces perform:
`setNote({ level, id, text }, db)` — updates the `notes` column on `logged_set`, `session_exercise` or `workout_session` depending on level, writing null for an empty string. Three independent columns: a write to one reads or writes neither of the others.
`setSessionExerciseTargets(sessionExerciseId, targets, db)` — updates only the five `target_*` columns on that `session_exercise` row.
`resolveWriteBackTarget({ routineExerciseId, cycleId, field }, db)` — returns which row a write-back for that field must hit: the `routine_exercise_cycle_target` override when a row exists for that `(routine_exercise_id, cycle_id)` pair and that field is non-null on it, otherwise the base `routine_exercise` row. This is the write-side mirror of `resolvePrescriptionForCycle`'s `override ?? base` read, and the two must agree or a user's saved edit disappears behind a shadowing override next cycle.
`writeBackTargets({ sessionExerciseId, targets }, db)` — resolves per field through `resolveWriteBackTarget` and applies each. Do not create an override row that would override nothing; `isEmptyOverride` from `@fitness/api-contracts` is the existing guard for that.
Record in a comment that this is a user-initiated edit and is deliberately distinct from Phase 4's D-17, which reserves future-cycle overrides as the progression engine's only write target — D-17 constrains the engine, not the person.

`apps/mobile/lib/db/log-set.ts` — add `updateLoggedSet(id, patch, db)` for the narrow field updates the note sheet and the set-type control need, in `logSet`'s shape (no read-modify-write, no second insert path). This is the one file 05-01 also owned; extend it in place.

`apps/mobile/components/ExercisePage.tsx` — fill the `actionBarSlot` with `ExerciseActionBar` and wire each id to its sheet. Render every absent target through `displayOrDash` so a one-off exercise reads as the em dash rather than 0. Render the set-number tap target as a real, tappable control offering the working and warm-up values only; do not render it disabled or hidden waiting for Phase 7 (R7).
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "(ExerciseActionBar|TargetsSheet|session-mutations)"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/components/ExerciseActionBar.tsx` exports `EXERCISE_ACTIONS` with exactly four entries in the order warmup, targets, note, overflow, and `ExerciseActionBarView` renders by mapping that constant.
    - `ExerciseActionBar.test.tsx` asserts by direct invocation that all four items render when `warmupSetsEnabled` is false, and that the note dot renders only when `hasNote` is true.
    - `session-mutations.test.ts` asserts `setNote` at each of the three levels writes only its own column and leaves the other two tables' note columns untouched.
    - `session-mutations.test.ts` asserts `setNote` with an empty or all-whitespace string writes null.
    - `session-mutations.test.ts` asserts `resolveWriteBackTarget` returns the override row when a `routine_exercise_cycle_target` row exists for that cycle and field, and the base `routine_exercise` row when it does not — both directions, both asserted.
    - `session-mutations.test.ts` asserts `setSessionExerciseTargets` changes the `session_exercise` row and leaves both `routine_exercise` and `routine_exercise_cycle_target` byte-identical.
    - `TargetsSheet.test.tsx` asserts the write-back action is a distinct handler from Save, and that Save alone never calls `writeBackTargets`.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>A user can adjust today's targets without touching their program, deliberately push those numbers back to the right program row when they mean to, and attach a note to a set, an exercise or the whole session.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-calculated warm-up sets, materialised as real rows</name>
  <reversibility rating="costly">Materialising generated warm-ups as real `logged_set` rows means force-quit recovery restores them; switching later to un-persisted suggestions would need those rows cleaned out of existing sessions.</reversibility>
  <read_first>
    - `packages/pr-rules/src/warmup.ts` — `warmupSets`, `WARMUP_STEPS`, `DEFAULT_ROUNDING_INCREMENT_KG` from 05-04; the component performs no percentage arithmetic of its own
    - `packages/api-contracts/src/session.ts` — `WARMUP_SET_TYPE`, `WORKING_SET_TYPE`
    - `apps/mobile/lib/db/log-set.ts` — `logSet`'s insert shape and its `MAX(set_index) + 1` arithmetic, which is what makes generation order matter
    - `apps/mobile/lib/db/session-query.ts` — `previousSetReference`, the D-16 weight source used to default the sheet's working weight when no working set has been logged yet
    - `apps/mobile/components/ExerciseStrip.tsx` — the completion fraction that must not count warm-ups
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Copywriting Contract, "Warm-up sheet" row; §Set Row, the warm-up "W" badge and the fraction-exclusion rule
    - `.planning/phases/05-in-gym-session-logging/05-RESEARCH.md` §3 and Common Pitfalls, Pitfall 2 (`set_index` position never implies set type)
  </read_first>
  <files>apps/mobile/components/WarmupSheet.tsx, apps/mobile/components/ExercisePage.tsx, apps/mobile/lib/db/session-mutations.ts, apps/mobile/lib/db/__tests__/session-mutations.test.ts</files>
  <action>
`apps/mobile/components/WarmupSheet.tsx` (LOG-17): heading "Add Warm-up Sets", body "Based on {weight}, we'll add 3 warm-up sets.", confirm "Add Warm-up Sets", plus a working-weight field when none is known yet. The working weight defaults, in order: the exercise's first logged working set's weight if one exists, else the D-16 history prefill from `previousSetReference`, else blank with the field required before confirm is enabled. All weights displayed here convert through `fromCanonicalKg` at the display boundary only. Tapping the action bar's Warm-up item when warm-up rows already exist for the exercise opens this same sheet in a regenerate context — adjust the working weight and regenerate — rather than a second, different sheet.

`apps/mobile/lib/db/session-mutations.ts` — add `generateWarmupSets({ sessionExerciseId, workingWeightKg, roundingIncrementKg }, db)`:
1. Delete the exercise's existing uncompleted warm-up rows (`set_type` equal to the warm-up value AND `completed` false). This is what makes a second tap a regenerate rather than an append, so an exercise can never end up with two ladders. Completed warm-up rows are left alone — the user did them.
2. Call `warmupSets(workingWeightKg, roundingIncrementKg)` from `@fitness/pr-rules` and insert one `logged_set` row per returned entry through the existing `logSet` helper with `setType` set to the warm-up value and `completed: false`, in the returned ascending-weight order. Every row is durable the instant it is written (D-01) — there is no draft list committed on confirm.
3. Return the created ids.
Write no percentage or rounding arithmetic in this module or in the component; the ladder is `@fitness/pr-rules`'s answer.

`set_index` interaction, which is the subtle part and must be stated in a comment rather than discovered: `logSet` computes `MAX(set_index) + 1`, and the schema describes `set_index` as strictly incrementing, not gap-free. Warm-up rows generated after working sets already exist therefore receive HIGHER indices than the working sets they precede in the ladder. Display order is resolved by sorting warm-up rows ahead of working rows and only then by `set_index` — never by renumbering existing rows, which would rewrite history to fix a display concern.

`apps/mobile/components/ExercisePage.tsx` — render warm-up rows with the leading "W" badge (14px circle, `bg-secondary`, muted Label glyph) ahead of the set-number column, and keep them out of the exercise strip's completion fraction on both sides of the ratio: completing a warm-up never moves `3/4` to `4/5`.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "session-mutations"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/lib/db/session-mutations.ts` exports `generateWarmupSets`, and a source scan of that file and of `WarmupSheet.tsx` for the numerals `0.4`, `0.6` and `0.8` returns no match — the ladder comes only from `@fitness/pr-rules`.
    - `session-mutations.test.ts` asserts a first `generateWarmupSets` call inserts exactly the rows `warmupSets()` returns, each with the warm-up `set_type` and `completed` false.
    - `session-mutations.test.ts` asserts a second call for the same exercise leaves the exercise with exactly one ladder, not two, and that a COMPLETED warm-up row from the first call survives the regenerate.
    - `session-mutations.test.ts` asserts a partially-written generation (simulated by inserting a subset first) converges to the full ladder on re-run.
    - `session-mutations.test.ts` asserts `generateWarmupSets` with a null or zero working weight writes no rows.
    - `ExercisePage` orders warm-up rows ahead of working rows regardless of raw `set_index`, asserted by a direct-invocation case with warm-ups at higher indices than the working sets.
    - `ExerciseStrip`'s fraction is unchanged by completing a warm-up row, asserted in `ExerciseStrip.test.tsx`.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>Tapping Warm-up produces a real, durable, deterministic ladder scaled off the working weight — and tapping it again fixes the ladder instead of doubling it.</done>
</task>

<task type="auto">
  <name>Task 3: Add, swap, remove and reorder exercises mid-workout</name>
  <reversibility rating="reversible">Removal is a `removed_at` stamp, not a delete, so undoing it is clearing one column.</reversibility>
  <read_first>
    - `apps/mobile/components/RoutineActionSheet.tsx` — the row shape, icon-plus-label anatomy and destructive-colour treatment `SessionActionSheet` mirrors
    - `apps/mobile/components/ExercisePickerModal.tsx` — the Phase 4 multi-select picker; reuse it through its existing props, do NOT modify it (05-07 also consumes it in this same wave)
    - `apps/mobile/lib/catalog/smart-swap.ts` and `apps/mobile/components/SwapSuggestionList.tsx` — the shipped swap-suggestion surface to reuse for the Swap action
    - `apps/mobile/components/ArchiveDialog.tsx` — the shipped destructive-confirmation dialog shape
    - `apps/mobile/lib/db/log-set.ts` — `addSessionExercise` and its `EMPTY_PRESCRIPTION` path for an exercise added with no routine linkage
    - `apps/mobile/lib/db/schema.ts` — `sessionExercise.removedAt` and `orderIndex`
    - `apps/mobile/lib/programs/reorder-drag.ts` — the existing reorder helper, if its shape fits; otherwise follow its conventions
    - `.planning/phases/05-in-gym-session-logging/05-UI-SPEC.md` §Per-Exercise Action Bar (the overflow paragraph), §Copywriting Contract (the Remove Exercise confirmation), §Exercise Strip (the trailing add chip)
  </read_first>
  <files>apps/mobile/components/SessionActionSheet.tsx, apps/mobile/components/ExercisePage.tsx, apps/mobile/lib/db/session-mutations.ts, apps/mobile/components/__tests__/SessionActionSheet.test.tsx, apps/mobile/lib/db/__tests__/session-mutations.test.ts</files>
  <action>
`apps/mobile/components/SessionActionSheet.tsx` (D-13's overflow): export `SESSION_EXERCISE_ACTIONS` as a fixed four-entry constant — Swap, Remove, Reorder, Info — and a hook-free `SessionActionSheetView` mapping over it, mirroring `RoutineActionSheet`'s row anatomy: an `Ionicons` glyph plus a label, Remove rendered in the destructive colour and the other three in default foreground. Every row is always actionable; there is no disabled-row state. At large OS font scale the sheet grows in height and scrolls internally rather than clipping a row.

`apps/mobile/lib/db/session-mutations.ts` — add the four writes:
`addExerciseToSession({ sessionId, exerciseIds }, db)` — appends one `session_exercise` per selected exercise at the next `order_index` values, calling the existing `addSessionExercise` with no `routineExerciseId`, so `EMPTY_PRESCRIPTION` applies and every target renders as the em dash. Selecting the same exercise twice creates two distinct rows with distinct client-generated ids and consecutive `order_index` values; there is no deduplication, because doing an exercise twice in one session is a legitimate thing to do.
`swapSessionExercise({ sessionExerciseId, newExerciseId }, db)` — updates `exercise_id` on the row in place, preserving `order_index` and every logged set already attached to it.
`removeSessionExercise(sessionExerciseId, db)` — stamps `removed_at` with the current time. It does NOT delete the row and does NOT delete its `logged_set` children. This is what makes the confirmation copy true: "Any sets already logged for this exercise stay in your history." Every session read added by this phase filters `removed_at` as null for the live strip and pager, while the finish summary and the history detail continue to include a removed exercise that has completed sets.
`reorderSessionExercises(sessionId, orderedIds, db)` — rewrites `order_index` across the session's non-removed rows in one pass.

`apps/mobile/components/ExercisePage.tsx` — wire the overflow item to `SessionActionSheet`. Swap opens `SwapSuggestionList` backed by `smart-swap.ts`. Remove opens the `ArchiveDialog`-shaped confirmation with the exact copy from 05-UI-SPEC §Copywriting Contract: title "Remove Exercise", body "Any sets already logged for this exercise stay in your history. Remove it from this workout?", confirm "Remove". Declining dismisses with no write. The strip's trailing add chip and this sheet's Swap action both open the unmodified Phase 4 `ExercisePickerModal`; Info pushes the existing exercise-detail route.

A removal must not race a keypad entry: cancel the active keypad field before performing the remove, so no in-flight value is written to a row whose exercise has just left the session.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --testPathPattern "(SessionActionSheet|session-mutations)" &amp;&amp; pnpm --filter mobile test:e2e:durability -- workout-screen.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/components/SessionActionSheet.tsx` exports `SESSION_EXERCISE_ACTIONS` with exactly four entries and a hook-free `SessionActionSheetView`.
    - `SessionActionSheet.test.tsx` asserts the Remove row resolves the destructive colour and the other three resolve the default foreground colour.
    - `session-mutations.test.ts` asserts `addExerciseToSession` called twice with the same exercise id produces two rows with distinct ids and consecutive `order_index` values.
    - `session-mutations.test.ts` asserts `removeSessionExercise` stamps `removed_at` and that the exercise's `logged_set` rows are all still present afterwards — a row-count assertion before and after, equal.
    - `session-mutations.test.ts` asserts `swapSessionExercise` preserves `order_index` and every attached `logged_set` id.
    - `session-mutations.test.ts` asserts `reorderSessionExercises` produces a contiguous `order_index` sequence over the non-removed rows.
    - `apps/mobile/components/ExercisePickerModal.tsx` appears in no plan's diff — it is reused through its existing props, unmodified.
    - `pnpm --filter mobile test:e2e:durability -- workout-screen.spec.ts` exits 0 with a case adding an exercise mid-session and asserting the strip grows by one chip.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>The rack being taken is no longer a reason to leave the workout screen: an exercise can be added, swapped, reordered or removed mid-session, and removing one never destroys what was already logged against it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free text → `notes` columns → rendered output | Note text authored on-device is stored, synced, and later rendered on both native and web targets |
| session-scoped edit → program rows | The write-back action crosses from session state into the user's authored program |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | Tampering | note text rendering | medium | mitigate | Notes render exclusively through React Native `Text`, which sets a string child and interprets no markup on either target; no `dangerouslySetInnerHTML`, no HTML string interpolation and no web-only rich-text renderer is introduced. The note sheet stores the raw string and the renderer never evaluates it (ASVS V5). |
| T-05-06-02 | Tampering | `writeBackTargets` | high | mitigate | Write-back resolves its destination row through `resolveWriteBackTarget`, which reads the actual `(routine_exercise_id, cycle_id)` linkage from the database rather than trusting a caller-supplied row id — the same database-linkage-first discipline the sync service's parent resolvers already use. A wrong destination is a silent corruption of the user's authored program. |
| T-05-06-03 | Tampering | `removeSessionExercise` | high | mitigate | Removal is a `removed_at` stamp, never a delete, so no cascade can reach `logged_set` rows. Pinned by a before-and-after row-count assertion in the unit suite. |
| T-05-06-04 | Denial of service | `generateWarmupSets` | low | mitigate | The ladder's arity is fixed at three by `WARMUP_STEPS`, and regeneration deletes before inserting, so repeated taps cannot grow the row count without bound. |
| T-05-06-05 | Information disclosure | `notes` synced to the server | low | accept | Note rows are scoped to their aggregate root's owner by the existing sync rules; no new read path exposes them. |
</threat_model>

## Flagged planner assumptions (unresolved edge probes)

- **LOG-15 (unclassified)** — "adjust targets mid-workout for this session only or persistently" produced no shape classification. Planner assumption: the ambiguous boundary is a write-back performed on a session whose `routine_exercise_id` is null (a one-off or mid-workout-added exercise), which has no program row to write back to. This plan renders the write-back action inert for such an exercise rather than inventing a routine row for it. Flagged, not resolved.

<verification>
- `pnpm --filter mobile test` green with non-zero counts in the four new suites.
- `pnpm --filter mobile test:e2e:durability` green.
- `pnpm typecheck` and `pnpm lint` green at the repo root.
- Any claim requiring a native runtime (sheet presentation, native font scaling on the wrapped action bar) is filed in the reserved 153–162 ledger range against Phase 999.1.
</verification>

<success_criteria>
- The action bar renders from one exported constant and all four items are always visible.
- A targets edit changes today's numbers only; write-back lands on the row the displayed value resolved from.
- Notes save and clear at set, exercise and session level, independently.
- Warm-up generation is deterministic, durable and idempotent on re-tap.
- Add, swap, reorder and remove all work mid-session, and remove destroys nothing.
</success_criteria>

<output>
Create `.planning/phases/05-in-gym-session-logging/05-06-SUMMARY.md` when done.
</output>
