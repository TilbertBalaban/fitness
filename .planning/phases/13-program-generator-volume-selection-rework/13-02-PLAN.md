---
phase: 13-program-generator-volume-selection-rework
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/lib/programs/generation-wizard.ts
  - apps/mobile/lib/programs/__tests__/generation-wizard.test.ts
  - docs/volume-rir-landmarks.md
autonomous: true
requirements: [GEN-VOL-02, GEN-RIR-01]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-05: the wizard's day_trimmed sentence is true whether the generator reduced sets, removed exercises, or both — it no longer asserts that exercises were dropped."
    - "D-05: no new DegradationEntry.kind is introduced, so every existing consumer of the degradation list keeps working (D-10)."
    - "D-09: docs/volume-rir-landmarks.md states the daysPerWeek-keyed RIR ladder verbatim, as this project's own design decision, matching the D-15 provenance stance carried over from Phase 11."
    - "D-01: docs/volume-rir-landmarks.md records MAX_SETS_PER_EXERCISE and MIN_SETS_PER_EXERCISE alongside the other landmark constants, so the per-exercise cap has the same provenance trail as the volume bands."
    - "D-04: the document no longer claims the session-length fit never lowers a surviving slot's set count — Phase 11's D-14 is superseded and the document says so."
  artifacts:
    - docs/volume-rir-landmarks.md
    - apps/mobile/lib/programs/generation-wizard.ts
  key_links:
    - "describeDegradation's four sentences must stay mutually distinct — generation-wizard.test.ts and generate-screen.test.ts both assert uniqueness across DEGRADATION_KINDS, so a reword that collides with another kind's sentence fails in two suites."
    - "The reworded sentence must keep clearing the non-blaming regex in generation-wizard.test.ts (no 'you don't/do not/can't/cannot', no 'too weak', no 'not enough time to train')."
---

<objective>
Make the wizard's trimmed-day copy true under the new fit semantics, and record the new landmark
constants in the provenance document.

Purpose: D-04 changes what a trimmed day actually means — sets can be reduced without any exercise
being removed — so the shipped sentence "left room for fewer exercises, so some were dropped" is now
false for one of the two outcomes. D-09 and D-01 add two new landmark tables that, per D-15's
provenance stance carried forward from Phase 11, have to be written down as this project's own
design decisions rather than left implicit in a constants module.

Output: a reworded `day_trimmed` sentence with a test that pins its truthfulness for both outcomes,
and three updated sections of `docs/volume-rir-landmarks.md`.

This plan touches no file under `packages/program-generator`, so it runs in wave 1 alongside
13-01 without collision. Its content is authored from the locked decisions in 13-CONTEXT.md, not
from 13-01's implementation.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-program-generator-volume-selection-rework/13-CONTEXT.md
@apps/mobile/lib/programs/generation-wizard.ts
@docs/volume-rir-landmarks.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reword the trimmed-day sentence so it is true for both concessions</name>

  <files>
    apps/mobile/lib/programs/generation-wizard.ts,
    apps/mobile/lib/programs/__tests__/generation-wizard.test.ts
  </files>

  <read_first>
    apps/mobile/lib/programs/generation-wizard.ts (the `describeDegradation` switch at lines 286-299
    and the `nameOf` / `whereOf` helpers above it),
    apps/mobile/lib/programs/__tests__/generation-wizard.test.ts (the `describeDegradation` describe
    block at lines 193-215 — distinctness, muscle-group naming, and the non-blaming regex),
    apps/mobile/app/programs/__tests__/generate-screen.test.ts (line 188 onward — the second
    consumer that also asserts sentence uniqueness),
    .planning/phases/13-program-generator-volume-selection-rework/13-CONTEXT.md (D-05 and D-10)
  </read_first>

  <behavior>
    - `describeDegradation` on a `day_trimmed` entry returns a sentence that mentions both sets and
      exercises, so it stays true whether the generator reduced sets, removed exercises, or both.
    - The four sentences returned across `DEGRADATION_KINDS` remain mutually distinct and non-empty.
    - The `day_trimmed` sentence still clears the non-blaming regex already asserted for every kind.
    - `describeDegradation`'s signature, the `DegradationEntry` type it takes, and the
      `DEGRADATION_KINDS` tuple are all unchanged (D-10).
  </behavior>

  <action>
    In `generation-wizard.ts`, replace the `day_trimmed` branch's returned sentence. The new sentence
    must state that fitting the chosen session length meant the day does fewer sets, fewer exercises,
    or both, and must keep using `whereOf(entry.dayKey)` so it still names where the reduction
    landed. Per D-05 the generator's own `DegradationEntry.detail` is the source of truth for exactly
    what happened; this copy exists to be truthful about the shape of the concession, not to
    enumerate it. Do not add a `DegradationEntry.kind`, do not change the function signature, and do
    not start rendering `entry.detail` — D-10 keeps the compile surface fixed and no screen renders
    `detail` today.

    Keep the comment above `describeDegradation` accurate: it currently states that a trimmed day
    reports the time the session had rather than what the user lacks. That framing still holds, so
    leave it, but if the sentence's new shape makes any clause of that comment untrue, correct the
    clause rather than deleting the comment.

    In `generation-wizard.test.ts`, add one case inside the existing `describeDegradation` describe
    block asserting the `day_trimmed` sentence mentions both concessions — a case-insensitive match
    for sets and a case-insensitive match for exercises. Leave the three existing cases untouched;
    they already guard distinctness, muscle-group naming and the non-blaming rule.

    `apps/mobile` resolves `@fitness/program-generator` through its compiled `dist/`, so build the
    package before running mobile's jest — a direct filtered run against a stale `dist` would prove
    nothing about the current types.
  </action>

  <verify>
    <automated>pnpm --filter @fitness/program-generator build && pnpm --filter mobile test -- generation-wizard generate-screen</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter mobile test -- generation-wizard generate-screen` exits 0 with zero failing and
      zero skipped tests.
    - `node -e "const s=require('fs').readFileSync('apps/mobile/lib/programs/generation-wizard.ts','utf8'); const m=s.match(/case 'day_trimmed':[\s\S]*?return \`([^\`]*)\`/); if(!m) process.exit(1); const t=m[1].toLowerCase(); process.exit(t.includes('set') && t.includes('exercise') ? 0 : 1)"`
      exits 0.
    - `grep -c \"case 'day_trimmed':\" apps/mobile/lib/programs/generation-wizard.ts` prints `1`.
    - `grep -c 'DEGRADATION_KINDS' apps/mobile/lib/programs/__tests__/generation-wizard.test.ts` is
      at least 2, proving the enumerated-distinctness guard is still in place.
  </acceptance_criteria>

  <done>
    The trimmed-day sentence is true for a set reduction, an exercise removal and both together; both
    mobile suites that consume it are green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the RIR ladder and the per-exercise set cap in the provenance document</name>

  <files>docs/volume-rir-landmarks.md</files>

  <read_first>
    docs/volume-rir-landmarks.md (the whole file — the per-constant section format is
    heading, table, "Literature anchor", "If wrong"; note the RIR section at lines 105-121 and the
    session-length section at lines 152-164),
    .planning/phases/13-program-generator-volume-selection-rework/13-CONTEXT.md (D-01, D-03, D-04,
    D-09),
    .planning/phases/11-program-generation/11-CONTEXT.md (D-14 and D-15 — the decision this phase
    supersedes and the provenance stance it inherits)
  </read_first>

  <action>
    Replace the RIR section with one headed `RIR_LADDER_BY_DAYS_PER_WEEK`, following the file's
    existing section format exactly. Its table has a row per `daysPerWeek` key and a column per cycle
    index, stating D-09's four ladders verbatim: 2 days `[2, 1, 0, 0]`, 3 days `[2, 1, 1, 0]`, 4 days
    `[3, 2, 1, 1]`, 5 days `[3, 2, 2, 1]`, 6 days `[3, 2, 2, 1]`. Say that the ladder floors at its
    final member for any cycle index past the end, exactly as the single ladder did. The literature
    anchor keeps the descending-RIR-within-a-block framing already in the file and adds the reasoning
    D-09 gives: fewer sessions per week mean more recovery between them, so a low-frequency block can
    end nearer failure, while six sessions a week never reach RIR 0. State plainly that the table is
    this project's own design decision, matching the D-15 stance the document opens with. The "If
    wrong" clause stays a perceived-difficulty effect, never a correctness failure.

    Add a section headed `MAX_SETS_PER_EXERCISE` and `MIN_SETS_PER_EXERCISE`, with a two-row constant
    table (5 and 2), placed adjacent to the volume-band sections since it governs how a weekly target
    is spread rather than how large it is. Explain D-01: a muscle group's per-session target is still
    `weeklySetTarget / frequency`, but when that exceeds the cap the group gets
    `ceil(sessionSets / cap)` exercises and the sets divide across them as evenly as possible, decided
    against the hardest training cycle so exercise count is stable across cycles. State that
    `MIN_SETS_PER_EXERCISE` is the floor the session-length fit may not reduce past, not a floor the
    split raises small targets to.

    Update the `WORK_SECONDS_PER_SET` / `SESSION_OVERHEAD_MINUTES` section's "If wrong" paragraph. It
    currently names the removed trim function and asserts Phase 11 D-14's rule that a surviving
    slot's own prescribed sets are never reduced. Phase 13 D-04 supersedes that rule: the fit now
    reduces sets uniformly down to the minimum before removing any exercise, and it evaluates the
    estimate against the hardest training cycle rather than the first (D-03). Rewrite the paragraph
    to name `fitDayToSessionLength`, describe the reduce-then-remove order of concessions, and add an
    explicit sentence recording that Phase 11's D-14 is superseded by Phase 13's D-04 so a later
    reader does not treat the older decision as still binding. Keep the section's structure and its
    "never a correctness failure" conclusion.

    Do not renumber, reorder or reword any other section. This document is provenance, so every
    change is additive or a correction of a now-false statement.
  </action>

  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/volume-rir-landmarks.md','utf8'); const need=['RIR_LADDER_BY_DAYS_PER_WEEK','MAX_SETS_PER_EXERCISE','MIN_SETS_PER_EXERCISE','fitDayToSessionLength','D-14']; const missing=need.filter(t=>!s.includes(t)); if(missing.length){console.error('missing:',missing.join(', '));process.exit(1);} const i=s.indexOf('RIR_LADDER_BY_DAYS_PER_WEEK'); const section=s.slice(i, s.indexOf('## ', i+10)); const rows=['| 2 |','| 3 |','| 4 |','| 5 |','| 6 |'].filter(r=>!section.includes(r)); if(rows.length){console.error('ladder rows missing:',rows.join(' '));process.exit(1);} process.exit(0)"</automated>
  </verify>

  <acceptance_criteria>
    - The verify command exits 0: the document contains a `RIR_LADDER_BY_DAYS_PER_WEEK` section with
      a table row for each of the five `daysPerWeek` keys, a section naming both
      `MAX_SETS_PER_EXERCISE` and `MIN_SETS_PER_EXERCISE`, a reference to `fitDayToSessionLength`,
      and an explicit mention of `D-14`.
    - `grep -c '^## ' docs/volume-rir-landmarks.md` is exactly one greater than it was before this
      task (the RIR section is replaced in place; the set-cap section is the only addition).
    - `node -e "const s=require('fs').readFileSync('docs/volume-rir-landmarks.md','utf8'); const n=(s.match(/\*\*Literature anchor:\*\*/g)||[]).length; const m=(s.match(/\*\*If wrong:\*\*/g)||[]).length; process.exit(n===m && n>=8 ? 0 : 1)"`
      exits 0 — every section, including the two new ones, keeps the file's anchor/if-wrong pairing.
  </acceptance_criteria>

  <done>
    The provenance document states the frequency-keyed RIR ladder and the per-exercise set cap as
    project-authored design decisions, and no longer asserts the superseded rule that the
    session-length fit leaves a surviving slot's sets alone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → user-visible copy | `DegradationEntry` values produced by the pure generator cross into a sentence rendered on the generation review screen before the program is written. |
| repository → future reader | `docs/volume-rir-landmarks.md` is the provenance record a later contributor trusts when deciding whether a constant is sourced or authored. |

This plan changes one copy string, one test and one markdown document. There is no I/O, no
persistence, no new dependency and no code path that handles untrusted input, so the register is
disclosure and provenance work rather than network or authentication work.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-11 | Information Disclosure | `describeDegradation`'s `day_trimmed` sentence | low | accept | The sentence composes only the user's own chosen session length and `whereOf(entry.dayKey)`; no exercise id, catalog internal or file path reaches user-visible copy, and the generator's raw `detail` is still not rendered. |
| T-13-12 | Repudiation | `docs/volume-rir-landmarks.md` provenance record | medium | mitigate | The new ladder and set-cap sections state explicitly that the tables are this project's own design decisions (the D-15 stance the document opens with), so no reader can later mistake them for sourced clinical recommendations; the superseded-D-14 sentence prevents the opposite error of treating a retired rule as binding. |
| T-13-13 | Tampering | degradation vocabulary stability | medium | mitigate | No `DegradationEntry.kind` is added or renamed (D-10), and the enumerated-distinctness test over `DEGRADATION_KINDS` in both mobile suites fails if the vocabulary or a sentence collides. |
| T-13-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. 13-RESEARCH.md's Package Legitimacy Audit records "N/A — this phase installs no new packages", so there is no install step to gate. |
</threat_model>

<verification>
- `pnpm --filter @fitness/program-generator build && pnpm --filter mobile test -- generation-wizard generate-screen`
  exits 0.
- The document verify command exits 0, proving all five ladder rows and both new constant names are
  present and that the superseded decision is named.
- `pnpm -w run lint` exits 0 (the copy change touches a linted TypeScript file).
</verification>

<success_criteria>
- The `day_trimmed` sentence is true when only sets were reduced, when only exercises were removed,
  and when both happened.
- The four degradation sentences remain distinct and non-blaming.
- `docs/volume-rir-landmarks.md` records the `daysPerWeek`-keyed RIR ladder and the per-exercise set
  cap in the file's existing per-constant format, and marks Phase 11's D-14 as superseded.
</success_criteria>

## Artifacts this phase produces

**New document sections**
- `docs/volume-rir-landmarks.md` → `## RIR_LADDER_BY_DAYS_PER_WEEK` (replaces the single-ladder
  section)
- `docs/volume-rir-landmarks.md` → `## MAX_SETS_PER_EXERCISE and MIN_SETS_PER_EXERCISE`

**Modified files**
- `apps/mobile/lib/programs/generation-wizard.ts` — reworded `day_trimmed` branch of
  `describeDegradation` (signature and exports unchanged)
- `apps/mobile/lib/programs/__tests__/generation-wizard.test.ts` — one added case
- `docs/volume-rir-landmarks.md` — one replaced section, one added section, one corrected
  "If wrong" paragraph

**New exported symbols**
- None. This plan adds no symbol and removes none (D-10).

<output>
Create `.planning/phases/13-program-generator-volume-selection-rework/13-02-SUMMARY.md` when done
</output>