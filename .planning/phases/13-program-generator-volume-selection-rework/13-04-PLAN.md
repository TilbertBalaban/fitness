---
phase: 13-program-generator-volume-selection-rework
plan: 04
type: execute
wave: 3
depends_on: ["13-03"]
files_modified:
  - scripts/derive-generator-regression-fixture.cjs
  - packages/program-generator/src/__fixtures__/catalog-2day-regression.ts
  - packages/program-generator/src/__tests__/regression-2day-60min.test.ts
autonomous: true
requirements: [GEN-VOL-01, GEN-VOL-02, GEN-SEL-01, GEN-SEL-02, GEN-RIR-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-11: a regression test pins the exact reported scenario — 2 days, 60 minutes, intermediate, hypertrophy, auto split, 4 cycles, no inventory — against a fixture derived from the real seeded catalog."
    - "D-11: the regression asserts at least 4 exercises per day, the two days sharing no exercise for the same muscle group, no exercise exceeding 5 sets in any cycle, the hardest-cycle estimate inside 60 minutes, and a cycle-4 RIR of 0."
    - "D-11: the fixture is a hand-committed .ts module under packages/program-generator/src/__fixtures__, not JSON, because the package's tsconfig has no resolveJsonModule and parity.ts already sets the .ts-fixture precedent."
    - "Claude's Discretion: the fixture's derivation is deterministic and re-runnable — a committed script under scripts/ regenerates it byte-identically from the catalog snapshot."
    - "D-10: the regression exercises only the public generateProgram entry point and the already-exported time-model constants; it introduces no new production symbol."
  artifacts:
    - scripts/derive-generator-regression-fixture.cjs
    - packages/program-generator/src/__fixtures__/catalog-2day-regression.ts
    - packages/program-generator/src/__tests__/regression-2day-60min.test.ts
  key_links:
    - "The fixture must carry enough exercises per muscle group for both days: chest and lats each need two slots per day across two days, so four distinct candidates each, and quads/hamstrings/glutes reach four slots in a single day at the hardest cycle before the fit trims — six per group covers every case with margin."
    - "The fixture is a GenerationCatalog (camelCase exerciseId/muscleGroupId/weightFactor/equipmentRequired/movementPattern), not a CatalogSnapshot (snake_case) — the seed pipeline owns that transform, so the derivation script must apply it."
    - "The fixture must NOT be re-exported from index.ts; only parity.ts is, because two other apps import it. This one is consumed by a single in-package suite."
---

<objective>
Pin the reported failure with a regression test that runs the real seeded catalog's data through the
public generator.

Purpose: every other plan in this phase is verified against hand-built test catalogs. D-11 exists
because the reported bug was a property of the *real* catalog — 82 chest-primary exercises all at
weight 1.00, 870 exercises, uneven per-group depth — and a synthetic fixture cannot reproduce that.
This is the test that fails if any of the five fixes regresses.

Output: a committed derivation script, the derived fixture module, and a regression suite asserting
D-11's five properties.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-program-generator-volume-selection-rework/13-CONTEXT.md
@.planning/phases/13-program-generator-volume-selection-rework/13-RESEARCH.md
@.planning/phases/13-program-generator-volume-selection-rework/13-01-SUMMARY.md
@.planning/phases/13-program-generator-volume-selection-rework/13-03-SUMMARY.md
@packages/program-generator/src/result.ts
@packages/program-generator/src/split-templates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the deterministic fixture derivation script</name>

  <precondition>
    `apps/api/src/seed/data/catalog-normalized.json` exists and parses as an object with
    `exercises` and `mappings` arrays (870 exercises, 3177 mappings at the time of research). The
    script must fail loudly with a named error if that file is absent or either array is empty,
    rather than emitting an empty fixture.
  </precondition>

  <files>scripts/derive-generator-regression-fixture.cjs</files>

  <read_first>
    scripts/sync-catalog-snapshot.cjs (the repo's existing convention for a committed `.cjs` script
    that reads the catalog snapshot — argument handling, error reporting, write style),
    apps/api/src/seed/data/catalog-normalized.json (read the first exercise and first mapping only,
    to confirm the snake_case field names: `equipment_required`, `movement_pattern`, `exercise_id`,
    `muscle_group_id`, `weight_factor`),
    packages/program-generator/src/result.ts (lines 23-41 — the camelCase `GenerationCatalog`,
    `GenerationCatalogExercise` and `GenerationCatalogMapping` shapes the fixture must satisfy),
    packages/program-generator/src/split-templates.ts (lines 74-101 — the `full_body_2` day patterns
    whose muscle groups define the fixture's coverage),
    packages/program-generator/src/__fixtures__/parity.ts (lines 1-20 — the header-comment style a
    committed fixture uses to explain itself),
    .planning/phases/13-program-generator-volume-selection-rework/13-RESEARCH.md (the "D-11 fixture
    strategy" section explaining why this is a `.ts` module and not JSON)
  </read_first>

  <action>
    Create `scripts/derive-generator-regression-fixture.cjs`. Declare two constants at the top:
    `TARGET_MUSCLE_GROUPS`, the ten ids the `full_body_2` template's two day patterns name —
    `chest`, `lats`, `quads`, `front_delts`, `biceps`, `abs`, `hamstrings`, `glutes`, `side_delts`,
    `triceps` — in that order, with a comment naming `split-templates.ts` as their source; and
    `EXERCISES_PER_GROUP = 6`, with a comment recording why six: chest and lats each take two slots
    on each of the two days and D-06 wants four distinct candidates for each, while quads, hamstrings
    and glutes each reach four slots in one day at the hardest cycle before the fit trims.

    Read and parse `apps/api/src/seed/data/catalog-normalized.json`. Throw a named error if the file
    is missing or either array is empty. Build an index of exercise ids that carry a `primary`
    mapping to each target group. For each group in `TARGET_MUSCLE_GROUPS` order, sort those ids
    ascending as strings and take the first `EXERCISES_PER_GROUP` that are not already selected for
    an earlier group — selection order and the string sort are what make the output deterministic,
    so do not use insertion order, a hash or a sample.

    Emit a `GenerationCatalog`-shaped object. `exercises` is every selected exercise mapped to
    `{ id, name, equipmentRequired, movementPattern }` from `id`, `name`, `equipment_required`,
    `movement_pattern`, sorted by `id` ascending. `mappings` is every mapping whose `exercise_id` is
    in the selected set, mapped to `{ exerciseId, muscleGroupId, role, weightFactor }`, sorted by
    `exerciseId` then `muscleGroupId` then `role` — keeping the secondary mappings is what gives
    D-07's compoundness tier something to read. Drop any mapping whose `muscle_group_id` is not one
    of the nineteen ids in `MUSCLE_GROUP_VOLUME_CLASS`, so the emitted module typechecks against
    `MuscleGroupId`; list those nineteen ids in the script with a comment naming
    `volume-landmarks.ts` as their source.

    Write `packages/program-generator/src/__fixtures__/catalog-2day-regression.ts` as a header
    comment followed by an import of the `GenerationCatalog` type from `../result` and
    `export const CATALOG_2DAY_REGRESSION: GenerationCatalog = ` plus
    `JSON.stringify(catalog, null, 2)` plus a semicolon and a trailing newline. Serializing through
    `JSON.stringify` rather than building the literal by hand is what stops an exercise name
    containing a quote, a backslash or a backtick from breaking or injecting into the emitted module
    (T-13-31). The header comment must state that the file is generated, name this script and its
    exact invocation, state the source snapshot path, and say the file is committed and must be
    regenerated rather than hand-edited.

    Do not add the script to any package's `scripts` block and do not wire it into the build — per
    13-CONTEXT.md's discretion clause the fixture is a checked-in file, not a build step, so that the
    test never depends on `apps/api` at run time.
  </action>

  <verify>
    <automated>node scripts/derive-generator-regression-fixture.cjs && cp packages/program-generator/src/__fixtures__/catalog-2day-regression.ts /tmp/fixture-run-1.ts && node scripts/derive-generator-regression-fixture.cjs && diff -q /tmp/fixture-run-1.ts packages/program-generator/src/__fixtures__/catalog-2day-regression.ts && pnpm --filter @fitness/program-generator typecheck</automated>
  </verify>

  <acceptance_criteria>
    - The verify command exits 0: two consecutive runs of the script produce a byte-identical
      fixture, and the emitted module typechecks as part of the package build surface.
    - Coverage check (parses the emitted literal directly, since the module is not runnable from
      plain node): `node -e "const s=require('fs').readFileSync('packages/program-generator/src/__fixtures__/catalog-2day-regression.ts','utf8'); const m=s.match(/=\s*(\{[\s\S]*\});\s*$/); const c=JSON.parse(m[1]); const groups=['chest','lats','quads','front_delts','biceps','abs','hamstrings','glutes','side_delts','triceps']; const byGroup={}; for(const x of c.mappings){ if(x.role!=='primary') continue; (byGroup[x.muscleGroupId] ||= new Set()).add(x.exerciseId); } const thin=groups.filter(g=>!byGroup[g]||byGroup[g].size<4); if(thin.length){console.error('under-covered:',thin.join(', '));process.exit(1);} process.exit(0)"`
      exits 0 — every target group has at least four distinct primary-mapped exercises.
    - `grep -c 'EXERCISES_PER_GROUP' scripts/derive-generator-regression-fixture.cjs` is at least 2.
    - `grep -c 'derive-generator-regression-fixture' packages/program-generator/src/__fixtures__/catalog-2day-regression.ts`
      is at least 1 — the generated file names the script that produced it.
    - `git diff --quiet -- packages/program-generator/src/index.ts` exits 0 — the public barrel is
      untouched, so the fixture is not re-exported from it (only `parity.ts` is, because two other
      apps import it).
  </acceptance_criteria>

  <done>
    A committed script regenerates the fixture byte-identically from the real catalog snapshot, and
    the emitted module typechecks as a `GenerationCatalog` with at least four primary-mapped
    exercises for every muscle group the two-day template names.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert D-11's five properties against the real-catalog fixture</name>

  <files>packages/program-generator/src/__tests__/regression-2day-60min.test.ts</files>

  <read_first>
    packages/program-generator/src/__fixtures__/catalog-2day-regression.ts (as written by Task 1 —
    read only the header and the first few entries; do not read the whole fixture),
    packages/program-generator/src/generate.ts (the four-stage day loop as it stands after 13-03),
    packages/program-generator/src/session-length.ts (`estimateSlotMinutes`,
    `SESSION_OVERHEAD_MINUTES` — how the test recomputes a day's estimate from the tree),
    packages/program-generator/src/volume-split.ts (`MAX_SETS_PER_EXERCISE`),
    packages/program-generator/src/split-templates.ts (`SPLIT_TEMPLATES`, `AUTO_SPLIT_BY_DAYS` — the
    2-day auto resolution the test asserts coverage against),
    packages/api-contracts/src/program.ts (`resolveTarget` — how a slot's per-cycle sets and RIR are
    read),
    .planning/phases/13-program-generator-volume-selection-rework/13-CONTEXT.md (D-11's five
    assertions, verbatim)
  </read_first>

  <behavior>
    - `generateProgram` with `routineName` fixed, `trainingGoal: 'hypertrophy'`, `experienceLevel:
      'intermediate'`, `daysPerWeek: 2`, `sessionLengthMinutes: 60`, `splitPreference: 'auto'`,
      `emphasis: {}`, `deloadPlacement: 'none'`, `deloadEveryNCycles: null`, `trainingCycleCount: 4`,
      a fixed `variantSeed`, `catalog: CATALOG_2DAY_REGRESSION`, `inventory: null` and
      `excludedExerciseIds: []` returns a tree with exactly 2 days and 4 training cycles.
    - Every day carries at least 4 slots.
    - For every muscle group, the exercise ids used on day 0 for that group and those used on day 1
      for that group do not intersect.
    - For every slot and every cycle, the resolved `targetSets` is at most `MAX_SETS_PER_EXERCISE`.
    - For every day, the estimate computed from the last training cycle's resolved sets — the sum of
      `estimateSlotMinutes(resolvedSets, resolvedRestSeconds)` plus `SESSION_OVERHEAD_MINUTES` — is
      at most 60.
    - Every slot's resolved `targetRir` in the fourth training cycle is 0.
    - The fixture covers every muscle group named by the resolved 2-day template, so a template
      change that adds a group fails here rather than silently degrading the fixture.
    - Two runs on the same input serialize to byte-identical JSON.
  </behavior>

  <action>
    Create `packages/program-generator/src/__tests__/regression-2day-60min.test.ts`. Import
    `CATALOG_2DAY_REGRESSION` from `../__fixtures__/catalog-2day-regression`, `generateProgram` from
    `../generate`, `resolveTarget` from `@fitness/api-contracts`, `estimateSlotMinutes` and
    `SESSION_OVERHEAD_MINUTES` from `../session-length`, `MAX_SETS_PER_EXERCISE` from
    `../volume-split`, and `resolveSplitTemplate` from `../split-templates`.

    Build the input once in a `regressionInput()` helper matching the field values listed above, and
    generate the tree once in the describe body so every case reads the same tree — the generator is
    pure, so a shared tree is safe and keeps the suite fast.

    Add a helper that maps each slot's `exerciseId` to its muscle group by reading
    `CATALOG_2DAY_REGRESSION.mappings` for that exercise's `primary` entries and intersecting with
    the muscle groups the resolved template's day patterns name; assert this resolution is
    unambiguous for every slot before using it, so a mis-attributed slot surfaces as a failure rather
    than a silently weakened variety assertion.

    Write one case per behaviour above, each named after the D-11 clause it pins so a failure reports
    what broke rather than an array index. For the fixture-coverage case, derive the group list from
    `resolveSplitTemplate('auto', 2)`'s day patterns rather than hardcoding it, and assert every one
    of them has at least one primary-mapped exercise in the fixture. For the hardest-cycle estimate
    case, take the last cycle in `tree.cycles` whose `kind` is `training`.

    Do not assert exact exercise ids, exact slot counts beyond the stated minimum, or exact set
    numbers beyond the cap — D-11 names properties, and pinning the exact output would make every
    future catalog resync a false failure. The RIR assertion is the one exact value D-11 calls for.
  </action>

  <verify>
    <automated>pnpm --filter @fitness/program-generator test -- regression-2day-60min.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @fitness/program-generator test -- regression-2day-60min.test.ts` exits 0 with
      zero failing and zero skipped tests.
    - `node -e "const s=require('fs').readFileSync('packages/program-generator/src/__tests__/regression-2day-60min.test.ts','utf8'); const n=(s.match(/\bit\(/g)||[]).length; process.exit(n>=6?0:1)"`
      exits 0 — at least one case per D-11 clause plus the coverage and determinism guards.
    - `grep -c 'MAX_SETS_PER_EXERCISE' packages/program-generator/src/__tests__/regression-2day-60min.test.ts`
      is at least 1 — the cap assertion reads the constant, not a literal 5.
    - `grep -c 'SESSION_OVERHEAD_MINUTES' packages/program-generator/src/__tests__/regression-2day-60min.test.ts`
      is at least 1 — the estimate is recomputed with the shipped time model, not a hand-copied one.
    - `node -e "const s=require('fs').readFileSync('packages/program-generator/src/__tests__/regression-2day-60min.test.ts','utf8'); process.exit(/toBe\(0\)/.test(s)?0:1)"`
      exits 0 — the cycle-4 RIR is asserted as an exact 0.
  </acceptance_criteria>

  <done>
    The reported scenario is pinned by a suite asserting all five D-11 properties against
    real-catalog data, and it is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the phase with a full workspace gate and a validation record</name>

  <precondition>
    `packages/program-generator/dist/` is rebuilt from the current source before any mobile or api
    suite runs — both apps resolve `@fitness/program-generator` through its compiled output, so a
    filtered jest run against a stale `dist` would report a pass that asserts nothing.
  </precondition>

  <files>
    .planning/phases/13-program-generator-volume-selection-rework/13-VALIDATION.md
  </files>

  <read_first>
    .planning/phases/13-program-generator-volume-selection-rework/13-VALIDATION.md (the skeleton to
    fill),
    .planning/phases/13-program-generator-volume-selection-rework/13-RESEARCH.md (the Validation
    Architecture section — framework, config path, commands, the build-before-typecheck note, and
    the Phase Requirements to Test Map),
    .planning/phases/13-program-generator-volume-selection-rework/13-01-SUMMARY.md,
    .planning/phases/13-program-generator-volume-selection-rework/13-02-SUMMARY.md,
    .planning/phases/13-program-generator-volume-selection-rework/13-03-SUMMARY.md
  </read_first>

  <action>
    Run the full workspace gate: rebuild the package, then `pnpm -w run typecheck` and
    `pnpm -w run test`. Turbo resolves `^build` for both tasks, but run the explicit package build
    first anyway so a direct filtered rerun during triage is also correct.

    Fill in `13-VALIDATION.md` from the research's Validation Architecture section and the three
    preceding summaries. Test Infrastructure: Jest 30 via `ts-jest`, config
    `packages/program-generator/jest.config.js`, quick command
    `pnpm --filter @fitness/program-generator test`, full command `pnpm -w run test`, with the
    measured runtime of the package suite rather than the skeleton's placeholder. Sampling Rate: the
    quick command after every task commit, the full command after every wave, full suite green before
    `/gsd-verify-work`, and the max feedback latency set to the measured package-suite runtime.

    Fill the Per-Task Verification Map with one row per task across all four plans (13-01 tasks 1-3,
    13-02 tasks 1-2, 13-03 tasks 1-2, 13-04 tasks 1-3), each carrying its plan, wave, requirement id,
    threat reference from that plan's register, expected secure behavior, test type, the automated
    command from that task's verify block, and its file-exists state. Mark every row green only if
    its command has actually been run.

    Wave 0 Requirements: record that every gap the research listed is now closed, naming the file
    that closed each — the two new modules and their suites, the retired trim block, the rewritten
    slot-fill call sites, the updated ladder call sites, the recomputed parity values, and the D-11
    fixture. Manual-Only Verifications: state that all phase behaviours have automated verification,
    since this phase is pure in-package logic with no UI surface and no device dependency. Complete
    the Validation Sign-Off checklist and set `nyquist_compliant: true` and `wave_0_complete: true`
    in the frontmatter only if every box is genuinely checked; leave them false and say which box
    failed otherwise.
  </action>

  <verify>
    <automated>pnpm --filter @fitness/program-generator build && pnpm -w run typecheck && pnpm -w run test && node -e "const s=require('fs').readFileSync('.planning/phases/13-program-generator-volume-selection-rework/13-VALIDATION.md','utf8'); const bad=['{quick command}','{full command}','REQ-{XX}','{path or','{behavior}','T-13-01 / —'].filter(t=>s.includes(t)); if(bad.length){console.error('unfilled placeholders:',bad.join(' | '));process.exit(1);} process.exit(0)"</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm -w run typecheck` exits 0.
    - `pnpm -w run test` exits 0 — every package and app suite green, including the three processes
      that import `__fixtures__/parity.ts`.
    - The verify command's placeholder scan exits 0: no skeleton placeholder text remains in
      `13-VALIDATION.md`.
    - `13-VALIDATION.md`'s Per-Task Verification Map has at least 10 data rows, one per task across
      the four plans.
    - `13-VALIDATION.md` frontmatter carries `nyquist_compliant: true` and `wave_0_complete: true`,
      or states in the Validation Sign-Off section exactly which checklist item is unmet.
  </acceptance_criteria>

  <done>
    The whole workspace typechecks and tests green with the rebuilt package, and
    `13-VALIDATION.md` records the phase's real test infrastructure, sampling rate and per-task
    verification map.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog snapshot → generated source file | `apps/api/src/seed/data/catalog-normalized.json` holds third-party-sourced exercise names and text that the derivation script turns into a committed TypeScript module — the only place in this phase where external data becomes code. |
| fixture → test assertions | The committed fixture is the evidence base for the phase's strongest regression; drift between it and the snapshot silently weakens every assertion built on it. |

This plan runs one local script and adds one test suite. There is no network call, no persistence,
no new dependency and no runtime code path — the register is integrity and provenance work.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-31 | Tampering | `scripts/derive-generator-regression-fixture.cjs` emitting a `.ts` module from catalog text | high | mitigate | The fixture body is produced by `JSON.stringify` and interpolated into a single `export const` assignment, so an exercise name containing a quote, backslash or backtick cannot terminate the literal or inject code. The script writes exactly one declared path and reads exactly one declared path. |
| T-13-32 | Repudiation | fixture drift from the catalog snapshot | medium | mitigate | The generated header names the script, its invocation and the source path, and Task 1's verify runs the script twice and diffs, proving regeneration is byte-identical — so a hand edit is detectable by re-running the script. |
| T-13-33 | Tampering | regression assertions weakening silently | high | mitigate | The suite derives its muscle-group list from `resolveSplitTemplate('auto', 2)` rather than hardcoding it, and asserts fixture coverage of every group the template names, so a template change or a thinned fixture fails the suite instead of quietly reducing what it proves. The repo's `scripts/jest-suite-integrity.cjs` reporter additionally fails a run containing a skipped or empty suite. |
| T-13-34 | Denial of Service | fixture size inflating the shipped `dist` | low | accept | The fixture lives under `src/__fixtures__`, which the package tsconfig compiles (only `src/__tests__` is excluded), so roughly sixty exercises and their mappings are emitted into `dist`. Accepted: the package is private and workspace-only, and D-11 locks the fixture's location. |
| T-13-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package. 13-RESEARCH.md's Package Legitimacy Audit records "N/A — this phase installs no new packages"; the derivation script uses only Node's built-in `fs` and `path`. |
</threat_model>

<verification>
- `node scripts/derive-generator-regression-fixture.cjs` run twice produces a byte-identical fixture.
- `pnpm --filter @fitness/program-generator typecheck` exits 0 with the fixture in the build surface.
- `pnpm --filter @fitness/program-generator test -- regression-2day-60min.test.ts` exits 0.
- `pnpm --filter @fitness/program-generator build && pnpm -w run typecheck && pnpm -w run test`
  exits 0.
- `13-VALIDATION.md` contains no skeleton placeholder text and carries a per-task verification row
  for every task in the phase.
</verification>

<success_criteria>
- The reported scenario — 2 days, 60 minutes, intermediate, hypertrophy, auto split, 4 cycles, no
  inventory — produces at least 4 exercises per day, no shared exercise for a muscle group across
  the two days, no exercise above 5 sets in any cycle, a hardest-cycle estimate inside 60 minutes,
  and a cycle-4 RIR of 0.
- The fixture is reproducible from the committed catalog snapshot by a committed script.
- The whole workspace typechecks and tests green.
- `13-VALIDATION.md` is filled in and signed off.
</success_criteria>

## Artifacts this phase produces

**New files**
- `scripts/derive-generator-regression-fixture.cjs`
- `packages/program-generator/src/__fixtures__/catalog-2day-regression.ts`
- `packages/program-generator/src/__tests__/regression-2day-60min.test.ts`

**New exported symbols**
- `CATALOG_2DAY_REGRESSION: GenerationCatalog` — `__fixtures__/catalog-2day-regression.ts`
  (deliberately NOT re-exported from `index.ts`; only `parity.ts` is, because two other apps import
  it)

**New script constants**
- `TARGET_MUSCLE_GROUPS` (ten ids from the `full_body_2` template) —
  `scripts/derive-generator-regression-fixture.cjs`
- `EXERCISES_PER_GROUP` (`6`) — `scripts/derive-generator-regression-fixture.cjs`

**Modified files**
- `.planning/phases/13-program-generator-volume-selection-rework/13-VALIDATION.md`

<output>
Create `.planning/phases/13-program-generator-volume-selection-rework/13-04-SUMMARY.md` when done
</output>
