---
phase: 12-body-metrics-dashboard
plan: 08
type: execute
wave: 6
depends_on: ["12-02", "12-03", "12-05", "12-07"]
files_modified:
  - apps/mobile/components/QuickActionSheet.tsx
  - apps/mobile/components/__tests__/QuickActionSheet.test.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/app/(tabs)/workout.tsx
  - apps/mobile/app/__tests__/quick-actions.test.ts
  - apps/mobile/lib/db/body-metrics.ts
  - apps/mobile/lib/db/__tests__/body-metrics.test.ts
  - packages/api-contracts/src/__tests__/sync.test.ts
  - apps/mobile/app/__durability.web.tsx
  - apps/mobile/e2e/quick-action.spec.ts
  - apps/mobile/playwright.config.ts
autonomous: true
requirements: [DASH-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One quick-action sheet, opened from the Home header, reaches all six destinations: quick weigh-in, quick measurement, progress photo, history, new program and one-off workout (DASH-03, D-27)."
    - "Quick weigh-in writes the `body_metric` row from inside the sheet, with no full-screen navigation, defaulting to the last recorded value for that kind and committing in one confirm (D-29)."
    - "The pre-filled last value is fully editable via the keypad and never locks manual correction (R31)."
    - "Three of the six destinations are pure navigation to routes that already existed before this phase — history, new program and one-off workout; the sheet adds no capability, it adds one door to six rooms (D-28)."
    - "A pure-navigation destination dismisses the sheet BEFORE navigating; it never navigates with the sheet still mounted underneath (R30)."
    - "The sheet reuses the shipped action-sheet idiom rather than introducing a floating action button, which would be a new interaction primitive on a scroll of cards and would collide with native tab-bar safe areas (D-27)."
    - "The six rows render in a fixed order and are never reordered and never conditionally hidden; the sheet has exactly one state, because every destination screen handles its own preconditions (S3 populated, D-27/D-28)."
    - "Row labels are short fixed constants and are never truncated (S3 long-text)."
    - "The Profile tab gains exactly two rows — `Body Metrics` and `Progress Photos` — placed after the existing Gym Profiles row and before Appearance (S5/S8 entry points)."
    - "`PUSH_DEFERRED_TABLES` is empty, and the assertion is a test rather than an observation: every table this project syncs now has a push apply path (CONTEXT Specific Ideas)."
    - "No tab is added, removed or reordered anywhere in this phase; the five-tab shell is untouched (D-20)."
  artifacts:
    - apps/mobile/components/QuickActionSheet.tsx
    - apps/mobile/e2e/quick-action.spec.ts
    - packages/api-contracts/src/__tests__/sync.test.ts
  key_links:
    - "Home header `Quick Actions` control (12-05) -> `quickActionsOpen` -> `QuickActionSheet` -> six destinations"
    - "`QUICK_ACTIONS[0]` -> `MetricEntrySheet` (12-02) with `kind: 'bodyweight'` pre-selected and `loadLatestMetric`'s value pre-filled -> `logMetric` -> local row -> sync push, with no route change (D-29)"
    - "`QUICK_ACTIONS[2]` -> `capturePhoto` (12-03) -> `PhotoCaptureConfirmSheet`, opening capture directly rather than routing to the gallery"
  prohibitions: []
---

<objective>
Give the user one door to the six things they do most, and make the two most frequent ones — a
weigh-in and a measurement — take two taps instead of a screen.

Purpose: DASH-03 adds no capability. Five of its six destinations already exist by the time this plan
runs, and the sixth (progress photo capture) is 12-03's. What this plan adds is the door, plus the
one behavior that makes the word "quick" honest: a weigh-in commits from inside the sheet, pre-filled
with yesterday's number, without a navigation (D-29).

It also closes the phase's stated milestone: `PUSH_DEFERRED_TABLES` is empty for the first time in
this project's life, and `12-CONTEXT.md` asks for that to be asserted rather than merely observed.

Output: `QuickActionSheet` with its six fixed rows, the two new Profile entry points, the inline
quick weigh-in, a Playwright spec, and a falsifiable test that every synced table now has an apply
path.

**Shared-seam ownership.** This plan is the wave-6 owner of `apps/mobile/app/__durability.web.tsx`
and `apps/mobile/playwright.config.ts`'s `durability` `testMatch` array. **Append only — never
rewrite, never reorder, never remove an existing entry.** It does not touch
`apps/mobile/lib/db/test-support.ts`: 12-04's `seedBodyMetrics` and 12-07's `seedDashboardWidgets`
are reused as-is. `apps/mobile/app/(tabs)/index.tsx` was restructured by 12-05 (wave 3) and extended
by 12-07 (wave 5); this plan only mounts `QuickActionSheet` on the `quickActionsOpen` flag 12-05
already holds.

**MVP note (planner flag):** the ROADMAP `**Goal:**` line for Phase 12 is not in user-story form, so
no story is framed here. See 12-01's `<planner_assumptions>`.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/12-body-metrics-dashboard/12-CONTEXT.md
@.planning/phases/12-body-metrics-dashboard/12-UI-SPEC.md
@.planning/phases/12-body-metrics-dashboard/12-PATTERNS.md
@.planning/phases/12-body-metrics-dashboard/12-05-SUMMARY.md
@.planning/phases/12-body-metrics-dashboard/12-07-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "quick weigh-in" — two taps, no navigation, in a real browser</name>
  <precondition>`corepack enable` has been run and `pnpm -w build` has completed in this worktree; 12-02's `MetricEntrySheet`, 12-05's Home dashboard and 12-07's picker are all present; and Chromium is in `~/Library/Caches/ms-playwright` (standing per `.planning/CONVENTIONS.md`).</precondition>
  <files>apps/mobile/components/QuickActionSheet.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/lib/db/body-metrics.ts, apps/mobile/app/__durability.web.tsx, apps/mobile/e2e/quick-action.spec.ts, apps/mobile/playwright.config.ts</files>
  <read_first>
    - `apps/mobile/components/SessionActionSheet.tsx` — `SessionExerciseAction` (`id`, `label`, `icon`, optional `destructive`), the exported fixed-order `SESSION_EXERCISE_ACTIONS` constant, the modal shell and the local glyph-colour constant
    - `apps/mobile/components/HistoryActionSheet.tsx` — the same triple, plus how an existing sheet dismisses before navigating on its `edit`/`view` rows
    - `apps/mobile/components/MetricEntrySheet.tsx` — as 12-02 left it, including its kind-picker step, its last-value pre-fill and its inline write-failure line
    - `apps/mobile/lib/db/body-metrics.ts` — `loadLatestMetric` and `logMetric`
    - `apps/mobile/app/(tabs)/index.tsx` — the header row and the `quickActionsOpen` flag 12-05 added
    - `apps/mobile/app/__durability.web.tsx` — the single-active-mount convention and 12-07's Home dashboard mount, which this spec reuses
    - `apps/mobile/e2e/dashboard-widgets.spec.ts` — 12-07's spec, the closest template for driving the Home header
    - `apps/mobile/playwright.config.ts` — the `durability` project's `testMatch` array (append only)
    - `.planning/phases/12-body-metrics-dashboard/12-UI-SPEC.md` §S3, §S4
  </read_first>
  <action>
    Deliver one working path: open Quick Actions from the Home header in a real browser, tap
    `Quick Weigh-In`, see yesterday's number pre-filled, change it, tap Log, and the row is written —
    all without the route changing. One row of the six in this task.

    Create `apps/mobile/components/QuickActionSheet.tsx` exporting a fixed-order `QUICK_ACTIONS`
    constant shaped exactly like `SESSION_EXERCISE_ACTIONS` (`id`, `label`, `icon`; no `destructive`
    flag, since none of the six is destructive), a hook-free `QuickActionSheetView`, and a
    `QuickActionSheet` modal wrapper using `SessionActionSheet`'s overlay and card shell
    (`bg-background/80` overlay, `max-w-[400px] rounded-md bg-surface p-lg` card, title, then a
    `gap-xs` row list). Title Heading `Quick Actions`. The six rows in this exact order with these
    exact labels and icons: `Quick Weigh-In` / `scale-outline`; `Quick Measurement` /
    `resize-outline`; `Progress Photo` / `camera-outline`; `History` / `time-outline`;
    `New Program` / `add-circle-outline`; `One-off Workout` / `barbell-outline`. Every row is a
    `Pressable` at `minHeight: 48`, `flex-row items-center gap-sm`, with the icon in
    `foregroundMuted` at 20px and `accessibilityRole="button"`. Resolve glyph colours through a
    local constant in this file rather than widening `theme-colors.ts`.

    The sheet has exactly one state. No row is ever hidden or disabled: no gym profile, no active
    program and no logged history are all handled by the destination screens themselves, never by
    hiding a row here (S3).

    Mount `QuickActionSheet` in `apps/mobile/app/(tabs)/index.tsx` on the `quickActionsOpen` flag
    12-05 already holds, driven by the header's `Quick Actions` control. In this task wire only the
    first row: it dismisses `QuickActionSheet` and opens `MetricEntrySheet` with `kind: 'bodyweight'`
    already selected, so the kind-picker step is skipped entirely, and with the value pre-filled from
    `loadLatestMetric` for that kind. Committing calls `logMetric` and dismisses. The route never
    changes — this whole interaction happens in modals over Home (D-29).

    The pre-filled value stays fully editable through the keypad (R31); it is a starting point, never
    a locked field.

    Append to `apps/mobile/app/__durability.web.tsx` only the window methods this spec needs on top of
    12-07's existing Home dashboard mount — at minimum a way to read back the `body_metric` rows the
    sheet wrote. Reuse 12-04's `seedBodyMetrics` harness method rather than adding a duplicate.
    **Append; do not restructure, reorder or remove anything already in this file.**

    Create `apps/mobile/e2e/quick-action.spec.ts` in the `durability` project and append
    `'quick-action.spec.ts'` to that project's `testMatch` array in
    `apps/mobile/playwright.config.ts`. In this task the spec proves, against a real
    `@powersync/web` database: tapping the Home header's Quick Actions control opens a sheet listing
    six rows in the specified order; tapping `Quick Weigh-In` opens the entry sheet pre-filled with
    the seeded last bodyweight value; and confirming writes exactly one new `body_metric` row while
    the page URL is unchanged from before the interaction.
  </action>
  <verify>
    <automated>pnpm --filter mobile typecheck &amp;&amp; pnpm --filter mobile test:e2e -- quick-action</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test:e2e -- quick-action` exits 0 with at least 3 passing cases in the `durability` project.
    - The spec asserts the page URL is identical before and after the weigh-in commits — the D-29 no-navigation claim is proven, not assumed.
    - The spec asserts the entry sheet's initial value equals the seeded last bodyweight value.
    - `node -e "const m=require('fs').readFileSync('apps/mobile/components/QuickActionSheet.tsx','utf8'); const order=['Quick Weigh-In','Quick Measurement','Progress Photo','History','New Program','One-off Workout']; let i=-1; for (const l of order){ const j=m.indexOf(l); if(j<=i) process.exit(1); i=j; }"` exits 0 — the six labels appear in the specified order.
    - A test asserts no member of `QUICK_ACTIONS` carries a truthy destructive flag, and that all six render with the non-accent muted glyph colour.
    - `git diff --stat apps/mobile/app/__durability.web.tsx apps/mobile/playwright.config.ts` shows insertions only — zero deletions in both files.
  </acceptance_criteria>
  <done>A bodyweight is logged in two taps from the Home header without the route changing, proven in a real browser.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The other five destinations and the two Profile entry points</name>
  <files>apps/mobile/components/QuickActionSheet.tsx, apps/mobile/components/__tests__/QuickActionSheet.test.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/(tabs)/profile.tsx, apps/mobile/app/(tabs)/workout.tsx, apps/mobile/app/__tests__/quick-actions.test.ts, apps/mobile/e2e/quick-action.spec.ts</files>
  <read_first>
    - `apps/mobile/components/QuickActionSheet.tsx` — as Task 1 left it
    - `apps/mobile/components/HistoryActionSheet.tsx` — the dismiss-before-navigate sequencing on its `edit`/`view` rows, which R30 generalizes
    - `apps/mobile/app/(tabs)/profile.tsx` — the existing settings-row chrome (a bordered 48px-minimum surface row), the Gym Profiles row and its `router.push` call, and where `AppearanceControl` sits
    - `apps/mobile/app/(tabs)/workout.tsx` — `handleStartOneOff`, `handleCancelOneOffPicker`, `handleAddOneOffExercises` and the `oneOffPickerOpen` state: the existing one-off start path the quick action must reach without re-implementing it
    - `apps/mobile/lib/photos/capture.ts` / `capture.web.ts` and `apps/mobile/components/PhotoCaptureConfirmSheet.tsx` — 12-03's capture flow, opened directly rather than routing to the gallery
    - `apps/mobile/components/MetricEntrySheet.tsx` — its kind-picker step, used by the Quick Measurement row
    - `.planning/phases/12-body-metrics-dashboard/12-UI-SPEC.md` §S3 destination table, §"Navigation & Placement" Profile tab additions, §"Phase-Wide Rules" R30
  </read_first>
  <behavior>
    - `resolveQuickAction('history')` yields a pure-navigation descriptor targeting the history tab; `'new_program'` targets `/programs/generate`; `'one_off_workout'` targets the workout tab's one-off entry.
    - `resolveQuickAction('quick_weigh_in')`, `'quick_measurement'` and `'progress_photo'` yield in-place descriptors carrying no route.
    - For every pure-navigation action, the dismiss callback fires before the navigate callback — asserted by recording call order, not by inspecting the final state.
    - `QuickActionSheetView` renders six rows for every possible input; there is no input that hides or disables a row.
    - `QuickActionSheetView` sets no `numberOfLines` on any row label.
    - Selecting `Quick Measurement` opens the entry sheet with the kind-picker step shown; selecting `Quick Weigh-In` opens it with no picker.
    - The Profile settings list contains a `Body Metrics` row targeting `/body-metrics` and a `Progress Photos` row targeting `/progress-photos`, both positioned after Gym Profiles and before Appearance.
  </behavior>
  <action>
    Wire the remaining five rows. Add and export `resolveQuickAction(id)` from
    `apps/mobile/components/QuickActionSheet.tsx`, returning a descriptor that distinguishes a
    pure-navigation destination (carrying its route) from an in-place destination (carrying none).
    That distinction is what makes R30 testable rather than a convention.

    For every pure-navigation destination — `History` to the history tab, `New Program` to
    `/programs/generate`, and `One-off Workout` to the workout tab's one-off entry — dismiss
    `QuickActionSheet` first and navigate second, matching every existing action-sheet-then-navigate
    call site in this app (R30).

    Reach the one-off workout without re-implementing it. `apps/mobile/app/(tabs)/workout.tsx`
    already owns the flow through its `oneOffPickerOpen` state and `handleStartOneOff` /
    `handleAddOneOffExercises` handlers. Add a single route parameter the workout screen reads once on
    mount to open that same picker, and navigate to the workout tab with it set. Do not duplicate the
    picker, the session creation, or any part of `startOneOffSession`.

    Wire `Quick Measurement` to `MetricEntrySheet` with no kind named, so its kind-picker step renders
    over the user's tracked kinds excluding `bodyweight` (12-02 built this). Wire `Progress Photo` to
    12-03's capture flow directly — capture then `PhotoCaptureConfirmSheet` — rather than routing to
    `/progress-photos`, per UI-SPEC's destination table.

    Add the two Profile rows to `apps/mobile/app/(tabs)/profile.tsx` using the file's existing
    settings-row chrome unchanged: `Body Metrics` pushing `/body-metrics` and `Progress Photos`
    pushing `/progress-photos`, both placed after the existing Gym Profiles row and before Appearance,
    grouped with the other manage-your-data entries rather than the app-configuration ones. Change
    nothing else in that file.

    Write `apps/mobile/components/__tests__/QuickActionSheet.test.tsx` and
    `apps/mobile/app/__tests__/quick-actions.test.ts` covering every bullet in `<behavior>`. The R30
    ordering case must record the sequence of callback invocations and assert dismissal came first —
    a final-state assertion would pass for a sheet that navigated while still mounted.

    Extend `apps/mobile/e2e/quick-action.spec.ts` with a real-browser case that opens the sheet, taps
    `History`, and asserts both that the route changed and that no modal overlay remains in the
    accessibility tree.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- "QuickActionSheet|quick-actions" &amp;&amp; pnpm --filter mobile test:e2e -- quick-action</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test -- "QuickActionSheet|quick-actions"` exits 0 with at least 9 passing cases.
    - A test records callback order for each of the three pure-navigation actions and asserts dismissal precedes navigation.
    - A test asserts all six rows render for every input the view accepts.
    - `grep -c "Body Metrics" apps/mobile/app/(tabs)/profile.tsx` is 1 and `grep -c "Progress Photos" apps/mobile/app/(tabs)/profile.tsx` is 1.
    - `git diff --name-only` does not list `apps/mobile/app/(tabs)/_layout.tsx` — no tab was added, removed or reordered (D-20).
    - `git diff --stat apps/mobile/app/(tabs)/workout.tsx` shows fewer than 30 changed lines — the one-off flow was reached, not rebuilt.
    - `grep -v '^//' apps/mobile/components/QuickActionSheet.tsx | grep -c "numberOfLines"` returns 0.
    - `pnpm --filter mobile test:e2e -- quick-action` exits 0 with at least 4 passing cases.
  </acceptance_criteria>
  <done>All six quick actions reach their destination, pure-navigation rows dismiss before navigating, and Profile carries the two new entry points.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert the milestone — every synced table now has an apply path</name>
  <files>packages/api-contracts/src/__tests__/sync.test.ts, apps/mobile/lib/db/body-metrics.ts, apps/mobile/lib/db/__tests__/body-metrics.test.ts</files>
  <read_first>
    - `packages/api-contracts/src/sync.ts` — `SYNCED_TABLES`, `PUSH_APPLIED_TABLES`, `PUSH_DEFERRED_TABLES` as 12-01, 12-03 and 12-05 left them, and the header comments each of those plans updated
    - `packages/api-contracts/src/__tests__/sync.test.ts` — the existing tuple-membership assertions, including 12-01's `body_metric` case and the last-member assertion 11-02 established
    - `.planning/phases/12-body-metrics-dashboard/12-CONTEXT.md` §"Specific Ideas" — the request that this be asserted rather than observed
  </read_first>
  <behavior>
    - `PUSH_DEFERRED_TABLES` has length zero.
    - Every member of `SYNCED_TABLES` is a member of `PUSH_APPLIED_TABLES` — the "every table this project syncs has a push apply path" claim, stated as one assertion rather than a table-by-table list.
    - `PUSH_APPLIED_TABLES` contains no member that is absent from `SYNCED_TABLES` — the converse, so the applied tuple cannot drift into naming a table nothing syncs.
    - `PUSH_APPLIED_TABLES`'s last member is `dashboard_widget` and `SYNCED_TABLES`'s last member is `dashboard_widget`, preserving the additive-only ordering rule those files' headers state.
  </behavior>
  <action>
    Extend `packages/api-contracts/src/__tests__/sync.test.ts` with the four assertions in
    `<behavior>`. `PUSH_DEFERRED_TABLES` going empty is the first time in this project's life that
    every synced table has a push apply path, and `12-CONTEXT.md` asks specifically for it to be
    falsifiable rather than merely true today: a future phase that adds a synced table without an
    apply path must turn this suite red.

    Write the set-containment assertions by iterating the tuples, never by hard-coding a second copy
    of either list — a hard-coded expectation would have to be edited alongside any new table, which
    is exactly the edit that would silently defeat the test.

    Also update `PUSH_DEFERRED_TABLES`'s header comment in `packages/api-contracts/src/sync.ts` to
    record that the tuple is empty as of this phase and that the test above is what keeps it honest —
    but make no functional change to the tuples themselves in this task.

    Separately, confirm the quick-weigh-in default is covered by a unit test: extend
    `apps/mobile/lib/db/__tests__/body-metrics.test.ts` with a case asserting `loadLatestMetric`
    returns the most recent entry for a kind when several exist across several days, since that is the
    value D-29's pre-fill depends on and it is otherwise only exercised end-to-end.
  </action>
  <verify>
    <automated>pnpm --filter @fitness/api-contracts test -- sync &amp;&amp; pnpm --filter mobile test -- body-metrics</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fitness/api-contracts test -- sync` exits 0 and the suite contains an assertion that `PUSH_DEFERRED_TABLES` has length zero.
    - `node -e "const s=require('./packages/api-contracts/dist/index.js'); if(s.PUSH_DEFERRED_TABLES.length!==0) process.exit(1); for (const t of s.SYNCED_TABLES) if(!s.PUSH_APPLIED_TABLES.includes(t)) process.exit(1); for (const t of s.PUSH_APPLIED_TABLES) if(!s.SYNCED_TABLES.includes(t)) process.exit(1)"` exits 0.
    - The new assertions iterate the tuples: `grep -c "for (const" packages/api-contracts/src/__tests__/sync.test.ts` is at least 2.
    - `pnpm --filter mobile test -- body-metrics` exits 0 and includes a case asserting `loadLatestMetric` returns the most recent of several entries.
    - `git diff packages/api-contracts/src/sync.ts` shows only comment changes in this task — no tuple member was added, removed or reordered.
  </acceptance_criteria>
  <done>A test, not a comment, asserts that every table this project syncs has a push apply path, and it turns red if a future phase adds one without.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quick-action row -> destination | Six user-initiated transitions, three of which are navigations into existing authenticated screens and three of which are in-place writes over the Home screen. |
| inline weigh-in -> `body_metric` write | The same untrusted keypad input boundary 12-02 established, now reachable one tap from Home. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-33 | Tampering | inline quick weigh-in write | high | mitigate | The value goes through the same `toCanonicalValue` converter and the same `logMetric` path 12-02 established, with 12-01's server-side `hasInvalidField` branch as the backstop; no second, looser write path is introduced for the quick case. |
| T-12-34 | Elevation of Privilege | quick-action navigation targets | medium | mitigate | Every navigation target is an existing authenticated in-app route reached through the router; no destination accepts a user-supplied route string, and the one-off row passes a fixed boolean-style parameter rather than an arbitrary value. |
| T-12-35 | Information Disclosure | Profile tab entry points | low | accept | Both new rows navigate to screens that read only the authenticated user's own local rows; no new data surface is introduced. |
| T-12-36 | Denial of Service | `QuickActionSheet` rendered over any Home state | low | accept | The sheet is a static six-row menu with no read behind it, so it cannot fail to render because of a data problem; the header controls remain reachable in every Home state by design (UI-SPEC S1). |
</threat_model>

<verification>
- `pnpm -w typecheck` passes.
- `pnpm -w test` passes, including the sync-tuple milestone assertions.
- `pnpm --filter mobile test:e2e` passes in full — every spec this phase added plus every spec that existed before it.
- `git diff --name-only` does not list `apps/mobile/app/(tabs)/_layout.tsx`.
- `git diff --stat` shows insertion-only changes to `apps/mobile/app/__durability.web.tsx` and `apps/mobile/playwright.config.ts`.
</verification>

<success_criteria>
- Six destinations from one sheet, in a fixed order, none hidden.
- A weigh-in takes two taps and no navigation, pre-filled with the last value.
- Profile carries the two new data entry points and the tab bar is untouched.
- A test asserts every synced table has a push apply path.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (12-08)

**New files**
- `apps/mobile/components/QuickActionSheet.tsx`
- `apps/mobile/components/__tests__/QuickActionSheet.test.tsx`
- `apps/mobile/app/__tests__/quick-actions.test.ts`
- `apps/mobile/e2e/quick-action.spec.ts`

**New exported symbols**
- `QUICK_ACTIONS`, `QuickActionId`, `QuickActionSheetView`, `QuickActionSheet`,
  `resolveQuickAction` (`apps/mobile/components/QuickActionSheet.tsx`)

**Changed surfaces**
- `apps/mobile/app/(tabs)/profile.tsx` gains two settings rows: `Body Metrics`, `Progress Photos`
- `apps/mobile/app/(tabs)/workout.tsx` gains one route parameter that opens its existing one-off picker
- `apps/mobile/app/(tabs)/index.tsx` mounts `QuickActionSheet` on the existing `quickActionsOpen` flag

**New route paths:** none — every destination already existed.
**New npm dependencies:** none.
**New tables:** none.
</artifacts_this_phase_produces>

<planner_assumptions>
1. **Spec-less edge probe, DASH-03 — `unclassified` (unresolved).** The deterministic edge probe
   returned no shape category for "User can reach high-frequency actions ... from a single
   quick-action menu"; the `must_haves.truths` above are derived from `12-CONTEXT.md` D-27/D-28/D-29
   and `12-UI-SPEC.md` S3 instead. Flagged, not dismissed.
2. **The one-off workout is reached with a route parameter** that the existing workout screen reads
   once to open its own picker. D-28 says the row is "the existing empty-session start path" without
   saying how it is entered; a parameter is the smallest change that reuses the flow rather than
   duplicating it.
</planner_assumptions>

<output>
Create `.planning/phases/12-body-metrics-dashboard/12-08-SUMMARY.md` when done
</output>
