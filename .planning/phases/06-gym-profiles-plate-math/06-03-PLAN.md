---
phase: 06-gym-profiles-plate-math
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/mobile/lib/db/equipment-profiles.ts
  - apps/mobile/lib/db/__tests__/equipment-profiles.test.ts
  - apps/mobile/app/gym-profiles/_layout.tsx
  - apps/mobile/app/gym-profiles/index.tsx
  - apps/mobile/app/gym-profiles/__tests__/gym-profiles-screen.test.tsx
  - apps/mobile/components/GymProfileActionSheet.tsx
  - apps/mobile/components/__tests__/GymProfileActionSheet.test.tsx
  - apps/mobile/components/ArchiveDialog.tsx
  - apps/mobile/components/__tests__/ArchiveDialog.test.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/app/(tabs)/__tests__/profile.test.tsx
  - apps/mobile/app/_layout.tsx
autonomous: true
requirements: [GYM-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user can see every gym they have configured, create a new one, set which is active, edit, duplicate, archive and restore — reachable from the Profile tab."
    - "Setting a gym active moves the single active pointer; it never changes any other gym's row, and archiving the active gym leaves the pointer resolvable rather than dangling."
    - "Archiving a gym removes it from the gym list without touching any workout already logged against it."
    - "E1 Gym Profiles List, empty: never truly empty after the seed on first plate-math need; the pre-seed instant renders no skeleton."
    - "E1 Gym Profiles List, loading: no loading state — gyms read from local SQLite."
    - "E1 Gym Profiles List, error: renders the shipped error surface with the Gym-Profiles-couldn't-load heading and the shared restart body."
    - "E1 Gym Profiles List, populated: active gym pinned first and visually distinguished, remaining gyms alphabetical, archived gyms in a collapsed trailing section — mirroring Program Library."
    - "E1 Gym Profiles List, partial: the row subtitle joins only the sections that actually have content, in the fixed order bar then plates then dumbbells then machines; a gym with nothing configured degrades to its name alone, and absence is never rendered as a zero count or an incompleteness marker."
    - "E1 Gym Profiles List, overflow: vertical scroll only; row content wraps and grows rather than truncating."
    - "E1 Gym Profiles List, zero/one/many: exactly one gym renders identically to many, as a single-row active partition with no rest section."
    - "E1 Gym Profiles List, long text: gym names and the summary subtitle wrap and grow."
    - "E7 Profile tab nav row, loading: no loading state — the active gym's name reads from already-local preference state."
    - "E7 Profile tab nav row, error: no failure mode of its own; if the active gym's name cannot be resolved the trailing label is omitted and the row still navigates, never rendered broken or disabled."
    - "E7 Profile tab nav row, overflow: the row wraps and grows within the Profile screen's own scroll and introduces no nested scroll."
    - "E7 Profile tab nav row, long text: a long active-gym name wraps and grows the row like every other row on that screen."
    - "E8 Gym Profile Action Sheet, empty: not reachable — the action list always contains at least Edit, Duplicate and Archive or Restore."
    - "E8 Gym Profile Action Sheet, loading: no loading state — actions are computed synchronously from the already-loaded row state."
    - "E8 Gym Profile Action Sheet, error: no async failure mode of its own; the archive and restore confirms own their outcome and the sheet dismisses on either result."
    - "E8 Gym Profile Action Sheet, populated: a dynamic action list per row state — Set Active omitted on the already-active row, then Edit, Duplicate, Archive or Restore."
    - "E8 Gym Profile Action Sheet, partial: rows are structurally included or excluded, never rendered disabled or half-populated."
    - "E8 Gym Profile Action Sheet, overflow: the existing sheet scroll geometry accommodates its three or four rows with no layout change."
    - "E8 Gym Profile Action Sheet, zero/one/many: the action list's arity varies by row state between three and four items by design."
    - "E8 Gym Profile Action Sheet, long text: all action labels are fixed short strings with no interpolation."
  artifacts:
    - apps/mobile/app/gym-profiles/index.tsx
    - apps/mobile/app/gym-profiles/_layout.tsx
    - apps/mobile/components/GymProfileActionSheet.tsx
  key_links:
    - "Profile tab Gyms nav row -> /gym-profiles route -> equipment_profile live read"
    - "GymProfileActionSheet Set Active -> user_preference.default_equipment_profile_id"
    - "ArchiveDialog subject union -> the gym copy entry -> archiveEquipmentProfile / restoreEquipmentProfile"
  prohibitions:
    - "No hard delete of a gym profile; archival is a nullable timestamp."
    - "No second modal shell; every new sheet reuses the shipped overlay geometry."
    - "No numberOfLines, ellipsizeMode or allowFontScaling={false} in new UI."
    - "No second semantic colour is introduced; marking or switching a gym is never rendered as destructive."
    - "No Platform.OS branch is introduced for platform divergence; a .web.tsx sibling is the only permitted split."
    - "No second guard is added at the gym-profiles segment layout; authorization stays in the root layout."
    - "No AI attribution or co-authorship line in any commit message, source file or document."
    - "No comment that merely restates in prose what the adjacent code already says."
---

<objective>
Give the user somewhere to see and manage their gyms: the Gym Profiles list, its per-row action
sheet, gym archival, and the Profile tab entry point that leads to all of it.

Purpose: GYM-01 is "create multiple gym profiles and pick which is active". 06-01 proved one
seeded profile can exist and be stamped on a session; this plan makes the plural real and gives the
active pointer a user-facing control.

Output: `apps/mobile/app/gym-profiles/` (segment layout + list route), `GymProfileActionSheet`,
`ArchiveDialog` extended with a gym subject, the Profile tab's Gyms section, and the remaining
gym-profile write helpers.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-gym-profiles-plate-math/06-CONTEXT.md
@.planning/phases/06-gym-profiles-plate-math/06-UI-SPEC.md
@.planning/phases/06-gym-profiles-plate-math/06-PATTERNS.md
@.planning/phases/06-gym-profiles-plate-math/06-01-SUMMARY.md
</context>

## Artifacts this phase produces

See `06-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide symbol list. New in
this plan: `loadEquipmentProfiles`, `createEquipmentProfile`, `archiveEquipmentProfile`,
`restoreEquipmentProfile`, `duplicateEquipmentProfile`, `setActiveEquipmentProfile`,
`GymProfileActionSheet`, `GymProfileAction`, `formatGymRowSubtitle`, `partitionGymProfiles`,
`deriveGymProfilesScreenState`, the `apps/mobile/app/gym-profiles/` route segment, and the `'gym'`
member of `ArchiveDialogSubject`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The remaining gym-profile write helpers and the list read</name>
  <files>apps/mobile/lib/db/equipment-profiles.ts, apps/mobile/lib/db/__tests__/equipment-profiles.test.ts</files>
  <read_first>
    - apps/mobile/lib/db/equipment-profiles.ts — everything 06-01 put there, especially `ensureDefaultEquipmentProfile` and the serialize helper it routes through
    - apps/mobile/lib/db/programs/lifecycle.ts — `activateRoutine`, `archiveRoutine`, `restoreRoutine`, `loadLibraryRoutines`, `loadActiveRoutineId`, `resolveLiveRoutineId`: the exact single-pointer, archive-wins precedent this mirrors
    - apps/mobile/lib/db/programs/duplicate-routine.ts — the duplicate-with-new-client-ids pattern
    - apps/mobile/lib/db/preferences.ts — the insert-or-update singleton write for `user_preference`
    - apps/mobile/lib/db/schema.ts — the SQLite `equipmentProfile` and `userPreference` columns
    - packages/api-contracts/src/equipment.ts — `parseEquipmentJson` / `serializeEquipmentJson`
    - apps/mobile/lib/db/__tests__/lifecycle.test.ts — the in-memory database test harness these tests reuse
  </read_first>
  <behavior>
    - `loadEquipmentProfiles(userId)` returns every profile including archived ones, each with its parsed inventory counts, ordered by name then id so the sequence is total when two gyms share a name.
    - `setActiveEquipmentProfile(userId, profileId)` writes only `default_equipment_profile_id` and leaves `weight_unit` and both workout flags untouched, inserting the preference row when none exists.
    - `archiveEquipmentProfile(id)` stamps `archived_at` and never deletes; `restoreEquipmentProfile(id)` clears it.
    - Archiving the currently active gym leaves the pointer resolvable: the read path resolves the live gym the same way the program library resolves a stale active pointer, so an archived gym is never presented as active.
    - `duplicateEquipmentProfile(userId, id)` produces a new row with a new client id, a distinct name, `is_default` false, `archived_at` null, and an inventory deep-equal to the source's.
    - Every helper accepts an optional injectable database handle defaulting to the production singleton.
  </behavior>
  <action>
Extend `apps/mobile/lib/db/equipment-profiles.ts` with the read and write helpers the list screen
needs, following `apps/mobile/lib/db/programs/lifecycle.ts` as the structural analog — it already
solves the identical problems (a single nullable active pointer, archive-wins-over-pointer
resolution, a total ordering when names collide) for programs, and reimplementing them differently
here would give the app two answers to "which one is in effect".

Add `EquipmentProfileRow` carrying `id`, `name`, `isDefault`, `archivedAt`, `nativeUnit`,
`barbellWeightKg` and the three parsed inventory arrays, plus `loadEquipmentProfiles(userId, db?)`
returning every row ordered by name then id. Add `loadActiveEquipmentProfileId(userId, db?)` if
06-01 did not already, and a `resolveLiveEquipmentProfileId(rows, activeId)` that mirrors
`resolveLiveRoutineId`: an archived gym never wins the active partition, and when the pointer names
a gym that no longer exists or is archived, the first non-archived gym by the total ordering is
returned. Add `createEquipmentProfile`, `updateEquipmentProfile`, `archiveEquipmentProfile`,
`restoreEquipmentProfile`, `duplicateEquipmentProfile` and `setActiveEquipmentProfile`. Route every
inventory read and write through the contract package's parse and serialize pair — never an inline
JSON call at a site — because the Postgres columns are real JSONB while the local mirror is text,
and a single missed conversion produces a value that reads back as an unusable string on the other
platform.

Also add `formatGymRowSubtitle({ barbellWeightKg, plateCount, dumbbellCount, machineCount, nativeUnit, archivedAt })`
as a pure exported function so the list screen's subtitle rule is unit-testable without a renderer,
mirroring `formatLibraryRowSubtitle` in `apps/mobile/app/programs/library.tsx`. It joins only the
sections that have content, in the order bar, plates, dumbbells, machines, and returns the archived
word alone for an archived row. A gym with nothing configured returns an empty string, and the row
renders no subtitle line at all — never a zero count and never an incompleteness marker, because
every section is legitimately optional.

Add tests against the same in-memory harness `lifecycle.test.ts` uses, covering every behaviour row
above plus a subtitle case per partial-configuration shape.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- equipment-profiles</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test -- equipment-profiles` exits 0.
    - `apps/mobile/lib/db/equipment-profiles.ts` contains `setActiveEquipmentProfile`, `archiveEquipmentProfile`, `restoreEquipmentProfile`, `duplicateEquipmentProfile` and `formatGymRowSubtitle`.
    - `grep -vE '^\s*(//|\*|/\*)' apps/mobile/lib/db/equipment-profiles.ts | grep -cE 'JSON\.(parse|stringify)'` is 0.
    - The test file contains a case asserting that archiving the active gym still resolves a live gym.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>Every gym-profile lifecycle operation the list screen offers exists as a tested write helper, and the active pointer behaves like the program library's, including when the gym it names is archived.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Gym Profiles list, its action sheet, and gym archival copy</name>
  <files>apps/mobile/app/gym-profiles/_layout.tsx, apps/mobile/app/gym-profiles/index.tsx, apps/mobile/app/gym-profiles/__tests__/gym-profiles-screen.test.tsx, apps/mobile/components/GymProfileActionSheet.tsx, apps/mobile/components/__tests__/GymProfileActionSheet.test.tsx, apps/mobile/components/ArchiveDialog.tsx, apps/mobile/components/__tests__/ArchiveDialog.test.tsx, apps/mobile/app/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/programs/library.tsx — the whole file: live read wiring, `deriveLibraryScreenState`, `partitionRoutines`, the archived trailing section, the overflow trigger, the text-link add CTA placement
    - apps/mobile/app/programs/_layout.tsx — the segment layout, its anchor setting, and its comment on why no second guard belongs here
    - apps/mobile/app/_layout.tsx — where the signed-in guard declares each route segment
    - apps/mobile/components/RoutineActionSheet.tsx — the verbatim sheet shape and its dynamic action list prop
    - apps/mobile/components/ArchiveDialog.tsx — the subject union and its copy table
    - apps/mobile/components/ErrorBanner.tsx — the shipped error surface
    - apps/mobile/lib/db/equipment-profiles.ts — the helpers from Task 1
    - .planning/phases/06-gym-profiles-plate-math/06-UI-SPEC.md — § Gym Profiles List, § Copywriting Contract, and E1/E8
  </read_first>
  <behavior>
    - `deriveGymProfilesScreenState` returns error on a failed read, loading while rows are null, and populated otherwise — there is no empty branch, because a user reaching this screen always has at least the seeded gym.
    - `partitionGymProfiles(rows, activeId)` puts the resolved live gym alone in the active partition, every other non-archived gym in the rest partition ordered by name then id, and every archived gym in the archived partition.
    - `GymProfileActionSheet` given an already-active row renders three actions; given any other row renders four, with Set Active first.
    - `GymProfileActionSheet` given an archived row renders Restore in place of Archive.
    - `ArchiveDialog` with the gym subject renders the gym archive heading, body and confirm label; with the unarchiving flag it renders the restore copy and drops the destructive fill.
    - Selecting Set Active on a non-active row calls the active-pointer write exactly once with that row's id.
  </behavior>
  <action>
Create the `gym-profiles` route segment and its list screen, mirroring the programs segment
structurally rather than inventing a second convention.

`apps/mobile/app/gym-profiles/_layout.tsx` copies `apps/mobile/app/programs/_layout.tsx`: a `Stack`
with the header shown, a function-valued header-left rendering `NavBackButton` with a fallback href
to the list, gestures enabled, an `unstable_settings` anchor naming the index route, and a single
`Stack.Screen` for `index` titled per the UI-SPEC. Declare only the index screen here; 06-04 adds
the create and edit screens when those routes exist. Reproduce the existing comment's rule that no
second authorization guard belongs in a segment layout, and add `gym-profiles` to the signed-in
guard's screen list in `apps/mobile/app/_layout.tsx` exactly as `programs` and `exercises` are
declared.

`apps/mobile/app/gym-profiles/index.tsx` reads the user's profiles and the active pointer through
the Task 1 helpers, using the same focus-effect-plus-local-state read wiring `programs/library.tsx`
uses. Export `deriveGymProfilesScreenState` and `partitionGymProfiles` as pure functions so the
screen's logic is testable without a renderer, matching how the program library exports
`deriveLibraryScreenState` and `partitionRoutines`. Render a scrollable list: the active gym pinned
first in a single-item active partition, rendered semibold with the accent border and accent name
treatment the program library and the selected-chip control already use for "this is the one in
effect"; then the rest alphabetically; then archived gyms in a collapsed trailing section identical
to the program library's. Each row shows the gym name, the subtitle from `formatGymRowSubtitle`
in muted Label (omitted entirely when empty), and a trailing overflow trigger at the standard
minimum touch size. The add CTA is a text-accent link at the top of the screen reading New Gym,
matching New Program's placement and weight — not a filled primary button. On a failed read, render
the shipped error surface with the Gym-Profiles-couldn't-load heading and the shared restart body
from the Copywriting Contract. Render no skeleton: gym rows come from local SQLite.

`apps/mobile/components/GymProfileActionSheet.tsx` copies `RoutineActionSheet.tsx` verbatim — same
overlay, same scroll container, same maximum width, same row geometry — and takes its action list
as a prop rather than deriving it inside the sheet, so the caller owns the Set-Active-omitted and
Archive-versus-Restore decisions and the sheet stays a pure renderer.

Extend `apps/mobile/components/ArchiveDialog.tsx` by adding a third member to the subject union and
a matching copy entry with archive and unarchive keys, using the exact strings from the UI-SPEC's
Destructive confirmation row. Add no new component: this union is the extension point. Extend
`ArchiveDialog.test.tsx` with the two new copy cases.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- gym-profiles-screen GymProfileActionSheet ArchiveDialog</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test -- gym-profiles-screen GymProfileActionSheet ArchiveDialog` exits 0.
    - `apps/mobile/app/gym-profiles/index.tsx` contains `export function deriveGymProfilesScreenState(` and `export function partitionGymProfiles(`.
    - `grep -vE '^\s*(//|\*|/\*)' apps/mobile/components/GymProfileActionSheet.tsx | grep -cF 'max-w-[400px]'` is at least 1.
    - `apps/mobile/components/ArchiveDialog.tsx`'s subject union has three members and its copy table has three entries.
    - `grep -vE '^\s*(//|\*|/\*)' apps/mobile/app/gym-profiles/index.tsx | grep -cE 'numberOfLines|ellipsizeMode|allowFontScaling'` is 0.
    - `apps/mobile/app/_layout.tsx` declares a screen named `gym-profiles`.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>A user can open a list of their gyms, see which one is active, and reach Set Active, Edit, Duplicate and Archive or Restore for any of them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The Profile tab's Gyms section</name>
  <files>apps/mobile/app/(tabs)/profile.tsx, apps/mobile/app/(tabs)/__tests__/profile.test.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/profile.tsx — the whole file: `ToggleRow`'s bordered row geometry, the Workout settings section header treatment, the focus-effect read, and where Sign Out sits
    - apps/mobile/app/(tabs)/__tests__/profile.test.tsx — the existing cases and their render approach
    - apps/mobile/lib/db/equipment-profiles.ts — `loadEquipmentProfiles`, `loadActiveEquipmentProfileId`, `resolveLiveEquipmentProfileId`
    - .planning/phases/06-gym-profiles-plate-math/06-UI-SPEC.md — § Profile tab amendment and E7
  </read_first>
  <behavior>
    - The Profile screen renders a Gyms section header between the Workout settings group and Sign Out, containing exactly one navigation row.
    - The row shows the Gym Profiles label and, when an active gym resolves, its name trailing in muted Label; when it does not resolve, the trailing label is absent and the row still navigates.
    - The row is announced as a link or button, never as a switch, and never renders an on/off pill.
    - Tapping the row navigates to the gym-profiles list route.
  </behavior>
  <action>
Add the Gyms section to `apps/mobile/app/(tabs)/profile.tsx`, positioned between the existing
Workout settings group and the Sign Out control, with a muted Label section header matching the
Workout settings header's own treatment.

Add a local `GymRow` component reusing `ToggleRow`'s row chrome — the bordered, surface-filled,
rounded row at the shipped minimum height — but with an accessibility role of link rather than
switch, an icon, the Gym Profiles label, the active gym's name trailing in muted Label, and a
trailing chevron in place of the on/off pill. Follow the icon-colour rule this codebase already
uses: resolve the glyph colour from the theme and pass it as the icon's colour prop rather than a
utility class, because the icon library owns that prop.

Read the active gym's name inside the existing focus effect alongside the workout preferences and
the notification permission, using the same all-settled read the effect already performs, so the
screen still issues one read pass per focus. When the read fails or resolves nothing, leave the
trailing name undefined and render the row without it — the row must never render disabled or
broken.

Extend `profile.test.tsx` with cases for the row's presence, its resolved trailing name, the
missing-name fallback, and its navigation target.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- profile</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test -- profile` exits 0 and the pre-existing profile case count has not decreased.
    - `apps/mobile/app/(tabs)/profile.tsx` contains `gym-profiles` as a navigation target.
    - `grep -cE 'accessibilityRole="switch"' "apps/mobile/app/(tabs)/profile.tsx"` is exactly 1 — the pre-existing toggle row's, and none added by the new navigation row.
    - The test file contains a case asserting the row renders with no trailing name when no active gym resolves.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <done>Gym Profiles is reachable in one tap from the Profile tab, and the row names the gym currently in effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local gym rows → list screen | Rows may have arrived from another device with a stale or dangling active pointer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Tampering | active-gym pointer resolution | medium | mitigate | `resolveLiveEquipmentProfileId` makes archival win over the pointer, so a stale pointer arriving from another device cannot present an archived gym as the one in effect; one owner of that rule, tested. |
| T-06-09 | Information Disclosure | gym profile reads | low | accept | Reads are scoped to the signed-in user's local database, which only ever contains rows the sync stream already filtered by the authenticated user; no new server read surface is added. |
| T-06-02 | Denial of Service | inventory parse on the list screen | medium | mitigate | Inherited from 06-01: the parse pair rejects an out-of-bounds shape rather than materialising it, so a hostile row cannot make the list render an unbounded subtitle. |
</threat_model>

<verification>
- `pnpm --filter mobile test` exits 0.
- `pnpm --filter mobile typecheck` exits 0.
- <human-check>On the web target: Profile tab shows a Gyms section naming the active gym; tapping it opens the list with the active gym pinned first and accent-tinted; the overflow trigger offers Set Active, Edit, Duplicate and Archive; archiving moves the gym into the collapsed archived section.</human-check>
</verification>

<success_criteria>
- Gym Profiles is reachable from the Profile tab and lists every configured gym.
- Exactly one gym reads as active, and archiving it does not leave the app without one.
- Archive and restore reuse the shipped confirmation dialog with gym-specific copy.
</success_criteria>

<output>
Create `.planning/phases/06-gym-profiles-plate-math/06-03-SUMMARY.md` when done
</output>
