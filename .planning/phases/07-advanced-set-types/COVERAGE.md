No external API integration: Phase 7 adds advanced set types (drop sets, myoreps, partials, warm-ups, supersets, per-side logging) entirely on top of the local-first set-logging path and the existing PowerSync/NestJS sync contract — no new external API, SDK, or service is consumed. The detector fired on the phrase "Android SDK" appearing in a native-toolchain availability note in 07-CONTEXT.md, not on an integration.
