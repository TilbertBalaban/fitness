---
phase: 02-data-model-sync-engine
plan: 09
type: execute
wave: 5
gap_closure: true
depends_on: ["02-02", "02-05"]
files_modified:
  - apps/mobile/package.json
  - pnpm-lock.yaml
  - apps/mobile/lib/db/powersync.ts
  - apps/mobile/lib/db/powersync.web.ts
  - apps/mobile/lib/db/log-set.ts
  - apps/mobile/lib/db/__tests__/log-set.test.ts
  - apps/mobile/lib/db/test-support.ts
  - apps/mobile/app/__durability.tsx
  - apps/mobile/app/__durability.web.tsx
  - apps/mobile/playwright.config.ts
  - apps/mobile/e2e/durability.spec.ts
autonomous: false
requirements: [PLAT-02, PLAT-07]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real @powersync/web database, constructed in a real browser page by openTestPowerSync, accepts a write from the real logSet helper and returns that row on a subsequent read — no mock, no fake, no stand-in for PowerSyncDatabase anywhere on the assertion path."
    - "A set written by logSet is readable after closeTestPowerSync() followed by reopenTestPowerSync(), with no finish step, no flush, no disconnect and no sync in between."
    - "startSession, addSessionExercise and logSet each accept a caller-supplied database and write to it, while every existing production call site that passes no database still resolves getPowerSync() exactly as before."
    - "reopenTestPowerSync returns a different JavaScript object than the instance closeTestPowerSync closed, reading the same underlying store — a reopen that hands back the same live instance fails the suite."
    - "A web export built without EXPO_PUBLIC_DURABILITY_HARNESS contains no harness global, so the durability entry point cannot be reached in a production bundle."
    - statement: "PLAT-07 adjacency — a close immediately followed by a reopen against the same dbFilename resolves to the same store rather than minting a second unrelated database, with no intervening delay required."
      verification: backstop
  artifacts:
    - "apps/mobile/playwright.config.ts — Playwright project pinned to chromium, webServer starting the Expo web target with the harness flag set"
    - "apps/mobile/e2e/durability.spec.ts — the browser-driven durability suite"
    - "apps/mobile/app/__durability.web.tsx — the web-only harness route that exposes the real write helpers against an isolated test database"
    - "apps/mobile/app/__durability.tsx — the native sibling stub, so no native build resolves @powersync/web"
    - "apps/mobile/lib/db/log-set.ts — startSession / addSessionExercise / logSet with a default-valued database parameter"
  key_links:
    - "e2e/durability.spec.ts -> app/__durability.web.tsx via the page's harness global"
    - "app/__durability.web.tsx -> lib/db/test-support.ts via openTestPowerSync / closeTestPowerSync / reopenTestPowerSync"
    - "app/__durability.web.tsx -> lib/db/log-set.ts via the new database parameter, not via a mocked module"
    - "lib/db/log-set.ts -> lib/db/powersync.ts via getPowerSync() as the parameter default"
  prohibitions:
    - "The durability suite must not be made to pass by constructing a fake, mocked, in-memory stand-in or otherwise simulated PowerSync database, and must not be made to pass by skipping, pending-marking or conditionally excluding a case. If a real, passing assertion against a real database cannot be produced, the task halts and says so."
    - "The harness must not ship a reachable path in a production build that lets a page driver invoke local write primitives."
---

<objective>
Build the vehicle that makes roadmap success criterion 4 and PLAT-07 provable at all, and prove it end
to end on one path: a set logged through the real `logSet` helper into a real `@powersync/web`
database inside a real browser, surviving a close and reopen with no finish or sync step.

**As a** lifter whose phone dies mid-workout, **I want to** find every set I already logged still
there when the app comes back, **so that** I never have to remember and re-enter what I just lifted.

Purpose: plan 02-05 halted after six documented attempts to construct a real PowerSync database under
Jest/Node (WINDOWS.md #22). Every one of those failures is an artifact of the Node sandbox — no real
`Worker`, no real WASM loader, no real IndexedDB. A real browser page has all three. This plan
establishes that browser as the test environment and proves it carries a genuine durability assertion
before plan 02-12 expands onto it.

Output: a Playwright harness, the dependency-injection seam WINDOWS.md #23 says a durability suite
needs, and one passing end-to-end durability assertion.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/02-data-model-sync-engine/02-VERIFICATION.md
@.planning/phases/02-data-model-sync-engine/02-01-TASK2-PACKAGE-GATE.md
@apps/mobile/lib/db/test-support.ts
@apps/mobile/lib/db/powersync.web.ts
@apps/mobile/lib/db/log-set.ts
</context>

<scope_authorization>
This plan launches a real browser and drives the app in it. `./.claude/CLAUDE.md` carries a standing
"Browser testing — only on request" rule. **The user made that request explicitly for this durability
and convergence work.** The authorization is scoped to `apps/mobile/e2e/*.spec.ts` and the
`playwright.config.ts` that runs them. It is not blanket permission: a future phase wanting to drive a
browser for any other purpose needs its own explicit request.
</scope_authorization>

<deliberate_deferrals>
Recorded so nothing drops silently. The user chose the narrower scope for this round; these are **not**
oversights and no task below addresses them:

- `02-REVIEW.md` WR-01 (`Math.random` id collision claim), WR-02 (`pendingWriteCount` swallowing
  errors), WR-03 (`sync_conflict_log` has no reader), WR-04 (`sync-status.ts` lazy `require`), and
  IN-01 (loose timestamp validation on `workout_session`) — all deferred.
- WINDOWS.md #18 (`Math.random` UUID → `expo-crypto`), #24 (native export share sheet), #25
  (`duration_seconds` / `distance_meters` columns) — all deferred.
- WINDOWS.md #16 and the device half of #26 (native iOS/Android verification) — this machine has no
  Xcode and no Android SDK, and Android UAT is deferred to the ROADMAP Phase 999.1 native sweep. No
  task here assumes a native device.
</deliberate_deferrals>

<flagged_assumptions>
None in this plan. The three unclassified edge-probe rows (PLAT-03, PLAT-10, LOG-22) are carried as
flagged assumptions in plan 02-12 so they are surfaced exactly once across this round.
</flagged_assumptions>

<artifacts_this_phase_produces>
New symbols and paths introduced by this plan. Drift verification must treat these as newly created,
not as references to pre-existing code:

| Symbol / path | Kind | Defined in |
|---|---|---|
| `WriteDb` | exported type alias | `apps/mobile/lib/db/powersync.ts`, `apps/mobile/lib/db/powersync.web.ts` |
| `startSession(input, db)` | second parameter added | `apps/mobile/lib/db/log-set.ts` |
| `addSessionExercise(input, db)` | second parameter added | `apps/mobile/lib/db/log-set.ts` |
| `logSet(input, db)` | second parameter added | `apps/mobile/lib/db/log-set.ts` |
| `readLoggedSets` | exported function | `apps/mobile/lib/db/test-support.ts` |
| `pendingCrudCount` | exported function | `apps/mobile/lib/db/test-support.ts` |
| `DURABILITY_HARNESS_GLOBAL` | exported const | `apps/mobile/lib/db/test-support.ts` |
| `DURABILITY_HARNESS_ENABLED` | exported const | `apps/mobile/lib/db/test-support.ts` |
| `EXPO_PUBLIC_DURABILITY_HARNESS` | env var / config key | `apps/mobile/lib/db/test-support.ts`, `apps/mobile/playwright.config.ts` |
| `/__durability` | Expo Router route | `apps/mobile/app/__durability.web.tsx`, `apps/mobile/app/__durability.tsx` |
| `apps/mobile/playwright.config.ts` | new file | — |
| `apps/mobile/e2e/durability.spec.ts` | new file | — |
| `test:e2e` (mobile) | package.json script | `apps/mobile/package.json` |
| `@playwright/test` | dependency | `apps/mobile/package.json` |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Package-legitimacy and adoption gate for @playwright/test</name>
  <decision>Adopt `@playwright/test` as this repository's browser test runner, or reject it and halt this gap-closure round.</decision>
  <context>
`02-RESEARCH.md`'s `## Package Legitimacy Audit` table predates this gap-closure round and does not
list `@playwright/test`. Under the gate's fallback policy an unlisted package is `[ASSUMED]`, and an
`[ASSUMED]` package requires a blocking human decision before install. This checkpoint is never
auto-approvable; `workflow.auto_advance` does not apply.

This same checkpoint is the reversibility gate for the adoption itself. Rating: **costly**, not
one-way — the package and the two new files can be deleted — but the harness route lands inside the
production app's route tree and CI grows a browser job, so backing out is real work rather than a
revert.

Live registry evidence gathered at plan time (`npm view @playwright/test`), for the human to check
rather than take on trust:

| Field | Value |
|---|---|
| Latest version | `1.62.1` |
| First published | 2020-09-24 |
| Last published | 2026-08-17 |
| Total published versions | 3326 |
| Repository | `github.com/microsoft/playwright` |
| Maintainers | `pavelfeldman`, `yurys`, `dgozman-ms`, `playwright-bot@microsoft.com` |

Read together: a Microsoft-published package with a six-year continuous release history and
thousands of releases. The `[ASSUMED]` verdict comes from the audit table's silence, not from any
adverse signal.

Why the alternative was not planned: the Jest/Node route is empirically closed. WINDOWS.md #22
records six confirmed failures — `jest-environment-jsdom` and `fake-indexeddb` absent from the
lockfile; the RN-Web export condition Jest resolves under forcing `@powersync/web`'s
`react_native_web` dist build, which needs a real `Worker` global even with `useWebWorker: false`;
the plain browser build needing `window.Worker` for its default path; the WASM sync-mode
`InMemoryVFS` path hanging with no companion browser context; and `--experimental-vm-modules`
requiring a project-wide Jest ESM migration. Installing the two missing Jest packages does not clear
the `Worker` or WASM-loader failures, so it is not offered as an option here.
  </context>
  <options>
    <option id="adopt-playwright">
      <name>Install `@playwright/test@1.62.1` as a devDependency of `apps/mobile`, chromium only</name>
      <pros>The only environment in this repository where a real `Worker`, real WASM and real IndexedDB exist together, which is what roadmap criterion 4 and PLAT-07 need. Microsoft-maintained, six-year history. Browser binaries are downloaded on demand and are not committed.</pros>
      <cons>A new dependency and a new binary download step in CI. Adds a browser-driving test surface the project has not had before.</cons>
    </option>
    <option id="reject-and-halt">
      <name>Reject the dependency</name>
      <pros>No new dependency, no new CI surface.</pros>
      <cons>Roadmap criteria 1 and 4 and PLAT-07 stay unprovable by any automated means, exactly as 02-05 left them. This plan's Tasks 2-3 and plan 02-12's Tasks 1-2 do not run. Plans 02-10 and 02-11 are unaffected and still run in full. A rejection is not a dead end — it routes to one of the three named follow-ups in the escalation clause below, which the acceptance criteria require to be chosen and written down.</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/02-data-model-sync-engine/02-01-TASK2-PACKAGE-GATE.md` — the shape this record must match
    - `.planning/phases/02-data-model-sync-engine/02-RESEARCH.md` `## Package Legitimacy Audit` — confirm `@playwright/test` is absent, which is what makes it `[ASSUMED]`
    - `.planning/WINDOWS.md` entry #22 — the six failures that rule out the Jest route; do not re-derive them
  </read_first>
  <acceptance_criteria>
    - A record file `.planning/phases/02-data-model-sync-engine/02-09-TASK1-PACKAGE-GATE.md` exists, following the section structure of `02-01-TASK2-PACKAGE-GATE.md`: outcome, evidence table, and any caveats recorded explicitly rather than folded into a clean pass.
    - The record states the outcome as one of `approved` or `rejected`, and names the human as the decider.
    - If `rejected`, this plan halts at this task and no task below is marked complete — **and** the record file and `02-09-SUMMARY.md` each carry an `## Escalation` section stating all four items below by name. A bare halt notice, "reconsider later", or "the gap stays open" does not satisfy this criterion; this phase already has one honest halt (WINDOWS #22) whose next step was never written down, and repeating that is the failure mode this clause exists to prevent.

      1. **What stays unproven, named individually:** roadmap criterion 1 (an offline write reaches Postgres after reconnect with no user action), roadmap criterion 4 (client schema redefinition against a populated pre-migration database), PLAT-07 (a logged set survives close and reopen), PLAT-02's web half, and the browser half of WINDOWS #26 (two-client convergence). PLAT-08's null-weight round trip is **not** on this list — plan 02-10 proves it independently.
      2. **What is unaffected and must still run:** plans 02-10 and 02-11 in full, neither of which touches Playwright, plus plan 02-12 Task 3's ledger, `REQUIREMENTS.md` and `02-VALIDATION.md` updates minus its `browser-durability` CI job. Only 02-09 Tasks 2-3 and 02-12 Tasks 1-2 stop. Do not cancel or defer 02-10 or 02-11 on the strength of this rejection.
      3. **One named recommended follow-up**, selected from exactly these three and justified in one line:
         - `human-uat` — extend `02-08-PLAN.md` Task 3's already-unresolved `checkpoint:human-verify` to also carry criterion 4 and PLAT-07, writing out the populate-then-redefine and close-then-reopen steps explicitly enough for a human to run them by hand in a browser. Accepts that neither claim ever becomes CI-enforced and that every later phase re-inherits the regression risk.
         - `phase-split` — carve criteria 1 and 4 into a new roadmap phase whose first act is choosing a different vehicle, naming the candidates already considered: Detox (blocked today — needs the native toolchain WINDOWS #16 records as absent), Cypress (poses the identical dependency-adoption question this checkpoint just answered, so a rejection on dependency grounds rejects it too), or a bespoke headless-chrome harness driven over the DevTools protocol with no new npm dependency.
         - `accept-gap` — leave PLAT-02 and PLAT-07 at `Gaps Found` in `.planning/REQUIREMENTS.md`, with a recorded waiver naming who accepted it and on what reasoning.
      4. **A new ledger entry**, appended with `node .claude/gsd-core/bin/gsd-tools.cjs windows append --kind unrun-verify --phase 02 --file apps/mobile/lib/db/test-support.ts --description "<text>"`, whose description names the rejected vehicle and the follow-up selected in item 3, so the gap is discoverable from the ledger and not only from this SUMMARY.
  </acceptance_criteria>
  <resume-signal>Select: adopt-playwright, or reject-and-halt</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The database-injection seam WINDOWS #23 names</name>
  <files>apps/mobile/lib/db/powersync.ts, apps/mobile/lib/db/powersync.web.ts, apps/mobile/lib/db/log-set.ts, apps/mobile/lib/db/__tests__/log-set.test.ts</files>
  <read_first>
    - `apps/mobile/lib/db/log-set.ts` — all three helpers currently call `getPowerSync()` on their first line
    - `apps/mobile/lib/db/powersync.ts` and `apps/mobile/lib/db/powersync.web.ts` — the platform-split pair; both export `getPowerSync` with the identical `wrapPowerSyncWithDrizzle` return type
    - `apps/mobile/lib/db/__tests__/log-set.test.ts` — the existing suite mocks `../powersync`; those cases must keep passing unchanged
    - `.planning/WINDOWS.md` entry #23 — the exact seam this closes; read the entry, do not infer it from this task's title
  </read_first>
  <behavior>
    - Passing a database to `logSet` writes to that database, and `getPowerSync` is never called.
    - Passing no database to `logSet` writes to whatever `getPowerSync()` returns, exactly as today.
    - The same two behaviors hold for `startSession` and `addSessionExercise`.
    - The existing lb-to-kg conversion, null-weight and set-index cases in `log-set.test.ts` still pass with no edit to their bodies.
  </behavior>
  <action>
Export `type WriteDb = ReturnType<typeof wrapPowerSyncWithDrizzle>` from both `powersync.ts` and
`powersync.web.ts` so the type resolves identically on native and web through Metro's platform
extension resolution.

Give `startSession`, `addSessionExercise` and `logSet` in `log-set.ts` a second parameter
`db: WriteDb = getPowerSync()`, and delete the `const db = getPowerSync()` line from each body. A
default parameter expression evaluates per call, so production call sites that pass nothing keep
resolving the singleton lazily at call time and nothing about the production path changes. Do not
introduce a module-level setter, a global override, or a mutable module variable — the seam must be
a parameter, because a mutable override reintroduces exactly the cross-test bleed the browser suite
in Task 3 depends on not having.

Keep the parameter second and optional so no existing call site in `apps/mobile` changes; confirm by
grepping for call sites rather than assuming there are none.

Add three cases to `log-set.test.ts`: one asserting an explicitly-passed fake database receives the
insert while the `getPowerSync` mock records zero calls, one asserting the no-argument call still
routes through the `getPowerSync` mock, and one covering `startSession` the same way. These cases
prove the seam, not durability — the durability proof is Task 3, where mocking is forbidden.
  </action>
  <verify>
    <automated>pnpm --filter mobile test && pnpm --filter mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const db = getPowerSync()' apps/mobile/lib/db/log-set.ts` returns 0, down from 3 today — every resolution moved into a parameter default position.
    - `grep -c 'db: WriteDb = getPowerSync()' apps/mobile/lib/db/log-set.ts` returns 3 — one per write helper.
    - `grep -v '^\s*//' apps/mobile/lib/db/log-set.ts | grep -c 'setPowerSyncForTests\|__setDb\|powersyncOverride'` returns 0 — the seam is a parameter, not a mutable override.
    - Both `apps/mobile/lib/db/powersync.ts` and `apps/mobile/lib/db/powersync.web.ts` export a type named `WriteDb`.
    - `pnpm --filter mobile test` passes with the three pre-existing `log-set.test.ts` cases unmodified in body.
    - `pnpm --filter mobile typecheck` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">An optional trailing parameter with a default is additive; removing it restores the previous signatures exactly.</reversibility>
  <done>All three write helpers accept a caller-supplied database, production call sites are unchanged, and the mobile suite is green.</done>
</task>

<task type="tracer">
  <name>Task 3: End-to-end — a set logged in a real browser survives a close and reopen</name>
  <files>apps/mobile/lib/db/test-support.ts, apps/mobile/app/__durability.web.tsx, apps/mobile/app/__durability.tsx, apps/mobile/playwright.config.ts, apps/mobile/e2e/durability.spec.ts, apps/mobile/package.json</files>
  <precondition>`@playwright/test` is approved by Task 1, its chromium binary is installed via `pnpm --filter mobile exec playwright install chromium`, and TCP port 8081 is free.</precondition>
  <read_first>
    - `apps/mobile/lib/db/test-support.ts` — `openTestPowerSync` / `closeTestPowerSync` / `reopenTestPowerSync` already exist, typecheck against the real types, and are called by nothing; this task is what finally exercises them
    - `apps/mobile/lib/db/powersync.web.ts` — the production web configuration `test-support.ts` mirrors, including the `/@powersync/worker.js` path
    - `apps/mobile/app/reset-password.tsx` and `apps/mobile/app/reset-password.web.tsx` — the established platform-split route precedent this harness route follows
    - `apps/mobile/lib/web-app-origin.ts` and `apps/mobile/lib/auth-storage.ts` — the `EXPO_PUBLIC_*` naming and default-value convention the harness flag follows
    - `apps/mobile/public/@powersync/worker.js` — confirm the worker asset is present; the `postinstall` script copies it and the web server serves `public/` at the root
    - `.planning/WINDOWS.md` entry #22 — why this runs in a browser and not under Jest
  </read_first>
  <action>
Wire one path from a Playwright browser page, through a harness route, through the real write
helpers, into a real `@powersync/web` database, and back out through a read — then close and reopen
that database and read again. One session, one exercise, one set. No batching, no second call site,
no other case.

**test-support.ts additions.** Export `DURABILITY_HARNESS_ENABLED`, computed from
`process.env.EXPO_PUBLIC_DURABILITY_HARNESS === '1'`, and `DURABILITY_HARNESS_GLOBAL`, the string
key the harness attaches under on `window`. Add `readLoggedSets(db, sessionExerciseId)` returning the
rows ordered by `set_index`, and `pendingCrudCount(db)` returning the crud-queue depth read from the
raw `PowerSyncDatabase` via `getUploadQueueStats()` — the Drizzle wrapper does not expose it, same
constraint `pending-write-count.ts` already works around. Keep the existing shared-`dbFilename`
behavior of `reopenTestPowerSync` intact; it is what makes a reopen read the same store.

**Harness route, web.** `app/__durability.web.tsx` imports `openTestPowerSync`,
`closeTestPowerSync`, `reopenTestPowerSync`, `readLoggedSets` and `pendingCrudCount` from
`../lib/db/test-support`, and `startSession`, `addSessionExercise`, `logSet` from
`../lib/db/log-set`. In an effect, and only when `DURABILITY_HARNESS_ENABLED` is true, assign an
object to `window` under `DURABILITY_HARNESS_GLOBAL` exposing async methods `open`, `close`,
`reopen`, `startSession`, `addSessionExercise`, `logSet`, `readSets` and `crudCount`, each holding
the currently-open database in module scope and passing it as the second argument to the real write
helpers. The route renders a single element carrying `testID="durability-harness-ready"` once the
global is attached, so the spec waits on a real signal rather than a timeout. The harness must not
re-implement any insert: every write goes through `log-set.ts`.

The guard must be a direct comparison against the inlined `process.env.EXPO_PUBLIC_DURABILITY_HARNESS`
literal so Metro's build-time substitution turns it into dead code when the flag is unset, and the
minifier removes the attach branch from a production export. That elimination is what the production
grep in the acceptance criteria measures, so do not route the flag through a runtime helper, a
config object, or an indirection that defeats the inlining.

**Harness route, native.** `app/__durability.tsx` renders a static "web-only" notice and imports
nothing from `test-support.ts` or `@powersync/web`, so a native build never resolves the web SDK.
This mirrors the existing `reset-password.tsx` / `reset-password.web.tsx` split.

**Playwright config.** `apps/mobile/playwright.config.ts` with `testDir: './e2e'`, a single chromium
project, `fullyParallel: false`, `forbidOnly: true`, `retries: 0`, and a `webServer` block running
the Expo web dev server on port 8081 with `EXPO_PUBLIC_DURABILITY_HARNESS=1` and `CI=1` in its env
(`CI=1` keeps `expo start` from trying to open a browser itself), `url` pointed at
`http://localhost:8081/__durability`, `reuseExistingServer: !process.env.CI`, and a startup timeout
generous enough for a first Metro bundle. Add a `test:e2e` script to `apps/mobile/package.json`
running `playwright test`. Do not add it to the `test` script — `pnpm --filter mobile test` must stay
the fast Jest-only loop.

**The spec.** `e2e/durability.spec.ts`, one test: open a fresh test database through the harness,
`startSession`, `addSessionExercise`, `logSet` one set with a known weight and rep count, read it
back and assert it is there, then `close` and `reopen` and read again, asserting the same row with
the same values. Assert the reopen produced a distinct database instance rather than the same live
object. There must be no finish, flush, disconnect, `waitForFirstSync` or connect call anywhere
between the log and the close — the claim under test is that the write was durable the instant it
was logged, and any intervening step would quietly make that claim about something else.

If a real, passing assertion against a real database cannot be produced, halt and say so in the
SUMMARY exactly as 02-05 did. Do not substitute a mocked `PowerSyncDatabase`, an in-memory
stand-in, a skipped case, or an assertion that only checks no error was thrown.

<!-- planner-discipline-allow: test.skip -->
<!-- planner-discipline-allow: __fitnessDurability -->
  </action>
  <verify>
    <automated>pnpm --filter mobile test:e2e && pnpm --filter mobile typecheck && pnpm --filter mobile test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test:e2e` exits 0 with 1 passed and 0 skipped.
    - `grep -rn 'test.skip\|test.fixme\|it.skip\|describe.skip' apps/mobile/e2e/` returns no matches.
    - `grep -rn "jest.mock\|createMock\|fakeDb\|stubPowerSync" apps/mobile/e2e/ apps/mobile/app/__durability.web.tsx` returns no matches — the assertion path constructs a real `PowerSyncDatabase`.
    - `grep -c 'from .*lib/db/log-set' apps/mobile/app/__durability.web.tsx` is at least 1, and `grep -c '\.insert(' apps/mobile/app/__durability.web.tsx` is 0 — the harness drives the real helpers and re-implements no insert.
    - `grep -nE '\.(finish|flush|disconnect)\(|waitForFirstSync\(|[^a-zA-Z]connect\(' apps/mobile/e2e/durability.spec.ts` shows no such call between the `logSet` call and the `close` call. The pattern is deliberately code-shaped so the words appearing in prose do not match.
    - Production-safety, two-sided: `pnpm --filter mobile build` with `EXPO_PUBLIC_DURABILITY_HARNESS` unset, then grepping the emitted `dist` for the harness global string, returns 0 matches; the same build with `EXPO_PUBLIC_DURABILITY_HARNESS=1` returns at least 1. Record both numbers in the SUMMARY.
    - `apps/mobile/app/__durability.tsx` contains no import from `@powersync/web` or `./lib/db/test-support`.
    - `pnpm --filter mobile test` and `pnpm --filter mobile typecheck` both still exit 0.
  </acceptance_criteria>
  <reversibility rating="costly">The harness route lands in the production route tree and CI gains a browser job; removing it means deleting a route, a config, a script and a CI step rather than reverting one commit.</reversibility>
  <done>A single logged set, written through the real helpers into a real browser-hosted PowerSync database, is proven readable after a close and reopen with no intervening finish or sync step, and the harness is proven absent from a production web bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright page context → app bundle | Automated driver evaluates JavaScript inside the app's own origin |
| Harness route → local PowerSync database | Test-only code path reaching a real on-device write primitive |
| npm registry → developer machine and CI | New dependency and downloaded browser binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-30 | Elevation of Privilege | `app/__durability.web.tsx` harness global | high | mitigate | Attach branch guarded on the build-time-inlined `EXPO_PUBLIC_DURABILITY_HARNESS` literal so it is dead-code-eliminated from a production export; proven by the two-sided grep in Task 3's acceptance criteria (0 matches unset, ≥1 set) |
| T-02-31 | Tampering | `@playwright/test` supply chain | high | mitigate | Package-legitimacy gate at Task 1 with a blocking human checkpoint, never auto-approvable; live registry evidence recorded in `02-09-TASK1-PACKAGE-GATE.md` |
| T-02-32 | Information Disclosure | Harness route / Playwright config | medium | mitigate | The harness touches only an isolated test database keyed on a generated `dbFilename`; it holds no credentials, reads no auth storage, and calls no authenticated endpoint. No secret is written to `playwright.config.ts` |
| T-02-33 | Spoofing | Harness route vs. production auth | medium | mitigate | The harness never bypasses, stubs or short-circuits `session-guard.ts` or `api-client.ts`; it performs local writes only, so no authentication decision is reachable from it |
| T-02-34 | Denial of Service | Expo web dev server started by `webServer` | low | accept | Bound to localhost, started and torn down per run, developer-machine and CI only |
</threat_model>

<verification>
- `pnpm --filter mobile test` — Jest suite green, including the three new seam cases
- `pnpm --filter mobile typecheck` — exits 0
- `pnpm --filter mobile test:e2e` — 1 passed, 0 skipped, against a real chromium
- `pnpm --filter @fitness/api-contracts test` and `pnpm --filter api test:e2e` — unchanged and still green (this plan touches neither workspace)
- Two-sided production-bundle grep for the harness global, both numbers recorded
</verification>

<success_criteria>
- A real `@powersync/web` database is constructed and written to in a real browser, with no mock on the assertion path
- A logged set survives close and reopen with no finish, flush or sync step in between
- The write helpers accept an injected database and production call sites are unchanged
- The harness cannot be reached in a production web bundle
- No case is skipped and no fake database is substituted anywhere
</success_criteria>

<output>
Create `.planning/phases/02-data-model-sync-engine/02-09-SUMMARY.md` when done.

Record in the SUMMARY: both production-bundle grep counts, and the Playwright and chromium versions
actually installed.

If Task 1 was **rejected**, the SUMMARY carries the `## Escalation` section Task 1's acceptance
criteria specify, in full — the five unproven claims, the plans that still run, the one selected
follow-up, and the appended ledger entry id.

If Task 1 was approved but **Task 3 could not produce a real passing assertion**, apply the same
escalation shape: name which of the five claims is still unproven, state that 02-12 Tasks 1-2 are
blocked on it, select a follow-up from the same three options, and append a ledger entry. Never
substitute a mock, a skip, or a narrowed claim for a proof that could not be produced.
</output>
