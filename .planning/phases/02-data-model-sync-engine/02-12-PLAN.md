---
phase: 02-data-model-sync-engine
plan: 12
type: execute
wave: 7
gap_closure: true
depends_on: ["02-09", "02-10", "02-11"]
files_modified:
  - apps/mobile/lib/db/test-support.ts
  - apps/mobile/app/__durability.web.tsx
  - apps/mobile/playwright.config.ts
  - apps/mobile/e2e/schema-redefinition.spec.ts
  - apps/mobile/e2e/sync.spec.ts
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
  - .planning/phases/02-data-model-sync-engine/02-VALIDATION.md
autonomous: true
requirements: [PLAT-02, PLAT-03, PLAT-07]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Starting from a database already holding a session, an exercise and several logged sets with a non-empty crud queue and no sync ever performed, redefining the client schema by adding one nullable column and removing another, then reopening, leaves every logged set readable and the crud queue depth unchanged — roadmap success criterion 4, against a populated pre-migration database."
    - "A crud queue that survived a client schema redefinition still pushes and drains when connectivity is restored."
    - "A set logged while the browser is offline reaches Postgres after connectivity is restored, with no manual sync action, no button tap and no reload — roadmap success criterion 1."
    - "Two browser contexts signed into the same account, each logging sets while offline, both converge after reconnect with no logged set lost — the browser half of WINDOWS #26 and 02-08's open checkpoint."
    - "A set logged with no weight reaches Postgres as SQL NULL through the real client, not only through a direct API push — the full client-to-database path for PLAT-08's null case."
    - "An in-progress workout's logged sets survive a close and reopen with their set_index order intact and no gaps — PLAT-07 ordering edge."
    - "Reopening a database that holds zero logged sets returns zero rows and an empty crud queue rather than an error — PLAT-07 empty-input edge."
    - statement: "PLAT-02 concurrency — if the app is interrupted at any point after logSet resolves, every set already logged is present on reopen; the guarantee is per-set at log time, not per-session at finish time."
      verification: backstop
  artifacts:
    - "apps/mobile/e2e/schema-redefinition.spec.ts — criterion 4's proof, local-only, CI-runnable"
    - "apps/mobile/e2e/sync.spec.ts — criterion 1's proof plus two-client convergence, requires the live service stack"
    - "apps/mobile/lib/db/test-support.ts — the alternate-schema variant the redefinition test reopens against"
    - ".github/workflows/ci.yml — a browser job running the local-only durability project"
  key_links:
    - "schema-redefinition.spec.ts -> test-support.ts openTestPowerSync with the v2 variant, reopening the same dbFilename"
    - "sync.spec.ts -> the real sign-in screen, so no production auth path is bypassed or stubbed"
    - "sync.spec.ts -> connector.ts uploadData -> POST /v1/sync/push -> Postgres, asserted at the database"
    - "ci.yml -> playwright --project=durability, excluding the specs that need the PowerSync Service"
  prohibitions:
    - "A verification gap must never be closed by narrowing the claim it was supposed to prove. Proving a schema change against an empty database, proving durability only after a finish step, or proving a drain by asserting that no error was thrown, all leave the user exactly as exposed as before while making the report read as green — which is worse than an honest gap, because nobody looks again."
---

<objective>
Expand the proven tracer into the two claims the roadmap actually makes and this phase could not
verify: that an app upgrade across a client schema change does not eat unsynced work, and that
offline writes sync themselves when the signal comes back.

**As a** lifter who updates the app between training days, **I want to** open it after an update and
find the session I logged in the basement gym still there and still on its way to my other devices,
**so that** upgrading never costs me a workout.

Purpose: plan 02-09 proved the vehicle on one path. Roadmap criterion 4 is specific about the
conditions — "verified against a populated pre-migration database" — and criterion 1 is specific
about the absence of user action — "with no manual sync action". Both wordings exist because the
weakened versions of these tests pass trivially and prove nothing. This plan writes the versions the
roadmap asked for.

Output: the two durability suites `02-VERIFICATION.md` lists as missing, the reconnect proof for
criterion 1, the browser half of the two-client convergence checkpoint, and a CI job that keeps the
local-only half of it honest from here on.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-model-sync-engine/02-VERIFICATION.md
@.planning/phases/02-data-model-sync-engine/02-08-PLAN.md
@apps/mobile/lib/db/test-support.ts
@apps/mobile/lib/db/connector.ts
@ops/powersync/sync-rules.yaml
</context>

<scope_authorization>
Same scoped waiver as plan 02-09: `./.claude/CLAUDE.md`'s "Browser testing — only on request" rule is
explicitly waived by the user for `apps/mobile/e2e/*.spec.ts` and the config that runs them, and for
nothing else.
</scope_authorization>

<deliberate_deferrals>
Recorded so nothing drops silently; no task below addresses these:

- `02-REVIEW.md` WR-01, WR-02, WR-03, WR-04 and IN-01 — deferred.
- WINDOWS.md #18 (`Math.random` UUID), #24 (native export share sheet), #25 (`duration_seconds` /
  `distance_meters`) — deferred.
- WINDOWS.md #16 and the **device half** of #26 — no Xcode and no Android SDK on this machine, and
  Android UAT is deferred to the ROADMAP Phase 999.1 native sweep. Task 2 closes only #26's browser
  half; the entry is amended, not marked fixed.
- WINDOWS.md #17 stays open for the native runtime specifically. This round proves the web runtime
  against a real engine; `offline-write.test.ts`'s fakes are unchanged and native op-sqlite behavior
  is still unexercised.
</deliberate_deferrals>

<flagged_assumptions>
Three edge-probe rows returned `unclassified` and are therefore surfaced as flagged planner
assumptions rather than resolved or dismissed. Each is stated here so the verifier sees it:

- **PLAT-03 / unclassified.** The planner's reading is that PLAT-03's meaningful edge is drain timing:
  whether the drain is automatic with no user action, and what happens if connectivity flaps partway
  through a drain. Task 2 asserts the no-user-action half against a real reconnect. The
  **flap-mid-drain** case is not asserted anywhere and remains an open assumption — PowerSync's own
  retry cadence is assumed to resume it, and that assumption is untested here.
- **PLAT-10 / unclassified.** Export. Out of scope for this round; `02-VERIFICATION.md` records it
  satisfied at unit level via `export.test.ts` and `buildExportDocument`, with the native share-sheet
  path unverified (WINDOWS #24). No new proof is added here and none is claimed.
- **LOG-22 / unclassified.** Calendar-day attribution. `02-VERIFICATION.md` records it satisfied by
  `calendar-day.test.ts`'s 8 cases including midnight-crossing and DST. No new proof is added here.
  The assumption carried forward is that a device changing timezone mid-session does not retroactively
  re-attribute an already-stamped session, which follows from `captureCalendarDay` being a single
  write-time capture point but is not asserted against a real timezone change.
</flagged_assumptions>

<artifacts_this_phase_produces>
| Symbol / path | Kind | Defined in |
|---|---|---|
| `TestAppSchemaV2` | exported const | `apps/mobile/lib/db/test-support.ts` |
| `SCHEMA_VARIANT_DELTA` | exported const | `apps/mobile/lib/db/test-support.ts` |
| `OpenTestPowerSyncOptions.variant` | new option field | `apps/mobile/lib/db/test-support.ts` |
| `apps/mobile/e2e/schema-redefinition.spec.ts` | new file | — |
| `apps/mobile/e2e/sync.spec.ts` | new file | — |
| Playwright project `durability` | config project name | `apps/mobile/playwright.config.ts` |
| Playwright project `sync` | config project name | `apps/mobile/playwright.config.ts` |
| `test:e2e:durability` | package.json script | `apps/mobile/package.json` |
| CI job `browser-durability` | workflow job | `.github/workflows/ci.yml` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: An app upgrade across a schema change does not eat unsynced work</name>
  <files>apps/mobile/lib/db/test-support.ts, apps/mobile/app/__durability.web.tsx, apps/mobile/e2e/schema-redefinition.spec.ts</files>
  <precondition>Plan 02-09's harness is in place and `pnpm --filter mobile test:e2e` passes its tracer case against a real chromium.</precondition>
  <read_first>
    - `apps/mobile/lib/db/test-support.ts` — as extended by plan 02-09; `reopenTestPowerSync` reuses the module-level `dbFilename`, which is what makes a reopen read the same store
    - `apps/mobile/lib/db/schema.ts` — the `loggedSet` sqlite table this variant redefines; note `side` is already nullable
    - `apps/mobile/app/__durability.web.tsx` — the harness global to extend
    - `.planning/phases/02-data-model-sync-engine/COVERAGE.md` row "Schemaless client schema (views re-derived on boot)" — the PowerSync capability this test is the proof of
    - `.planning/phases/02-data-model-sync-engine/02-VERIFICATION.md` — the two `missing` bullets that define what this file must prove; read the wording, do not paraphrase it from this task's title
  </read_first>
  <action>
Add an alternate client schema to `test-support.ts`. Export `SCHEMA_VARIANT_DELTA` naming the two
changes as data — one added nullable column `notes` on `logged_set`, one removed column `side` on
`logged_set` — and build `TestAppSchemaV2` from a `drizzleSchema` clone whose `logged_set` table is
redefined accordingly, wrapped in `DrizzleAppSchema` the same way `TestAppSchema` already is. Add a
`variant?: 'v1' | 'v2'` field to `OpenTestPowerSyncOptions`, defaulting to `v1`, selecting which
schema `openTestPowerSync` constructs the `PowerSyncDatabase` with. Both must be real
`DrizzleAppSchema` instances over real `sqliteTable` definitions — this test is meaningless if the
"redefinition" is a flag the database never sees.

Choose `side` as the removed column deliberately: it is already nullable in both the local and the
server schema, so removing it from the client view exercises PowerSync's view re-derivation without
also being a data-model change that would need a server migration. Do not remove `weight_kg`,
`reps` or `set_index` — those are load-bearing for other assertions in this round.

Extend the harness global with `openVariant(variant)` and `reopenVariant(variant)` delegating to
`openTestPowerSync` / `reopenTestPowerSync`, plus a `readRawColumns(table)` returning the column names
the current view actually exposes, so the spec can assert the view genuinely changed rather than
inferring it from the schema object it passed in.

Author `apps/mobile/e2e/schema-redefinition.spec.ts` with these cases:

- **Populated, then redefined.** Open `v1`. Through the real write helpers, start a session, add an
  exercise, and log at least three sets with distinct weights and rep counts, one of them with no
  weight at all. Never connect, never finish, never flush — the crud queue must be non-empty and
  nothing must ever have synced. Record the crud queue depth. Close. Reopen as `v2`. Assert every
  logged set is still readable with its original weight and reps, that the crud queue depth is
  unchanged, that `notes` is present on the view and reads null, and that `side` is absent from the
  view. This is roadmap criterion 4 and it is the reason the population step is not optional: the
  same reopen against an empty database proves nothing and must not be substituted.
- **Ordering survives.** Assert the sets read back in the same `set_index` order they were logged, with
  no gap and no repeat — PLAT-07's ordering edge.
- **Empty database.** Open a fresh `dbFilename`, log nothing, close, reopen as `v2`. Assert zero rows
  and a crud queue depth of zero, and that no error was raised by the reopen itself — PLAT-07's
  empty-input edge.
- **Round trip back.** Reopen as `v1` again and assert the sets are still readable, so the redefinition
  is shown to be non-destructive in both directions rather than only forwards.

If any of these cannot be made to pass against a real database, halt and report which one and why.
Do not weaken a case to make the file green: an empty-database variant of the first case, or an
assertion that only checks the reopen did not throw, does not close criterion 4 and would leave the
gap open behind a passing report.
  </action>
  <verify>
    <automated>pnpm --filter mobile test:e2e -- --project=durability && pnpm --filter mobile typecheck && pnpm --filter mobile test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test:e2e -- --project=durability` exits 0 with at least 5 passing cases (02-09's tracer plus these 4) and 0 skipped.
    - `grep -rn 'test.skip\|test.fixme\|it.skip\|describe.skip' apps/mobile/e2e/` returns no matches.
    - `grep -rn 'jest.mock\|createMock\|fakePowerSync\|stubDatabase' apps/mobile/e2e/schema-redefinition.spec.ts` returns no matches.
    - The populated case asserts a crud queue depth greater than zero **before** the close, and the identical value after the reopen; both numbers appear in the assertion, not just their equality.
    - `grep -nE '\.(finish|flush|disconnect)\(|waitForFirstSync\(|[^a-zA-Z]connect\(' apps/mobile/e2e/schema-redefinition.spec.ts` shows no such call anywhere in the populated case. The pattern is deliberately code-shaped so the words appearing in prose do not match.
    - `grep -c "notes" apps/mobile/lib/db/test-support.ts` and `grep -c "SCHEMA_VARIANT_DELTA" apps/mobile/lib/db/test-support.ts` are each at least 1.
    - `pnpm --filter mobile test` and `pnpm --filter mobile typecheck` both still exit 0.
  </acceptance_criteria>
  <done>Roadmap criterion 4 has a real passing proof against a populated pre-migration database, and PLAT-07's ordering and empty-input edges are covered.</done>
</task>

<task type="auto">
  <name>Task 2: Offline writes sync themselves, and two clients converge</name>
  <files>apps/mobile/app/__durability.web.tsx, apps/mobile/e2e/sync.spec.ts</files>
  <precondition>Postgres and the self-hosted PowerSync Service are running via `docker compose -f docker-compose.dev.yml up -d`, `GET $POWERSYNC_URL/probes/liveness` returns 200, and the API is running and reachable at `EXPO_PUBLIC_API_URL` with `POWERSYNC_URL` and `POWERSYNC_JWT_SECRET` set.</precondition>
  <read_first>
    - `apps/mobile/lib/db/connector.ts` — as fixed by plan 02-11; `fetchCredentials` needs a real authenticated session, which is why this spec signs in through the real screen
    - `apps/mobile/app/_layout.tsx` — where `connectPowerSync` / `disconnectPowerSync` are wired to session state; the harness must not duplicate or bypass that wiring
    - `apps/mobile/app/(auth)/sign-in.tsx` — the real sign-in screen this spec drives
    - `apps/api/test/sync-push.e2e-spec.ts` — how a throwaway account is created at runtime; reuse the same approach rather than a fixture credential
    - `ops/powersync/sync-rules.yaml` and `ops/powersync/powersync.yaml` — the per-user bucket definitions the pull leg depends on
    - `.planning/phases/02-data-model-sync-engine/02-08-PLAN.md` Task 3 — the open `checkpoint:human-verify` whose browser half this task closes; follow its how-to-verify steps as the specification for what to assert
    - `.planning/WINDOWS.md` entry #26
  </read_first>
  <action>
Extend the harness global with `connect()` and `disconnect()` delegating to the same
`connectPowerSync` / `disconnectPowerSync` the root layout uses, and a `crudCount()` already added in
02-09. Add nothing that stubs, replaces or short-circuits `SyncConnector`, `apiFetch` or
`session-guard.ts` — the whole value of this spec is that it drives the production connector.

Simulate loss of connectivity with Playwright's own `context.setOffline(true)` at the browser-context
level, not by pointing the app at a dead URL and not by mocking `fetch`. A real offline context is
what makes `classifyAuthOutcome`'s `offline` branch fire for the real reason.

Create the test account by calling the real sign-up route over HTTP in the spec's setup with a
runtime-generated email and the same shared throwaway password constant the API e2e suites already
use, then drive the real `/sign-in` screen in the page. Never embed a real credential, never read one
from the repository, and never inject a session cookie directly — a harness that fabricates a session
would be exactly the production-auth bypass this round's threat model forbids.

Author `apps/mobile/e2e/sync.spec.ts` with these cases:

- **Offline write, automatic drain.** Sign in, wait for the crud queue to be empty, take the context
  offline, log a session with several sets through the real helpers, assert the crud queue is
  non-empty and that nothing reached Postgres. Restore connectivity with `setOffline(false)` and then
  wait — with a bounded expect-poll, not a fixed sleep — for the crud queue to drain to zero and the
  rows to appear in Postgres, queried directly. Take no action in between: no button tap, no reload,
  no manual sync call. This is roadmap criterion 1, and the absence of any intervening action is the
  claim; a case that calls a drain helper to make the queue empty proves the opposite of what is
  wanted.
- **Null weight, full client path.** One of the sets logged above carries no weight. Assert its
  Postgres `weight_kg` is SQL NULL after the drain. Plan 02-10 proved this from a direct API push;
  this proves it from the real client, through the real crud queue, which is the path an actual
  bodyweight set takes.
- **Post-redefinition drain.** Repeat Task 1's populate-then-redefine sequence, then connect and
  assert the queue drains and every set reaches Postgres. This is the "still pushes and drains" half
  of criterion 4 that Task 1 deliberately leaves out.
- **Two clients converge.** Open two independent browser contexts signed into the same account. Take
  both offline. Log distinct sets in each. Bring both back online. Assert every set from both contexts
  is present in Postgres and that both contexts eventually read all of them, with no set lost. This is
  the browser half of WINDOWS #26 and 02-08's open checkpoint.
- **Service down stays usable.** With the PowerSync Service stopped, assert a local write still
  succeeds and the crud queue still accepts it — the app remains usable when the sync service is
  unreachable, which is 02-08 Task 3's second observation.

Register these under a Playwright project named `sync`, separate from `durability`, because they need
the live service stack. That separation is a documented scope boundary, not a skip: running
`pnpm --filter mobile test:e2e` locally runs both projects, and a `sync` case that cannot reach its
services must fail loudly rather than pass or self-exclude.
  </action>
  <verify>
    <automated>pnpm --filter mobile test:e2e -- --project=sync</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test:e2e -- --project=sync` exits 0 with at least 5 passing cases and 0 skipped.
    - `grep -c 'setOffline' apps/mobile/e2e/sync.spec.ts` is at least 4 — connectivity is toggled at the browser context, not simulated.
    - `grep -rn 'jest.mock\|route(\|fulfill(\|setExtraHTTPHeaders.*[Cc]ookie' apps/mobile/e2e/sync.spec.ts` returns no matches — no request interception and no injected session.
    - `grep -c 'sign-in\|signIn' apps/mobile/e2e/sync.spec.ts` is at least 1 — the session comes from the real screen.
    - The automatic-drain case contains no page interaction between restoring connectivity and the drain assertion; verified by reading the case and stated explicitly in the SUMMARY.
    - The drain assertion uses a bounded polling expectation, and `grep -c 'waitForTimeout' apps/mobile/e2e/sync.spec.ts` returns 0 — no fixed sleep standing in for a condition.
    - The null-weight case asserts against Postgres directly, and the assertion distinguishes NULL from `0.000`.
    - `pnpm --filter mobile test:e2e -- --project=durability` still exits 0.
  </acceptance_criteria>
  <done>Criterion 1 has an automated proof with no user action on the path, criterion 4's drain half is proven, two browser clients are proven to converge, and the app is proven usable with the sync service down.</done>
</task>

<task type="auto">
  <name>Task 3: Keep it honest — CI, the ledger, and the requirement statuses</name>
  <files>apps/mobile/playwright.config.ts, .github/workflows/ci.yml, .planning/REQUIREMENTS.md, .planning/phases/02-data-model-sync-engine/02-VALIDATION.md</files>
  <read_first>
    - `.github/workflows/ci.yml` — the existing `check` and `e2e` jobs, and the generated-throwaway-secret pattern to follow for any new job
    - `apps/mobile/playwright.config.ts` — as created by plan 02-09; this task splits it into the two named projects
    - `.planning/REQUIREMENTS.md` — the Phase 2 traceability table currently reading `Gaps Found` for PLAT-02, PLAT-03, PLAT-04, PLAT-07, PLAT-08 and LOG-22
    - `.planning/WINDOWS.md` entries #17, #19, #20, #21, #22, #23, #26 — the entries this round touches; read each before deciding its disposition
    - `.planning/phases/02-data-model-sync-engine/02-VALIDATION.md` — the per-task verification map still reading `_pending_` for every requirement
  </read_first>
  <action>
Split `playwright.config.ts` into two projects: `durability`, matching the specs that need only a
browser (`durability.spec.ts` and `schema-redefinition.spec.ts`), and `sync`, matching
`sync.spec.ts`. Add a `test:e2e:durability` script to `apps/mobile/package.json` running
`playwright test --project=durability`. Leave `test:e2e` running both.

Add a `browser-durability` job to `.github/workflows/ci.yml` that installs dependencies, runs
`pnpm --filter mobile exec playwright install --with-deps chromium`, and runs
`pnpm --filter mobile test:e2e:durability`. It needs no database, no API and no PowerSync Service,
because the `durability` project's specs write only to a local browser-hosted database.

State plainly in the workflow, as a comment on the job, that the `sync` project is **not** run in CI
and why: the PowerSync Service requires a Postgres started with `wal_level = logical`, and GitHub
Actions service containers cannot override a container's command to set it, so the reconnect and
convergence specs stay local-only until CI grows a compose-based Postgres. That is a documented
boundary with a named cause, not an unexplained omission — do not add a conditional skip to make the
suite appear to run.

Update `.planning/WINDOWS.md` using `gsd-tools windows fixed <id>` only for entries this round
genuinely closes, and amend rather than close the rest:

- #20 and #21 — closed by plan 02-10, if its schema push landed and `null-weight.e2e-spec.ts` passes.
- #22 — closed by plans 02-09 and 02-12: the two suites it says could not be authored now exist and
  pass, against a real database, in a real browser.
- #23 — closed by plan 02-09's parameter seam.
- #19 — amend, do not close: the 9 tables still have no apply path; what changed is that the boundary
  is now an explicit tested contract and the rejection is no longer silent.
- #26 — amend, do not close: the browser half is now automated; the device half is still blocked with
  no native toolchain on this machine.
- #17 — amend, do not close: the web runtime is now exercised against a real engine; native op-sqlite
  is not.

Update `.planning/REQUIREMENTS.md`'s Phase 2 traceability rows to what is now actually proven, and
nothing more. Change a row only if a named passing suite backs it; where a requirement is proven on
web but not on native, say so in the row rather than marking it complete. Do not mark PLAT-10 or any
row this round did not touch.

Fill in `.planning/phases/02-data-model-sync-engine/02-VALIDATION.md`'s per-task verification map for
the requirements this round covers, replacing the `_pending_` rows with the plan, wave, threat ref and
the exact automated command, and record the measured quick-suite and full-suite runtimes in the
`{N} seconds` fields.
  </action>
  <verify>
    <automated>pnpm --filter mobile test:e2e:durability && node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); if(!y.includes('browser-durability')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test:e2e:durability` exits 0 and runs strictly fewer specs than `pnpm --filter mobile test:e2e`.
    - `.github/workflows/ci.yml` parses as YAML and contains a `browser-durability` job whose steps include `playwright install --with-deps chromium`.
    - `grep -c 'wal_level' .github/workflows/ci.yml` is at least 1 — the reason the `sync` project is excluded is written down at the exclusion site.
    - `grep -rn 'continue-on-error\|if: always()' .github/workflows/ci.yml` shows no such flag on the `browser-durability` job — a failing durability run fails CI.
    - `node .claude/gsd-core/bin/gsd-tools.cjs query windows list` shows #20, #21, #22 and #23 as `fixed`, and #17, #19 and #26 still `open` with amended descriptions.
    - `.planning/REQUIREMENTS.md`'s Phase 2 traceability table contains no row changed to a complete state without a named suite in this round's SUMMARY files backing it.
    - `.planning/phases/02-data-model-sync-engine/02-VALIDATION.md` has no remaining `_pending_` row for PLAT-02, PLAT-03, PLAT-07 or PLAT-08, and no remaining `{N} seconds` placeholder.
    - `pnpm turbo run typecheck lint test build` exits 0.
  </acceptance_criteria>
  <done>The local-only durability proof runs on every push, the ledger says what is and is not closed, and no requirement is marked further along than its evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright context → real authenticated session | The spec obtains a session the same way a user does |
| Browser client → `POST /v1/sync/push` → Postgres | The full production write path under test |
| CI runner → downloaded browser binaries | Untrusted binaries executed on a build machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-45 | Spoofing | Test session acquisition in `sync.spec.ts` | high | mitigate | The session comes from driving the real `/sign-in` screen against a runtime-created throwaway account; no injected cookie, no `setExtraHTTPHeaders` credential, no request interception — asserted by negative grep |
| T-02-46 | Information Disclosure | Credentials in the spec or CI | high | mitigate | Runtime-generated email plus the shared throwaway password constant already used by the API e2e suites; no repository secret referenced, following `ci.yml`'s existing generated-secret pattern |
| T-02-47 | Tampering | Playwright browser binaries in CI | medium | mitigate | Installed via `playwright install --with-deps chromium` pinned to the lockfile's `@playwright/test` version cleared at plan 02-09's legitimacy gate |
| T-02-48 | Elevation of Privilege | Harness `connect` / `disconnect` methods | medium | mitigate | They delegate to the same `connectPowerSync` / `disconnectPowerSync` the root layout uses and add no privileged path; the harness itself is dead-code-eliminated from a production bundle, proven by plan 02-09's two-sided grep |
| T-02-49 | Repudiation | CI job masking a failure | medium | mitigate | No `continue-on-error` and no `if: always()` on the `browser-durability` job, asserted by negative grep |
| T-02-50 | Denial of Service | Test accounts accumulating in the dev database | low | accept | Throwaway accounts in a development database, same footprint the existing API e2e suites already leave |
</threat_model>

<verification>
- `pnpm --filter mobile test:e2e -- --project=durability` — at least 5 passing, 0 skipped, no live services needed
- `pnpm --filter mobile test:e2e -- --project=sync` — at least 5 passing, 0 skipped, against the running stack
- `pnpm --filter mobile test` — Jest suite still green
- `pnpm --filter @fitness/api-contracts test` and `pnpm --filter api test:e2e` — still green
- `pnpm turbo run typecheck lint test build` — exits 0
- `.github/workflows/ci.yml` parses and carries the `browser-durability` job
- WINDOWS ledger dispositions match the evidence, entry by entry

**Roadmap criteria after this plan:** criterion 1 proven by `sync.spec.ts`; criterion 4 proven by
`schema-redefinition.spec.ts` plus `sync.spec.ts`'s post-redefinition drain; criteria 2, 3 and 5
already verified and re-run unchanged.
</verification>

<success_criteria>
- Roadmap criterion 4 has a passing automated proof against a populated pre-migration database
- Roadmap criterion 1 has a passing automated proof with no user action between reconnect and drain
- Two browser clients are proven to converge, closing the browser half of WINDOWS #26
- The null weight is proven NULL in Postgres through the real client path, not only a direct push
- The local-only half runs in CI on every push; the service-dependent half is excluded with its cause written down
- No suite is skipped, no database is faked, and no claim is narrowed to fit
</success_criteria>

<output>
Create `.planning/phases/02-data-model-sync-engine/02-12-SUMMARY.md` when done.

Record: the crud queue depths asserted before and after the schema redefinition, the observed drain
latency after reconnect, the per-project case counts, the exact WINDOWS entries closed versus amended
with the evidence for each, and — for anything that could not be proven — a plain statement of what
remains unproven plus a new ledger entry, never a narrowed claim.
</output>
