---
phase: 04-program-builder
plan: 12
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - apps/api/src/db/schema/program.ts
  - apps/api/src/sync/patch-update-set.ts
  - apps/api/src/sync/sync.service.ts
  - apps/api/test/schema-parity.e2e-spec.ts
  - apps/api/test/program-sync.e2e-spec.ts
  - ops/powersync/sync-rules.yaml
  - apps/mobile/lib/db/schema.ts
  - apps/mobile/lib/db/programs/days.ts
  - apps/mobile/lib/db/programs/load-program.ts
  - apps/mobile/lib/db/programs/duplicate-routine.ts
  - apps/mobile/lib/db/__tests__/programs.test.ts
  - apps/mobile/lib/db/__tests__/duplicate-routine.test.ts
autonomous: false
requirements: [PROG-07]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A training day carries an archive stamp on both the Postgres and the local SQLite side, and the live database actually has the column — not merely the TypeScript type"
    - "A routine_day push carrying archived_at applies, and a second push clearing it restores the day; neither push deletes the day nor tombstones its exercises"
    - "Archiving a day is a timestamp and never a delete: the archive write issues zero deletes, so a workout_session that names the day by id keeps pointing at a row that still exists (D-05, D-01)"
    - "Every day-listing read path returns live days only — loadProgramTree, and therefore the builder deck, the Home next-up read and duplicateRoutine, all stop seeing an archived day (D-29)"
    - "An archived day is still reachable for restore through its own dedicated read, exactly as an archived program is reachable through loadLibraryRoutines rather than loadRoutines"
    - "The PowerSync stream still delivers every routine_day row, archived or not — a row that leaves a sync bucket is deleted locally, which would make restore impossible on any device that did not perform the archive"
    - "Duplicating a program copies its live days only, and that is an asserted behaviour rather than an accident of the filtered read"
  artifacts:
    - apps/api/src/db/schema/program.ts
    - apps/mobile/lib/db/schema.ts
    - apps/mobile/lib/db/programs/days.ts
    - apps/mobile/lib/db/programs/load-program.ts
  key_links:
    - "`routineDay.archivedAt` (Postgres) -> `ROUTINE_DAY_PATCH_FIELDS.archivedAt` -> `toRoutineDayValues` -> `isInvalidRoutineDay`: all four must name the column or a PATCH silently drops it, which is the one way a synced archive stamp goes missing without any test turning red"
    - "`loadProgramTree` is the single day-listing read for the builder, `loadNextUp` and `duplicateRoutine` — filtering there is what makes all three agree, and a second filter anywhere else is how they start disagreeing"
    - "`loadArchivedDays` -> the restore surface: the `loadRoutines` / `loadLibraryRoutines` split reproduced one level down, so the filtered read stays total and the archive stays reachable"
  prohibitions:
    - "Archiving a day never deletes a row — workout_session.routine_day_id points into it with no foreign key and the history cannot be reconstructed"
    - "No second implementation of the archived-day filter is added anywhere: loadProgramTree owns it, and loadArchivedDays is the one deliberate exception"
    - "No comments are added to code by default — a comment is written only for a trap, a deliberate deviation, an external-contract quirk, or an absence"
    - "No AI attribution appears in any commit message, source file, or planning document"
    - "No npm or pnpm package is installed by this plan"
    - "No task launches a browser or drives the app; the Playwright suite belongs to 04-15"
---

<objective>
Give `routine_day` an archive stamp, carry it through the sync apply path, and make every day-listing
read return live days only — the data half of D-29.

Purpose: `04-VERIFICATION.md` gap 1 records that archive/restore of an individual workout "does not
exist in any form: `routine_day` carries no `archived_at` column and the only day-level removal is a
hard delete". D-29 resolves the assumption `04-11-PLAN.md` carried as unresolved (A-PROG-07):
"individual workouts" in PROG-07 means `routine_day`, and day-level archive/restore ships in full.
This plan builds the column and the filtered reads; 04-13 wires the controls that reach them.

Output: `routine_day.archived_at` on both schemas and in the live database, a `routine_day` sync
apply path that accepts and clears it, `archiveDay`/`restoreDay`/`loadArchivedDays` write and read
helpers, and a `loadProgramTree` that returns live days only.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-program-builder/04-CONTEXT.md
@.planning/phases/04-program-builder/04-VERIFICATION.md
@.planning/phases/04-program-builder/04-PATTERNS.md
@docs/program-vocabularies.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new — not pre-existing, do not flag as drift):

| Symbol | Kind | File |
|---|---|---|
| `routineDay.archivedAt` | column | `apps/api/src/db/schema/program.ts` |
| `routineDay.archivedAt` | column | `apps/mobile/lib/db/schema.ts` |
| `archiveDay`, `restoreDay`, `loadArchivedDays`, `ArchivedDayRow` | functions/type | `apps/mobile/lib/db/programs/days.ts` |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: the one-way column, the sync-rule scope, and the restore surface (D-29)</name>
  <decision>
    Adding `routine_day.archived_at` is a one-way schema change once it has synced, and D-29's own
    text asks for two things that pull against each other. Confirm the shape before the column is
    pushed.
  </decision>
  <context>
    D-29 says: add `routine_day.archived_at`, **filter archived days out of every day-listing read
    path and out of `ops/powersync/sync-rules.yaml`**, and wire an Archive/Restore Day control.

    Filtering the sync rule and shipping a Restore control cannot both be true. PowerSync removes a
    row from the local database when it leaves the user's sync bucket. If the pull query gains an
    `archived_at` predicate, archiving a day on phone deletes that row from the browser — where the
    Restore control then has nothing to restore, and where `workout_session.routine_day_id` points at
    a row the device no longer holds. The existing `routine` table is the precedent and it does the
    opposite: `SELECT * FROM routine WHERE user_id = auth.user_id()` streams archived programs too,
    which is exactly why the library's Restore works today.

    `routine_day.archived_at` is also one-way in the sense D-29 records: once a client in the field
    has written the column, removing it means an ID-and-column migration across every synced device.

    The third open item is UI shape, which D-29 leaves to this plan: the day page today offers
    Rename and **Remove** (a hard delete, cascade-proven). Archive is being added beside it.
  </context>
  <options>
    <option id="option-a">
      <name>Stream every day; filter at the read paths only (RECOMMENDED)</name>
      <pros>
        Restore works on every device, matching how archived programs already behave. An archived day
        stays local, so `workout_session.routine_day_id` never dangles. The filtering D-29 asks for
        still happens — in `loadProgramTree`, the one read the builder, the Home card and
        `duplicateRoutine` all share. `sync-rules.yaml` gains a comment recording the deviation and
        why, which is the kind of comment the house rule sanctions (a deliberate deviation plus an
        external-contract quirk about bucket removal).
      </pros>
      <cons>
        Departs from the literal wording of D-29 on one clause, so the deviation has to be written
        down rather than assumed. Archived days keep consuming sync bandwidth and local storage.
      </cons>
    </option>
    <option id="option-b">
      <name>Filter the sync rule literally, as D-29's text says</name>
      <pros>Honours D-29 word for word. Archived days stop syncing.</pros>
      <cons>
        Restore becomes unreachable: the row is deleted locally the moment the archive stamp lands,
        on every device including the one that archived it. The Archive/Restore control D-29 also
        asks for would ship half-dead — the same orphaned-capability failure this whole gap-closure
        run exists to fix.
      </cons>
    </option>
    <option id="option-c">
      <name>Option A, and additionally replace the day page's Remove with Archive</name>
      <pros>One removal mechanism instead of two. Nothing about a day can ever be hard-deleted.</pros>
      <cons>
        Not asked for by D-29, and drops a shipped, cascade-tested capability (`removeDay` plus the
        three-level tombstone cascade the verifier probed). Widens this gap-closure run's blast
        radius into behaviour that is currently verified green.
      </cons>
    </option>
  </options>
  <also-confirm>
    Under option A or C, the day-level confirmation copy `ArchiveDialog` will carry (04-13 adds the
    `day` subject; this is the copy the user will read):

    - Archive — heading **"Archive Day"**, body **"Archiving removes it from this program, but any
      workouts you logged from it stay in your history. Archive anyway?"**, confirm **"Archive"**.
    - Restore — heading **"Restore Day"**, body **"This day will reappear in this program."**,
      confirm **"Restore"**.

    And the restore surface: an **"Archived days"** section below the day deck, present only when the
    routine has archived days (the own-empty-omits-header convention `DetailSection` and the library
    already follow), each row receded at `opacity 0.6` like an archived library row, with a 48x48
    Restore control.
  </also-confirm>
  <resume-signal>Select: option-a, option-b, or option-c — and confirm or amend the copy and restore surface above</resume-signal>
</task>

<task type="tracer">
  <name>Task 1: The column, everywhere it has to exist, proven against the live database</name>
  <files>apps/api/src/db/schema/program.ts, apps/api/src/sync/patch-update-set.ts, apps/api/src/sync/sync.service.ts, apps/api/test/schema-parity.e2e-spec.ts, apps/api/test/program-sync.e2e-spec.ts, ops/powersync/sync-rules.yaml, apps/mobile/lib/db/schema.ts</files>
  <read_first>
    - `apps/api/src/db/schema/program.ts` in full — `routineDay` (the table being changed) and
      `routine`, whose `archivedAt: timestamp('archived_at')` is the exact column shape to copy. Note
      `routineDay` carries no `userId` and no `serverSeq`: ownership resolves through `routine.userId`,
      and that does not change here.
    - `apps/api/src/sync/patch-update-set.ts` lines 185–215 (the `PatchFieldMap` doc comment — read
      the mapping-direction warning before adding a key), lines 123–129 (`RoutineDayValues`), lines
      292–302 (`ROUTINE_PATCH_FIELDS`, where `archivedAt: 'archived_at'` already appears and is the
      pattern to copy) and lines 337–343 (`ROUTINE_DAY_PATCH_FIELDS`).
    - `apps/api/src/sync/sync.service.ts` — `RoutineDayOpData` (line ~339), `toRoutineDayValues`
      (line ~637), `toRoutineValues` (line ~528, for the `d.archived_at ? new Date(...) : null`
      conversion), `isInvalidRoutineDay` (line ~772), and `isValidOptionalIsoOrNull` (used at line
      ~986) — the existing ISO-or-null validator this column reuses rather than a new one.
    - `apps/api/test/schema-parity.e2e-spec.ts` in full — `REQUIRED_COLUMNS`, its comment explaining
      why a live-database read is the only thing that catches an unmigrated column, and
      `FORBIDDEN_COLUMNS` (this project pushes with `drizzle-kit push` and keeps no migration files,
      so the parity gate is the migration proof).
    - `apps/api/test/program-sync.e2e-spec.ts` lines 89–92 (`routineDayOp`), lines 161–168
      (`routineDayRow` — the raw column list it selects), and the `describe('routine_day /
      routine_exercise sync (e2e)')` block at line ~530 for the push/assert shape.
    - `ops/powersync/sync-rules.yaml` in full — the comment block at the top and the `routine`,
      `routine_day` and `routine_exercise` stream queries.
    - `apps/mobile/lib/db/schema.ts` lines 64–83 — the local `routine` table (its
      `archivedAt: text('archived_at')`, the SQLite mirror shape) and the `routineDay` table.
    - `apps/mobile/lib/db/powersync.ts` lines 1–25 — `DrizzleAppSchema` is built from `drizzleSchema`,
      so the local PowerSync table definition follows the Drizzle table automatically; no second
      registration exists to update.
  </read_first>
  <action>
    One nullable timestamp column, added at every layer that has to name it. This is the tracer: the
    thinnest path from Postgres to the device that proves an archive stamp survives a round trip
    before any UI is built on top of it (D-29, D-05).

    **`apps/api/src/db/schema/program.ts`.** Add `archivedAt: timestamp('archived_at')` to
    `routineDay`, copying `routine`'s own column exactly. Nullable, no default, no index — the
    filtered read is a client-side SQLite query, not a Postgres one. Leave the existing table comment
    alone; it explains why there is no `ProgramWeek` and no ownership column, both still true.

    **`apps/api/src/sync/patch-update-set.ts`.** Add `archivedAt: Date | null` to `RoutineDayValues`
    and `archivedAt: 'archived_at'` to `ROUTINE_DAY_PATCH_FIELDS`. Read that file's mapping-direction
    comment first: a wire name means "write this column only when the PATCH names it", which is what
    this column needs, and the alternative (`null`) would make every PATCH clobber it.

    **`apps/api/src/sync/sync.service.ts`.** Three edits, all mirroring what `routine` already does:
    add `archived_at?: string | null` to `RoutineDayOpData`; add
    `archivedAt: d.archived_at ? new Date(d.archived_at) : null` to `toRoutineDayValues`; and add an
    `isValidOptionalIsoOrNull(d.archived_at)` guard to `isInvalidRoutineDay`. Do not add a hard-delete
    carve-out and do not touch `HARD_DELETE_FORBIDDEN` — a day DELETE stays exactly as legal as it is
    today, because `removeDay` still exists and its three-level tombstone cascade is behaviourally
    proven.

    **`apps/api/test/schema-parity.e2e-spec.ts`.** Add a `routine_day` entry to `REQUIRED_COLUMNS`
    listing `id`, `routine_id`, `order_index`, `name`, `is_rest_day`, `archived_at`. This is the gate
    that turns "the type compiles" into "the live database has it" — without it an unpushed column is
    a silent green across the whole suite, which is the exact failure mode that file exists for.

    **`ops/powersync/sync-rules.yaml`.** Per the checkpoint's resolved option. Under option A the
    `routine_day` stream query is left byte-identical and a short comment is added to the file's
    existing header block recording that archived days deliberately keep streaming, and why: a row
    that leaves a sync bucket is removed from the local database, so a filtered pull would delete the
    day on every device that did not perform the archive and leave the Restore control with nothing
    to act on — the same reason the `routine` query has never filtered `archived_at`. That comment
    earns its place under the house rule twice over: a deliberate deviation from D-29's literal text,
    and an external-contract quirk of PowerSync a reader cannot infer from this repository.

    **`apps/mobile/lib/db/schema.ts`.** Add `archivedAt: text('archived_at')` to the local `routineDay`
    table, matching the local `routine` table's own column (SQLite stores the ISO string; there is no
    timestamp type on this side).

    **Then push the schema — this is `[BLOCKING]`, and typecheck passing is not evidence.** Run
    `pnpm --filter api db:push`. This project keeps no migration files; `drizzle-kit push` is the
    migration. Until it has run, every API test still passes against a database with no such column,
    and the first thing to actually fail would be a user's write.

    **`apps/api/test/program-sync.e2e-spec.ts`.** Extend `routineDayRow`'s select to include
    `archived_at`, then add four cases to the existing `routine_day / routine_exercise sync` describe:

    - A `routine_day` PATCH naming only `archived_at` applies the stamp and leaves `name`,
      `order_index` and `is_rest_day` untouched — the field-presence guarantee `ROUTINE_DAY_PATCH_FIELDS`
      exists to provide.
    - A second PATCH naming `archived_at: null` clears it and the row is still there. Archive is
      reversible; restore is not a re-create.
    - Archiving a day emits **no** `sync_tombstone` rows and leaves its `routine_exercise` children
      present — an archive is not a delete, and the cascade that a DELETE triggers must not fire.
      Assert the exercise row still exists and the tombstone count for the day and its children is
      zero.
    - A PATCH carrying a non-ISO `archived_at` (for example the string `banana`) is rejected as
      `invalid_field` and the stored row is unchanged.
  </action>
  <verify>
    <automated>pnpm --filter api db:push</automated>
    <automated>pnpm --filter api db:verify</automated>
    <automated>pnpm --filter api test:e2e -- program-sync</automated>
    <automated>npx turbo run typecheck lint</automated>
    <automated>grep -q "archived_at" apps/api/src/db/schema/program.ts</automated>
    <automated>grep -q "archivedAt: 'archived_at'" apps/api/src/sync/patch-update-set.ts</automated>
    <automated>grep -c 'FROM routine_day INNER JOIN routine ON routine_day.routine_id = routine.id WHERE routine.user_id = auth.user_id()$' ops/powersync/sync-rules.yaml</automated>
    <automated>test "$(grep -c 'routine_day' apps/api/test/schema-parity.e2e-spec.ts)" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/db/schema/program.ts`'s `routineDay` declares `archivedAt: timestamp('archived_at')`
    - `ROUTINE_DAY_PATCH_FIELDS` maps `archivedAt` to the wire name `archived_at`, not to `null`
    - `toRoutineDayValues` converts `archived_at` through `new Date(...)` and `isInvalidRoutineDay`
      rejects a non-ISO value through the existing `isValidOptionalIsoOrNull`
    - `pnpm --filter api db:push` has been executed in this task, and `pnpm --filter api db:verify`
      exits 0 with `routine_day` listed in `REQUIRED_COLUMNS` including `archived_at` — proving the
      live database has the column, not merely the TypeScript type
    - The `routine_day` stream query in `ops/powersync/sync-rules.yaml` ends at `auth.user_id()` with
      no appended predicate (the anchored grep above returns 1), and the file's header comment records
      why archived days keep streaming
    - `apps/mobile/lib/db/schema.ts`'s `routineDay` declares `archivedAt: text('archived_at')`
    - `pnpm --filter api test:e2e -- program-sync` exits 0 including the four new cases: stamp applies
      without clobbering siblings, null clears it, archiving emits zero tombstones and leaves children
      present, and a non-ISO value is rejected
  </acceptance_criteria>
  <done>
    A training day can carry an archive stamp from a client push all the way into Postgres and back,
    the live database provably has the column, archiving emits no tombstones and destroys nothing, and
    every device still receives archived days so a restore has something to act on.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Archive and restore writes, and a day-listing read that returns live days only</name>
  <files>apps/mobile/lib/db/programs/days.ts, apps/mobile/lib/db/programs/load-program.ts, apps/mobile/lib/db/programs/duplicate-routine.ts, apps/mobile/lib/db/__tests__/programs.test.ts, apps/mobile/lib/db/__tests__/duplicate-routine.test.ts</files>
  <read_first>
    - `apps/mobile/lib/db/programs/days.ts` in full — the `db: WriteDb = getPowerSync()` seam every
      helper uses, `addDay`'s insert (which gains an explicit `archivedAt: null`), `removeDay` and its
      comment explaining the cascade (unchanged by this task), and `moveDay`/`computeReorder`.
    - `apps/mobile/lib/db/programs/lifecycle.ts` — `archiveRoutine` and `restoreRoutine` are the
      shape `archiveDay`/`restoreDay` copy one level down (an ISO timestamp write, then null to
      undo), and `loadLibraryRoutines`' comment explains exactly why the archive-reachable read is a
      separate function rather than a flag on the filtered one. `loadArchivedDays` is that same split.
    - `apps/mobile/lib/db/programs/load-program.ts` in full — `loadProgramTree`'s five-query read, the
      `dayRows` select at line ~83 (the one place a day list is produced), and the comment explaining
      why the whole tree is read per table rather than per row.
    - `apps/mobile/lib/db/programs/create-routine.ts` — `loadRoutines`, which filters archived
      routines at the SQL level; this is the precedent for filtering in the query rather than in
      JavaScript.
    - `apps/mobile/lib/db/programs/duplicate-routine.ts` lines 1–60 — `duplicateRoutine` reads its
      source through `loadProgramTree`, so filtering there silently changes what a duplicate contains;
      and `duplicateDay` at line ~144, whose `routineDay` insert gains an explicit `archivedAt: null`.
    - `apps/mobile/lib/db/__tests__/programs.test.ts` — the `fakeDb` shape, the
      `describe('the database-injection seam (WINDOWS #23)')` convention every helper repeats, and
      `fakeLoadProgramDb` at line ~539 plus `describe('loadProgramTree')` at line ~562. **Read
      `fakeLoadProgramDb`'s `where` carefully: it takes no argument and returns `rows.dayRows`
      unconditionally, so it cannot prove a filter.** A behavioural assertion needs the
      condition-resolving store instead — see the next entry.
    - `apps/mobile/lib/db/__tests__/log-set.test.ts` lines 520–660 — `propertyKeyForColumn`,
      `collectEqualities`, `rowMatches` and `inMemoryDb()`: the store that walks drizzle's real query
      chunks and therefore filters on the actual conditions the shipped helpers pass. Note
      `set-groups.test.ts` line 51's comment recording that each suite keeps its own copy — that
      duplication is the established convention here, not an accident. **`collectEqualities` handles
      `eq()` only:** an `isNull()` chunk carries an array `value` that the walker skips, so an
      unextended store silently matches every row and a filter test would pass without a filter.
    - `apps/mobile/lib/db/__tests__/duplicate-routine.test.ts` — the fixture builder and the
      source-id-membership assertion.
  </read_first>
  <behavior>
    `apps/mobile/lib/db/__tests__/programs.test.ts`:
    - `archiveDay('d1', db)` issues exactly one update writing only `archivedAt` as an ISO string, and
      issues **zero** deletes. Assert the delete count directly — this is the assertion that makes
      D-05 and D-01 true for days, not a comment.
    - `archiveDay` writes neither `name`, nor `orderIndex`, nor `isRestDay`.
    - `restoreDay('d1', db)` issues exactly one update setting `archivedAt` to null and issues zero
      deletes and zero inserts. Restoring is not re-creating.
    - Both helpers fall back to `getPowerSync()` when no database argument is passed, and neither
      resolves it when one is (the WINDOWS #23 seam pair every helper in this file already has).
    - `addDay` inserts a row whose `archivedAt` is null — a new day starts live, explicitly, not by
      omission.
    - `collectEqualities` (extended, see `<action>`) collects `{ column: 'archived_at', value: null }`
      from an `isNull(routineDay.archivedAt)` condition — assert this directly, first, before any
      filter test relies on it. Without this assertion a green filter suite proves nothing, because an
      unextended walker matches every row.
    - `loadArchivedDays('r1', db)` returns only the routine's days whose `archivedAt` is non-null,
      ordered by the same order-then-id rule the deck uses, and returns an empty array for a routine
      with none.
    - `loadProgramTree`, run against the condition-resolving store seeded with three days one of which
      carries an `archivedAt` string, returns two days and the archived one is absent from `tree.days`.
    - `loadProgramTree` still returns the archived day's live siblings in their original relative
      order — filtering a day does not renumber or reorder the rest.
    - A `routine_exercise` belonging to an archived day does not appear anywhere in the returned tree,
      because its parent day is gone from the day list.
    - `loadProgramTree` over a routine whose days are all archived returns a tree with zero days and
      does not return null — the routine still exists, it just has nothing live in it.
    - `moveDay` over a routine containing an archived day still computes a single midpoint for a move
      between two live neighbours: the archived row keeps its `order_index` and does not force a
      renumber.

    `apps/mobile/lib/db/__tests__/duplicate-routine.test.ts`:
    - `duplicateRoutine` over a source with two live days and one archived day inserts exactly two
      day rows, and the copied program contains no archived day. The archived day's exercises and
      overrides are not copied either.
    - Every day row `duplicateRoutine` inserts has `archivedAt: null`.
    - `duplicateDay` inserts its new day with `archivedAt: null` — duplicating an archived day
      produces a live one.
  </behavior>
  <action>
    Three helpers and one filtered query. The whole point is that the filter lives in exactly one
    place (D-29).

    **`apps/mobile/lib/db/programs/days.ts`.**

    - `archiveDay(dayId, db = getPowerSync())` — one update setting `archivedAt` to
      `new Date().toISOString()`. Nothing else. A day is never destroyed: `workout_session.routine_day_id`
      points at it with no foreign key, so a hard delete orphans history that cannot be reconstructed
      (D-05, and D-01's snapshot-on-use is what keeps the logged session itself intact either way).
      Deliberately no pointer reconciliation of the kind `archiveRoutine` performs — a day is not
      addressed by `user_preference`, so there is nothing to clear.
    - `restoreDay(dayId, db = getPowerSync())` — one update setting `archivedAt` to null.
    - `loadArchivedDays(routineId, db = getPowerSync()): Promise<ArchivedDayRow[]>` — select the
      routine's days where `archivedAt` is non-null, returning `{ id, name, orderIndex, archivedAt }`,
      sorted through the shared `sortByOrderThenId`. Export the `ArchivedDayRow` interface. This is
      deliberately a second read rather than a flag on `loadProgramTree`: it is the same split
      `loadRoutines` and `loadLibraryRoutines` already make one level up, and it keeps the tree read
      total instead of conditionally partial.
    - `addDay` — add an explicit `archivedAt: null` to the insert. Explicit rather than relying on the
      column's absence, matching how every other nullable column in this file is written out.

    **`apps/mobile/lib/db/programs/load-program.ts`.** Add `isNull(routineDay.archivedAt)` to the
    `dayRows` `where` clause, alongside the existing routine filter. One line, at the SQL level, not a
    JavaScript filter over the result — `loadRoutines` sets that precedent and the reason is the same:
    a partial read that gets filtered afterwards invites a second, divergent filter later.

    This one line is what makes the whole of D-29's "filter archived days out of every day-listing
    read path" true, because `loadProgramTree` **is** that read path for all three consumers: the
    builder deck (`programs.tsx`), the Home next-up read (`loadNextUp` in `next-up-query.ts`, and
    therefore `resolveNextUp`'s rotation), and `duplicateRoutine`. Do not add a second filter in any
    of them.

    **The test fixture the filter assertions need.** `fakeLoadProgramDb` returns `dayRows` whatever
    the `where` says, so adding a filter and re-running it proves nothing. Bring the
    condition-resolving store into `apps/mobile/lib/db/__tests__/programs.test.ts` — copy
    `propertyKeyForColumn`, `collectEqualities`, `rowMatches` and `inMemoryDb()` from
    `log-set.test.ts`, which is the duplication convention `set-groups.test.ts` line 51 already
    records — and extend `collectEqualities` so it recognises a null check: when a chunk carries an
    array `value` whose joined text is an is-null fragment and a column name is currently held, push
    `{ column, value: null }` and clear the held column, exactly as the scalar branch does. Without
    that extension the walker skips the fragment, `rowMatches` collects nothing for the column, and
    every row matches — a filter suite that is green with the filter deleted. The first behaviour
    listed above asserts the walker itself for precisely that reason.

    Leave `fakeLoadProgramDb` and its existing query-count tests alone; the new archived-day cases use
    the store, the old cases keep proving the five-query bound.

    **`apps/mobile/lib/db/programs/duplicate-routine.ts`.** Two changes, both making an existing
    consequence explicit rather than accidental:

    - `duplicateRoutine` now copies live days only, as a direct consequence of reading through the
      filtered `loadProgramTree`. Nothing in the function changes; the behaviour is pinned by the test
      instead. Record the reasoning in the SUMMARY: a duplicate is a fresh draft to train from, and
      carrying another program's archived days into it would reintroduce rows the user put away.
    - `duplicateDay`'s `routineDay` insert gains an explicit `archivedAt: null`, so duplicating an
      archived day yields a live one.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- programs</automated>
    <automated>pnpm --filter mobile test -- duplicate-routine</automated>
    <automated>pnpm --filter mobile typecheck</automated>
    <automated>grep -q "loadArchivedDays" apps/mobile/lib/db/programs/days.ts</automated>
    <automated>grep -q "isNull(routineDay.archivedAt)" apps/mobile/lib/db/programs/load-program.ts</automated>
    <automated>test "$(grep -c 'isNull(routineDay.archivedAt)' apps/mobile/lib/db/programs/load-program.ts)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/lib/db/programs/days.ts` exports `archiveDay`, `restoreDay`, `loadArchivedDays` and
      the `ArchivedDayRow` type, each with the `db: WriteDb = getPowerSync()` seam
    - The archive test asserts a delete count of zero, not merely that an update happened
    - `loadProgramTree`'s day query filters archived days at the SQL level, and that filter appears
      exactly once in the file
    - `programs.test.ts` asserts `collectEqualities` resolves an `isNull` condition to
      `{ column: 'archived_at', value: null }`, and the archived-day cases run against the
      condition-resolving store rather than `fakeLoadProgramDb` — deleting the filter from
      `load-program.ts` must turn those cases red
    - `pnpm --filter mobile test -- programs` exits 0 including the archived-day tree cases and the
      all-days-archived case returning a tree rather than null
    - `pnpm --filter mobile test -- duplicate-routine` exits 0 including the assertion that a
      duplicate carries only live days and every copied day is unarchived
    - No archived-day filter exists in `programs.tsx`, `next-up-query.ts` or `duplicate-routine.ts` —
      all three inherit it from `loadProgramTree`
  </acceptance_criteria>
  <done>
    Archiving a day is one timestamp write with zero deletes, restoring is one write back to null, the
    builder / Home / duplicate reads all return live days from a single filter, and the archived days
    are still readable through their own query so a restore control has something to show.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client CRUD push -> `routine_day` apply path | Untrusted `archived_at` crosses here as a client-supplied string |
| PowerSync pull query -> local database | A row that leaves the bucket is deleted locally; the pull query decides what a device is allowed to keep |
| `loadProgramTree` -> every day-listing consumer | One query decides what the builder, the Home card and the duplicate operation each believe the program contains |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-58 | Tampering | `routine_day` PATCH naming `archived_at` | high | mitigate | `ROUTINE_DAY_PATCH_FIELDS` maps the column to a wire name so it is written only when the PATCH names it, and `isInvalidRoutineDay` rejects a non-ISO value through the existing `isValidOptionalIsoOrNull`. Both asserted by new `program-sync` e2e cases against live Postgres, including that a stamp-only PATCH leaves `name`/`order_index`/`is_rest_day` untouched. |
| T-04-59 | Repudiation | day archive implemented as a delete | high | mitigate | `archiveDay` writes a timestamp; the unit test asserts a delete count of zero. `workout_session.routine_day_id` has no foreign key, so a destroyed day orphans history that cannot be reconstructed. The e2e additionally asserts archiving emits zero `sync_tombstone` rows and leaves the day's exercises present. |
| T-04-60 | Denial of Service | archived rows leaving the sync bucket | high | mitigate | The `routine_day` stream query is left unfiltered and grep-anchored at `auth.user_id()$`, so archiving on one device cannot delete the row on another and strand both the Restore control and a logged session's day reference. Recorded as a deliberate deviation in the yaml header. |
| T-04-61 | Information Disclosure | unmigrated live database passing as green | high | mitigate | `pnpm --filter api db:push` is executed in-task and `schema-parity` gains a `routine_day` entry naming `archived_at`, converting "the type compiles" into "the live database has the column". |
| T-04-62 | Tampering | a second, divergent archived-day filter | medium | mitigate | The filter lives only in `loadProgramTree`'s day query, grep-asserted to appear exactly once; the archive-reachable read is a separate named function rather than a flag, mirroring `loadRoutines`/`loadLibraryRoutines`. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
- `pnpm --filter api db:push` executed, then `pnpm --filter api db:verify` exits 0
- `pnpm --filter api test:e2e -- program-sync` exits 0 against live Postgres
- `pnpm --filter mobile test` and `pnpm --filter mobile typecheck` exit 0
- `npx turbo run typecheck lint` exits 7/7 successful
- The `routine_day` stream query in `ops/powersync/sync-rules.yaml` still ends at `auth.user_id()`
- WINDOWS: record a `deviation` entry via `gsd-tools windows append --kind deviation` noting that
  `ops/powersync/sync-rules.yaml` deliberately does not filter archived `routine_day` rows, against
  D-29's literal text, because bucket removal deletes the row locally and would make restore
  unreachable — with the checkpoint decision as its authority
</verification>

<success_criteria>
- `routine_day.archived_at` exists in the live Postgres database, in the local SQLite schema, and on
  the sync apply path in both directions
- Archiving a day destroys nothing: zero deletes, zero tombstones, children intact
- The builder, the Home next-up read and `duplicateRoutine` all see live days only, from one filter
- Archived days remain readable through `loadArchivedDays` and remain deliverable through sync
</success_criteria>

<output>
Create `.planning/phases/04-program-builder/04-12-SUMMARY.md` when done
</output>
