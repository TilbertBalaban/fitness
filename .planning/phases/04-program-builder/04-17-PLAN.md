---
phase: 04-program-builder
plan: 17
type: execute
wave: 5
depends_on: [04-13, 04-14, 04-16]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/04-program-builder/04-UI-SPEC.md
  - docs/program-vocabularies.md
autonomous: true
requirements: [PROG-06, PROG-07, PROG-09]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PROG-07 is marked Complete only after the day-lifecycle controls exist, are wired, and have been driven in a browser — the ledger follows the screens, not the functions"
    - "PROG-09's text says 'the next workout' rather than 'upcoming workouts', and the change is recorded as a dated amendment with its authority named"
    - "The amendment record names D-32 as the authority and states plainly that the earlier justification cited D-27, a placement decision, which does not support a scope cap"
    - "PROG-06 is marked Complete against an executed proof of the conversion path, not against the diff that closed it"
    - "04-UI-SPEC.md's Home card section no longer cites D-27 as the authority for the single-card scope"
    - "04-UI-SPEC.md's Confirmations table carries the day archive and restore rows the shipped ArchiveDialog now renders, so the contract and the code agree again"
    - "docs/program-vocabularies.md records that a routine_day carries its own archive stamp and that archive is still not a status"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/phases/04-program-builder/04-UI-SPEC.md
    - docs/program-vocabularies.md
  key_links:
    - "the executed 04-16 run -> the PROG-06 and PROG-07 status flips: the ledger's authority is a green browser run plus a non-test call site, which is exactly the evidence the earlier Complete marks lacked"
    - "D-32 -> the PROG-09 amendment row -> the UI-SPEC correction: three places that must agree, or the drift the verifier found simply moves somewhere else"
  prohibitions:
    - "No requirement is marked Complete before its plan has landed and its verification command has been run — the failure this whole run exists to correct is a ledger updated from functions rather than from screens"
    - "PROG-09's amendment narrows the requirement's wording; it must not be presented as the implementation having grown to meet it"
    - "No source code is modified by this plan"
    - "No comments are added to code by default — a comment is written only for a trap, a deliberate deviation, an external-contract quirk, or an absence"
    - "No AI attribution appears in any commit message, source file, or planning document"
    - "No npm or pnpm package is installed by this plan"
---

<objective>
Correct the requirements ledger and the two contracts that drifted from it, with an audit trail —
D-32, plus the PROG-06 and PROG-07 status corrections the preceding plans earned.

Purpose: `04-VERIFICATION.md`'s closing paragraph names the pattern precisely: "this phase's plans
verified functions, and the requirements ledger was updated from those functions rather than from the
screens. The remedy is not more tests — it is a reachability pass before a requirement is marked
Complete." This plan is the ledger half of that remedy, and it runs last on purpose: every status it
writes is backed by a landed plan and an executed command.

D-32 is the one requirement change in this run that is text-only. PROG-09's plural "upcoming
workouts" has been marked Complete against a single `NextUpCard`, justified by citing D-27 — which is
a placement decision ("Upcoming workouts appear on the Home tab, not the Programs tab"), not a scope
cap. The verifier's judgment was that the substance is served (the full sequence is visible in the
Programs tab day deck) but "its stated authority does not support it, and I would not let it stand as
written". D-32 resolves it by amending the wording and recording the amendment. Home keeps its single
card; no code changes.

Output: a corrected `REQUIREMENTS.md` with an Amendments section, a corrected `04-UI-SPEC.md`, and a
`docs/program-vocabularies.md` note on the day archive.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-program-builder/04-CONTEXT.md
@.planning/phases/04-program-builder/04-VERIFICATION.md
@.planning/phases/04-program-builder/04-13-SUMMARY.md
@.planning/phases/04-program-builder/04-14-SUMMARY.md
@.planning/phases/04-program-builder/04-16-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new — not pre-existing, do not flag as drift):

| Symbol | Kind | File |
|---|---|---|
| `## Amendments` section | document section | `.planning/REQUIREMENTS.md` |

<tasks>

<task type="auto">
  <name>Task 1: The requirements ledger, corrected with its authority written down</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full — the `### Program Builder` checklist at lines 37–49 (PROG-01
      through PROG-11 verbatim), the `## Traceability` table at line ~186 and its Phase 4 rows at
      lines ~213–223, and the file's header note that "the table-stakes feature set therefore *is* v1
      — sequencing is the lever, not scope reduction". Note there is **no** existing amendment
      convention in this file; this task establishes one.
    - `.planning/phases/04-program-builder/04-CONTEXT.md` D-29 through D-32 verbatim — the binding
      authorities for every change here.
    - `.planning/phases/04-program-builder/04-VERIFICATION.md` — the gaps block, the Requirements
      Coverage table's PROG-06/PROG-07/PROG-09 rows, and the "PROG-09 Narrowing — Explicit Judgment"
      section in full, which is the reasoning the amendment record has to reproduce faithfully.
    - `.planning/phases/04-program-builder/04-13-SUMMARY.md`, `04-14-SUMMARY.md` and
      `04-16-SUMMARY.md` — the actual evidence: which call sites exist, which greps return files, and
      the executed browser run's case count. **Do not write a status this evidence does not support.**
  </read_first>
  <action>
    Three corrections and one new section (D-29, D-30, D-31, D-32).

    **PROG-09's wording.** In the `### Program Builder` checklist, change PROG-09 from
    `User can view the active program's upcoming workouts with target muscles and per-cycle rep/RIR targets`
    to
    `User can view the active program's next workout with target muscles and per-cycle rep/RIR targets`.
    Nothing else on the line changes; it stays checked.

    **PROG-07's checklist box.** Change it from unchecked to checked, and update its Traceability row
    from `Partial` to `Complete` — but only if 04-13's and 04-14's SUMMARYs record non-test call sites
    for `duplicateDay`, `archiveDay`, `restoreDay` and `loadArchivedDays`, and 04-16's records a green
    executed run. If any of those is absent, leave the status as it is and say so in the SUMMARY. That
    conditional is the entire point of this plan running last.

    **PROG-06's Traceability row.** It currently reads `Complete` against a conversion path
    `04-VERIFICATION.md` proved was broken. Leave it `Complete` only on the strength of 04-16's
    executed cycle-conversion cases; if those did not run green, change it to `Partial`. Either way,
    record in the SUMMARY which evidence decided it — a status carried forward unexamined is how this
    ledger got wrong the first time.

    **A new `## Amendments` section**, placed after `## Traceability`, establishing the convention:

    ```
    ## Amendments

    Changes to a requirement's own text after it was first written, with the decision that authorised
    each one. A requirement is never silently reworded to match what shipped.

    | Date | Requirement | Change | Authority | Rationale |
    ```

    One row, dated 2026-08-28:

    - **Requirement:** PROG-09
    - **Change:** "upcoming workouts" narrowed to "the next workout"
    - **Authority:** D-32 (`04-CONTEXT.md`)
    - **Rationale:** written out in full, and it must say all three of these: the substance is served
      because the full upcoming sequence is visible in the Programs tab day deck while Home answers
      "what am I training next, and what are the numbers"; the earlier justification cited D-27, which
      is a **placement** decision and does not support a scope cap, and that authority drift is what
      is being removed here; and extending to N cards later stays additive because `resolveNextUp`
      already computes cycle position and rotation.

    Write the rationale as the narrowing it is. PROG-09's text is being brought down to meet the
    implementation, deliberately and on the record — not the other way round — and a reader six months
    from now must be able to see that at a glance rather than infer it.
  </action>
  <verify>
    <automated>grep -q "next workout" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "## Amendments" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "D-32" .planning/REQUIREMENTS.md</automated>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs requirements 2>/dev/null || true</automated>
    <automated>test "$(grep -c 'PROG-09' .planning/REQUIREMENTS.md)" -ge 3</automated>
  </verify>
  <acceptance_criteria>
    - PROG-09's checklist line reads "the active program's next workout" and no longer says "upcoming
      workouts"
    - A `## Amendments` section exists with a dated PROG-09 row naming D-32 as its authority and giving
      all three rationale points, including that D-27 is a placement decision
    - PROG-07's checklist box and Traceability row are Complete **only** if the preceding SUMMARYs
      record the four non-test call sites and a green executed browser run; otherwise unchanged, with
      the reason stated in the SUMMARY
    - PROG-06's Traceability row is decided from 04-16's executed cases, and the SUMMARY names the
      evidence
    - No other requirement's text or status is touched
  </acceptance_criteria>
  <done>
    The ledger says what is true, and the one requirement whose text changed says why it changed, who
    authorised it, and which earlier justification was wrong.
  </done>
</task>

<task type="auto">
  <name>Task 2: The UI contract and the vocabulary doc catch up</name>
  <files>.planning/phases/04-program-builder/04-UI-SPEC.md, docs/program-vocabularies.md</files>
  <read_first>
    - `.planning/phases/04-program-builder/04-UI-SPEC.md` — the `## Home "Next Up" Card` section
      (line ~234) and its sentence citing "D-27's own decision text … narrows this to one card"; the
      `## Confirmations` table (line ~269), including its "Duplicate (program or single day) — None"
      row and the Archive/Restore Program rows; and the `## Day Deck & Drag Handle` section (line ~206)
      which describes the day page but predates its new controls.
    - `.planning/phases/04-program-builder/04-VERIFICATION.md` § "PROG-09 Narrowing — Explicit
      Judgment" — the wording the correction should not contradict.
    - `apps/mobile/components/ArchiveDialog.tsx` as 04-13 left it — the day subject's exact heading,
      body and confirm strings, which the Confirmations table rows must reproduce verbatim so the
      contract and the code cannot drift again.
    - `apps/mobile/app/(tabs)/programs.tsx` as 04-13 left it — the day-page header controls and the
      Archived days section, so the Day Deck section describes what actually ships.
    - `docs/program-vocabularies.md` § "Active, frozen and archived are not statuses" (line ~21) — the
      section the day-level archive note extends, and the file's overall register.
  </read_first>
  <action>
    **`04-UI-SPEC.md` — Home "Next Up" Card.** Replace the D-27 justification sentence. The single-card
    scope is now authorised by **D-32**, which amended PROG-09's text; say that, and add one line
    recording that the earlier citation of D-27 was a placement decision and did not support a scope
    reduction. Do not delete the history — the correction is more useful than a clean-looking
    paragraph, and this document is where a reader will look for why Home shows one card.

    **`04-UI-SPEC.md` — Confirmations.** Add two rows, reproducing the shipped `ArchiveDialog` day
    copy verbatim from the component:

    - **Archive Day** — `ArchiveDialog`'s two-button shape, day subject; heading **"Archive Day"**;
      body **"Archiving removes it from this program, but any workouts you logged from it stay in your
      history. Archive anyway?"**; confirm **"Archive"** (destructive fill).
    - **Restore Day** — same shape, `unarchiving` variant; heading **"Restore Day"**; body **"This day
      will reappear in this program."**; confirm **"Restore"** (no destructive fill).

    Amend the existing Duplicate row's note to say the day case is now shipped and still takes no
    confirmation, so the row stops describing a capability that did not exist when it was written.

    **`04-UI-SPEC.md` — Day Deck.** Add a short bullet describing what the day page now carries: a
    header row of Rename, Duplicate, Archive and Remove that wraps rather than shrinking (R4), and an
    Archived days section below the deck, present only when the routine has archived days, with rows
    receded at `opacity-60` — the same treatment archived library rows and time-off chips already get,
    so "archived" and "off" keep reading the same way. Mark the section with its authorising decision
    (D-29) the way the rest of the document does.

    **`docs/program-vocabularies.md`.** Extend "Active, frozen and archived are not statuses" with a
    short paragraph: a training day now carries its own `archived_at`, on the same
    archive-is-a-timestamp rule as `routine` and `exercise` (D-05); it is not a `routine_day` status
    and there is no day status column; `loadProgramTree` filters archived days out of every listing
    while `loadArchivedDays` keeps them reachable for restore; and archived days are still delivered
    by the sync rules, because a row that leaves a bucket is removed locally and restore would then be
    impossible. Match the file's existing register — it explains why a fact lives on the column it
    lives on, and that is what this paragraph should do too.

    Modify no source code in this task.
  </action>
  <verify>
    <automated>grep -q "D-32" .planning/phases/04-program-builder/04-UI-SPEC.md</automated>
    <automated>grep -q "Archive Day" .planning/phases/04-program-builder/04-UI-SPEC.md</automated>
    <automated>grep -q "Restore Day" .planning/phases/04-program-builder/04-UI-SPEC.md</automated>
    <automated>grep -q "Archived days" .planning/phases/04-program-builder/04-UI-SPEC.md</automated>
    <automated>grep -q "routine_day" docs/program-vocabularies.md</automated>
    <automated>git diff --quiet -- apps packages ops</automated>
  </verify>
  <acceptance_criteria>
    - The Home card section names D-32 as the authority for the single-card scope and records that the
      earlier D-27 citation was a placement decision
    - The Confirmations table carries Archive Day and Restore Day rows whose copy matches
      `ArchiveDialog.tsx` string for string
    - The Duplicate row's note covers the shipped day case and still specifies no confirmation
    - The Day Deck section describes the four header controls and the Archived days section, cited to D-29
    - `docs/program-vocabularies.md` records the day-level archive, the single filtered read, the
      separate archive-reachable read, and why the sync rules still deliver archived days
    - `git diff --quiet -- apps packages ops` succeeds — this plan touched documentation only
  </acceptance_criteria>
  <done>
    The UI contract describes the screen that actually ships, its Home-card scope cites an authority
    that supports it, and the vocabulary doc explains where the day archive lives and why.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation evidence -> ledger status | The boundary this phase already crossed wrongly once, marking PROG-07 Complete from a function rather than a screen |
| decision text -> contract justification | Where authority drift entered: a placement decision quoted as a scope cap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-79 | Repudiation | a status marked Complete without evidence | high | mitigate | This plan runs last, in its own wave, and every status flip is conditioned on a named artifact in a preceding SUMMARY — the non-test call-site greps and 04-16's executed run. A missing artifact leaves the status unchanged and the reason recorded. |
| T-04-80 | Tampering | a requirement silently reworded to match what shipped | high | mitigate | The new `## Amendments` section makes the change dated, attributed to D-32, and explicitly described as a narrowing, with the superseded D-27 justification named. |
| T-04-81 | Tampering | contract and code drifting apart again | medium | mitigate | The Confirmations rows reproduce `ArchiveDialog.tsx`'s strings verbatim and the acceptance criterion requires a string-for-string match, so the next reader can diff them. |
| T-04-82 | Tampering | a documentation plan changing code | low | mitigate | `git diff --quiet -- apps packages ops` is a gate, not a guideline. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` PROG-09 reads "the next workout" and the Amendments section names D-32
- PROG-06 and PROG-07 statuses each trace to a named artifact in a preceding SUMMARY
- `04-UI-SPEC.md`'s Home card section no longer rests on D-27
- `git diff --quiet -- apps packages ops` succeeds
- WINDOWS: mark any entries this gap-closure run closed as fixed via `gsd-tools windows fixed <id>`,
  and confirm the run's own `unrun-verify` entries (native observation of the day-page controls and
  the Edit Cycle form) are open and pointed at ROADMAP Phase 999.1
</verification>

<success_criteria>
- Every PROG requirement's status is backed by a screen and an executed command, not by an exported function
- PROG-09's text and its authority agree, and the correction is on the record
- The UI contract and the vocabulary doc describe what shipped
</success_criteria>

<output>
Create `.planning/phases/04-program-builder/04-17-SUMMARY.md` when done
</output>
