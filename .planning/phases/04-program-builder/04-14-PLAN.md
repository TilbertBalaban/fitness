---
phase: 04-program-builder
plan: 14
type: execute
wave: 2
depends_on: [04-12]
gap_closure: true
files_modified:
  - apps/mobile/app/programs/library.tsx
  - apps/mobile/app/programs/__tests__/library-screen.test.ts
  - apps/mobile/lib/db/__tests__/next-up-query.test.ts
  - apps/mobile/lib/programs/__tests__/next-up.test.ts
  - apps/mobile/lib/db/__tests__/log-set.test.ts
autonomous: true
requirements: [PROG-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A program can be marked ready from the library action sheet, so routine.status can leave 'draft' for the first time in the shipped app"
    - "markRoutineReady has a non-test call site under apps/mobile/app — the grep that returned zero in 04-VERIFICATION.md now returns a file"
    - "The Mark Ready action is absent on a program that is already ready and on an archived program, rather than present and inert"
    - "partitionRoutines' ready bucket and the library row's Title-Case status word are reachable states rather than dead paths"
    - "An archived day does not appear in the Home card's rotation: loadNextUp reads through the same filtered loadProgramTree the builder does, and its day list is asserted to exclude it"
    - "Archiving the day a session was logged against rewinds the rotation rather than pushing the lifter forward, exactly as deleting one already does — countableHistory drops a session whose day is no longer in the list"
    - "Archiving a day never changes what an already-logged workout shows: session_exercise keeps its five frozen target columns and its routine_day_id (D-01, success criterion 4)"
  artifacts:
    - apps/mobile/app/programs/library.tsx
    - apps/mobile/lib/db/__tests__/next-up-query.test.ts
    - apps/mobile/lib/db/__tests__/log-set.test.ts
  key_links:
    - "`actionsForRow` -> `RoutineActionSheet` -> `handleSelectAction` -> `markRoutineReady`: three hops, and the sheet's action list is computed in exactly one place because which actions apply is a property of the row"
    - "`loadProgramTree`'s archived-day filter -> `loadNextUp` -> `resolveNextUp`: the Home rotation inherits the filter rather than reimplementing it, which is why archiving a day cannot make the builder and the Home card disagree"
    - "`countableHistory(sessions, days)` -> the archived day is absent from `days` -> its sessions stop consuming a rotation position: the same self-healing path a deleted day already takes"
  prohibitions:
    - "Status is never advanced implicitly — not on activation, not on a first session, not on a duplicate (D-31 rejected implicit advancement explicitly)"
    - "No second archived-day filter is added to next-up-query.ts or next-up.ts; both inherit loadProgramTree's"
    - "No production code under lib/programs/ or lib/db/programs/ is modified by tasks 2 and 3 — they are regressions over behaviour 04-12 already shipped, and a change there would mean the behaviour was not actually inherited"
    - "No comments are added to code by default — a comment is written only for a trap, a deliberate deviation, an external-contract quirk, or an absence"
    - "No AI attribution appears in any commit message, source file, or planning document"
    - "No npm or pnpm package is installed by this plan"
    - "No task launches a browser or drives the app; the Playwright suite belongs to 04-15"
---

<objective>
Make `routine.status` reachable from the library (D-31), and pin the two consequences of 04-12's
archived-day filter that nothing else asserts: the Home rotation, and history safety.

Purpose: `04-VERIFICATION.md` gap 3 is a **failed** truth, not a partial one — "`markRoutineReady` is
implemented and unit-tested but has no UI call site … nothing in the shipped app can produce a
'ready' routine, so `library.tsx`'s `partitionRoutines` 'ready' bucket and its 'Ready' subtitle word
are unreachable, and every program the user ever sees reads 'Draft' forever." Recorded as WINDOWS
#89. D-31 resolves it with an explicit action rather than implicit advancement, because advancing on
first activation "conflates 'I am training this' with 'I have finished writing it', and would make a
finished-but-inactive program unrepresentable".

The two regression tasks close the other half of D-29: archived days must not appear in
`resolveNextUp`'s rotation, and archiving one must not disturb success criterion 4, which is
currently VERIFIED and must stay that way.

Output: a Mark Ready action in the routine action sheet, a Home-rotation regression proving archived
days are excluded, and a D-01 regression proving an archived day leaves logged history untouched.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-program-builder/04-CONTEXT.md
@.planning/phases/04-program-builder/04-VERIFICATION.md
@.planning/phases/04-program-builder/04-12-SUMMARY.md
@docs/program-vocabularies.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new — not pre-existing, do not flag as drift):

| Symbol | Kind | File |
|---|---|---|
| `MARK_READY` action key and its `actionsForRow` branch | constant/branch | `apps/mobile/app/programs/library.tsx` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mark Ready in the routine action sheet</name>
  <files>apps/mobile/app/programs/library.tsx, apps/mobile/app/programs/__tests__/library-screen.test.ts</files>
  <read_first>
    - `apps/mobile/app/programs/library.tsx` in full — `actionsForRow` (line ~148) and its comment
      explaining why activating is hidden rather than disabled on an already-active or archived row
      (the same reasoning governs Mark Ready); `partitionRoutines` and its `ready` bucket (line ~64);
      `formatLibraryRowSubtitle` and `titleCaseStatus`; `handleSelectAction` and its switch; and the
      `mutate` funnel that never rejects.
    - `apps/mobile/components/RoutineActionSheet.tsx` in full — including its comment recording that
      the action list is **passed in** rather than derived inside the sheet, because which actions
      apply is a property of the row and computing it twice is how the two answers drift apart. Add
      nothing to this component.
    - `apps/mobile/lib/db/programs/lifecycle.ts` — `markRoutineReady` and the `READY_STATUS`
      annotation typed against `RoutineStatus`, plus `resolveLiveRoutineId`.
    - `apps/mobile/app/programs/__tests__/library-screen.test.ts` in full — how `actionsForRow` and
      the other pure helpers are asserted; the screen is never rendered in this suite.
    - `docs/program-vocabularies.md` §`ROUTINE_STATUSES` and §"Active, frozen and archived are not
      statuses" — the two-value vocabulary and why archive and activation live on other columns.
    - `.planning/phases/04-program-builder/04-UI-SPEC.md` § Program Library and its Correction Note
      item 3 (status renders Title Case, never the raw enum) — the contract the now-reachable `Ready`
      word has to satisfy.
  </read_first>
  <behavior>
    `apps/mobile/app/programs/__tests__/library-screen.test.ts`:
    - `actionsForRow({ status: 'draft', archivedAt: null, ... }, false)` includes an action whose key
      is the mark-ready key and whose label is `Mark Ready`.
    - `actionsForRow` for a row whose `status` is already `ready` omits that action entirely — absent,
      not disabled, matching how Activate is treated on the already-active row.
    - `actionsForRow` for an archived row omits it too: an archived program is not being authored, and
      offering the action would advertise a transition the user has no reason to make from the archive.
    - `actionsForRow` for the **active** draft row still includes it — a program can be both the one
      being trained and one the user has not finished writing, so activation must not gate it. This is
      the assertion that pins D-31's rejection of implicit advancement.
    - The action ordering is stable and Mark Ready sits between Activate and Duplicate, so the sheet's
      list does not reshuffle between rows.
    - `partitionRoutines` places a non-archived, non-active row whose status is `ready` into the
      `ready` bucket and not into `drafts` — the bucket 04-VERIFICATION.md calls a dead path.
    - `formatLibraryRowSubtitle({ status: 'ready', archivedAt: null, isActive: false })` contains
      `Ready` in Title Case, never the raw lowercase value.
    - The `lifecycle` module mock exposes `markRoutineReady`, and importing the screen module still
      succeeds.
  </behavior>
  <action>
    One constant, one branch, one switch case (D-31).

    Add `const MARK_READY = 'mark-ready';` beside the existing `ACTIVATE` / `DUPLICATE` / `RENAME` /
    `ARCHIVE` / `RESTORE` constants.

    In `actionsForRow`, push `{ key: MARK_READY, label: 'Mark Ready' }` after the Activate branch and
    before Duplicate, guarded by `!archived && row.status !== 'ready'`. Hidden rather than disabled,
    for the reason the Activate branch already records: an action that cannot change anything
    advertises a state the write would immediately undo.

    In `handleSelectAction`, add a `MARK_READY` case calling
    `mutate(() => markRoutineReady(row.id), \`Couldn't mark ${row.name} ready.\`)`. No confirmation:
    marking ready is not destructive and is undone by nothing the user needs protecting from — the
    Confirmations table in `04-UI-SPEC.md` has no row for it and none is being added.

    Import `markRoutineReady` from `@/lib/db/programs/lifecycle` alongside the lifecycle writes the
    screen already imports.

    Do not advance status anywhere else. D-31 considered and rejected implicit advancement on first
    activation; `createRoutine` and `duplicateRoutine` both keep hardcoding `'draft'`, which is what
    keeps "finished authoring" and "currently training" two independent facts.

    This is the wiring `04-VERIFICATION.md` gap 3 records as absent. The acceptance criterion below is
    the verifier's own grep, inverted into a gate.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- library-screen</automated>
    <automated>pnpm --filter mobile test</automated>
    <automated>pnpm --filter mobile typecheck</automated>
    <automated>pnpm --filter mobile build</automated>
    <automated>grep -rl "markRoutineReady" apps/mobile/app apps/mobile/components --include="*.tsx" | grep -v "__tests__" | grep -q .</automated>
    <automated>grep -q "Mark Ready" apps/mobile/app/programs/library.tsx</automated>
    <automated>test "$(grep -c 'markRoutineReady' apps/mobile/app/programs/library.tsx)" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "markRoutineReady" apps/mobile/app apps/mobile/components --include="*.tsx" | grep -v "__tests__"`
      names `apps/mobile/app/programs/library.tsx` — non-empty, where `04-VERIFICATION.md` recorded zero
    - `library.tsx` both imports and calls `markRoutineReady` (the count gate above), so the import is
      not a dangling one
    - `actionsForRow` omits Mark Ready on a ready row and on an archived row, and includes it on the
      active draft row — all three asserted green
    - `partitionRoutines`' `ready` bucket is covered by a test that would fail if the bucket were removed
    - `RoutineActionSheet.tsx` is unmodified — the action list stays a property of the row
    - `createRoutine` and `duplicateRoutine` still write `'draft'`; nothing advances status implicitly
    - `pnpm --filter mobile test`, `typecheck` and `build` all exit 0
  </acceptance_criteria>
  <done>
    A program can be marked ready by an explicit act, the library's `ready` bucket and its `Ready`
    subtitle word are reachable for the first time, and `status` is no longer a one-way door into
    `draft`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Archived days are out of the Home rotation</name>
  <files>apps/mobile/lib/db/__tests__/next-up-query.test.ts, apps/mobile/lib/programs/__tests__/next-up.test.ts</files>
  <read_first>
    - `apps/mobile/lib/db/programs/next-up-query.ts` in full — `loadNextUp` reads its day list through
      `loadProgramTree`, which 04-12 filtered; there is no day query of its own here and none is being
      added. Note the comment explaining why the history read is unlimited (`countableHistory` counts
      these rows to place the lifter, so a LIMIT would move them backwards).
    - `apps/mobile/lib/programs/next-up.ts` in full — `countableHistory`'s rule ("a session whose day
      was deleted stops consuming a position, so deleting a day rewinds the rotation instead of
      pushing the lifter forward"), `lastLoggedDayIndex`'s null-on-missing-day behaviour and the
      04-UI-SPEC Pitfall-5 fallback it feeds, and `resolveNextUp`'s rotation walk.
    - `apps/mobile/lib/db/__tests__/next-up-query.test.ts` in full — `fakeNextUpDb`, the twelve-select
      bound, and the comment recording that the completed-session filter is asserted **structurally**
      (which selects carried a where clause) rather than by evaluating SQL. That fake does not resolve
      conditions, so an archived-day case here must be written against what `loadNextUp` is *handed*,
      not against a where clause the fake ignores.
    - `apps/mobile/lib/programs/__tests__/next-up.test.ts` — the existing deleted-day cases, which are
      the exact shape the archived-day cases mirror.
    - `.planning/phases/04-program-builder/04-12-SUMMARY.md` — where 04-12 recorded that
      `loadProgramTree` is the single filtered read all three consumers share.
  </read_first>
  <behavior>
    `apps/mobile/lib/db/__tests__/next-up-query.test.ts`:
    - `loadNextUp` issues no day select of its own: the select count for a full load is unchanged from
      the shipped bound, proving the archived-day filter was inherited rather than re-added here. If
      the count moved, a second query was introduced and the two reads can now disagree.
    - The day select `loadNextUp` performs through `loadProgramTree` carries a where clause — recorded
      by the fake's existing where-tracking, the same structural claim this suite already makes for
      the completed-session filter. State plainly in the SUMMARY that this is structural: the
      behavioural proof that the predicate actually excludes rows lives in `programs.test.ts`'s
      condition-resolving store (04-12) and in the browser suite (04-15).

    `apps/mobile/lib/programs/__tests__/next-up.test.ts`:
    - `countableHistory(sessions, days)` where a completed session names a day that is **not** in
      `days` (the shape an archived day now produces) drops that session — identical to the
      deleted-day case, asserted here for the archive path so the two cannot drift.
    - `resolveNextUp` over a three-day routine reduced to two days rewinds rather than skipping: with
      two completed sessions against the two remaining days and one against the absent one, the next
      day is the one following the last countable session, not a position two ahead.
    - `lastLoggedDayIndex` returns null when the most recent completed session names a day absent from
      the list, and `resolveNextUp` then falls back to day index 0 — the 04-UI-SPEC Pitfall-5 rule,
      silent, never a visible error state, now also reached by archiving.
    - A routine whose every day is archived resolves to `{ kind: 'no-days' }` rather than throwing —
      the empty-day-list branch, now reachable without deleting anything.
  </behavior>
  <action>
    Regressions only. **Modify no production file in this task.** If a test here needs a source change
    to pass, the archived-day filter was not actually inherited by the Home read, and that is a defect
    to record and fix in `load-program.ts` rather than to paper over in `next-up-query.ts` (D-29).

    Add the cases above. The point of the select-count assertion is negative: it fails if someone
    "helpfully" adds an archived-day filter to `loadNextUp`, which would give the Home card its own
    opinion about which days exist. One filter, in `loadProgramTree`, is what keeps the builder deck
    and the Home card describing the same program.

    In `next-up.test.ts`, mirror the existing deleted-day cases rather than inventing a new fixture
    shape. `resolveNextUp` is pure and takes `days` as an argument, so "archived" is expressed here as
    "absent from the list the caller passed" — which is exactly what the filtered read produces, and
    saying so in the test names is what keeps the connection legible to the next reader.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- next-up</automated>
    <automated>pnpm --filter mobile test</automated>
    <automated>pnpm --filter mobile typecheck</automated>
    <automated>git diff --quiet -- apps/mobile/lib/db/programs/next-up-query.ts apps/mobile/lib/programs/next-up.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test -- next-up` exits 0 across both suites, including the rewind case, the
      Pitfall-5 fallback case and the all-days-archived case
    - `loadNextUp`'s select count for a full load is unchanged from the shipped assertion, proving no
      second day query was added
    - `git diff --quiet` over `next-up-query.ts` and `next-up.ts` succeeds — this task changed tests only
    - The SUMMARY states which archived-day claims here are structural and where the behavioural proof
      lives, rather than implying this suite evaluates SQL
  </acceptance_criteria>
  <done>
    The Home card's rotation provably inherits the archived-day filter instead of owning a second one,
    and archiving a day rewinds the rotation the same self-healing way deleting one already does.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Archiving a day never changes what a logged workout shows</name>
  <files>apps/mobile/lib/db/__tests__/log-set.test.ts</files>
  <read_first>
    - `apps/mobile/lib/db/__tests__/log-set.test.ts` — `describe('PROG-11 — editing a program never
      changes a logged session')` at line ~728 in full, including the first case
      ("resolves each helper against the row it names, never a sibling") whose purpose is to prove the
      shared store is discriminating enough for the rest of the suite to mean anything; `inMemoryDb()`
      at line ~569 and its `cascadeDelete`, which mirrors the real `routine_day -> routine_exercise ->
      routine_exercise_cycle_target` cascade; and `seedProgramAndSnapshot`.
    - `apps/mobile/lib/db/log-set.ts` — `resolvePrescriptionForCycle` and `addSessionExercise`'s copy
      of the five `target_*` columns onto `session_exercise`; the mechanism D-01 rests on.
    - `apps/mobile/lib/db/programs/days.ts` as 04-12 left it — `archiveDay`, and `removeDay` beside it.
    - `apps/api/src/db/schema/session.ts` — `sessionExercise`'s five frozen `target_*` columns and its
      no-FK `routineExerciseId`, plus `workoutSession.routineDayId`.
    - `.planning/ROADMAP.md` Phase 4 success criterion 4, and `04-VERIFICATION.md`'s row for it: this
      criterion is currently VERIFIED and the point of this task is that it stays that way.
  </read_first>
  <behavior>
    `apps/mobile/lib/db/__tests__/log-set.test.ts`, inside the existing PROG-11 describe:
    - After a session has been logged against a day and its `session_exercise` snapshot taken,
      `archiveDay(dayId)` leaves all five `target_*` columns on that `session_exercise` row exactly as
      they were.
    - `archiveDay` leaves the session's `routine_day_id` pointing at the same id, and the
      `routine_day` row still exists in the store afterwards — an archive is not a delete, so nothing
      is orphaned.
    - `archiveDay` does not cascade: the day's `routine_exercise` rows and their
      `routine_exercise_cycle_target` rows are all still present afterwards. The store's own
      `cascadeDelete` fires only on a delete, so a green result here also proves the archive did not
      take the delete path.
    - `restoreDay(dayId)` after the archive changes nothing about the snapshot either — the round trip
      is inert with respect to history in both directions.
    - The existing case `removeDay` covers stays green and unchanged: deleting still cascades. The two
      operations are deliberately different and this suite now asserts both.
  </behavior>
  <action>
    Tests only. **Modify no production file in this task.**

    Extend the existing `PROG-11 — editing a program never changes a logged session` describe rather
    than opening a new one: this is the same truth, extended to cover the new lifecycle operation
    (D-01, D-29). Reuse `seedProgramAndSnapshot` and the shared `inMemoryDb` store already in the file
    — a second fixture would let the archive case and the edit cases diverge about what a seeded
    program looks like.

    Assert the snapshot columns by value, not by identity: the whole claim is that a frozen copy stays
    frozen, and comparing object references would pass even if both sides moved together.

    This is the regression the planning context names as non-negotiable — success criterion 4 is
    currently VERIFIED, proven twice (7 unit regressions here plus 6 e2e against live Postgres), and a
    new day-lifecycle operation is exactly the kind of change that quietly breaks it. The
    corresponding server-side proof (archiving emits no tombstones and leaves children present) is
    04-12's `program-sync` e2e; this is the client half.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- log-set</automated>
    <automated>pnpm --filter mobile test</automated>
    <automated>pnpm --filter mobile typecheck</automated>
    <automated>git diff --quiet -- apps/mobile/lib/db/log-set.ts apps/mobile/lib/db/programs/days.ts</automated>
  </verify>
  <acceptance_criteria>
    - The PROG-11 describe gains the archive and restore cases and every pre-existing case in it stays
      green and unedited
    - The archive cases assert the five snapshot columns by value, the surviving `routine_day` row, and
      the surviving `routine_exercise` / `routine_exercise_cycle_target` children
    - `git diff --quiet` over `log-set.ts` and `days.ts` succeeds — this task changed tests only
    - `pnpm --filter mobile test -- log-set` exits 0
  </acceptance_criteria>
  <done>
    Success criterion 4 is still true after the day lifecycle grew an archive: a logged workout shows
    the same numbers, points at the same day, and none of the program rows beneath it were destroyed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library action sheet -> `routine.status` write | A user gesture crosses into a write on a column with a Postgres CHECK constraint |
| archived-day filter -> two independent readers | The builder and the Home card must not form separate opinions about which days exist |
| day lifecycle -> already-logged history | The boundary success criterion 4 is defined on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-67 | Tampering | a status value the server rejects | high | mitigate | `markRoutineReady` writes from the `READY_STATUS` constant typed against `RoutineStatus`, so a value outside `ROUTINE_STATUSES` fails the typecheck rather than the Postgres `routine_status_check`; the screen adds no literal of its own. |
| T-04-68 | Repudiation | archive silently rewriting logged history | high | mitigate | The PROG-11 describe gains cases asserting the five frozen `session_exercise` columns, the surviving `routine_day_id` and the surviving cascade children by value after an archive and after a restore. |
| T-04-69 | Tampering | a second archived-day filter in the Home read | medium | mitigate | `loadNextUp`'s select count is asserted unchanged and `git diff --quiet` gates that no production file under `lib/programs/` or `next-up-query.ts` was touched — a second query would move the count and fail. |
| T-04-70 | Spoofing | an action offered on a row it cannot act on | low | mitigate | Mark Ready is omitted on already-ready and archived rows, asserted green, matching how Activate is already hidden rather than disabled. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
- `pnpm --filter mobile test`, `pnpm --filter mobile typecheck` and `pnpm --filter mobile build` exit 0
- `npx turbo run typecheck lint` exits 7/7 successful
- A non-test grep across `apps/mobile/app` and `apps/mobile/components` returns a reference to `markRoutineReady`
- `git diff` shows tasks 2 and 3 changed test files only
- WINDOWS: mark #89 fixed via `gsd-tools windows fixed 89` once Mark Ready is wired and green — that
  entry is the ledger record of `status` being unreachable
</verification>

<success_criteria>
- `routine.status` can advance to `ready` from the shipped app, by an explicit act, and only from a
  row where that transition means something
- The library's `ready` bucket and `Ready` subtitle word are reachable states
- An archived day is absent from the Home card's rotation and archiving one rewinds rather than skips
- Archiving a day leaves every already-logged workout showing exactly what it showed before
</success_criteria>

<output>
Create `.planning/phases/04-program-builder/04-14-SUMMARY.md` when done
</output>
