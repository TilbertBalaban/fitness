---
phase: 04-program-builder
plan: 16
type: execute
wave: 4
depends_on: [04-15]
gap_closure: true
files_modified:
  - apps/mobile/e2e/program-day-lifecycle.spec.ts
  - apps/mobile/playwright.config.ts
autonomous: true
requirements: [PROG-06, PROG-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Duplicating a day from the shipped day page is proven by an executed browser run: the copy appears in the deck and a real routine_day row exists with the copied exercises beneath it"
    - "Archiving a day is proven to be an archive and not a delete: the row is still in the database with a non-null archived_at, and its exercises are still there"
    - "An archived day leaves the deck and appears in the Archived days section, and restoring it puts it back — the full round trip, driven, not asserted"
    - "Converting an existing cycle to time off with a duration is proven from the shipped Edit Cycle form: the row ends with kind time_off and a non-null duration_days (D-30, PROG-06)"
    - "Saving a time-off conversion with no duration surfaces the validation message and writes nothing — the trap 04-VERIFICATION.md gap 2 described is unreachable from the form"
    - "The spec is registered in the durability project and runs in the same single-worker lane as every other real-database case"
  artifacts:
    - apps/mobile/e2e/program-day-lifecycle.spec.ts
  key_links:
    - "`readRoutineDayRaw(...).archived_at` -> the archive/delete distinction: the rendered deck looks identical either way, so only the raw row can tell them apart"
    - "`readRoutineCycleRaw(...).duration_days` -> PROG-06: a time-off cycle with a null duration is the exact inert state gap 2 recorded, and this is the read that proves it cannot be produced"
    - "playwright.config.ts's durability `testMatch` list -> the spec actually running: a spec file that is not listed is silently never executed, which would be a green suite that asserts nothing"
  prohibitions:
    - "No production source file is modified by this plan — if a case cannot pass without one, that is a defect to record and fix in its owning file, not to work around in the spec"
    - "No case is marked skipped, fixmed or conditionally skipped: a case that cannot reach its dependencies must fail loudly, per this repository's suite-integrity discipline"
    - "The spec goes in the durability project, never the sync project — it needs a browser and nothing else, no PowerSync Service, no API, no Postgres"
    - "No comments are added to code by default — a comment is written only for a trap, a deliberate deviation, an external-contract quirk, or an absence"
    - "No AI attribution appears in any commit message, source file, or planning document"
    - "No npm or pnpm package is installed by this plan"
---

<objective>
Drive the day-lifecycle controls and the Edit Cycle conversion in a real browser against a real
`@powersync/web` database, and execute the result.

Purpose: this whole gap-closure run exists because three tested functions had no way in, and the unit
suite was green throughout. `04-VERIFICATION.md` names the limitation directly: "every UI surface in
this phase is verified by unit tests + typecheck + a successful web export only. That is a real
limitation of the evidence, not a formality." `.claude/CLAUDE.md` grants standing authorization for
Playwright in this repository, notes Chromium is already installed, and instructs preferring an
executed spec to an asserted one. This is that spec.

It also carries the PROG-06 evidence. Gap 2's conversion trap was closed in code before this run began
(`setCycleKind`/`setCycleDuration` were replaced by a single validated `updateCycle`, and the Edit
Cycle form gained the Days off field D-30 asks for) — but that fix has never been driven through the
form. This plan executes it rather than taking the diff's word for it.

Output: `apps/mobile/e2e/program-day-lifecycle.spec.ts`, registered and green.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-program-builder/04-13-SUMMARY.md
@.planning/phases/04-program-builder/04-15-SUMMARY.md
@.planning/phases/04-program-builder/04-VERIFICATION.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new — not pre-existing, do not flag as drift):

| Symbol | Kind | File |
|---|---|---|
| `program-day-lifecycle` durability spec | test suite | `apps/mobile/e2e/program-day-lifecycle.spec.ts` |

<tasks>

<task type="tracer">
  <name>Task 1: Duplicate, archive and restore a day, driven end to end</name>
  <files>apps/mobile/e2e/program-day-lifecycle.spec.ts, apps/mobile/playwright.config.ts</files>
  <read_first>
    - `apps/mobile/e2e/gym-profiles.spec.ts` in full — the closest analog: it mounts a real screen
      through the harness, drives it by accessibility label, and reads rows back raw. Copy its
      `openHarness`/`page.goto('/__durability')`/`durability-harness-ready` opening, its
      `HarnessWindow` typing, and especially its comment recording that `page.evaluate` serializes
      only the passed function's source and carries no outer closure — which is why every callback
      re-declares its cast inline.
    - `apps/mobile/playwright.config.ts` in full — the `durability` project's `testMatch` array, its
      `workers: 1` and the long comment explaining why (real WASM SQLite plus a SharedWorker per page
      against one dev server), and the `sync` project which this spec must **not** join.
    - `apps/mobile/app/__durability.web.tsx` as 04-15 left it — `seedRoutineTree`, `openProgramsScreen`,
      `readRoutineDayRaw`, `readRoutineDaysRaw`, `openWithFilename` and `close`.
    - `apps/mobile/app/(tabs)/programs.tsx` as 04-13 and 04-15 left it — the exact
      `accessibilityLabel` strings on the day-page controls (`Rename {name}`, `Duplicate {name}`,
      `Archive {name}`, `Remove {name}`, `Restore {name}`, `Add exercises to {name}`), the
      `Archived days` heading, and the `ArchiveDialog` confirm/cancel labels the confirmation renders.
    - `apps/mobile/components/DayDeck.tsx` — `TabView` with `renderTabBar={() => null}`: only the
      current page is guaranteed mounted, so a spec must not assert on a day page it has not paged to.
      Prefer asserting through `readRoutineDaysRaw` plus the currently visible page rather than
      expecting every day's controls in the DOM at once.
    - `apps/mobile/components/ArchiveDialog.tsx` as 04-13 left it — the `day` subject's exact heading
      and confirm-label strings.
  </read_first>
  <action>
    Create `apps/mobile/e2e/program-day-lifecycle.spec.ts` and add `'program-day-lifecycle.spec.ts'`
    to the `durability` project's `testMatch` in `playwright.config.ts`. It needs a browser and
    nothing else — no PowerSync Service, no API, no Postgres — so it belongs in that project and not
    in `sync`. A spec file absent from `testMatch` is never executed and its greenness means nothing;
    the registration is not a formality.

    Open the harness the way `gym-profiles.spec.ts` does, seed with `seedRoutineTree`, then
    `openProgramsScreen`.

    Three cases (D-29, PROG-07):

    - **Duplicate.** With the deck on the `Push` page, press `Duplicate Push`. Poll
      `readRoutineDaysRaw(routineId)` until it returns three rows, and assert exactly one is named
      `Push copy`, that its `order_index` is greater than every other day's (it is appended), and that
      it has the same number of `routine_exercise` children as the source. Assert no confirmation
      dialog appeared — `04-UI-SPEC.md`'s Confirmations table says a duplicate needs none, and a
      dialog materialising here would be a silent contract break.
    - **Archive.** Press `Archive Push`, confirm in the dialog, then assert three things that only
      hold together for an archive and not for a delete: `readRoutineDayRaw(pushDayId)` still returns
      a row, its `archived_at` is non-null, and the day's `routine_exercise` children are still
      present. Then assert the UI: `Push` is gone from the deck and the `Archived days` heading is
      visible with a `Restore Push` control beneath it. The raw read is the load-bearing half — the
      rendered deck looks identical whether the row was archived or destroyed.
    - **Restore.** Press `Restore Push`, confirm, then assert `archived_at` is back to null, the day is
      back in the deck, and the `Archived days` section is **absent** rather than rendered empty.

    Use `expect.poll` against the raw reads rather than fixed waits, matching how every other spec in
    this suite waits on a real database. Address controls by accessibility label, never by test id or
    DOM structure — the labels are the contract `04-UI-SPEC.md` sets and driving them is what proves
    the screen is usable by assistive tech as well as by a pointer.

    **Execute the spec.** `pnpm --filter mobile test:e2e:durability -- program-day-lifecycle`. A green
    run is the evidence this plan exists to produce; do not report the cases as written without one.
    If a case fails because the screen is wrong, fix the screen in its own file and record the
    correction as a WINDOWS entry — that is the spec doing its job, and it is the outcome this
    repository's four prior harness-found defects predict.

    Modify no production source otherwise.
  </action>
  <verify>
    <automated>pnpm --filter mobile test:e2e:durability -- program-day-lifecycle</automated>
    <automated>pnpm --filter mobile typecheck</automated>
    <automated>grep -q "program-day-lifecycle.spec.ts" apps/mobile/playwright.config.ts</automated>
    <automated>test "$(grep -c 'test.skip\|test.fixme' apps/mobile/e2e/program-day-lifecycle.spec.ts)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter mobile test:e2e:durability -- program-day-lifecycle` has been **executed** and
      reports every case passing; the SUMMARY quotes the run's case count and result
    - `program-day-lifecycle.spec.ts` is listed in the `durability` project's `testMatch` and not in
      `sync`
    - The archive case asserts the surviving row and its non-null `archived_at` through
      `readRoutineDayRaw`, not only through the rendered deck
    - The restore case asserts the `Archived days` section is absent, not empty
    - The duplicate case asserts no confirmation dialog appeared
    - No case is skipped or fixmed
    - `git diff` shows no production source change, or a change accompanied by a WINDOWS entry
      explaining the defect the spec found
  </acceptance_criteria>
  <done>
    Duplicating, archiving and restoring a training day have been driven through the shipped screen in
    a real browser against a real database, and the archive is proven to be a timestamp rather than a
    destroyed row.
  </done>
</task>

<task type="auto">
  <name>Task 2: Converting a cycle to time off, driven through the Edit Cycle form</name>
  <files>apps/mobile/e2e/program-day-lifecycle.spec.ts</files>
  <read_first>
    - `apps/mobile/app/(tabs)/programs.tsx` — the Edit Cycle form block (around line ~719): the
      `Cycle name` field, the three kind `Pressable`s and their `accessibilityLabel`s from
      `CYCLE_KIND_OPTIONS`, the `Days off` `TextField` rendered only when the staged kind is time off,
      the `Save` `PrimaryButton`, and `handleSaveCycleEdit`'s validate-then-write order. Also
      `handleEditCycle`, and `parseCycleDuration` / `cycleDurationFieldValue`.
    - `apps/mobile/components/CycleStrip.tsx` — the chip labels and the inline `Edit Cycle` control,
      which is visible only while a cycle is selected; the spec must select the chip first.
    - `apps/mobile/lib/db/programs/cycles.ts` — `validateCycle`'s `duration-required` and
      `duration-too-small` codes and `cycleErrorMessage`'s exact user-facing strings, and
      `updateCycle`'s single-write comment recording that splitting the write into a kind write and a
      duration write "is what let the Edit Cycle form produce `kind = 'time_off'` with
      `duration_days = null` — a cycle `resolveNextUp` can only step over".
    - `.planning/phases/04-program-builder/04-VERIFICATION.md` gap 2 in full — the exact trap being
      proven unreachable.
    - `apps/mobile/lib/programs/next-up.ts` lines ~145–158 — the defensive skip that remains for the
      concurrent-edit case, and its comment saying the user repairs the duration in this form.
  </read_first>
  <action>
    Two more cases in the same spec file, covering D-30 and PROG-06.

    - **Conversion succeeds with a duration.** Select the `Week 1` chip, press `Edit Cycle`, press the
      time-off kind control, assert the `Days off` field appears (it is absent for the other two
      kinds — that conditional render is the form's whole shape), type `7`, press `Save`. Poll
      `readRoutineCycleRaw(cycleId)` until `kind` is `time_off` and assert `duration_days` is `7`. This
      is the exact state gap 2 said was unreachable: a converted time-off cycle that
      `resolveNextUp` will actually honour.
    - **Conversion with no duration writes nothing.** Re-open the form on a training cycle, press the
      time-off control, leave `Days off` empty, press `Save`. Assert the form stays open, the
      validation message `Time off needs a length in days.` is visible, and — the load-bearing half —
      `readRoutineCycleRaw(cycleId)` still reports `kind` as `training` with a null `duration_days`.
      A form that showed the error but had already written the kind would be the same trap wearing a
      warning label.

    Reuse the spec's existing harness opening rather than adding a second one, and seed through the
    same `seedRoutineTree` (04-15 seeds `Week 1` as a training cycle precisely for this).

    Modify no production source. If the second case fails — if a kind press writes before Save — that
    is gap 2 still open in a form that looks fixed, and it must be fixed in `programs.tsx` with a
    WINDOWS entry rather than accommodated in the spec.
  </action>
  <verify>
    <automated>pnpm --filter mobile test:e2e:durability -- program-day-lifecycle</automated>
    <automated>pnpm --filter mobile test</automated>
    <automated>pnpm --filter mobile typecheck</automated>
    <automated>npx turbo run typecheck lint</automated>
  </verify>
  <acceptance_criteria>
    - Both cycle cases are present and green in an **executed** run, reported with the full spec's case
      count in the SUMMARY
    - The success case asserts `duration_days` from `readRoutineCycleRaw`, not merely that the chip
      changed appearance
    - The failure case asserts the stored row is **unchanged**, not only that an error message appeared
    - The `Days off` field is asserted absent for a non-time-off kind and present for time off
    - `git diff` shows no production source change, or a change accompanied by a WINDOWS entry
    - `pnpm --filter mobile test` and `npx turbo run typecheck lint` both exit 0
  </acceptance_criteria>
  <done>
    PROG-06's conversion path has been driven through the shipped Edit Cycle form: a cycle can be
    turned into time off with a length, and cannot be turned into one without.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered UI vs stored row | An archive and a delete look identical in the deck; only the raw row distinguishes them |
| spec file vs `testMatch` | An unregistered spec never runs, and a suite that never runs is green |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-75 | Repudiation | an archive that is really a delete passing the UI assertions | high | mitigate | Every archive case asserts the surviving `routine_day` row, its non-null `archived_at` and its surviving `routine_exercise` children through `readRoutineDayRaw`/`readRoutineDaysRaw`, not through the deck. |
| T-04-76 | Information Disclosure | a spec that is written but never executed | high | mitigate | The spec is added to the `durability` project's `testMatch`, grep-gated, and the acceptance criteria require an executed run with its case count quoted in the SUMMARY rather than a claim that it would pass. |
| T-04-77 | Tampering | a durationless time-off cycle still writable from the form | high | mitigate | The negative case asserts the stored row is unchanged after a failed save, so a form that validates only after writing the kind fails here. |
| T-04-78 | Denial of Service | a skipped case reading as a pass | medium | mitigate | Skips and fixmes are grep-gated to zero, matching this repository's Jest suite-integrity reporter discipline (`scripts/jest-suite-integrity.cjs`) — a zero-test or all-skipped run must fail identically. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; Chromium is already present in `~/Library/Caches/ms-playwright`. |
</threat_model>

<verification>
- `pnpm --filter mobile test:e2e:durability -- program-day-lifecycle` executed and green, case count recorded
- `pnpm --filter mobile test:e2e:durability` executed once in full, to confirm the new spec has not
  destabilised the single-worker lane it shares with sixteen other real-database specs
- `pnpm --filter mobile test` and `npx turbo run typecheck lint` exit 0
- WINDOWS: one `unrun-verify` entry recording that these interactions remain unobserved on iOS and
  Android (no Xcode, no Android SDK — ROADMAP Phase 999.1); plus a `deviation` entry for any
  production fix the spec forced
</verification>

<success_criteria>
- Duplicate, archive and restore of a training day are proven by an executed browser run against a
  real database, with the archive proven to be a timestamp rather than a destroyed row
- A cycle can be converted to time off with a duration, and cannot be converted without one
- The spec is registered in the durability project and the whole lane still passes
</success_criteria>

<output>
Create `.planning/phases/04-program-builder/04-16-SUMMARY.md` when done
</output>
