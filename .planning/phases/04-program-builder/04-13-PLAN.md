---
phase: 04-program-builder
plan: 13
type: execute
wave: 2
depends_on: [04-12]
gap_closure: true
files_modified:
  - apps/mobile/components/ArchiveDialog.tsx
  - apps/mobile/components/__tests__/ArchiveDialog.test.tsx
  - apps/mobile/app/(tabs)/programs.tsx
  - apps/mobile/app/(tabs)/__tests__/programs-screen.test.ts
autonomous: true
requirements: [PROG-07]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The day page carries a Duplicate control that reaches duplicateDay — a non-test file under apps/mobile/app references it, which is the evidence the verifier's grep found missing"
    - "The day page carries an Archive control that reaches archiveDay, behind the house confirmation dialog"
    - "An archived day leaves the day deck and appears in an Archived days section beneath it, where a Restore control returns it to the deck"
    - "The Archived days section is absent, not empty, when the routine has no archived days — the own-empty-omits-header convention DetailSection and the library already follow"
    - "Duplicating a day needs no confirmation and archiving one does, matching 04-UI-SPEC.md's Confirmations table"
    - "ArchiveDialog gains a day variant rather than a second dialog component — one modal surface, four subjects"
    - "Every new control carries an explicit accessibility label naming its day, because a program with three days renders three of each"
  artifacts:
    - apps/mobile/components/ArchiveDialog.tsx
    - apps/mobile/app/(tabs)/programs.tsx
  key_links:
    - "day page control -> `mutate(...)` -> `archiveDay`/`restoreDay`/`duplicateDay` -> `reloadTree`: the screen's single write funnel, which is what makes a failed write land in the banner instead of silently reverting"
    - "`loadArchivedDays` -> the Archived days section: the only surface from which a restore is reachable, so a missing call here makes the archive one-way in practice even though the data says otherwise"
    - "`DayDeck`'s internal clamp -> archiving the currently-shown day: the deck's index is component state and re-clamps when the day list shrinks, so nothing extra is needed to avoid a blank page"
  prohibitions:
    - "The existing Remove control and removeDay stay exactly as they are — the hard delete and its three-level tombstone cascade are verified behaviour and are not in this plan's scope"
    - "No second archived-day filter is added to this screen; the day list comes from loadProgramTree, which already filters"
    - "No confirmation dialog is added to the Duplicate control"
    - "No comments are added to code by default — a comment is written only for a trap, a deliberate deviation, an external-contract quirk, or an absence"
    - "No AI attribution appears in any commit message, source file, or planning document"
    - "No npm or pnpm package is installed by this plan"
    - "No task launches a browser or drives the app; the Playwright suite belongs to 04-15"
---

<objective>
Give the day page the three controls D-29 asks for — Duplicate Day, Archive Day, Restore Day — so
that PROG-07's "individual workouts" half is reachable from the shipped app.

Purpose: `04-VERIFICATION.md` gap 1 is not a missing implementation, it is a missing way in.
`duplicateDay` is "implemented, tested and exported but has ZERO UI call sites — no screen, action
sheet or control in the shipped app invokes it", and the day-page header "offers Rename and Remove
only". 04-12 built the archive column and the filtered reads; this plan is the reach. Per D-29 the
requirement text is honoured as written rather than amended to match what shipped.

Output: a day page whose header offers Rename, Duplicate, Archive and Remove, an Archived days
section beneath the deck with a Restore control, and an `ArchiveDialog` day variant.
</objective>

<execution_context>
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/workflows/execute-plan.md
@/Users/tilbertbalaban/work/fitness/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-program-builder/04-CONTEXT.md
@.planning/phases/04-program-builder/04-UI-SPEC.md
@.planning/phases/04-program-builder/04-12-SUMMARY.md
@apps/mobile/app/programs/library.tsx
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new — not pre-existing, do not flag as drift):

| Symbol | Kind | File |
|---|---|---|
| `'day'` member of `ArchiveDialogSubject` | union member | `apps/mobile/components/ArchiveDialog.tsx` |
| `duplicateDayName` | function | `apps/mobile/app/(tabs)/programs.tsx` |
| `handleDuplicateDay`, `handleArchiveDay`, `handleRestoreDay` | handlers | `apps/mobile/app/(tabs)/programs.tsx` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ArchiveDialog gains a day subject</name>
  <files>apps/mobile/components/ArchiveDialog.tsx, apps/mobile/components/__tests__/ArchiveDialog.test.tsx</files>
  <read_first>
    - `apps/mobile/components/ArchiveDialog.tsx` in full — the `ArchiveDialogSubject` union, the `COPY`
      map's three existing entries, and the comment above it recording which strings come verbatim
      from a UI-SPEC Copywriting Contract row and which were written directly because no contract row
      exists. The `gym` entry is the precedent for extending the union from a later phase's spec.
    - `apps/mobile/components/__tests__/ArchiveDialog.test.tsx` in full — how the existing subjects are
      asserted, including the `unarchiving` variant's dropped destructive fill.
    - `.planning/phases/04-program-builder/04-UI-SPEC.md` § Confirmations — the row reading
      "Duplicate (program or single day) — **None**", which is why no dialog is added for duplicate,
      and the Archive/Restore Program rows whose shape the day copy mirrors.
  </read_first>
  <behavior>
    `apps/mobile/components/__tests__/ArchiveDialog.test.tsx`:
    - `subject="day"` with `unarchiving` unset renders the heading `Archive Day`, a body naming that
      logged workouts stay in history, and a confirm control labelled `Archive` carrying the
      destructive fill.
    - `subject="day"` with `unarchiving` renders the heading `Restore Day`, a body saying the day will
      reappear in this program, and a confirm control labelled `Restore` **without** the destructive
      fill — restoring is not a destructive act, the same treatment the program and gym variants get.
    - The three pre-existing subjects render exactly what they rendered before; the union grew, no
      existing copy moved.
    - The day variant renders the same two-button row, the same 48x48 control geometry and the same
      overlay as every other subject — assert the control count and the accessibility roles rather
      than re-asserting layout classes.
  </behavior>
  <action>
    Add `'day'` to `ArchiveDialogSubject` and a fourth entry to `COPY`. No new component, no new
    props, no branching outside the map — the union's fourth extension point, exactly as `gym` was
    its third.

    Copy, confirmed at 04-12's decision checkpoint:

    - archive — heading `Archive Day`, body
      `Archiving removes it from this program, but any workouts you logged from it stay in your history. Archive anyway?`,
      confirm label `Archive`.
    - unarchive — heading `Restore Day`, body `This day will reappear in this program.`, confirm
      label `Restore`.

    These strings are not in `04-UI-SPEC.md`'s Copywriting Contract, because that contract predates
    D-29. Extend the comment above `COPY` to say so, in the same shape the existing note uses for the
    exercise unarchive variant — that comment is load-bearing provenance for which strings a future
    editor may rewrite freely and which are lifted from a fixed table. 04-16 adds the rows to the
    UI-SPEC's Confirmations table so the contract and the code agree again.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- ArchiveDialog</automated>
    <automated>pnpm --filter mobile typecheck</automated>
    <automated>grep -q "Archive Day" apps/mobile/components/ArchiveDialog.tsx</automated>
    <automated>grep -q "Restore Day" apps/mobile/components/ArchiveDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ArchiveDialogSubject` has four members and `COPY` has four entries
    - `pnpm --filter mobile test -- ArchiveDialog` exits 0 including both day cases and the three
      unchanged subjects
    - The restore variant renders without the destructive fill, asserted rather than assumed
    - No second confirmation component was introduced
  </acceptance_criteria>
  <done>
    One modal surface now speaks for four subjects, and archiving or restoring a day reads in the same
    voice as archiving or restoring a program.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Archive and Restore on the day page</name>
  <files>apps/mobile/app/(tabs)/programs.tsx, apps/mobile/app/(tabs)/__tests__/programs-screen.test.ts</files>
  <read_first>
    - `apps/mobile/app/(tabs)/programs.tsx` in full, and specifically: the `mutate` callback (line
      ~315) that is this screen's single write funnel and the reason a failed write reaches the banner
      rather than silently reverting; `handleRemoveDay` and the day-page header block inside
      `DayDeck`'s `renderDay` (line ~795 onward), which today renders a rename `Pressable` and a
      Remove `Pressable`; the `renamingDayId`/`renameValue` state pair as the model for a
      dialog-confirmation state pair; and `reloadTree`.
    - `apps/mobile/app/programs/library.tsx` — `handleSelectAction`'s `ARCHIVE`/`RESTORE` cases and
      `handleConfirm`, the shipped pattern for "open a confirm dialog, then call the write on confirm".
      Note the `confirming: { routineId, unarchiving }` state shape; the day equivalent is
      `{ dayId, unarchiving }`.
    - `apps/mobile/lib/db/programs/days.ts` as 04-12 left it — `archiveDay`, `restoreDay`,
      `loadArchivedDays` and `ArchivedDayRow`.
    - `apps/mobile/app/(tabs)/__tests__/programs-screen.test.ts` — the `jest.mock` block at the top
      (the `days` module mock must gain the three new functions or the screen import fails), and the
      pure-helper assertion style this file uses; the screen itself is never rendered here.
    - `.planning/phases/04-program-builder/04-UI-SPEC.md` § Phase-Wide Rules R4 — new surfaces wrap and
      grow, never truncate; a header row that now holds four controls must wrap rather than shrink.
  </read_first>
  <behavior>
    `apps/mobile/app/(tabs)/__tests__/programs-screen.test.ts`:
    - The `days` module mock exposes `archiveDay`, `restoreDay` and `loadArchivedDays`, and importing
      the screen module still succeeds — a missing mock entry is how this suite fails first.
    - A pure helper deriving the Archived days section's visibility returns false for an empty
      archived list and true for a non-empty one, so the section is absent rather than rendered empty.
    - The archived-day rows the section renders come back in order-then-id order for a list given out
      of order — the same total ordering the deck uses, so a restored day does not appear to jump.
  </behavior>
  <action>
    Two controls and one section, all wired through the screen's existing `mutate` funnel (D-29).

    **State.** Add `const [confirmingDay, setConfirmingDay] = useState<{ dayId: string; unarchiving: boolean } | null>(null)`
    and `const [archivedDays, setArchivedDays] = useState<ArchivedDayRow[]>([])`, mirroring
    `library.tsx`'s `confirming` shape exactly rather than inventing a second one.

    **Loading.** `reloadTree` already runs on mount and after every mutation; extend it to also call
    `loadArchivedDays(routineId, db)` and set `archivedDays`. One reload path, not two — the archived
    list and the tree must never disagree about which days exist, and two independent loaders is how
    they start to.

    **Handlers.** `handleArchiveDay(dayId)` sets `confirmingDay` to `{ dayId, unarchiving: false }`;
    `handleRestoreDay(dayId)` sets it to `{ dayId, unarchiving: true }`; `handleConfirmDayArchive`
    clears the state and calls `mutate(() => archiveDay(dayId), "Couldn't archive that day.")` or
    `mutate(() => restoreDay(dayId), "Couldn't restore that day.")`. Route both through `mutate`, not
    through a bare `await`: that funnel is what sets the banner, reloads the tree and guarantees no
    async handler reaches `onPress` as an unhandled rejection.

    **Day page header.** Add an Archive control beside the existing Rename and Remove, labelled
    `Archive {day.name}`, 48x48 minimum, `text-destructive` like Remove. The header row now holds up
    to four controls: give it `flex-wrap` so it grows downward at large font scales instead of
    shrinking below the 48x48 minimum (R4). **Leave Remove and `removeDay` exactly as they are** —
    the hard delete and its three-level tombstone cascade are verified behaviour and changing them is
    not in this plan's scope.

    **Archived days section.** Below the deck and above the "Add Day" field, render — only when
    `archivedDays` is non-empty — a `Text` heading `Archived days` and one row per archived day: the
    day name at `opacity: 0.6` (the receded treatment the library row and the cycle strip's time-off
    chip already use, so "archived" and "off" keep reading the same way), and a 48x48 Restore
    `Pressable` labelled `Restore {day.name}`. Absent, not empty, when there are none — the
    own-empty-omits-header convention.

    **Dialog.** When `confirmingDay` is set, render `<ArchiveDialog subject="day" unarchiving={confirmingDay.unarchiving} ... />`
    in the same early-return position `library.tsx` uses for its own confirmation, so the dialog owns
    the screen rather than layering over a live deck.

    Archiving the day the deck is currently showing needs nothing extra: `DayDeck` owns its page index
    as component state and re-clamps whenever the day list shrinks, which is the same path a delete
    already takes.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- programs-screen</automated>
    <automated>pnpm --filter mobile test</automated>
    <automated>pnpm --filter mobile typecheck</automated>
    <automated>pnpm --filter mobile build</automated>
    <automated>grep -rl "archiveDay" apps/mobile/app --include="*.tsx" | grep -v "__tests__" | grep -q .</automated>
    <automated>grep -rl "restoreDay" apps/mobile/app --include="*.tsx" | grep -v "__tests__" | grep -q .</automated>
    <automated>grep -rl "loadArchivedDays" apps/mobile/app --include="*.tsx" | grep -v "__tests__" | grep -q .</automated>
    <automated>grep -q 'subject="day"' "apps/mobile/app/(tabs)/programs.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "archiveDay" apps/mobile/app --include="*.tsx" | grep -v "__tests__"` names
      `apps/mobile/app/(tabs)/programs.tsx` — a non-test call site, which is the exact evidence
      `04-VERIFICATION.md` looked for and did not find
    - The same grep is non-empty for `restoreDay` and for `loadArchivedDays`
    - `programs.tsx` renders `ArchiveDialog` with `subject="day"` and threads `unarchiving`
    - Both writes go through the screen's `mutate` funnel, so a failure sets `mutationError` and
      reloads the tree
    - The Archived days section is rendered only when the archived list is non-empty
    - The header row wraps rather than shrinking, and every new control carries an accessibility label
      naming its day
    - `removeDay` and the Remove control are byte-identical to how 04-02 left them
    - `pnpm --filter mobile test`, `typecheck` and `build` all exit 0
  </acceptance_criteria>
  <done>
    A day can be archived from the page it lives on, disappears from the deck, and comes back from an
    Archived days section beneath it — with the same confirmation voice the program library already
    uses.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Duplicate Day, wired to the function that has been waiting for it</name>
  <files>apps/mobile/app/(tabs)/programs.tsx, apps/mobile/app/(tabs)/__tests__/programs-screen.test.ts</files>
  <read_first>
    - `apps/mobile/lib/db/programs/duplicate-routine.ts` line ~144 onward — `duplicateDay`'s signature
      `({ routineDayId, name }, db)`, its `DuplicateResult` return, the `Day name is required` and
      `Day not found` throws the screen's error copy has to absorb, and the comment recording that a
      copied override keeps its `cycleId` because the copy lives under the same cycles.
    - `apps/mobile/app/programs/library.tsx` `handleSelectAction`'s `DUPLICATE` case — the shipped
      naming convention (`${row.name} copy`) and the fact that duplicating a *program* navigates into
      the copy. A day has nowhere to navigate to; say so in the SUMMARY rather than inventing a
      controlled deck index.
    - `apps/mobile/app/(tabs)/programs.tsx` as Task 2 left it — the day-page header row and `mutate`.
    - `.planning/phases/04-program-builder/04-UI-SPEC.md` § Confirmations — the
      "Duplicate (program or single day) — **None**" row: additive and non-destructive, so no dialog.
  </read_first>
  <behavior>
    `apps/mobile/app/(tabs)/__tests__/programs-screen.test.ts`:
    - `duplicateDayName('Push')` returns `Push copy` — the same suffix convention the library uses for
      a duplicated program, so the two copy operations do not name their output differently.
    - `duplicateDayName('  Push  ')` returns a trimmed base plus the suffix; `duplicateDay` throws on a
      blank name and the screen must never hand it one.
    - `duplicateDayName('Push copy')` returns `Push copy copy` — duplicating a duplicate is allowed and
      produces a distinct, if inelegant, name; deduplicating names is deliberately not attempted,
      because a day name is not unique in this model and pretending otherwise would need a uniqueness
      rule the schema does not have.
    - The `duplicate-routine` module mock exposes `duplicateDay`, and importing the screen module
      still succeeds.
  </behavior>
  <action>
    Export a pure `duplicateDayName(name: string): string` returning the trimmed name suffixed with
    ` copy`, and add a `handleDuplicateDay(dayId, name)` that calls
    `mutate(() => duplicateDay({ routineDayId: dayId, name: duplicateDayName(name) }), "Couldn't duplicate that day.")`.

    Add the control to the day-page header beside Rename, labelled `Duplicate {day.name}`, 48x48
    minimum, `text-accent` (it is additive, not destructive). No confirmation — 04-UI-SPEC.md's
    Confirmations table says none for a duplicate, program or day.

    The copy is appended at the end of the day order by `duplicateDay` itself. Do not add a deck
    navigation to it: `DayDeck` owns its index as component state with no controlled prop, and adding
    one to jump to the copy would be a structural change to the deck for a convenience this phase did
    not ask for. The reload re-renders the deck with the new page present at the end; the user swipes.

    This is the wiring `04-VERIFICATION.md` gap 1 records as absent — `duplicateDay` has been complete,
    correct and tested since 04-11 with zero non-test references. The acceptance criterion below is
    the grep the verifier ran, inverted into a gate (D-29).
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- programs-screen</automated>
    <automated>pnpm --filter mobile test</automated>
    <automated>pnpm --filter mobile typecheck</automated>
    <automated>pnpm --filter mobile build</automated>
    <automated>grep -rl "duplicateDay" apps/mobile/app apps/mobile/components --include="*.tsx" | grep -v "__tests__" | grep -q .</automated>
    <automated>grep -q "duplicateDayName" "apps/mobile/app/(tabs)/programs.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "duplicateDay" apps/mobile/app apps/mobile/components --include="*.tsx" | grep -v "__tests__"`
      names `apps/mobile/app/(tabs)/programs.tsx` — non-empty, where `04-VERIFICATION.md` recorded zero
    - `programs.tsx` exports `duplicateDayName` and the helper is asserted directly, not through a render
    - The Duplicate control is rendered per day page with an accessibility label naming that day, and
      opens no confirmation dialog
    - The write goes through `mutate`, so a `Day not found` or `Day name is required` throw becomes a
      banner rather than an unhandled rejection
    - `pnpm --filter mobile test`, `typecheck` and `build` all exit 0
  </acceptance_criteria>
  <done>
    Duplicating a training day is one tap on the page that day lives on, and `duplicateDay` is no
    longer a tested function nothing can call.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| day-page control -> local write helper | A user gesture crosses into an unconditional local SQLite write |
| confirmation dialog -> destructive write | The only thing between a tap and a day leaving the deck |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-63 | Repudiation | archive control reaching a delete | high | mitigate | The control calls `archiveDay`, which 04-12 proves issues zero deletes; `removeDay` keeps its own separate, explicitly-labelled Remove control so the two acts can never be confused at the call site. |
| T-04-64 | Denial of Service | an archived day with no way back | high | mitigate | The Archived days section is the restore surface and is fed by `loadArchivedDays` from the same `reloadTree` that feeds the deck, so a day can never be archived into a state the screen cannot show. Grep-asserted as a non-test call site. |
| T-04-65 | Tampering | a failed write silently reverting | medium | mitigate | Both new writes go through the screen's existing `mutate` funnel, which sets `mutationError` and reloads the tree, so the row snaps back to what is actually stored with the banner saying why. |
| T-04-66 | Spoofing | unlabelled per-day controls | medium | mitigate | Every new control carries `accessibilityLabel` naming its day; a three-day program renders three Archive controls and an unlabelled one is indistinguishable to assistive tech. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
- `pnpm --filter mobile test`, `pnpm --filter mobile typecheck` and `pnpm --filter mobile build` exit 0
- `npx turbo run typecheck lint` exits 7/7 successful
- A non-test grep across `apps/mobile/app` and `apps/mobile/components` returns a reference to
  `duplicateDay`, `archiveDay`, `restoreDay` and `loadArchivedDays`
- WINDOWS: record an `unrun-verify` entry noting the four new day-page controls have been observed on
  neither iOS nor Android (no Xcode, no Android SDK — ROADMAP Phase 999.1); the web observation lands
  in 04-15
</verification>

<success_criteria>
- The day page offers Rename, Duplicate, Archive and Remove, each labelled and each at least 48x48
- Archiving a day removes it from the deck and puts it in an Archived days section with a Restore control
- Duplicating a day appends a copy with no confirmation
- Every one of the three previously-orphaned day functions now has a non-test call site
</success_criteria>

<output>
Create `.planning/phases/04-program-builder/04-13-SUMMARY.md` when done
</output>
