// Reserved slot (ARCHITECTURE.md §4). The progression rules are a single pure package imported by
// BOTH the client and the server so the two can never drift. Nothing is implemented in Phase 1 —
// this package exists so the workspace layout can host it without restructuring later.

export const PROGRESSION_ENGINE_PLACEHOLDER = true;
